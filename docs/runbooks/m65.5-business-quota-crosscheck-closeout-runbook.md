# M65.5 Business Quota Cross-check Runbook

Purpose: operate and verify the Business quota cross-check closeout before high-privilege admin automation.

## Operator Steps

1. Confirm `.codexhub/business-quota-readings/` is ignored and contains only local runtime probe data.
2. Run focused tests for contracts, Business quota kernel, App Server adapter, and SQLite store.
3. Run static audits and full foundation verification before committing.
4. Treat any quota mismatch as a readiness blocker, not as success.

## Stop Conditions

- Raw browser storage, cookie, session, token, MFA, password, network body, or raw prompt/body appears in public results.
- App Server quota and UI observation are reported as matched without at least one compared field.
- Local reading SQLite is staged for commit.

## Rollback

- Revert the M65.5 commit to remove the cross-check projection and docs.
- Delete local `.codexhub/business-quota-readings/` runtime data only if the operator explicitly wants to discard probe evidence.
