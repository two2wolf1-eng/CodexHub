# M32.3 Control-Plane Coverage Hardening

Date: 2026-05-06

## Scope

This debug-only round tightened M0-M32 late-stage Supervisor route coverage.

Covered areas:

- Supervisor late-stage mutating route gate tests
- Control-plane route family classification
- Release/scaffold registration

## Changes

- Replaced the flat late-stage route list in tests with a family-level control-plane matrix.
- Each family now records its route prefix and whether approval is managed by the route family or by child control planes.
- Added a regression test that every family has the required `dry-runs` and `runs` routes, and approval routes when approval is not externally managed.
- Kept the existing source-drift check that compares the matrix-derived route list with registered `server.post(...)` routes.

## Safety Outcome

- No new route.
- No new provider.
- No new store repository.
- No new live boundary.
- No behavior change to Supervisor runtime routes.
- Existing local-control token, trusted Origin, non-wildcard CORS, and request-body authority rejection tests remain active.

## Verification

Required before commit:

- `pnpm nx run supervisor:test --skip-nx-cache`
- `pnpm scaffold:health`
- `pnpm audit:boundaries`
- `pnpm audit:sqlite-isolation`
- `pnpm audit:no-live-automation`
- `pnpm audit:skills`
- `pnpm verify:foundation`
- `git diff --check`

