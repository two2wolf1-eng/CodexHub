# M61.20-M61.24 Store Supervisor Diagnosis Debug

## GSD

- Goal: persist M61 debug probes and expose Supervisor projections safely.
- Scope: SQLite round trip, Supervisor probes/report endpoints, scheduler impact, diagnosis mapping, recovery mapping.
- Non-scope: no recovery execution and no live adapter call.
- Acceptance: new store tables, Supervisor GET projections, POST rehearsal shell, and negative raw-content test.
- Hard boundaries: Supervisor routes call governance/kernel/store projection only.
- Risk: high.

## Rounds Covered

21. M61.20 Store Round Trip: six M61 metadata record families round-trip through SQLite.
22. M61.21 Supervisor Projection Probe: new routes expose only metadata records and counts.
23. M61.22 Scheduler Impact Probe: quota unknown or limited remains a scheduler block reason.
24. M61.23 Diagnosis Mapping: quota/auth/workspace/human-waiting map into diagnosis inputs.
25. M61.24 Recovery Mapping: recovery remains dry-run/manual checkpoint only.

## Evidence

- Store tables: `business_quota_source_probes`, `business_quota_permission_probes`, `local_capability_probes`, `forbidden_path_probes`, `quota_evidence_matrices`, `quota_readiness_debug_reports`.
- Routes: `GET /api/business-quota-debug/probes`, `GET /api/business-quota-debug/report`, `POST /api/business-quota-debug/rehearsals`.
- Focused tests: `pnpm nx test store-sqlite`, `pnpm nx test supervisor -- --run src/server.test.ts`.
