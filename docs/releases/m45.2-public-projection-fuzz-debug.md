# M45.2 Public Projection Fuzz Debug

Status: completed

## Goal

Deepen the public-output leak net for M0-M45, with specific attention to the M44 real policy/telemetry surfaces and the M45 controlled Browser, Electron, and MCP write surfaces.

## Scope

- Shared adversarial public-output fixture terms.
- Contracts public object round-trip tests for policy telemetry and controlled writes.
- Existing CLI and Dashboard public-output checks that consume the shared fixture.
- Documentation and scaffold health registration.

## Findings

- CLI and Dashboard already consumed the shared adversarial fixture.
- Contracts had targeted raw-output assertions for M44/M45 objects, but they did not use the shared round-trip fixture directly.
- No raw prompt, stdout, stderr, diff, path, URL, body, token, cookie, session, env, policy input, span/log body, JavaScript source, selector, typed text, or patch leak was found.

## Hardening

- Added M44/M45-specific adversarial terms for policy input/source/output, telemetry span/log payloads, Browser action input, Electron JavaScript source, and MCP patch content.
- Added shared fixture round-trip checks to the M44 real policy/telemetry contract test.
- Added shared fixture round-trip checks to the M45 controlled write contract test.

## Verification

- Focused contracts, CLI, and Dashboard tests must pass.
- Full foundation gates remain required before commit.

## Rollback

Revert this round to remove the additional adversarial terms and contract round-trip checks.
