# M32.4 Public Projection Round-Trip Fuzz

Date: 2026-05-06

## Scope

This debug-only round hardened public projection and store round-trip regression coverage.

Covered areas:

- Store list/get round-trip checks for late-stage GitHub workflow records
- Existing adversarial projection fixtures for governance, readiness, approval history, review packages, release candidates, GitHub summaries, and workflow recovery
- Scaffold and orchestration registration

## Changes

- Added an explicit round-trip leak assertion for GitHub publish-to-draft-PR chain store records.
- Confirmed representative public projection packages already hash or summarize adversarial raw input before JSON round-trip.
- Registered the M32.4 release and review documents in scaffold health.

## Safety Outcome

- No new route.
- No new provider.
- No new store repository.
- No new live boundary.
- No schema or public API change.
- Public outputs remain restricted to ids, hashes, counts, statuses, summaries, evidence refs, audit ids, and boundary booleans.

## Verification

Required before commit:

- `pnpm nx run store-sqlite:test --skip-nx-cache`
- `pnpm nx run-many --target=test "--projects=governance-projection-kernel,operator-readiness-kernel,approval-ux-kernel,review-package-kernel,release-candidate-kernel,workflow-kernel,github-provider-adapter" --skip-nx-cache`
- `pnpm scaffold:health`
- `pnpm audit:boundaries`
- `pnpm audit:sqlite-isolation`
- `pnpm audit:no-live-automation`
- `pnpm audit:skills`
- `pnpm verify:foundation`
- `git diff --check`
