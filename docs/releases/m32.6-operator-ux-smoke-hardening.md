# M32.6 Operator UX Smoke Hardening

Status: completed

Scope:
- Dashboard operator smoke routes: governance, readiness, GitHub, workflows, approvals, pilot, and release candidates.
- CLI read-only smoke helpers for governance, GitHub branch/PR lifecycle/cleanup, workflow validation/rehearsal, approval history, and production recovery.
- MCP production source read-only boundary scan.

Changes:
- Added a Dashboard route registration regression for the M32.6 smoke set.
- Added CLI source-level guards for operator smoke list/show helpers to stay GET-only, token-free, and adapter-execute-free.
- Removed the MCP HTTP gate fallback to the Supervisor local-control token; MCP HTTP now uses only its MCP-specific local-control key.
- Extended MCP source scanning to reject Supervisor local-control token fragments.

Safety:
- No route, provider, store repository, live boundary, or remote write path was added.
- Dashboard mutation remains limited to existing approval decision UI and the governed recovery wizard.
- CLI remains read-only for these operator smoke surfaces.
- MCP tools remain read-only and do not call adapter execute helpers.

Verification:
- Focused Dashboard, CLI, and MCP tests.
- Focused lint/build for changed apps.
- Full scaffold, boundary, sqlite isolation, no-live automation, skills, foundation verification, and diff-check gates.

Rollback:
- Revert this commit to restore the prior tests and MCP Supervisor-token fallback.
