# M57.6-M57.7 Dispatch Lifecycle Review

Status: completed.

## Findings

No blocking findings were found in this round.

## Review Scope

Reviewed:

- `runCodexTaskDispatchLifecycle` preflight guard and App Server lifecycle
  ordering.
- Mapping of missing controller, initialize failure, thread/turn failure, event
  failure, and stalled event stream into `CodexTaskDiagnosis`.
- Focused tests for successful terminal-event completion, blocked preflight, and
  initialization failure.
- Metadata-only serialization checks for private JSON-RPC ids and thread/turn
  ids.

Not reviewed:

- Supervisor `POST /tasks` wiring.
- Real Codex App Server binary invocation.
- GitHub branch or PR closure.
- Browser/Electron observation.

## Boundary Review

The round preserves these invariants:

- App Server is not called unless preflight is ready and dispatch is allowed.
- Completion requires a terminal event from the App Server event stream.
- Failed or stalled lifecycle paths produce a diagnosis id on `CodexTaskRun`.
- The lifecycle result exposes only metadata, evidence refs, and audit ids.
- Process boundary flags are explicit and carried from the governed boundary
  input instead of inferred from route authority.

## Residual Risks

- M57.8 must ensure Supervisor routes call only the orchestrator/store shell and
  never call adapters directly.
- M57.9 must run wider foundation verification after route integration.
- M58 should replace the first-pass lifecycle diagnosis mapping with the
  dedicated diagnosis/recovery kernels.

## Workflow Skills Used and Why

- `gsd-spec-driver`: reviewed acceptance and non-scope.
- `gstack-delivery-workflow`: reviewed small-step delivery.
- `superpowers-engineering-discipline`: reviewed evidence-over-claims and no
  scope creep.

## Project Skills Used and Why

- `codexhub-architecture-planner`: reviewed kernel ownership.
- `codexhub-contract-designer`: reviewed metadata-only output shape.
- `codexhub-workflow-policy-reviewer`: reviewed approval/evidence/audit flags.
- `codexhub-codex-exec-adapter`: reviewed App Server lifecycle composition.
- `codexhub-release-auditor`: reviewed verification evidence.

## Skills Not Used and Why

- `codexhub-electron-cdp-observer`: no Electron/CDP surface changed.
- `codexhub-browser-profile-observer`: no profile registry surface changed.
- `codexhub-playwright-qa`: no UI surface changed.
