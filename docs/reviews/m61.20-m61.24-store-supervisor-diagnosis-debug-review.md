# M61.20-M61.24 Review

## Findings

- Supervisor does not call adapters directly for M61.
- POST rehearsal rejects untrusted authority and raw content fields.
- Store uses the existing metadata JSON repository pattern.

## Residual Risk

- The route response currently returns full M61 metadata records; this is acceptable because contracts restrict their shape, but future UI should prefer compact summaries.
- Diagnosis and recovery are mapped conceptually in M61; executable recovery remains out of scope.

## Verification

- `pnpm nx build store-core` passed.
- `pnpm nx build store-sqlite` passed.
- `pnpm nx test store-sqlite` passed.
- `pnpm nx test supervisor -- --run src/server.test.ts` passed after adding the Vitest alias.
