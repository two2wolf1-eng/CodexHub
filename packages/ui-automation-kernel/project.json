{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "ui-automation-kernel",
  "projectType": "library",
  "sourceRoot": "packages/ui-automation-kernel/src",
  "implicitDependencies": ["contracts", "evidence-kernel"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/ui-automation-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/ui-automation-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/ui-automation-kernel"
      }
    }
  }
}
