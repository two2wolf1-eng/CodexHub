import { describe, expect, it } from 'vitest';
import {
  adversarialPublicOutputFixture,
  findAdversarialPublicOutputRoundTripLeaks,
} from '../../../test-fixtures/adversarial-public-output-fixture';
import {
  CodexExecLiveRunRecordSchema,
  CodexExecLiveConfigSchema,
  CodexExecConfigLoadResultSchema,
  CodexExecApprovalTransitionResultSchema,
  CodexExecControlPlaneTimelineSchema,
  CodexExecTimelineDetailViewSchema,
  CodexExecAuditDetailViewSchema,
  CodexExecAuditSearchResultSchema,
  CodexExecControlPlaneDrilldownViewSchema,
  CodexExecEvidenceDetailViewSchema,
  CodexExecEvidenceSearchResultSchema,
  CodexExecControlPlaneReportExportResultSchema,
  CodexExecControlPlaneReportSchema,
  CodexExecAdrReadinessChecklistItemSchema,
  CodexExecGovernanceBlockerSchema,
  CodexExecGovernanceReviewPackageQuerySchema,
  CodexExecGovernanceReviewPackageSchema,
  CodexExecGovernanceReviewPackageSummarySchema,
  CodexExecLiveAdapterAdrDraftExportResultSchema,
  CodexExecLiveAdapterAdrDraftQuerySchema,
  CodexExecLiveAdapterAdrDraftSchema,
  CodexExecLiveAdapterAdrDraftSectionSchema,
  CodexExecLiveAdapterAdrDraftSummarySchema,
  CodexExecLiveAdapterAdrDecisionGatePolicySchema,
  CodexExecLiveAdapterAdrDecisionQuerySchema,
  CodexExecLiveAdapterAdrDecisionRecordSchema,
  CodexExecLiveAdapterAdrDecisionSummarySchema,
  CodexExecReadOnlyAdapterOperatorChecklistItemSchema,
  CodexExecReadOnlyAdapterPreflightSimulationBlockerSchema,
  CodexExecReadOnlyAdapterPreflightSimulationCheckSchema,
  CodexExecReadOnlyAdapterPreflightSimulationInputSchema,
  CodexExecReadOnlyAdapterPreflightSimulationResultSchema,
  CodexExecReadOnlyAdapterPreflightSimulationSummarySchema,
  CodexExecReadOnlyAdapterSimulatorReviewChecklistItemSchema,
  CodexExecReadOnlyAdapterSimulatorReviewDecisionRecordSchema,
  CodexExecReadOnlyAdapterSimulatorReviewFindingSchema,
  CodexExecReadOnlyAdapterSimulatorReviewQuerySchema,
  CodexExecReadOnlyAdapterSimulatorReviewSummarySchema,
  CodexExecReadOnlyAdapterImplementationPlanReviewChecklistItemSchema,
  CodexExecReadOnlyAdapterImplementationPlanReviewDecisionRecordSchema,
  CodexExecReadOnlyAdapterImplementationPlanReviewFindingSchema,
  CodexExecReadOnlyAdapterImplementationPlanReviewQuerySchema,
  CodexExecReadOnlyAdapterImplementationPlanReviewSummarySchema,
  CodexExecReadOnlyAdapterSkeletonConfigSchema,
  CodexExecReadOnlyAdapterSkeletonPreviewSchema,
  CodexExecReadOnlyAdapterSkeletonReviewDecisionRecordSchema,
  CodexExecReadOnlyAdapterSkeletonReviewSummarySchema,
  CodexExecReadOnlyAdapterFixtureBoundaryResultSchema,
  CodexExecReadOnlyAdapterFixtureBoundarySummarySchema,
  CodexExecReadOnlyAdapterFinalReadinessDecisionRecordSchema,
  CodexExecReadOnlyAdapterFinalReadinessSummarySchema,
  CodexExecRealReadOnlyAdapterReadinessBlockerSchema,
  CodexExecRealReadOnlyAdapterReadinessChecklistItemSchema,
  CodexExecRealReadOnlyAdapterReadinessFindingSchema,
  CodexExecRealReadOnlyAdapterReadinessGateSchema,
  CodexExecRealReadOnlyAdapterReadinessPackageSchema,
  CodexExecRealReadOnlyAdapterReadinessQuerySchema,
  CodexExecRealReadOnlyAdapterReadinessReviewChecklistItemSchema,
  CodexExecRealReadOnlyAdapterReadinessReviewDecisionRecordSchema,
  CodexExecRealReadOnlyAdapterReadinessReviewFindingSchema,
  CodexExecRealReadOnlyAdapterReadinessReviewQuerySchema,
  CodexExecRealReadOnlyAdapterReadinessReviewSummarySchema,
  CodexExecRealReadOnlyAdapterReadinessSummarySchema,
  CodexExecRealReadOnlyAdapterAuditSummarySchema,
  CodexExecRealReadOnlyAdapterApprovalAuthoritySummarySchema,
  CodexExecRealReadOnlyAdapterApprovalAuthorityTraceQuerySchema,
  CodexExecRealReadOnlyAdapterApprovalAuthorityTraceRecordSchema,
  CodexExecRealReadOnlyAdapterApprovalAuthorityTraceSummarySchema,
  CodexExecRealReadOnlyAdapterAttemptQuerySchema,
  CodexExecRealReadOnlyAdapterAttemptRecordSchema,
  CodexExecRealReadOnlyAdapterAttemptSummarySchema,
  CodexExecRealReadOnlyAdapterAttemptTimelineEntrySchema,
  CodexExecRealReadOnlyAdapterAttemptTimelineQuerySchema,
  CodexExecRealReadOnlyAdapterAttemptTimelineSummarySchema,
  CodexExecRealReadOnlyAdapterBoundaryDeferredDiagnosticsSchema,
  CodexExecRealReadOnlyAdapterBoundaryDiagnosticsSchema,
  CodexExecRealReadOnlyAdapterBoundaryPlanSchema,
  CodexExecRealReadOnlyAdapterConfigSchema,
  CodexExecRealReadOnlyAdapterErrorSchema,
  CodexExecRealReadOnlyAdapterEvidenceSummarySchema,
  CodexExecRealReadOnlyAdapterPolicySourceBlockerSchema,
  CodexExecRealReadOnlyAdapterPolicySourceChecklistItemSchema,
  CodexExecRealReadOnlyAdapterPolicySourceFindingSchema,
  CodexExecRealReadOnlyAdapterPolicySourceGateSchema,
  CodexExecRealReadOnlyAdapterPolicySourceQuerySchema,
  CodexExecRealReadOnlyAdapterPolicySourceRecordSchema,
  CodexExecRealReadOnlyAdapterPolicySourceSummarySchema,
  CodexExecRealReadOnlyAdapterPilotSourcePreparationBlockerSchema,
  CodexExecRealReadOnlyAdapterPilotSourcePreparationChecklistItemSchema,
  CodexExecRealReadOnlyAdapterPilotSourcePreparationFindingSchema,
  CodexExecRealReadOnlyAdapterPilotSourcePreparationGateSchema,
  CodexExecRealReadOnlyAdapterPilotSourcePreparationQuerySchema,
  CodexExecRealReadOnlyAdapterPilotSourcePreparationRecordSchema,
  CodexExecRealReadOnlyAdapterPilotSourcePreparationSummarySchema,
  CodexExecRealReadOnlyAdapterPilotPrerequisiteBlockerSchema,
  CodexExecRealReadOnlyAdapterPilotPrerequisiteChecklistItemSchema,
  CodexExecRealReadOnlyAdapterPilotPrerequisiteFindingSchema,
  CodexExecRealReadOnlyAdapterPilotPrerequisiteGateSchema,
  CodexExecRealReadOnlyAdapterPilotPrerequisiteQuerySchema,
  CodexExecRealReadOnlyAdapterPilotPrerequisiteRecordSchema,
  CodexExecRealReadOnlyAdapterPilotPrerequisiteSummarySchema,
  CodexExecRealReadOnlyAdapterPreflightCheckSchema,
  CodexExecRealReadOnlyAdapterPreflightSchema,
  CodexExecRealReadOnlyAdapterPostRunVerificationSkipReasonSchema,
  CodexExecRealReadOnlyAdapterRequestSchema,
  CodexExecRealReadOnlyAdapterResultSchema,
  CodexExecNoLiveEvidenceSummarySchema,
  CodexExecReportReviewComparisonSchema,
  CodexExecReportReviewComparisonItemSchema,
  CodexExecReportReviewHistoryViewSchema,
  CodexExecReportReviewRecordSchema,
  CodexExecReportReviewSummarySchema,
  CodexExecReviewerHandoffSummarySchema,
  CodexExecTimelineFilterSchema,
  CodexExecTimelineQuerySchema,
  CodexExecManualApprovalRecordSchema,
  CodexExecManualApprovalStateSchema,
  CodexExecPreflightResultSchema,
  DevelopmentRequestSchema,
  CodexReplayRecordSchema,
  CodexExecReplayResultSchema,
  ActionModeSchema,
  CapabilityAuditEventSchema,
  CapabilityDryRunSchema,
  CapabilityExecutionResultSchema,
  CapabilityManifestSchema,
  ExecutionAuthoritySchema,
  EvidenceRefSchema,
  McpToolDefinitionSchema,
  McpToolInvocationSummarySchema,
  McpToolNameSchema,
  AffectedProjectSchema,
  BrowserForbiddenActionSchema,
  BrowserObservationApprovalArtifactRecordSchema,
  BrowserObservationCapabilitySchema,
  BrowserObservationControlPlaneApprovalStatusSchema,
  BrowserObservationControlPlaneRunSchema,
  BrowserObservationDryRunRecordSchema,
  BrowserObservationRunnerModeSchema,
  BrowserObservationRunSchema,
  BrowserObservationRunStatusSchema,
  BrowserObservationTimelineEventSchema,
  BrowserPageObservationPlanSchema,
  BrowserPageObservationSummarySchema,
  BrowserProfileReadinessSchema,
  BrowserProfileRefSchema,
  ElectronCdpAllowedCommandSchema,
  ElectronCdpBlockReasonSchema,
  ElectronCdpCommandAllowlistDecisionSchema,
  ElectronCdpConsoleSummarySchema,
  ElectronCdpEventMetadataSummarySchema,
  ElectronCdpForbiddenActionSchema,
  ElectronCdpNetworkMetadataSummarySchema,
  ElectronCdpControlPlaneApprovalStatusSchema,
  CodexDesktopDiagnosticHintSchema,
  CodexDesktopHealthSnapshotSchema,
  CodexDesktopHealthStatusSchema,
  ElectronCdpObservationApprovalArtifactRecordSchema,
  ElectronCdpObservationCapabilitySchema,
  ElectronCdpObservationControlPlaneRunSchema,
  ElectronCdpObservationDryRunRecordSchema,
  ElectronCdpObservationPlanSchema,
  ElectronCdpObservationRunnerModeSchema,
  ElectronCdpObservationRunSchema,
  ElectronCdpObservationRunStatusSchema,
  ElectronCdpObservationSummarySchema,
  ElectronCdpObservationTimelineEventSchema,
  ElectronDebugEndpointSummarySchema,
  ElectronProcessKindSchema,
  ElectronProcessSummarySchema,
  ElectronTargetSummarySchema,
  ElectronTargetTypeSchema,
  OsProcessMetadataSummarySchema,
  OsProcessObservationStatusSchema,
  OsProcessObservedKindSchema,
  OrchestrationRunSchema,
  OrchestrationRunStatusSchema,
  OrchestrationTimelineEventSchema,
  ControlledPatchLifecycleRunSchema,
  ControlledPatchPlanSchema,
  ControlledPatchReadinessSchema,
  ControlledPatchRejectionReasonSchema,
  ControlledPatchRetryCleanupProjectionSchema,
  ControlledPatchRunSchema,
  ControlledPatchVerificationGateSchema,
  DiffReviewSummarySchema,
  GovernedCodexPatchPlanSchema,
  GovernedCodexPatchRunSchema,
  GithubDraftPrAcceptanceRehearsalRunSchema,
  GithubBranchPublishAcceptanceRehearsalRunSchema,
  GithubBranchPublishPlanSchema,
  GithubBranchPublishReadinessSchema,
  GithubBranchPublishRunSchema,
  GithubCommitContentManifestSchema,
  GithubDraftPrCreationSummarySchema,
  GithubDraftPrPlanSchema,
  GithubDraftPrReadinessSchema,
  GithubDraftPrRunSchema,
  GithubMetadataApprovalArtifactRecordSchema,
  GithubMetadataControlPlaneRunSchema,
  GithubMetadataDryRunRecordSchema,
  GithubMergeAcceptanceRehearsalRunSchema,
  GithubMergeApprovalArtifactSchema,
  GithubMergeReadinessPlanSchema,
  GithubMergeReadinessSummarySchema,
  GithubMergeResultSummarySchema,
  GithubMergeRunSchema,
  CicdProviderManifestSchema,
  GithubActionsAcceptanceRehearsalRunSchema,
  GithubActionsCancelPlanSchema,
  GithubActionsDispatchApprovalArtifactSchema,
  GithubActionsDispatchPlanSchema,
  GithubActionsDispatchRunSchema,
  GithubActionsObservationApprovalArtifactRecordSchema,
  GithubActionsObservationPlanSchema,
  GithubActionsObservationRunSchema,
  GithubActionsReadinessSchema,
  GithubActionsRerunPlanSchema,
  GithubActionsRunControlApprovalArtifactSchema,
  GithubActionsRunControlRunSchema,
  ReleaseVersionPlanSchema,
  ReleaseChangelogSummarySchema,
  GithubReleaseTagPlanSchema,
  GithubReleaseTagApprovalArtifactSchema,
  GithubReleaseTagRunSchema,
  GithubReleaseDraftPlanSchema,
  GithubReleaseDraftApprovalArtifactSchema,
  GithubReleaseDraftRunSchema,
  ReleaseLifecycleAcceptanceScenarioSchema,
  ReleaseLifecycleAcceptanceRehearsalRunSchema,
  DeploymentAcceptanceScenarioSchema,
  DeploymentProviderManifestSchema,
  DeploymentReadinessSchema,
  DeploymentObservationPlanSchema,
  DeploymentObservationApprovalArtifactSchema,
  DeploymentProviderStatusSummarySchema,
  DeploymentDriftSummarySchema,
  DeploymentPlanDiffHashSummarySchema,
  DeploymentObservationRunSchema,
  DeploymentAcceptanceRehearsalRunSchema,
  DeploymentEnvironmentApprovalPolicySchema,
  DeploymentOperationReadinessSchema,
  DeploymentRollbackPlanSchema,
  DeploymentOperationPlanSchema,
  DeploymentOperationApprovalArtifactSchema,
  DeploymentOperationResultSummarySchema,
  DeploymentOperationRunSchema,
  DeploymentOperationAcceptanceScenarioSchema,
  DeploymentOperationAcceptanceRehearsalRunSchema,
  SecretGovernanceAcceptanceScenarioSchema,
  SecretProviderManifestSchema,
  SecretProviderReadinessSchema,
  SecretEnvironmentReadinessSchema,
  ConfiguredSecretReferenceSummarySchema,
  SecretLeakAuditSummarySchema,
  SecretReadinessPlanSchema,
  SecretReadinessApprovalArtifactSchema,
  SecretReadinessRunSchema,
  SecretGovernanceAcceptanceRehearsalRunSchema,
  GithubPrLifecycleAcceptanceRehearsalRunSchema,
  GithubPrLifecycleApprovalArtifactRecordSchema,
  GithubPrLifecycleObservationPlanSchema,
  GithubPrLifecycleObservationRunSchema,
  GithubProviderHostSchema,
  GithubRemoteCleanupAcceptanceRehearsalRunSchema,
  GithubRemoteCleanupApprovalArtifactRecordSchema,
  GithubRemoteCleanupPlanSchema,
  GithubRemoteCleanupRunSchema,
  GithubRemoteCleanupSummarySchema,
  CustomWorkflowCatalogEntrySchema,
  CustomWorkflowCatalogReadinessSchema,
  CustomWorkflowApprovalArtifactRecordSchema,
  CustomWorkflowCapabilityBindingSchema,
  CustomWorkflowPlanSchema,
  CustomWorkflowProductionTemplateValidationSummarySchema,
  CustomWorkflowRehearsalRunSchema,
  CustomWorkflowRunSchema,
  CustomWorkflowStepTemplateSchema,
  CustomWorkflowTemplateSchema,
  CustomWorkflowTemplateFamilySummarySchema,
  CustomWorkflowValidationReportSchema,
  ProductionWorkflowPilotEvidenceSummarySchema,
  LocalProductionWorkflowAcceptanceRunSchema,
  LocalProductionWorkflowChildBoundarySummarySchema,
  LocalProductionWorkflowPilotPlanSchema,
  LocalProductionWorkflowPilotRunSchema,
  LocalProductionWorkflowPilotStepSchema,
  ProductionWorkflowPilotPlanSchema,
  ProductionWorkflowPilotReadinessSchema,
  ProductionWorkflowPilotRunSchema,
  ProductionWorkflowPilotStepSchema,
  ProductionWorkflowOperationsProjectionSchema,
  ProductionWorkflowOperationsSmokeRunSchema,
  ProductionWorkflowPauseSummarySchema,
  CodexPatchChildRecordSchema,
  LocalProductionWorkflowChildRecordSetSchema,
  NxVerificationChildRecordSchema,
  ProductionWorkflowChildActionPlanSchema,
  ProductionWorkflowChildRecordRefSchema,
  ProductionWorkflowChildRecordResolutionSchema,
  ProductionWorkflowChildActionStateRecordSchema,
  ProductionWorkflowChildActionStateSchema,
  ProductionWorkflowRecoveryApprovalArtifactSchema,
  ProductionWorkflowRecoveryPlanSchema,
  ProductionWorkflowRecoveryPublicSummarySchema,
  ProductionWorkflowRecoveryRunSchema,
  ProductionWorkflowRecoveryScenarioSchema,
  ProductionWorkflowRecoveryStepSchema,
  ProductionWorkflowRecoveryTimelineEventSchema,
  ProductionWorkflowResumeSummarySchema,
  ProductionWorkflowRollbackSummarySchema,
  GithubPublishDraftPrAcceptanceRehearsalRunSchema,
  GithubPublishDraftPrChainPlanSchema,
  GithubPublishDraftPrChainRunSchema,
  GithubPublishDraftPrChainStepSchema,
  GithubRemoteRefSummarySchema,
  GithubRemoteCommitSummarySchema,
  GithubRemotePrLifecycleSummarySchema,
  GithubTokenReadinessSchema,
  RemoteCleanupReadinessSchema,
  RemoteSupersedeAcceptanceRehearsalRunSchema,
  RemoteSupersedeChainProjectionSchema,
  RemoteSupersedePlanSchema,
  RemoteSupersedeRunSchema,
  RemoteSupersedeTargetSummarySchema,
  ReworkAttemptSummarySchema,
  ReworkLoopAcceptanceRehearsalRunSchema,
  ReworkLoopApprovalArtifactRecordSchema,
  ReworkLoopPlanSchema,
  ReworkLoopRunSchema,
  ReworkSupersedeProjectionSchema,
  ReworkTriggerSummarySchema,
  LocalReviewPackageApprovalArtifactRecordSchema,
  LocalReviewPackageControlPlaneRunSchema,
  LocalReviewPackageDryRunRecordSchema,
  LocalReviewDecisionProjectionSchema,
  LocalReviewFindingSummarySchema,
  LocalReviewPackagePlanSchema,
  LocalReviewPackageRunSchema,
  LocalReviewPackageSummarySchema,
  LocalRcBundleApprovalArtifactRecordSchema,
  LocalRcAcceptanceRehearsalRunSchema,
  LocalRcAcceptanceRehearsalStepSchema,
  LocalRcBundleControlPlaneRunSchema,
  LocalRcBundleDryRunRecordSchema,
  LocalRcAuditChainSchema,
  LocalRcEvidenceBundleSchema,
  LocalRcReadinessPlanSchema,
  LocalRcReadinessSummarySchema,
  PatchRunSchema,
  PatchSummarySchema,
  PolicyBackendEvaluationPlanSchema,
  PolicyBackendEvaluationRunSchema,
  PolicyBackendEvaluatorSourceSchema,
  PolicyBackendKindSchema,
  PolicyBackendNormalizedDecisionTraceSchema,
  PolicyBackendRawEvaluationSummarySchema,
  PolicyDecisionSchema,
  PullRequestSummaryDraftSchema,
  ReleaseAuditDraftSchema,
  SchemaVersionSchema,
  SkillResolutionResultSchema,
  TelemetryExportRunSchema,
  TelemetryExporterKindSchema,
  TelemetrySignalKindSchema,
  TelemetrySpanSummarySchema,
  TelemetryTraceExportPlanSchema,
  AuditChainProjectionSchema,
  ConfigHashSummarySchema,
  EvidenceBundleProjectionSchema,
  GoldenPathEvidenceBundleSchema,
  GoldenPathRehearsalRunSchema,
  GoldenPathStepSchema,
  GoldenPathStepStatusSchema,
  ApprovalDecisionHistoryItemSchema,
  ApprovalDecisionHistoryProjectionSchema,
  ApprovalDecisionHistorySummarySchema,
  ApprovalDecisionRequestSchema,
  ApprovalDecisionResultSchema,
  ApprovalInboxItemSchema,
  ApprovalInboxProjectionSchema,
  ApprovalUxDecisionSchema,
  ApprovalUxTypeSchema,
  M9PilotEvidenceSummarySchema,
  M11PilotEvidenceSummarySchema,
  M11PilotCleanupApprovalStatusSchema,
  M11PilotCleanupHandoffSchema,
  M11PilotFailureSummarySchema,
  M11PilotReadinessSchema,
  M11PilotRecoveryActionSchema,
  M11PilotRecoveryProjectionSchema,
  M11PilotRunSchema,
  M11PilotStepSchema,
  M11PilotAcceptanceScenarioSchema,
  M11PilotAcceptanceSmokeRunSchema,
  M11PilotAcceptanceSmokeStepSchema,
  M10PilotChecklistSchema,
  M10PilotChecklistStatusSchema,
  M10PilotAcceptanceEvidenceSummarySchema,
  M10PilotAcceptanceRehearsalRunSchema,
  M10PilotAcceptanceScenarioSchema,
  M10PilotAcceptanceStepSchema,
  M10PilotOperatorStepPhaseSchema,
  M10PilotOperatorStepSchema,
  M10PilotOperatorStepStatusSchema,
  M10PilotRunbookSummarySchema,
  M11PilotEnablementChecklistSchema,
  M11PilotEnablementRunbookSummarySchema,
  M11PilotEnablementStatusSchema,
  M11PilotEnablementStepPhaseSchema,
  M11PilotEnablementStepSchema,
  M11PilotEnablementStepStatusSchema,
  M9PilotReadinessSchema,
  M9PilotRunSchema,
  M9PilotStepSchema,
  GovernanceProjectionSummarySchema,
  IntegrationReadinessSummarySchema,
  OperatorReadinessCheckSchema,
  OperatorReadinessReportSchema,
  OperatorReadinessStatusSchema,
  UnifiedRunProjectionSchema,
  UnifiedRunSourceSchema,
  UnifiedTimelineEventSchema,
  VerificationCommandResultSchema,
  VerificationPlanSchema,
  VerificationRunSchema,
  VerificationTargetSchema,
  WorktreeApprovalArtifactRecordSchema,
  WorktreeCleanupApprovalArtifactRecordSchema,
  WorktreeCleanupControlPlaneRunSchema,
  WorktreeCleanupDryRunRecordSchema,
  WorktreeCleanupPlanSchema,
  WorktreeCleanupRunSchema,
  WorktreeControlPlaneRunSchema,
  WorktreeDryRunRecordSchema,
  WorktreePlanSchema,
  WorktreeRunSchema,
  BrowserActionApprovalArtifactSchema,
  BrowserActionPlanSchema,
  BrowserActionRunSchema,
  BrowserActionStepSummarySchema,
  ControlledWriteAcceptanceScenarioSchema,
  ControlledWriteAcceptanceRehearsalRunSchema,
  ElectronMainInspectorApprovalArtifactSchema,
  ElectronMainInspectorPlanSchema,
  ElectronMainInspectorRunSchema,
  ExternalAgentApprovalArtifactSchema,
  ExternalAgentManifestSchema,
  ExternalAgentPatchPlanSchema,
  ExternalAgentPatchSummarySchema,
  ExternalAgentReadinessSchema,
  ExternalAgentRehearsalScenarioSchema,
  ExternalAgentRehearsalRunSchema,
  ExternalAgentRunSchema,
  AuditExportPlanSchema,
  AuditExportRunSchema,
  DisasterRecoveryScenarioSchema,
  DisasterRecoveryRehearsalRunSchema,
  McpWriteToolApprovalArtifactSchema,
  McpWriteToolManifestSchema,
  McpWriteToolPlanSchema,
  McpWriteToolRunSchema,
  OperatorRoleAssignmentPlanSchema,
  OperatorRoleAssignmentRunSchema,
  PlatformBackupPlanSchema,
  PlatformBackupRunSchema,
  PlatformOperationApprovalArtifactSchema,
  PlatformRestorePlanSchema,
  PlatformRestoreRunSchema,
  ProductionGaApprovalArtifactSchema,
  ProductionGaCapabilityMatrixSchema,
  ProductionGaE2EScenarioSchema,
  ProductionGaE2ERehearsalPlanSchema,
  ProductionGaE2ERehearsalRunSchema,
  ProductionGaEvidenceBundleSummarySchema,
  ProductionGaOperatorTrainingCompletionSummarySchema,
  ProductionGaOperatorTrainingPlanSchema,
  ProductionGaReadinessPlanSchema,
  ProductionGaReadinessSummarySchema,
  ProductionGaReleaseCandidateSignoffPlanSchema,
  ProductionGaResidualRiskRegisterSchema,
  ProductionGaSignoffRunSchema,
  ProductionGaThreatModelSchema,
  RetentionPolicyPlanSchema,
  RetentionPolicyRunSchema,
  RealPolicyAdvisoryDecisionSummarySchema,
  RealPolicyBackendAcceptanceScenarioSchema,
  RealPolicyBackendAcceptanceRehearsalRunSchema,
  RealPolicyBackendApprovalArtifactSchema,
  RealPolicyBackendEvaluationPlanSchema,
  RealPolicyBackendEvaluationRunSchema,
  RealPolicyBackendManifestSchema,
  RealPolicyBackendReadinessSchema,
  RealTelemetryAcceptanceScenarioSchema,
  RealTelemetryAcceptanceRehearsalRunSchema,
  RealTelemetryExportApprovalArtifactSchema,
  RealTelemetryExportPlanSchema,
  RealTelemetryExportRunSchema,
  RealTelemetryLocalExportSummarySchema,
  RealTelemetryNetworkExportSummarySchema,
  RealTelemetryReadinessSchema,
  RealTelemetryRuntimeManifestSchema,
  RuntimeCheckpointSchema,
  RuntimeConcurrencyPolicySchema,
  RuntimeJobPlanSchema,
  RuntimeJobRunSchema,
  RuntimeLeaseSchema,
  RuntimeLockSchema,
  RuntimeQueueEntrySchema,
  RuntimeRetryPolicySchema,
  RuntimeSchedulerRehearsalScenarioSchema,
  RuntimeSchedulerRehearsalRunSchema,
  StoreMigrationPlanSchema,
  StoreMigrationRunSchema,
  MultiAgentCoordinationPlanSchema,
  MultiAgentSlotSummarySchema,
  AccountPoolSchema,
  BusinessMembershipMirrorSchema,
  BusinessWorkspaceSchema,
  ChatGptSessionHealthSchema,
  ChromeProfileBindingSchema,
  ClientPoolSchema,
  CodexAccountBindingSchema,
  CodexAppServerApprovalBridgeRecordSchema,
  CodexAppServerEventSummarySchema,
  CodexAppServerProtocolDriftReportSchema,
  CodexAppServerSessionSchema,
  CodexAppServerThreadMirrorSchema,
  CodexAppServerTurnMirrorSchema,
  CodexAppServerWireMessageSummarySchema,
  CodexClientInstanceSchema,
  CodexRecoveryRunSchema,
  CodexTaskDiagnosisSchema,
  CodexTaskIntentSchema,
  CodexTaskRunSchema,
  EvidenceBundleSchema,
  HumanCheckpointSchema,
  LeaseSchema,
  QuotaSnapshotSchema,
  WorkflowRunSchema,
} from './index';

const createdAt = '2026-04-28T00:00:00.000Z';
const schemaVersion = SchemaVersionSchema.value;

describe('contracts schemas', () => {
  it('parses core created models', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_1',
      schemaVersion,
      createdAt,
      kind: 'dry-run',
      hash: 'sha256:abc',
      metadata: { source: 'test' },
    });

    const decision = PolicyDecisionSchema.parse({
      id: 'policy_1',
      schemaVersion,
      createdAt,
      actionId: 'action_1',
      actionType: 'filesystem.read',
      actionMode: 'read',
      riskLevel: 'low',
      outcome: 'allow',
      requiresDryRun: false,
      requiresApproval: false,
    });

    const run = WorkflowRunSchema.parse({
      id: 'run_1',
      schemaVersion,
      createdAt,
      workflowName: 'development.bootstrap',
      status: 'created',
      dryRun: true,
      evidenceRefs: [evidence],
    });

    const request = DevelopmentRequestSchema.parse({
      id: 'devreq_1',
      schemaVersion,
      createdAt,
      title: 'Bootstrap',
      description: 'Create a foundation-only scaffold.',
    });

    expect(decision.outcome).toBe('allow');
    expect(run.workflowName).toBe('development.bootstrap');
    expect(request.constraints).toEqual([]);
  });

  it('parses integration-first capability contracts', () => {
    expect(ActionModeSchema.options).toEqual(['read', 'dry-run', 'write', 'admin']);
    expect(() => ActionModeSchema.parse('execute')).toThrow();

    const manifests = [
      {
        id: 'capability_codex_cli',
        schemaVersion,
        createdAt,
        name: 'codex-cli',
        kind: 'codex',
        version: 'foundation',
        provider: 'external-process',
        capabilities: ['codex.exec.jsonl'],
        defaultRisk: 'medium',
        defaultActionMode: 'dry-run',
        requiresApprovalByDefault: true,
        evidencePolicy: {
          collect: true,
          redactMetadata: true,
          bodyStorage: 'hash-only',
        },
        processBoundary: {
          mayStartExternalProcess: true,
          requiresProcessAudit: true,
        },
      },
      {
        id: 'capability_nx_affected',
        schemaVersion,
        createdAt,
        name: 'nx-affected',
        kind: 'verification',
        version: 'foundation',
        provider: 'open-source',
        capabilities: ['nx.affected.lint', 'nx.affected.test', 'nx.affected.build'],
        defaultRisk: 'low',
        defaultActionMode: 'read',
        requiresApprovalByDefault: false,
        evidencePolicy: {
          collect: true,
          redactMetadata: true,
          bodyStorage: 'hash-only',
        },
        processBoundary: {
          mayStartExternalProcess: true,
          requiresProcessAudit: true,
        },
      },
      {
        id: 'capability_mcp_server',
        schemaVersion,
        createdAt,
        name: 'codexhub-mcp-server',
        kind: 'mcp',
        version: 'foundation',
        provider: 'official-sdk',
        capabilities: ['codexhub.getPolicySummary'],
        defaultRisk: 'low',
        defaultActionMode: 'read',
        requiresApprovalByDefault: false,
        evidencePolicy: {
          collect: true,
          redactMetadata: true,
          bodyStorage: 'forbidden',
        },
        processBoundary: {
          mayStartExternalProcess: false,
          requiresProcessAudit: false,
        },
      },
      {
        id: 'capability_browser_observer',
        schemaVersion,
        createdAt,
        name: 'playwright-observer',
        kind: 'browser',
        version: 'foundation',
        provider: 'open-source',
        capabilities: ['browser.observe.title', 'browser.observe.aria'],
        defaultRisk: 'medium',
        defaultActionMode: 'read',
        requiresApprovalByDefault: false,
        evidencePolicy: {
          collect: true,
          redactMetadata: true,
          bodyStorage: 'forbidden',
        },
        processBoundary: {
          mayStartExternalProcess: true,
          requiresProcessAudit: true,
        },
      },
      {
        id: 'capability_electron_cdp',
        schemaVersion,
        createdAt,
        name: 'electron-cdp',
        kind: 'electron',
        version: 'foundation',
        provider: 'open-source',
        capabilities: ['electron.target.summary'],
        defaultRisk: 'medium',
        defaultActionMode: 'read',
        requiresApprovalByDefault: false,
        evidencePolicy: {
          collect: true,
          redactMetadata: true,
          bodyStorage: 'forbidden',
        },
        processBoundary: {
          mayStartExternalProcess: false,
          requiresProcessAudit: true,
        },
      },
    ];

    for (const manifest of manifests) {
      expect(CapabilityManifestSchema.parse(manifest).name).toBe(manifest.name);
    }

    const dryRun = CapabilityDryRunSchema.parse({
      id: 'capability_dry_run_1',
      schemaVersion,
      createdAt,
      adapterName: 'codex-cli',
      inputSummary: {
        inputHash: 'sha256:prompt',
      },
      plannedActions: [
        {
          action: 'codex.exec.jsonl',
          actionMode: 'dry-run',
          risk: 'medium',
          target: 'workspace',
          requiresApproval: false,
        },
      ],
      requiredEvidence: ['dry-run-plan'],
      warnings: ['process boundary is planned only'],
    });

    const authority = ExecutionAuthoritySchema.parse({
      id: 'execution_authority_1',
      schemaVersion,
      createdAt,
      policyDecisionId: 'policy_1',
      approvalArtifactId: 'approval_1',
      allowed: true,
      constraints: ['read-only sandbox'],
      expiresAt: '2026-05-28T00:00:00.000Z',
    });

    const results = ['blocked', 'completed', 'failed', 'aborted'].map((status) =>
      CapabilityExecutionResultSchema.parse({
        id: `capability_result_${status}`,
        schemaVersion,
        createdAt,
        status,
        processBoundaryInvoked: status !== 'blocked',
        externalProcessStarted: status !== 'blocked',
        noRealWrite: true,
        evidenceRefs: ['evidence_1'],
        auditEventIds: ['audit_1'],
        summary: `${status} result`,
      }),
    );

    expect(dryRun.plannedActions[0]?.actionMode).toBe('dry-run');
    expect(authority.policyDecisionId).toBe('policy_1');
    expect(results.map((result) => result.status)).toEqual([
      'blocked',
      'completed',
      'failed',
      'aborted',
    ]);
    expect(results[0]?.externalProcessStarted).toBe(false);
    expect(results[1]?.processBoundaryInvoked).toBe(true);
  });

  it('parses read-only MCP tool contracts and evidence kinds', () => {
    expect(McpToolNameSchema.options).toEqual([
      'codexhub.getArchitectureMap',
      'codexhub.getPolicySummary',
      'codexhub.getRiskMatrix',
      'codexhub.getEvidenceSummary',
      'codexhub.getOpenDevelopmentRequests',
      'codexhub.getAffectedProjectsDryRun',
      'codexhub.readObservationSnapshot',
    ]);

    const definition = McpToolDefinitionSchema.parse({
      id: 'mcp_tool_get_architecture',
      schemaVersion,
      createdAt,
      name: 'codexhub.getArchitectureMap',
      title: 'Get architecture map',
      description: 'Return read-only project architecture metadata.',
      enabled: true,
      riskLevel: 'low',
      actionMode: 'read',
      approvalPolicy: 'not-required',
      evidencePolicy: {
        collect: true,
        redactMetadata: true,
        bodyStorage: 'hash-only',
      },
    });
    const invocation = McpToolInvocationSummarySchema.parse({
      id: 'mcp_invocation_1',
      schemaVersion,
      createdAt,
      toolName: definition.name,
      status: 'completed',
      policyDecisionId: 'policy_mcp',
      evidenceRefIds: ['evidence_mcp'],
      auditEventIds: ['audit_mcp'],
      inputHash: 'sha256:input',
      outputHash: 'sha256:output',
      summary: 'Read-only MCP tool completed.',
    });
    const manifestEvidence = EvidenceRefSchema.parse({
      id: 'evidence_mcp_manifest',
      schemaVersion,
      createdAt,
      kind: 'mcp.tool_manifest',
      hash: 'sha256:manifest',
    });
    const invocationEvidence = EvidenceRefSchema.parse({
      id: 'evidence_mcp_invocation',
      schemaVersion,
      createdAt,
      kind: 'mcp.tool_invocation_summary',
      hash: 'sha256:invocation',
    });

    expect(definition.actionMode).toBe('read');
    expect(invocation.bodyStored).toBe(false);
    expect(invocation.rawPathStored).toBe(false);
    expect(manifestEvidence.kind).toBe('mcp.tool_manifest');
    expect(invocationEvidence.kind).toBe('mcp.tool_invocation_summary');
    expect(() =>
      EvidenceRefSchema.parse({
        id: 'evidence_bad',
        schemaVersion,
        createdAt,
        kind: 'mcp.tool_raw_body',
        hash: 'sha256:bad',
      }),
    ).toThrow();
  });

  it('parses browser read-only observation contracts without raw bodies or paths', () => {
    expect(BrowserObservationCapabilitySchema.options).toEqual([
      'title',
      'url',
      'accessibility_snapshot',
      'console_summary',
      'network_metadata_summary',
    ]);
    expect(BrowserObservationRunnerModeSchema.options).toEqual([
      'fixture',
      'controlled-local-browser',
    ]);
    expect(BrowserForbiddenActionSchema.options).toContain('cookie_extraction');
    expect(BrowserObservationRunStatusSchema.options).toEqual([
      'planned',
      'completed',
      'failed',
      'blocked',
      'aborted',
    ]);

    const profileRef = BrowserProfileRefSchema.parse({
      id: 'browser_profile_ref_1',
      schemaVersion,
      createdAt,
      profileId: 'default',
      displayName: 'Default profile',
      profilePathHash: 'sha256:profile-path',
      rawPathStored: false,
      readOnly: true,
    });
    const readiness = BrowserProfileReadinessSchema.parse({
      id: 'browser_profile_readiness_1',
      schemaVersion,
      observedAt: createdAt,
      profileRef,
      status: 'blocked',
      blockReasons: ['profile_probe_disabled'],
      allowedCapabilities: ['title', 'url'],
      forbiddenActions: ['click', 'type', 'cookie_extraction'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Profile readiness is metadata-only and probe-disabled.',
    });
    const plan = BrowserPageObservationPlanSchema.parse({
      id: 'browser_observation_plan_1',
      schemaVersion,
      createdAt,
      adapterName: 'playwright-observer',
      profileRef,
      runnerMode: 'fixture',
      requestedCapabilities: ['title', 'accessibility_snapshot', 'console_summary'],
      forbiddenActions: ['screenshot', 'network_body', 'click'],
      blockReasons: [],
      screenshotPlanned: false,
      networkBodyStorage: 'forbidden',
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Plan browser read-only observation with fixture runner only.',
    });
    const controlledPlan = BrowserPageObservationPlanSchema.parse({
      ...plan,
      id: 'browser_observation_plan_controlled_1',
      runnerMode: 'controlled-local-browser',
      targetUrlHash: 'sha256:target-url',
      processBoundaryPlanned: true,
      summary: 'Plan browser read-only observation with controlled local browser runner.',
    });
    const pageSummary = BrowserPageObservationSummarySchema.parse({
      id: 'browser_page_summary_1',
      schemaVersion,
      observedAt: createdAt,
      source: 'playwright-observer.fixture',
      kind: 'browser.page.summary',
      summary: 'Fixture page observation completed.',
      severity: 'info',
      planId: plan.id,
      profileRef,
      titleObserved: true,
      pageTitleHash: 'sha256:title',
      urlObserved: true,
      pageUrlHash: 'sha256:url',
      accessibilitySnapshotHash: 'sha256:aria',
      accessibilityNodeCount: 3,
      consoleSummary: {
        messageCount: 2,
        warningCount: 1,
        errorCount: 0,
        bodyStored: false,
      },
      networkSummary: {
        requestCount: 4,
        responseCount: 4,
        failedRequestCount: 0,
        bodyStored: false,
      },
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
    });
    const controlledPageSummary = BrowserPageObservationSummarySchema.parse({
      ...pageSummary,
      id: 'browser_page_summary_controlled_1',
      source: 'playwright-observer.controlled-local-browser',
      planId: controlledPlan.id,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
    });
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_browser_summary',
      schemaVersion,
      createdAt,
      kind: 'browser.observation_summary',
      hash: 'sha256:browser-summary',
    });
    const run = BrowserObservationRunSchema.parse({
      id: 'browser_observation_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan,
      readiness,
      pageSummary,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_browser_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Browser observation fixture run completed.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_browser_1',
      schemaVersion,
      createdAt,
      actionId: controlledPlan.id,
      actionType: 'browser.observe.read_only',
      actionMode: 'read',
      riskLevel: 'high',
      outcome: 'approval_required',
      requiresDryRun: false,
      requiresApproval: true,
    });
    const capabilityDryRun = CapabilityDryRunSchema.parse({
      id: 'capability_dry_run_browser_1',
      schemaVersion,
      createdAt,
      adapterName: 'playwright-observer',
      inputSummary: {
        targetUrlHash: controlledPlan.targetUrlHash,
        rawPathStored: false,
        bodyStored: false,
      },
      plannedActions: [
        {
          action: 'browser.observe.read_only',
          actionMode: 'read',
          risk: 'high',
          target: profileRef.profilePathHash,
          requiresApproval: true,
        },
      ],
    });
    const timelineEvent = BrowserObservationTimelineEventSchema.parse({
      id: 'browser_timeline_1',
      schemaVersion,
      createdAt,
      phase: 'dry-run',
      status: 'planned',
      summary: 'Browser observation dry-run persisted.',
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_browser_1'],
      bodyStored: false,
      rawPathStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
    });
    const dryRunRecord = BrowserObservationDryRunRecordSchema.parse({
      id: 'browser_dry_run_record_1',
      schemaVersion,
      createdAt,
      dryRunId: 'browser_dry_run_1',
      status: 'ready',
      plan: controlledPlan,
      capabilityDryRun,
      policyDecision,
      targetUrlHash: controlledPlan.targetUrlHash,
      blockReasons: [],
      timeline: [timelineEvent],
      evidenceRefs: [evidence],
      auditEventIds: ['audit_browser_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryPlanned: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Browser observation control-plane dry-run persisted.',
    });
    const approvalRecord = BrowserObservationApprovalArtifactRecordSchema.parse({
      id: 'browser_approval_record_1',
      schemaVersion,
      createdAt,
      dryRunId: dryRunRecord.dryRunId,
      dryRunRecordId: dryRunRecord.id,
      approvalRequestId: 'browser_approval_request_1',
      approvalArtifactId: 'browser_approval_artifact_1',
      status: 'approved',
      requestedBy: 'local-operator',
      decidedBy: 'local-operator',
      dryRunPlanHash: 'sha256:dry-run',
      policyDecisionId: policyDecision.id,
      policyDecisionHash: 'sha256:policy',
      approved: true,
      requestedAt: createdAt,
      decidedAt: createdAt,
      expiresAt: '2026-04-28T01:00:00.000Z',
      evidenceRefs: [evidence],
      auditEventIds: ['audit_browser_approval_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Browser observation approval stores hashes and ids only.',
    });
    const controlRun = BrowserObservationControlPlaneRunSchema.parse({
      id: 'browser_control_run_1',
      schemaVersion,
      createdAt,
      dryRunId: dryRunRecord.dryRunId,
      dryRunRecordId: dryRunRecord.id,
      approvalArtifactId: approvalRecord.approvalArtifactId,
      status: 'completed',
      planId: controlledPlan.id,
      targetUrlHash: controlledPlan.targetUrlHash,
      browserRun: {
        ...run,
        id: 'browser_observation_run_controlled_1',
        plan: controlledPlan,
        pageSummary: controlledPageSummary,
        processBoundaryInvoked: true,
        externalProcessStarted: true,
      },
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_browser_run_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      summary: 'Browser observation control-plane run completed.',
    });

    expect(run.status).toBe('completed');
    expect(BrowserObservationControlPlaneApprovalStatusSchema.options).toContain('used');
    expect(dryRunRecord.targetUrlHash).toBe('sha256:target-url');
    expect(approvalRecord.approved).toBe(true);
    expect(controlRun.processBoundaryInvoked).toBe(true);
    expect(run.pageSummary?.pageUrlHash).toBe('sha256:url');
    expect(controlledPlan.processBoundaryPlanned).toBe(true);
    expect(controlledPageSummary.externalProcessStarted).toBe(true);
    expect(run.rawPathStored).toBe(false);
    expect(JSON.stringify({ dryRunRecord, approvalRecord, controlRun })).not.toContain(
      'http://localhost',
    );
    expect(JSON.stringify({ dryRunRecord, approvalRecord, controlRun })).not.toContain(
      'session=secret',
    );
    expect(() =>
      BrowserProfileRefSchema.parse({
        ...profileRef,
        profilePath: 'C:\\Users\\Thomas\\AppData\\Local\\Chrome\\Default',
      }),
    ).toThrow();
    expect(() =>
      BrowserPageObservationSummarySchema.parse({
        ...pageSummary,
        rawUrl: 'https://example.test/?token=secret',
      }),
    ).toThrow();
    expect(() =>
      BrowserPageObservationSummarySchema.parse({
        ...pageSummary,
        cookies: ['session=secret'],
      }),
    ).toThrow();
  });

  it('parses electron cdp read-only contracts without raw paths or live actions', () => {
    expect(ElectronProcessKindSchema.options).toContain('renderer');
    expect(ElectronTargetTypeSchema.options).toContain('webview');
    expect(ElectronCdpObservationCapabilitySchema.options).toEqual([
      'process_summary',
      'debug_endpoint_summary',
      'target_summary',
      'console_summary',
      'network_metadata_summary',
    ]);
    expect(ElectronCdpForbiddenActionSchema.options).toContain('runtime_evaluate');
    expect(ElectronCdpBlockReasonSchema.options).toContain(
      'non_loopback_endpoint_forbidden',
    );
    expect(ElectronCdpAllowedCommandSchema.options).toEqual([
      'Browser.getVersion',
      'Target.getTargets',
      'Log.enable',
      'Runtime.enable',
      'Network.enable',
    ]);
    expect(ElectronCdpObservationRunStatusSchema.options).toEqual([
      'planned',
      'completed',
      'failed',
      'blocked',
      'aborted',
    ]);
    expect(OsProcessObservedKindSchema.options).toContain('codex-desktop');
    expect(OsProcessObservationStatusSchema.options).toContain('observed');
    expect(CodexDesktopHealthStatusSchema.options).toEqual([
      'ready',
      'degraded',
      'blocked',
      'unknown',
    ]);
    expect(CodexDesktopDiagnosticHintSchema.options).toContain('desktop_ui_frozen');

    const processSummary = ElectronProcessSummarySchema.parse({
      id: 'electron_process_summary_1',
      schemaVersion,
      createdAt,
      processIdHash: 'sha256:pid',
      executablePathHash: 'sha256:exe-path',
      commandLineHash: 'sha256:argv',
      processKind: 'renderer',
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Electron process metadata stores hashes only.',
    });
    const osProcessSummary = OsProcessMetadataSummarySchema.parse({
      id: 'os_process_summary_1',
      schemaVersion,
      observedAt: createdAt,
      observedKind: 'codex-desktop',
      processNameHash: 'sha256:codex',
      processIdHash: 'sha256:pid',
      executablePathHash: 'sha256:exe-path',
      commandLineHash: 'sha256:argv',
      allowlistMatched: true,
      status: 'observed',
      cpuSampleCount: 1,
      cpuPercentRounded: 2.5,
      memoryBytesRounded: 128_000_000,
      durationMs: 4_000,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'OS process observer stores hashes and rounded metrics only.',
    });
    const endpoint = ElectronDebugEndpointSummarySchema.parse({
      id: 'electron_endpoint_summary_1',
      schemaVersion,
      createdAt,
      endpointIdHash: 'sha256:endpoint',
      hostHash: 'sha256:loopback-host',
      portHash: 'sha256:9222',
      protocol: 'cdp',
      loopbackOnly: true,
      userEnabled: true,
      mainInspectorEnabled: false,
      runtimeEvaluateAllowed: false,
      genericCommandPassthrough: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Loopback Electron debug endpoint summary.',
    });
    const target = ElectronTargetSummarySchema.parse({
      id: 'electron_target_summary_1',
      schemaVersion,
      observedAt: createdAt,
      endpointIdHash: endpoint.endpointIdHash,
      targetIdHash: 'sha256:target',
      targetType: 'webview',
      titleHash: 'sha256:title',
      urlHash: 'sha256:url',
      attached: false,
      mainInspector: false,
      runtimeEvaluateAllowed: false,
      genericCommandPassthrough: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Renderer target summary is metadata-only.',
    });
    const commandDecision = ElectronCdpCommandAllowlistDecisionSchema.parse({
      id: 'electron_command_decision_1',
      schemaVersion,
      createdAt,
      command: 'Target.getTargets',
      allowed: true,
      riskLevel: 'low',
      reason: 'Read-only target listing is allowlisted.',
      runtimeEvaluateAllowed: false,
      genericCommandPassthrough: false,
      bodyStored: false,
      noRealWrite: true,
    });
    const consoleSummary = ElectronCdpConsoleSummarySchema.parse({
      messageCount: 2,
      warningCount: 1,
      errorCount: 0,
      bodyStored: false,
    });
    const networkSummary = ElectronCdpNetworkMetadataSummarySchema.parse({
      requestCount: 3,
      responseCount: 3,
      failedRequestCount: 0,
      bodyStored: false,
    });
    const eventSummary = ElectronCdpEventMetadataSummarySchema.parse({
      observationWindowMs: 5_000,
      eventCount: 2,
      consoleEventCount: 1,
      networkEventCount: 1,
      payloadHashes: ['sha256:event'],
      bodyStored: false,
      rawPathStored: false,
      noRealWrite: true,
    });
    const plan = ElectronCdpObservationPlanSchema.parse({
      id: 'electron_observation_plan_1',
      schemaVersion,
      createdAt,
      adapterName: 'electron-cdp',
      processSummary,
      debugEndpoint: endpoint,
      targets: [target],
      requestedCapabilities: ['target_summary', 'console_summary'],
      forbiddenActions: ['runtime_evaluate', 'generic_cdp_command'],
      blockReasons: [],
      commandDecisions: [commandDecision],
      mainInspectorEnabled: false,
      runtimeEvaluateAllowed: false,
      genericCommandPassthrough: false,
      screenshotPlanned: false,
      domSnapshotPlanned: false,
      networkBodyStorage: 'forbidden',
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Plan Electron/CDP fixture-only read-only observation.',
    });
    const observationSummary = ElectronCdpObservationSummarySchema.parse({
      id: 'electron_observation_summary_1',
      schemaVersion,
      observedAt: createdAt,
      source: 'electron-cdp.fixture',
      kind: 'electron.cdp.summary',
      severity: 'info',
      planId: plan.id,
      processSummary,
      debugEndpoint: endpoint,
      targets: [target],
      consoleSummary,
      networkSummary,
      eventSummary,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Fixture Electron/CDP observation completed.',
    });
    const desktopHealth = CodexDesktopHealthSnapshotSchema.parse({
      id: 'codex_desktop_health_1',
      schemaVersion,
      observedAt: createdAt,
      status: 'degraded',
      processSummary,
      osProcessSummary,
      debugEndpoint: endpoint,
      targetCount: 1,
      consoleErrorCount: 1,
      networkFailedRequestCount: 0,
      appServerResponsive: false,
      desktopUiResponsive: false,
      quotaAvailable: false,
      loggedIn: true,
      accountMatched: true,
      workspaceMatched: true,
      diagnosticHints: [
        'desktop_ui_frozen',
        'app_server_unresponsive',
        'quota_depleted',
      ],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Codex Desktop health uses diagnosis hints only.',
    });
    const evidence = EvidenceRefSchema.parse({
      id: 'electron_evidence_summary_1',
      schemaVersion,
      createdAt,
      kind: 'electron.observation_summary',
      hash: 'sha256:electron-summary',
    });
    const run = ElectronCdpObservationRunSchema.parse({
      id: 'electron_observation_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan,
      observationSummary,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_electron_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Electron/CDP fixture observation run completed.',
    });

    expect(run.status).toBe('completed');
    expect(plan.mainInspectorEnabled).toBe(false);
    expect(plan.runtimeEvaluateAllowed).toBe(false);
    expect(plan.genericCommandPassthrough).toBe(false);
    expect(target.urlHash).toBe('sha256:url');
    expect(desktopHealth.diagnosticHints).toContain('desktop_ui_frozen');
    expect(JSON.stringify({ run, desktopHealth })).not.toContain('C:\\');
    expect(JSON.stringify(run)).not.toContain('Runtime.evaluate');
    expect(() =>
      OsProcessMetadataSummarySchema.parse({
        ...osProcessSummary,
        executablePath: 'C:\\Users\\Thomas\\AppData\\Local\\Codex\\Codex.exe',
      }),
    ).toThrow();
    expect(() =>
      CodexDesktopHealthSnapshotSchema.parse({
        ...desktopHealth,
        rawPath: 'C:\\Users\\Thomas\\AppData\\Local\\Codex\\Codex.exe',
      }),
    ).toThrow();
    expect(() =>
      ElectronProcessSummarySchema.parse({
        ...processSummary,
        executablePath: 'C:\\Users\\Thomas\\AppData\\Local\\Codex\\Codex.exe',
      }),
    ).toThrow();
    expect(() =>
      ElectronDebugEndpointSummarySchema.parse({
        ...endpoint,
        loopbackOnly: false,
      }),
    ).toThrow();
    expect(() =>
      ElectronCdpObservationPlanSchema.parse({
        ...plan,
        mainInspectorEnabled: true,
      }),
    ).toThrow();
    expect(() =>
      ElectronCdpCommandAllowlistDecisionSchema.parse({
        ...commandDecision,
        id: 'electron_command_decision_bad',
        command: 'Runtime.evaluate',
        runtimeEvaluateAllowed: true,
      }),
    ).toThrow();
    expect(() =>
      ElectronTargetSummarySchema.parse({
        ...target,
        rawUrl: 'devtools://devtools/bundled/inspector.html?token=secret',
      }),
    ).toThrow();
    expect(() =>
      EvidenceRefSchema.parse({
        id: 'electron_evidence_bad',
        schemaVersion,
        createdAt,
        kind: 'electron.raw_body',
        hash: 'sha256:bad',
      }),
    ).toThrow();
  });

  it('parses electron cdp controlled HTTP control-plane records as metadata only', () => {
    expect(ElectronCdpObservationRunnerModeSchema.options).toEqual([
      'fixture',
      'controlled-local-http',
      'controlled-websocket-events',
    ]);
    expect(ElectronCdpControlPlaneApprovalStatusSchema.options).toEqual([
      'requested',
      'approved',
      'denied',
      'expired',
      'used',
      'revoked',
    ]);

    const endpoint = ElectronDebugEndpointSummarySchema.parse({
      id: 'electron_endpoint_summary_control_1',
      schemaVersion,
      createdAt,
      endpointIdHash: 'sha256:endpoint',
      hostHash: 'sha256:host',
      portHash: 'sha256:port',
      protocol: 'cdp',
      loopbackOnly: true,
      userEnabled: true,
      mainInspectorEnabled: false,
      runtimeEvaluateAllowed: false,
      genericCommandPassthrough: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Loopback endpoint metadata only.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_electron_control_1',
      schemaVersion,
      createdAt,
      actionId: 'electron_cdp_observe_1',
      actionType: 'electron.cdp.observe.read_only',
      actionMode: 'read',
      riskLevel: 'medium',
      outcome: 'allow',
      reasons: ['read-only with explicit approval for live local HTTP'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const evidence = EvidenceRefSchema.parse({
      id: 'electron_evidence_control_1',
      schemaVersion,
      createdAt,
      kind: 'electron.observation_plan',
      hash: 'sha256:evidence',
    });
    const plan = ElectronCdpObservationPlanSchema.parse({
      id: 'electron_observation_plan_control_1',
      schemaVersion,
      createdAt,
      adapterName: 'electron-cdp',
      runnerMode: 'controlled-local-http',
      debugEndpoint: endpoint,
      requestedCapabilities: ['debug_endpoint_summary', 'target_summary'],
      forbiddenActions: [],
      blockReasons: [],
      commandDecisions: [],
      mainInspectorEnabled: false,
      runtimeEvaluateAllowed: false,
      genericCommandPassthrough: false,
      screenshotPlanned: false,
      domSnapshotPlanned: false,
      networkBodyStorage: 'forbidden',
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      cdpHttpBoundaryPlanned: true,
      cdpHttpBoundaryInvoked: false,
      processBoundaryPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Controlled HTTP Electron/CDP dry-run stores endpoint hashes only.',
    });
    const timelineEvent = ElectronCdpObservationTimelineEventSchema.parse({
      id: 'electron_timeline_control_1',
      schemaVersion,
      createdAt,
      phase: 'dry-run',
      status: 'planned',
      summary: 'Electron/CDP dry-run planned.',
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_electron_control_1'],
      bodyStored: false,
      rawPathStored: false,
      noRealWrite: true,
      cdpHttpBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
    });
    const dryRunRecord = ElectronCdpObservationDryRunRecordSchema.parse({
      id: 'electron_dry_run_record_1',
      schemaVersion,
      createdAt,
      dryRunId: 'electron_dry_run_1',
      status: 'ready',
      plan,
      capabilityDryRun: {
        id: 'electron_capability_dry_run_1',
        schemaVersion,
        createdAt,
        adapterName: 'electron-cdp',
        inputSummary: { endpointIdHash: endpoint.endpointIdHash },
        plannedActions: [
          {
            action: 'electron.cdp.observe.read_only',
            actionMode: 'read',
            risk: 'medium',
            target: endpoint.endpointIdHash,
            requiresApproval: true,
          },
        ],
      },
      policyDecision,
      endpointIdHash: endpoint.endpointIdHash,
      endpointHostHash: endpoint.hostHash,
      endpointPortHash: endpoint.portHash,
      blockReasons: [],
      timeline: [timelineEvent],
      evidenceRefs: [evidence],
      auditEventIds: ['audit_electron_control_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      cdpHttpBoundaryPlanned: true,
      cdpHttpBoundaryInvoked: false,
      processBoundaryPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Electron/CDP dry-run record stores hashes only.',
    });
    const approvalRecord = ElectronCdpObservationApprovalArtifactRecordSchema.parse({
      id: 'electron_approval_record_1',
      schemaVersion,
      createdAt,
      dryRunId: dryRunRecord.dryRunId,
      dryRunRecordId: dryRunRecord.id,
      approvalRequestId: 'electron_approval_request_1',
      approvalArtifactId: 'electron_approval_artifact_1',
      status: 'approved',
      requestedBy: 'local-operator',
      decidedBy: 'local-operator',
      dryRunPlanHash: 'sha256:dry-run',
      policyDecisionId: policyDecision.id,
      policyDecisionHash: 'sha256:policy',
      approved: true,
      requestedAt: createdAt,
      decidedAt: createdAt,
      expiresAt: '2026-04-28T01:00:00.000Z',
      evidenceRefs: [evidence],
      auditEventIds: ['audit_electron_approval_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      cdpHttpBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Electron/CDP approval stores hashes and ids only.',
    });
    const runRecord = ElectronCdpObservationControlPlaneRunSchema.parse({
      id: 'electron_control_run_1',
      schemaVersion,
      createdAt,
      dryRunId: dryRunRecord.dryRunId,
      dryRunRecordId: dryRunRecord.id,
      approvalArtifactId: approvalRecord.approvalArtifactId,
      status: 'completed',
      planId: plan.id,
      endpointIdHash: endpoint.endpointIdHash,
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_electron_run_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      cdpHttpBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Electron/CDP controlled HTTP run completed.',
    });
    const serialized = JSON.stringify({ dryRunRecord, approvalRecord, runRecord });

    expect(dryRunRecord.cdpHttpBoundaryPlanned).toBe(true);
    expect(approvalRecord.approved).toBe(true);
    expect(runRecord.cdpHttpBoundaryInvoked).toBe(true);
    expect(runRecord.processBoundaryInvoked).toBe(false);
    expect(serialized).not.toContain('127.0.0.1');
    expect(serialized).not.toContain('localhost');
    expect(serialized).not.toContain('ws://');
    expect(serialized).not.toContain('session=secret');
    expect(() =>
      ElectronCdpObservationDryRunRecordSchema.parse({
        ...dryRunRecord,
        host: '127.0.0.1',
      }),
    ).toThrow();
    expect(() =>
      ElectronCdpObservationControlPlaneRunSchema.parse({
        ...runRecord,
        body: '{"targets":[]}',
      }),
    ).toThrow();
  });

  it('requires capability audit events to carry authority context', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_capability_1',
      schemaVersion,
      createdAt,
      kind: 'audit',
      hash: 'sha256:evidence',
      summary: 'Capability audit evidence',
    });

    const audit = CapabilityAuditEventSchema.parse({
      id: 'audit_capability_1',
      schemaVersion,
      createdAt,
      actor: 'supervisor',
      action: 'capability.execute',
      target: 'codex-cli',
      reason: 'execution authority accepted by workflow gate',
      outcome: 'completed',
      evidenceRefs: [evidence],
      policyDecisionId: 'policy_1',
    });

    expect(audit.target).toBe('codex-cli');
    expect(() =>
      CapabilityAuditEventSchema.parse({
        id: 'audit_capability_invalid',
        schemaVersion,
        createdAt,
        actor: 'supervisor',
        action: 'capability.execute',
        outcome: 'completed',
        evidenceRefs: [evidence],
      }),
    ).toThrow();
  });

  it('parses M6a worktree, patch, PR, and release draft contracts as metadata-only records', () => {
    const worktreeEvidence = EvidenceRefSchema.parse({
      id: 'evidence_worktree_1',
      schemaVersion,
      createdAt,
      kind: 'worktree.plan',
      hash: 'sha256:worktree-plan',
    });
    const patchEvidence = EvidenceRefSchema.parse({
      id: 'evidence_patch_1',
      schemaVersion,
      createdAt,
      kind: 'patch.diff_summary',
      hash: 'sha256:patch',
    });
    const prEvidence = EvidenceRefSchema.parse({
      id: 'evidence_pr_1',
      schemaVersion,
      createdAt,
      kind: 'pr.draft_summary',
      hash: 'sha256:pr',
    });
    const releaseEvidence = EvidenceRefSchema.parse({
      id: 'evidence_release_1',
      schemaVersion,
      createdAt,
      kind: 'release.audit_draft',
      hash: 'sha256:release',
    });

    const plan = WorktreePlanSchema.parse({
      id: 'worktree_plan_1',
      schemaVersion,
      createdAt,
      adapterName: 'worktree-manager',
      status: 'planned',
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      branchNameHash: 'sha256:branch',
      worktreeSlugHash: 'sha256:slug',
      defaultRootKind: 'sibling',
      plannedActions: [
        {
          action: 'git.worktree.plan',
          actionMode: 'dry-run',
          risk: 'medium',
          target: 'sha256:path',
          requiresApproval: false,
        },
      ],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Plan a sibling worktree without invoking git.',
    });
    const run = WorktreeRunSchema.parse({
      id: 'worktree_run_1',
      schemaVersion,
      createdAt,
      planId: plan.id,
      status: 'completed',
      worktreePathHash: 'sha256:path',
      branchNameHash: 'sha256:branch',
      changedFiles: ['packages/contracts/src/index.ts'],
      changedFileCount: 1,
      diffHash: 'sha256:diff',
      evidenceRefs: [worktreeEvidence],
      auditEventIds: ['audit_worktree_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Fixture worktree run completed.',
    });
    const patchRun = PatchRunSchema.parse({
      id: 'patch_run_1',
      schemaVersion,
      createdAt,
      taskId: 'task_1',
      status: 'blocked',
      changedFiles: ['packages/contracts/src/index.ts'],
      diffHash: 'sha256:diff',
      worktreePathHash: 'sha256:path',
      evidenceRefs: [patchEvidence],
      auditEventIds: ['audit_patch_1'],
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Patch is blocked until verification passes.',
    });
    const patchSummary = PatchSummarySchema.parse({
      id: 'patch_summary_1',
      schemaVersion,
      createdAt,
      patchRunId: patchRun.id,
      changedFiles: ['packages/contracts/src/index.ts'],
      changedFileCount: 1,
      diffHash: 'sha256:diff',
      diffLineCount: 12,
      evidenceRefs: [patchEvidence],
      auditEventIds: ['audit_patch_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Patch summary stores the diff hash only.',
    });
    const prDraft = PullRequestSummaryDraftSchema.parse({
      id: 'pr_draft_1',
      schemaVersion,
      createdAt,
      status: 'blocked',
      titleHash: 'sha256:title',
      bodyHash: 'sha256:body',
      sectionCount: 4,
      changedFiles: ['packages/contracts/src/index.ts'],
      evidenceRefs: [prEvidence],
      auditEventIds: ['audit_pr_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'PR draft blocked because verification has not passed.',
    });
    const releaseDraft = ReleaseAuditDraftSchema.parse({
      id: 'release_audit_draft_1',
      schemaVersion,
      createdAt,
      status: 'blocked',
      verificationStatus: 'blocked',
      rollbackNotesHash: 'sha256:rollback',
      riskNotesHash: 'sha256:risk',
      evidenceRefs: [releaseEvidence],
      auditEventIds: ['audit_release_1'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Release audit draft is metadata-only.',
    });

    expect(run.processBoundaryInvoked).toBe(false);
    expect(patchSummary.bodyStored).toBe(false);
    expect(prDraft.status).toBe('blocked');
    expect(releaseDraft.noRealWrite).toBe(true);
    expect(() =>
      WorktreePlanSchema.parse({
        ...plan,
        rawWorktreePath: 'C:\\Users\\Thomas\\CodexHub-worktrees\\feature',
      }),
    ).toThrow();
    expect(() =>
      PatchSummarySchema.parse({
        ...patchSummary,
        rawDiffBody: 'diff --git a/secret b/secret',
      }),
    ).toThrow();
    expect(() =>
      PullRequestSummaryDraftSchema.parse({
        ...prDraft,
        rawPrBody: 'Full PR markdown body',
      }),
    ).toThrow();
    expect(() =>
      WorktreeRunSchema.parse({
        ...run,
        changedFiles: ['../outside.ts'],
      }),
    ).toThrow();
  });

  it('parses M12 controlled patch lifecycle contracts as fixture-only metadata', () => {
    expect(ControlledPatchRejectionReasonSchema.options).toContain('verification_failed');

    const planEvidence = EvidenceRefSchema.parse({
      id: 'evidence_m12_patch_plan',
      schemaVersion,
      createdAt,
      kind: 'patch.lifecycle_plan',
      hash: 'sha256:m12-plan',
    });
    const runEvidence = EvidenceRefSchema.parse({
      id: 'evidence_m12_patch_run',
      schemaVersion,
      createdAt,
      kind: 'patch.lifecycle_run_summary',
      hash: 'sha256:m12-run',
    });
    const reviewEvidence = EvidenceRefSchema.parse({
      id: 'evidence_m12_diff_review',
      schemaVersion,
      createdAt,
      kind: 'patch.diff_review_summary',
      hash: 'sha256:m12-review',
    });
    const readinessEvidence = EvidenceRefSchema.parse({
      id: 'evidence_m12_readiness',
      schemaVersion,
      createdAt,
      kind: 'patch.readiness_summary',
      hash: 'sha256:m12-readiness',
    });
    const plan = ControlledPatchPlanSchema.parse({
      id: 'm12_patch_plan_1',
      schemaVersion,
      createdAt,
      requestIdHash: 'sha256:request',
      worktreeRunIdHash: 'sha256:worktree-run',
      worktreePathHash: 'sha256:worktree-path',
      plannedChangedFileCount: 1,
      plannedChangedFilePathHashes: ['sha256:file'],
      patchBodyHash: 'sha256:patch-body',
      codexPatchAllowed: false,
      fixtureOnly: true,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'M12 fixture patch lifecycle plan stores hashes only.',
    });
    const patchRun = ControlledPatchRunSchema.parse({
      id: 'm12_patch_run_1',
      schemaVersion,
      createdAt,
      planId: plan.id,
      status: 'verified',
      attemptNumber: 1,
      changedFiles: ['packages/contracts/src/index.ts'],
      changedFileCount: 1,
      diffHash: 'sha256:diff',
      diffLineCount: 12,
      diffSummaryHash: 'sha256:diff-summary',
      worktreePathHash: plan.worktreePathHash,
      rejectionReasons: ['none'],
      evidenceRefs: [runEvidence],
      auditEventIds: ['audit_m12_patch_run'],
      fixtureOnly: true,
      codexPatchExecuted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'M12 fixture patch run verified without live execution.',
    });
    const diffReview = DiffReviewSummarySchema.parse({
      id: 'm12_diff_review_1',
      schemaVersion,
      createdAt,
      patchRunId: patchRun.id,
      status: 'passed',
      changedFileCount: 1,
      diffHash: patchRun.diffHash,
      diffLineCount: patchRun.diffLineCount,
      findingCount: 0,
      reviewerLabel: 'fixture-reviewer',
      evidenceRefs: [reviewEvidence],
      auditEventIds: ['audit_m12_diff_review'],
      rawDiffStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Diff review stores counts and hashes only.',
    });
    const readiness = ControlledPatchReadinessSchema.parse({
      id: 'm12_patch_readiness_1',
      schemaVersion,
      createdAt,
      patchRunId: patchRun.id,
      status: 'ready_for_review_draft_only',
      verificationStatus: 'passed',
      changedFileCount: 1,
      blockerCount: 0,
      blockers: [],
      readyForReviewDraftOnly: true,
      pushAllowed: false,
      pullRequestOpened: false,
      evidenceRefs: [readinessEvidence],
      auditEventIds: ['audit_m12_readiness'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Patch is ready only as local PR draft metadata.',
    });
    const lifecycle = ControlledPatchLifecycleRunSchema.parse({
      id: 'm12_patch_lifecycle_run_1',
      schemaVersion,
      createdAt,
      status: 'verified',
      plan,
      patchRun,
      diffReview,
      readiness,
      evidenceRefs: [planEvidence, runEvidence, reviewEvidence, readinessEvidence],
      auditEventIds: [
        'audit_m12_patch_plan',
        'audit_m12_patch_run',
        'audit_m12_diff_review',
        'audit_m12_readiness',
      ],
      evidenceRefIds: [planEvidence.id, runEvidence.id, reviewEvidence.id, readinessEvidence.id],
      auditEventCount: 4,
      fixtureOnly: true,
      codexPatchExecuted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      pushAllowed: false,
      pullRequestOpened: false,
      summary: 'M12 controlled patch lifecycle is fixture-only.',
    });
    const blockedReadiness = ControlledPatchReadinessSchema.parse({
      ...readiness,
      id: 'm12_patch_readiness_blocked',
      status: 'blocked_verification_failed',
      verificationStatus: 'failed',
      blockerCount: 1,
      blockers: ['verification_failed'],
      readyForReviewDraftOnly: false,
    });

    const serialized = JSON.stringify(lifecycle);
    expect(lifecycle.readiness.status).toBe('ready_for_review_draft_only');
    expect(blockedReadiness.status).toBe('blocked_verification_failed');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('Full PR markdown body');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('secret-token');
    expect(() =>
      ControlledPatchLifecycleRunSchema.parse({
        ...lifecycle,
        rawDiffBody: 'diff --git a/private b/private',
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchRunSchema.parse({
        ...patchRun,
        id: 'm12_patch_run_bad_count',
        changedFileCount: 2,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchPlanSchema.parse({
        ...plan,
        id: 'm12_patch_plan_bad_count',
        plannedChangedFileCount: 2,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchReadinessSchema.parse({
        ...readiness,
        id: 'm12_patch_readiness_bad_ready',
        changedFileCount: 0,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchLifecycleRunSchema.parse({
        ...lifecycle,
        patchRun: {
          ...patchRun,
          planId: 'm12_patch_plan_other',
        },
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchLifecycleRunSchema.parse({
        ...lifecycle,
        evidenceRefIds: ['evidence_missing'],
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchPlanSchema.parse({
        ...plan,
        id: 'm12_patch_plan_bad_metadata',
        metadata: { nested: { rawDiff: 'diff --git a/private b/private' } },
      }),
    ).toThrow();
    expect(() =>
      EvidenceRefSchema.parse({
        id: 'evidence_m12_unknown',
        schemaVersion,
        createdAt,
        kind: 'patch.raw_diff_body',
        hash: 'sha256:bad',
      }),
    ).toThrow();
  });

  it('parses M12 governed Codex patch contracts for isolated worktree handoff', () => {
    const planEvidence = EvidenceRefSchema.parse({
      id: 'evidence_codex_patch_plan',
      schemaVersion,
      createdAt,
      kind: 'codex.patch_plan',
      hash: 'sha256:codex-patch-plan',
    });
    const runEvidence = EvidenceRefSchema.parse({
      id: 'evidence_codex_patch_run',
      schemaVersion,
      createdAt,
      kind: 'codex.patch_run_summary',
      hash: 'sha256:codex-patch-run',
    });
    const plan = GovernedCodexPatchPlanSchema.parse({
      id: 'codex_patch_plan_1',
      schemaVersion,
      createdAt,
      mode: 'governed-worktree',
      dryRunIdHash: 'sha256:dry-run',
      policyDecisionIdHash: 'sha256:policy',
      approvalArtifactIdHash: 'sha256:approval',
      worktreeRunIdHash: 'sha256:worktree-run',
      worktreePathHash: 'sha256:worktree-path',
      governedInputHash: 'sha256:governed-input',
      expectedInputHash: 'sha256:expected-input',
      sandboxMode: 'workspace-write-limited',
      writeScope: 'isolated-worktree-only',
      approvalRequired: true,
      persistedApprovalRequired: true,
      hashBoundWorktreeRequired: true,
      repoRootWriteAllowed: false,
      pushAllowed: false,
      pullRequestOpened: false,
      rawPromptStored: false,
      rawPathStored: false,
      bodyStored: false,
      processBoundaryPlanned: true,
      externalProcessStarted: false,
      evidenceRefs: [planEvidence],
      summary: 'Codex patch is governed and limited to the approved isolated worktree.',
    });
    const run = GovernedCodexPatchRunSchema.parse({
      id: 'codex_patch_run_1',
      schemaVersion,
      createdAt,
      planId: plan.id,
      mode: 'governed-worktree',
      status: 'completed',
      changedFiles: ['packages/orchestrator-kernel/src/m12-patch-lifecycle.ts'],
      changedFileCount: 1,
      diffHash: 'sha256:diff',
      diffLineCount: 42,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      codexPatchExecuted: true,
      realWriteExecuted: true,
      writeScope: 'isolated-worktree-only',
      repoRootWriteAllowed: false,
      pushAllowed: false,
      pullRequestOpened: false,
      rawStdoutStored: false,
      rawStderrStored: false,
      rawDiffStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [runEvidence],
      auditEventIds: ['audit_codex_patch_run'],
      summary: 'Codex patch completed inside the isolated worktree only.',
    });
    const serialized = JSON.stringify({ plan, run });

    expect(run.codexPatchExecuted).toBe(true);
    expect(run.realWriteExecuted).toBe(true);
    expect(run.repoRootWriteAllowed).toBe(false);
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('Full PR markdown body');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('secret-token');
    expect(() =>
      GovernedCodexPatchPlanSchema.parse({
        ...plan,
        id: 'codex_patch_plan_bad_boundary',
        processBoundaryPlanned: false,
      }),
    ).toThrow();
    expect(() =>
      GovernedCodexPatchRunSchema.parse({
        ...run,
        id: 'codex_patch_run_bad_count',
        changedFileCount: 2,
      }),
    ).toThrow();
    expect(() =>
      GovernedCodexPatchRunSchema.parse({
        ...run,
        id: 'codex_patch_run_missing_boundary',
        processBoundaryInvoked: false,
      }),
    ).toThrow();
    expect(() =>
      GovernedCodexPatchRunSchema.parse({
        ...run,
        id: 'codex_patch_run_raw_metadata',
        metadata: { nested: { rawDiff: 'diff --git a/private b/private' } },
      }),
    ).toThrow();
  });

  it('parses M12 verification gate contracts and only allows draft readiness after passed verification', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_m12c_verification',
      schemaVersion,
      createdAt,
      kind: 'verification.run_summary',
      hash: 'sha256:m12c-verification',
    });
    const gate = ControlledPatchVerificationGateSchema.parse({
      id: 'm12c_verification_gate_1',
      schemaVersion,
      createdAt,
      patchRunId: 'm12b_patch_run_1',
      lifecycleRunIdHash: 'sha256:lifecycle-run',
      verificationRunId: 'verification_run_1',
      verificationStatus: 'passed',
      targets: ['lint', 'test', 'build'],
      changedFileCount: 2,
      affectedProjectCount: 2,
      commandResultCount: 2,
      readyForReviewDraftOnly: true,
      pushAllowed: false,
      pullRequestOpened: false,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_m12c_verification'],
      metadata: {
        m12c: true,
        commandOutputStored: false,
      },
      summary: 'M12c verification gate passed; local draft readiness is allowed.',
    });
    const serialized = JSON.stringify(gate);

    expect(gate.readyForReviewDraftOnly).toBe(true);
    expect(() =>
      ControlledPatchVerificationGateSchema.parse({
        ...gate,
        id: 'm12c_verification_gate_bad_ready',
        verificationStatus: 'failed',
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchVerificationGateSchema.parse({
        ...gate,
        id: 'm12c_verification_gate_bad_empty_patch',
        changedFileCount: 0,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchVerificationGateSchema.parse({
        ...gate,
        id: 'm12c_verification_gate_raw_metadata',
        metadata: { rawStdout: 'Successfully ran target lint,test,build' },
      }),
    ).toThrow();
    expect(serialized).not.toContain('Successfully ran target');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('C:\\');
    expect(serialized).not.toContain('secret-token');
  });

  it('parses M12 retry cleanup projection contracts as metadata-only handoff state', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_m12d_retry_cleanup',
      schemaVersion,
      createdAt,
      kind: 'patch.readiness_summary',
      hash: 'sha256:m12d-retry-cleanup',
    });
    const projection = ControlledPatchRetryCleanupProjectionSchema.parse({
      id: 'm12d_retry_cleanup_1',
      schemaVersion,
      createdAt,
      sourceLifecycleRunIdHash: 'sha256:lifecycle',
      sourcePatchRunIdHash: 'sha256:patch-run',
      status: 'retry_planned',
      lastSafeStep: 'verification_failed',
      attemptCount: 2,
      previousAttemptHash: 'sha256:previous-attempt',
      retryReasonHash: 'sha256:retry-reason',
      retryRequiresNewApproval: true,
      resumeAllowed: true,
      cleanupRequired: true,
      cleanupReady: true,
      cleanupRequiresApproval: true,
      cleanupForceAllowed: false,
      filesystemDeleteFallbackAllowed: false,
      dirtyWorktree: false,
      dirtyFileCount: 0,
      pushAllowed: false,
      pullRequestOpened: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_m12d_retry_cleanup'],
      metadata: {
        m12d: true,
        retryReasonStored: false,
      },
      summary: 'M12d retry requires a new approval and cleanup remains governed.',
    });
    const serialized = JSON.stringify(projection);

    expect(projection.retryRequiresNewApproval).toBe(true);
    expect(projection.cleanupForceAllowed).toBe(false);
    expect(() =>
      ControlledPatchRetryCleanupProjectionSchema.parse({
        ...projection,
        id: 'm12d_retry_cleanup_dirty_ready',
        dirtyWorktree: true,
        dirtyFileCount: 1,
        dirtySummaryHash: 'sha256:dirty',
        cleanupReady: true,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchRetryCleanupProjectionSchema.parse({
        ...projection,
        id: 'm12d_retry_cleanup_dirty_no_count',
        dirtyWorktree: true,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchRetryCleanupProjectionSchema.parse({
        ...projection,
        id: 'm12d_retry_cleanup_raw_metadata',
        metadata: { worktreePath: 'C:\\private\\worktree' },
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchRetryCleanupProjectionSchema.parse({
        ...projection,
        id: 'm12d_retry_cleanup_first_attempt_retry',
        attemptCount: 1,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchRetryCleanupProjectionSchema.parse({
        ...projection,
        id: 'm12d_retry_cleanup_missing_reason',
        retryReasonHash: undefined,
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchRetryCleanupProjectionSchema.parse({
        ...projection,
        id: 'm12d_retry_cleanup_raw_pr_body',
        metadata: { rawPrBody: '## Summary\nraw pull request body' },
      }),
    ).toThrow();
    expect(() =>
      ControlledPatchRetryCleanupProjectionSchema.parse({
        ...projection,
        id: 'm12d_retry_cleanup_bad_terminal',
        status: 'terminal',
        resumeAllowed: true,
      }),
    ).toThrow();
    expect(serialized).not.toContain('C:\\private');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('secret-token');
    expect(serialized).not.toContain('raw retry reason');
  });

  it('parses M13 local review package contracts as metadata-only projections', () => {
    const plan = LocalReviewPackagePlanSchema.parse({
      id: 'local_review_plan_1',
      schemaVersion,
      createdAt,
      sourceLifecycleRunIdHash: 'sha256:lifecycle',
      sourcePatchRunIdHash: 'sha256:patch-run',
      sourceVerificationGateIdHash: 'sha256:verification-gate',
      changedFileCount: 2,
      changedFilePathHashes: ['sha256:file-a', 'sha256:file-b'],
      diffHash: 'sha256:diff',
      verificationStatus: 'passed',
      readinessStatus: 'ready_for_review_draft_only',
      readyForReviewDraftOnly: true,
      evidenceRefIds: ['evidence_patch', 'evidence_verification'],
      auditEventIds: ['audit_patch', 'audit_verification'],
      fileExportPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      metadata: {
        source: 'm13a',
        rawBodyStored: false,
      },
      summary: 'Local review package plan is metadata-only.',
    });
    const packageSummary = LocalReviewPackageSummarySchema.parse({
      id: 'local_review_summary_1',
      schemaVersion,
      createdAt,
      planId: plan.id,
      status: 'ready_for_review',
      changedFileCount: 2,
      diffHash: 'sha256:diff',
      verificationStatus: 'passed',
      readyForReviewDraftOnly: true,
      evidenceRefCount: 2,
      auditEventCount: 2,
      packageHash: 'sha256:package',
      exported: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Local review package is ready for human review.',
    });
    const finding = LocalReviewFindingSummarySchema.parse({
      id: 'local_review_finding_1',
      schemaVersion,
      createdAt,
      reviewPackageIdHash: 'sha256:package',
      severity: 'info',
      findingCount: 0,
      findingHash: 'sha256:finding',
      rawFindingStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'No blocking findings are recorded.',
    });
    const decision = LocalReviewDecisionProjectionSchema.parse({
      id: 'local_review_decision_1',
      schemaVersion,
      createdAt,
      reviewPackageIdHash: 'sha256:package',
      status: 'pending',
      findingCount: 0,
      blockerCount: 0,
      nextAction: 'none',
      retryHandoffRequired: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Review decision is pending.',
    });
    const run = LocalReviewPackageRunSchema.parse({
      id: 'local_review_run_1',
      schemaVersion,
      createdAt,
      plan,
      packageSummary,
      findings: [finding],
      decision,
      evidenceRefIds: ['evidence_patch', 'evidence_verification'],
      auditEventIds: ['audit_patch', 'audit_verification'],
      exported: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local review package projection is ready.',
    });
    const serialized = JSON.stringify(run);

    expect(run.packageSummary.status).toBe('ready_for_review');
    expect(run.exported).toBe(false);
    expect(() =>
      LocalReviewPackagePlanSchema.parse({
        ...plan,
        id: 'local_review_plan_bad_count',
        changedFileCount: 1,
      }),
    ).toThrow();
    expect(() =>
      LocalReviewPackageSummarySchema.parse({
        ...packageSummary,
        id: 'local_review_summary_bad_ready',
        readyForReviewDraftOnly: false,
      }),
    ).toThrow();
    expect(() =>
      LocalReviewDecisionProjectionSchema.parse({
        ...decision,
        id: 'local_review_decision_bad_approval',
        status: 'approved_for_local_rc',
        blockerCount: 1,
        nextAction: 'local_rc_readiness',
      }),
    ).toThrow();
    expect(() =>
      LocalReviewPackageRunSchema.parse({
        ...run,
        id: 'local_review_run_raw_metadata',
        metadata: { rawReason: 'please change private implementation details' },
      }),
    ).toThrow();
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('pull request body');
    expect(serialized).not.toContain('C:\\');
    expect(serialized).not.toContain('secret-token');
  });

  it('parses M13 governed local review package export records and rejects raw metadata', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_review_export',
      schemaVersion,
      createdAt,
      kind: 'review.package_export_plan',
      hash: 'sha256:review-export-plan',
      redacted: true,
      metadata: {
        artifactRootHash: 'sha256:artifact-root',
        artifactDirectoryHash: 'sha256:artifact-dir',
        rawPathStored: false,
        bodyStored: false,
      },
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_review_export',
      schemaVersion,
      createdAt,
      actionId: 'review_export_dry_run',
      actionType: 'review_package.export_local_artifact',
      actionMode: 'write',
      riskLevel: 'medium',
      outcome: 'approval_required',
      reasons: ['local artifact export requires approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const reviewPlan = LocalReviewPackagePlanSchema.parse({
      id: 'review_export_source_plan',
      schemaVersion,
      createdAt,
      sourceLifecycleRunIdHash: 'sha256:lifecycle',
      sourcePatchRunIdHash: 'sha256:patch',
      changedFileCount: 1,
      changedFilePathHashes: ['sha256:file'],
      diffHash: 'sha256:diff',
      verificationStatus: 'passed',
      readinessStatus: 'ready_for_review_draft_only',
      readyForReviewDraftOnly: true,
      fileExportPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Review export source plan.',
    });
    const reviewPackage = LocalReviewPackageRunSchema.parse({
      id: 'review_export_source_package',
      schemaVersion,
      createdAt,
      plan: reviewPlan,
      packageSummary: LocalReviewPackageSummarySchema.parse({
        id: 'review_export_source_summary',
        schemaVersion,
        createdAt,
        planId: reviewPlan.id,
        status: 'ready_for_review',
        changedFileCount: 1,
        diffHash: 'sha256:diff',
        verificationStatus: 'passed',
        readyForReviewDraftOnly: true,
        evidenceRefCount: 1,
        auditEventCount: 1,
        packageHash: 'sha256:package',
        exported: false,
        rawPathStored: false,
        bodyStored: false,
        noRealWrite: true,
        summary: 'Review export source package is ready.',
      }),
      findings: [],
      decision: LocalReviewDecisionProjectionSchema.parse({
        id: 'review_export_source_decision',
        schemaVersion,
        createdAt,
        reviewPackageIdHash: 'sha256:package',
        status: 'pending',
        findingCount: 0,
        blockerCount: 0,
        nextAction: 'none',
        retryHandoffRequired: false,
        rawReasonStored: false,
        rawPathStored: false,
        bodyStored: false,
        noRealWrite: true,
        summary: 'Review export source decision is pending.',
      }),
      evidenceRefIds: ['evidence_review_export'],
      auditEventIds: ['audit_review_export_plan'],
      exported: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Review export source package.',
    });
    const dryRun = LocalReviewPackageDryRunRecordSchema.parse({
      id: 'review_export_dry_run_record',
      schemaVersion,
      createdAt,
      dryRunId: 'review_export_dry_run',
      status: 'planned',
      runnerMode: 'controlled-local-artifact',
      reviewPackage,
      reviewPackageIdHash: 'sha256:review-package',
      packageHash: 'sha256:package',
      artifactRootHash: 'sha256:artifact-root',
      artifactDirectoryHash: 'sha256:artifact-dir',
      plannedFileCount: 2,
      plannedFileNameHashes: ['sha256:json', 'sha256:md'],
      policyDecision,
      requiresApproval: true,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_review_export_plan'],
      artifactWriteBoundaryPlanned: true,
      artifactWriteBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local review package export is planned.',
    });
    const approval = LocalReviewPackageApprovalArtifactRecordSchema.parse({
      id: 'review_export_approval_record',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalRequestId: 'review_export_approval_request',
      approvalArtifactId: 'review_export_approval_artifact',
      status: 'approved',
      approved: true,
      policyDecisionId: policyDecision.id,
      reasonHash: 'sha256:reason',
      evidenceRefs: [evidence],
      auditEventIds: ['audit_review_export_approval'],
      artifactWriteBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local review package export approval is approved.',
    });
    const run = LocalReviewPackageControlPlaneRunSchema.parse({
      id: 'review_export_run',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      reviewPackageIdHash: dryRun.reviewPackageIdHash,
      packageHash: dryRun.packageHash,
      artifactRootHash: dryRun.artifactRootHash,
      artifactDirectoryHash: dryRun.artifactDirectoryHash,
      exportedFileCount: 2,
      byteCount: 2048,
      contentHash: 'sha256:content',
      evidenceRefs: [EvidenceRefSchema.parse({ ...evidence, id: 'evidence_review_export_summary', kind: 'review.package_export_summary' })],
      auditEventIds: ['audit_review_export_run'],
      artifactWriteBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local review package export completed.',
    });
    const serialized = JSON.stringify({ dryRun, approval, run });

    expect(run.status).toBe('completed');
    expect(run.artifactWriteBoundaryInvoked).toBe(true);
    expect(() =>
      LocalReviewPackageDryRunRecordSchema.parse({
        ...dryRun,
        id: 'review_export_bad_count',
        plannedFileCount: 1,
      }),
    ).toThrow();
    expect(() =>
      LocalReviewPackageApprovalArtifactRecordSchema.parse({
        ...approval,
        id: 'review_export_bad_approval',
        approved: false,
      }),
    ).toThrow();
    expect(() =>
      LocalReviewPackageControlPlaneRunSchema.parse({
        ...run,
        id: 'review_export_bad_boundary',
        artifactWriteBoundaryInvoked: false,
      }),
    ).toThrow();
    expect(() =>
      LocalReviewPackageControlPlaneRunSchema.parse({
        ...run,
        id: 'review_export_raw_metadata',
        metadata: { rawPath: 'C:\\private\\artifact', rawDiff: 'diff --git' },
      }),
    ).toThrow();
    expect(serialized).not.toContain('C:\\private');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('pull request body');
    expect(serialized).not.toContain('secret-token');
  });

  it('parses M14a local RC readiness projections and rejects raw release metadata', () => {
    const plan = LocalRcReadinessPlanSchema.parse({
      id: 'local_rc_plan_1',
      schemaVersion,
      createdAt,
      reviewPackageIdHash: 'sha256:review-package',
      reviewDecisionIdHash: 'sha256:review-decision',
      reviewDecisionStatus: 'approved_for_local_rc',
      verificationStatus: 'passed',
      operatorReadinessStatus: 'pass',
      plannedReadinessStatus: 'ready_for_local_acceptance',
      evidenceRefIds: ['evidence_review', 'evidence_verification'],
      auditEventIds: ['audit_review', 'audit_verification'],
      exportPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      metadata: { stage: 'm14a', projectionOnly: true },
      summary: 'Local RC readiness plan is ready for local acceptance.',
    });
    const summary = LocalRcReadinessSummarySchema.parse({
      id: 'local_rc_summary_1',
      schemaVersion,
      createdAt,
      planId: plan.id,
      status: 'ready_for_local_acceptance',
      reviewDecisionStatus: plan.reviewDecisionStatus,
      verificationStatus: plan.verificationStatus,
      operatorReadinessStatus: plan.operatorReadinessStatus,
      blockerCount: 0,
      evidenceRefCount: 2,
      auditEventCount: 2,
      localAcceptanceReady: true,
      bundleExported: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Local RC is ready for local acceptance.',
    });
    const evidenceBundle = LocalRcEvidenceBundleSchema.parse({
      id: 'local_rc_evidence_bundle_1',
      schemaVersion,
      createdAt,
      rcReadinessIdHash: 'sha256:rc-summary',
      evidenceRefIds: ['evidence_review', 'evidence_verification'],
      evidenceCount: 2,
      bundleHash: 'sha256:evidence-bundle',
      artifactExported: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Local RC evidence bundle is metadata-only.',
    });
    const auditChain = LocalRcAuditChainSchema.parse({
      id: 'local_rc_audit_chain_1',
      schemaVersion,
      createdAt,
      rcReadinessIdHash: 'sha256:rc-summary',
      auditEventIds: ['audit_review', 'audit_verification'],
      auditEventCount: 2,
      chainHash: 'sha256:audit-chain',
      processBoundaryCount: 0,
      externalProcessStartedCount: 0,
      networkBoundaryCount: 0,
      artifactExported: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Local RC audit chain is metadata-only.',
    });
    const serialized = JSON.stringify({ plan, summary, evidenceBundle, auditChain });

    expect(summary.status).toBe('ready_for_local_acceptance');
    expect(evidenceBundle.evidenceCount).toBe(2);
    expect(auditChain.auditEventCount).toBe(2);
    expect(() =>
      LocalRcReadinessPlanSchema.parse({
        ...plan,
        id: 'local_rc_plan_bad_ready',
        verificationStatus: 'failed',
      }),
    ).toThrow();
    expect(() =>
      LocalRcReadinessSummarySchema.parse({
        ...summary,
        id: 'local_rc_summary_bad_ready',
        blockerCount: 1,
      }),
    ).toThrow();
    expect(() =>
      LocalRcEvidenceBundleSchema.parse({
        ...evidenceBundle,
        id: 'local_rc_evidence_bad_count',
        evidenceCount: 1,
      }),
    ).toThrow();
    expect(() =>
      LocalRcAuditChainSchema.parse({
        ...auditChain,
        id: 'local_rc_audit_bad_count',
        auditEventCount: 1,
      }),
    ).toThrow();
    expect(() =>
      LocalRcReadinessSummarySchema.parse({
        ...summary,
        id: 'local_rc_raw_metadata',
        metadata: { rawPath: 'C:\\private\\rc', rawDiff: 'diff --git', rawReason: 'body' },
      }),
    ).toThrow();
    expect(serialized).not.toContain('C:\\private');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('pull request body');
    expect(serialized).not.toContain('secret-token');
  });

  it('parses M14b governed local RC bundle export records and rejects raw metadata', () => {
    const plan = LocalRcReadinessPlanSchema.parse({
      id: 'local_rc_export_plan_1',
      schemaVersion,
      createdAt,
      reviewPackageIdHash: 'sha256:review-package',
      reviewDecisionIdHash: 'sha256:review-decision',
      reviewDecisionStatus: 'approved_for_local_rc',
      verificationStatus: 'passed',
      operatorReadinessStatus: 'pass',
      plannedReadinessStatus: 'ready_for_local_acceptance',
      evidenceRefIds: ['evidence_review', 'evidence_verification'],
      auditEventIds: ['audit_review', 'audit_verification'],
      exportPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local RC readiness plan is ready for local acceptance.',
    });
    const summary = LocalRcReadinessSummarySchema.parse({
      id: 'local_rc_export_summary_1',
      schemaVersion,
      createdAt,
      planId: plan.id,
      status: 'ready_for_local_acceptance',
      reviewDecisionStatus: plan.reviewDecisionStatus,
      verificationStatus: plan.verificationStatus,
      operatorReadinessStatus: plan.operatorReadinessStatus,
      blockerCount: 0,
      evidenceRefCount: 2,
      auditEventCount: 2,
      localAcceptanceReady: true,
      bundleExported: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Local RC is ready for local acceptance.',
    });
    const evidenceBundle = LocalRcEvidenceBundleSchema.parse({
      id: 'local_rc_export_evidence_bundle_1',
      schemaVersion,
      createdAt,
      rcReadinessIdHash: 'sha256:rc-summary',
      evidenceRefIds: ['evidence_review', 'evidence_verification'],
      evidenceCount: 2,
      bundleHash: 'sha256:evidence-bundle',
      artifactExported: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Local RC evidence bundle is metadata-only.',
    });
    const auditChain = LocalRcAuditChainSchema.parse({
      id: 'local_rc_export_audit_chain_1',
      schemaVersion,
      createdAt,
      rcReadinessIdHash: 'sha256:rc-summary',
      auditEventIds: ['audit_review', 'audit_verification'],
      auditEventCount: 2,
      chainHash: 'sha256:audit-chain',
      processBoundaryCount: 0,
      externalProcessStartedCount: 0,
      networkBoundaryCount: 0,
      artifactExported: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Local RC audit chain is metadata-only.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_rc_bundle_export',
      schemaVersion,
      createdAt,
      actionId: 'rc_bundle_export_dry_run',
      actionType: 'release_candidate.export_local_artifact',
      actionMode: 'write',
      riskLevel: 'medium',
      outcome: 'approval_required',
      reasons: ['local RC bundle export requires persisted approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_rc_bundle_export',
      schemaVersion,
      createdAt,
      kind: 'release.rc_bundle_export_plan',
      hash: 'sha256:rc-export-plan',
      redacted: true,
      metadata: {
        artifactRootHash: 'sha256:artifact-root',
        artifactDirectoryHash: 'sha256:artifact-dir',
        bodyStored: false,
      },
    });
    const dryRun = LocalRcBundleDryRunRecordSchema.parse({
      id: 'rc_bundle_export_dry_run_record',
      schemaVersion,
      createdAt,
      dryRunId: 'rc_bundle_export_dry_run',
      status: 'planned',
      runnerMode: 'controlled-local-artifact',
      readinessPlan: plan,
      readinessSummary: summary,
      evidenceBundle,
      auditChain,
      rcReadinessIdHash: 'sha256:rc-summary',
      rcBundleHash: 'sha256:rc-bundle',
      artifactRootHash: 'sha256:artifact-root',
      artifactDirectoryHash: 'sha256:artifact-dir',
      plannedFileCount: 2,
      plannedFileNameHashes: ['sha256:json', 'sha256:md'],
      policyDecision,
      requiresApproval: true,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_rc_bundle_export_plan'],
      artifactWriteBoundaryPlanned: true,
      artifactWriteBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local RC bundle export is planned.',
    });
    const approval = LocalRcBundleApprovalArtifactRecordSchema.parse({
      id: 'rc_bundle_export_approval_record',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalRequestId: 'rc_bundle_export_approval_request',
      approvalArtifactId: 'rc_bundle_export_approval_artifact',
      status: 'approved',
      approved: true,
      policyDecisionId: policyDecision.id,
      reasonHash: 'sha256:reason',
      evidenceRefs: [evidence],
      auditEventIds: ['audit_rc_bundle_export_approval'],
      artifactWriteBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local RC bundle export approval is approved.',
    });
    const run = LocalRcBundleControlPlaneRunSchema.parse({
      id: 'rc_bundle_export_run',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      rcReadinessIdHash: dryRun.rcReadinessIdHash,
      rcBundleHash: dryRun.rcBundleHash,
      artifactRootHash: dryRun.artifactRootHash,
      artifactDirectoryHash: dryRun.artifactDirectoryHash,
      exportedFileCount: 2,
      byteCount: 2048,
      contentHash: 'sha256:content',
      evidenceRefs: [
        EvidenceRefSchema.parse({
          ...evidence,
          id: 'evidence_rc_bundle_export_summary',
          kind: 'release.rc_bundle_export_summary',
        }),
      ],
      auditEventIds: ['audit_rc_bundle_export_run'],
      artifactWriteBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Local RC bundle export completed.',
    });
    const serialized = JSON.stringify({ dryRun, approval, run });

    expect(run.status).toBe('completed');
    expect(() =>
      LocalRcBundleDryRunRecordSchema.parse({
        ...dryRun,
        id: 'rc_bundle_export_bad_count',
        plannedFileCount: 1,
      }),
    ).toThrow();
    expect(() =>
      LocalRcBundleApprovalArtifactRecordSchema.parse({
        ...approval,
        id: 'rc_bundle_export_bad_approval',
        approved: false,
      }),
    ).toThrow();
    expect(() =>
      LocalRcBundleControlPlaneRunSchema.parse({
        ...run,
        id: 'rc_bundle_export_bad_boundary',
        artifactWriteBoundaryInvoked: false,
      }),
    ).toThrow();
    expect(() =>
      LocalRcBundleControlPlaneRunSchema.parse({
        ...run,
        id: 'rc_bundle_export_raw_metadata',
        metadata: { rawPath: 'C:\\private\\rc', rawDiff: 'diff --git', rawReason: 'body' },
      }),
    ).toThrow();
    expect(serialized).not.toContain('C:\\private');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('pull request body');
    expect(serialized).not.toContain('secret-token');
  });

  it('parses M14c local RC acceptance rehearsal records and rejects raw metadata', () => {
    const step = LocalRcAcceptanceRehearsalStepSchema.parse({
      id: 'local_rc_acceptance_step_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      phase: 'operator-acceptance',
      status: 'passed',
      order: 4,
      evidenceRefIds: ['evidence_rc_acceptance'],
      auditEventIds: ['audit_rc_acceptance'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      artifactWriteBoundaryInvoked: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      metadata: { bundleHash: 'sha256:rc-bundle', fixtureOnly: true },
      summary: 'Operator acceptance fixture step passed.',
    });
    const run = LocalRcAcceptanceRehearsalRunSchema.parse({
      id: 'local_rc_acceptance_run_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      status: 'passed',
      rcReadinessStatus: 'ready_for_local_acceptance',
      reviewDecisionStatus: 'approved_for_local_rc',
      verificationStatus: 'passed',
      exportSummaryStatus: 'fixture_completed',
      operatorAcceptanceStatus: 'accepted',
      stepCount: 1,
      steps: [step],
      evidenceRefIds: ['evidence_rc_acceptance'],
      auditEventIds: ['audit_rc_acceptance'],
      evidenceRefCount: 1,
      auditEventCount: 1,
      bundleHash: 'sha256:rc-bundle',
      artifactWriteBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      metadata: { fixtureOnly: true, stage: 'm14c' },
      summary: 'Local RC acceptance rehearsal passed.',
    });
    const serialized = JSON.stringify(run);

    expect(run.status).toBe('passed');
    expect(() =>
      LocalRcAcceptanceRehearsalRunSchema.parse({
        ...run,
        id: 'local_rc_acceptance_bad_step_count',
        stepCount: 2,
      }),
    ).toThrow();
    expect(() =>
      LocalRcAcceptanceRehearsalRunSchema.parse({
        ...run,
        id: 'local_rc_acceptance_bad_scenario_status',
        scenario: 'review-blocked',
      }),
    ).toThrow();
    expect(() =>
      LocalRcAcceptanceRehearsalStepSchema.parse({
        ...step,
        id: 'local_rc_acceptance_raw_step',
        metadata: { rawPath: 'C:\\private\\rc', rawReason: 'body', rawDiff: 'diff --git' },
      }),
    ).toThrow();
    expect(serialized).not.toContain('C:\\private');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('pull request body');
    expect(serialized).not.toContain('secret-token');
  });

  it('parses M6b worktree control-plane records and keeps raw bodies out', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_worktree_m6b',
      schemaVersion,
      createdAt,
      kind: 'worktree.run_summary',
      hash: 'sha256:worktree-run',
    });
    const plan = WorktreePlanSchema.parse({
      id: 'worktree_plan_m6b',
      schemaVersion,
      createdAt,
      adapterName: 'worktree-manager',
      status: 'planned',
      runnerMode: 'controlled-git-worktree',
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      branchNameHash: 'sha256:branch',
      worktreeSlugHash: 'sha256:slug',
      baseRefHash: 'sha256:base',
      commandSummaryHash: 'sha256:command',
      defaultRootKind: 'sibling',
      plannedActions: [
        {
          action: 'git.worktree.create.real',
          actionMode: 'write',
          risk: 'high',
          target: 'sha256:path',
          requiresApproval: true,
        },
      ],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      gitProcessBoundaryPlanned: true,
      gitProcessBoundaryInvoked: false,
      cleanupRequired: false,
      cleanupDeferred: false,
      processBoundaryPlanned: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Controlled git plan.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_worktree_m6b',
      schemaVersion,
      createdAt,
      actionId: plan.id,
      actionType: 'git.worktree.create',
      actionMode: 'write',
      riskLevel: 'high',
      outcome: 'approval_required',
      reasons: ['real write action requires explicit approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const dryRun = WorktreeDryRunRecordSchema.parse({
      id: plan.id,
      dryRunId: plan.id,
      schemaVersion,
      createdAt,
      status: 'ready',
      plan,
      capabilityDryRun: {
        id: 'capability_dry_run_worktree_m6b',
        schemaVersion,
        createdAt,
        adapterName: 'worktree-manager',
        inputSummary: { worktreePathHash: 'sha256:path' },
        plannedActions: plan.plannedActions,
      },
      policyDecision,
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      branchNameHash: 'sha256:branch',
      worktreeSlugHash: 'sha256:slug',
      baseRefHash: 'sha256:base',
      timeline: [],
      evidenceRefs: [evidence],
      auditEventIds: ['audit_worktree_m6b'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      gitProcessBoundaryPlanned: true,
      gitProcessBoundaryInvoked: false,
      processBoundaryPlanned: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'M6b dry-run ready.',
    });
    const approval = WorktreeApprovalArtifactRecordSchema.parse({
      id: 'worktree_approval_record_m6b',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalRequestId: 'worktree_approval_request_m6b',
      approvalArtifactId: 'worktree_approval_artifact_m6b',
      status: 'approved',
      requestedBy: 'local-operator',
      decidedBy: 'local-operator',
      dryRunPlanHash: 'sha256:plan',
      policyDecisionId: policyDecision.id,
      policyDecisionHash: 'sha256:policy',
      approved: true,
      requestedAt: createdAt,
      decidedAt: createdAt,
      expiresAt: '2026-04-28T01:00:00.000Z',
      rawPathStored: false,
      bodyStored: false,
      summary: 'Approved.',
    });
    const worktreeRun = WorktreeRunSchema.parse({
      id: 'worktree_run_m6b',
      schemaVersion,
      createdAt,
      planId: plan.id,
      status: 'completed',
      runnerMode: 'controlled-git-worktree',
      worktreePathHash: 'sha256:path',
      branchNameHash: 'sha256:branch',
      baseRefHash: 'sha256:base',
      commandSummaryHash: 'sha256:command',
      changedFiles: ['packages/contracts/src/index.ts'],
      changedFileCount: 1,
      diffHash: 'sha256:diff',
      evidenceRefs: [evidence],
      auditEventIds: ['audit_worktree_m6b'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: false,
      gitProcessBoundaryInvoked: true,
      cleanupRequired: true,
      cleanupDeferred: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      summary: 'Controlled git worktree completed.',
    });
    const run = WorktreeControlPlaneRunSchema.parse({
      id: 'worktree_control_run_m6b',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      planId: plan.id,
      runnerMode: 'controlled-git-worktree',
      worktreeRun,
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      branchNameHash: 'sha256:branch',
      worktreeSlugHash: 'sha256:slug',
      baseRefHash: 'sha256:base',
      changedFileCount: 1,
      diffHash: 'sha256:diff',
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_worktree_m6b'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: false,
      cleanupRequired: true,
      cleanupDeferred: true,
      gitProcessBoundaryInvoked: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      summary: 'M6b controlled git run completed.',
    });

    expect(dryRun.gitProcessBoundaryPlanned).toBe(true);
    expect(approval.approved).toBe(true);
    expect(run.noRealWrite).toBe(false);
    expect(() =>
      WorktreeControlPlaneRunSchema.parse({
        ...run,
        rawDiffBody: 'diff --git a/private b/private',
      }),
    ).toThrow();
    expect(() =>
      WorktreeDryRunRecordSchema.parse({
        ...dryRun,
        rawWorktreePath: 'C:\\Users\\Thomas\\CodexHub-worktrees\\feature',
      }),
    ).toThrow();
  });

  it('parses M6c worktree cleanup control-plane records and keeps raw bodies out', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_worktree_cleanup_m6c',
      schemaVersion,
      createdAt,
      kind: 'worktree.cleanup_summary',
      hash: 'sha256:worktree-cleanup',
    });
    const plan = WorktreeCleanupPlanSchema.parse({
      id: 'worktree_cleanup_plan_m6c',
      schemaVersion,
      createdAt,
      adapterName: 'worktree-manager',
      sourceRunId: 'worktree_control_run_m6b',
      status: 'planned',
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      sourceRunHash: 'sha256:source-run',
      commandSummaryHash: 'sha256:command',
      plannedActions: [
        {
          action: 'git.worktree.cleanup.remove',
          actionMode: 'write',
          risk: 'high',
          target: 'sha256:path',
          requiresApproval: true,
        },
      ],
      dirtyCheckPlanned: true,
      cleanupDeletePlanned: true,
      cleanupRequired: true,
      cleanupDeferred: true,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      gitProcessBoundaryPlanned: true,
      gitProcessBoundaryInvoked: false,
      processBoundaryPlanned: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Cleanup planned.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_worktree_cleanup_m6c',
      schemaVersion,
      createdAt,
      actionId: plan.id,
      actionType: 'git.worktree.cleanup',
      actionMode: 'write',
      riskLevel: 'high',
      outcome: 'approval_required',
      reasons: ['real write action requires explicit approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const dryRun = WorktreeCleanupDryRunRecordSchema.parse({
      id: plan.id,
      dryRunId: plan.id,
      schemaVersion,
      createdAt,
      sourceRunId: plan.sourceRunId,
      status: 'ready',
      plan,
      capabilityDryRun: {
        id: 'capability_dry_run_worktree_cleanup_m6c',
        schemaVersion,
        createdAt,
        adapterName: 'worktree-manager',
        inputSummary: { worktreePathHash: 'sha256:path' },
        plannedActions: plan.plannedActions,
      },
      policyDecision,
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      sourceRunHash: 'sha256:source-run',
      timeline: [],
      evidenceRefs: [evidence],
      auditEventIds: ['audit_worktree_cleanup_m6c'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      gitProcessBoundaryPlanned: true,
      gitProcessBoundaryInvoked: false,
      processBoundaryPlanned: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'M6c cleanup dry-run ready.',
    });
    const approval = WorktreeCleanupApprovalArtifactRecordSchema.parse({
      id: 'worktree_cleanup_approval_record_m6c',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      sourceRunId: dryRun.sourceRunId,
      approvalRequestId: 'worktree_cleanup_approval_request_m6c',
      approvalArtifactId: 'worktree_cleanup_approval_artifact_m6c',
      status: 'approved',
      requestedBy: 'local-operator',
      decidedBy: 'local-operator',
      dryRunPlanHash: 'sha256:plan',
      policyDecisionId: policyDecision.id,
      policyDecisionHash: 'sha256:policy',
      approved: true,
      requestedAt: createdAt,
      decidedAt: createdAt,
      expiresAt: '2026-04-28T01:00:00.000Z',
      rawPathStored: false,
      bodyStored: false,
      summary: 'Cleanup approved.',
    });
    const cleanupRun = WorktreeCleanupRunSchema.parse({
      id: 'worktree_cleanup_run_m6c',
      schemaVersion,
      createdAt,
      planId: plan.id,
      dryRunId: dryRun.dryRunId,
      sourceRunId: dryRun.sourceRunId,
      status: 'completed',
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      sourceRunHash: 'sha256:source-run',
      commandSummaryHash: 'sha256:command',
      dirtyFileCount: 0,
      cleanupAttempted: true,
      cleanupCompleted: true,
      cleanupRequired: false,
      cleanupDeferred: false,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_worktree_cleanup_m6c'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: false,
      gitProcessBoundaryInvoked: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      summary: 'Cleanup completed.',
    });
    const run = WorktreeCleanupControlPlaneRunSchema.parse({
      id: 'worktree_cleanup_control_run_m6c',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      sourceRunId: dryRun.sourceRunId,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      planId: plan.id,
      cleanupRun,
      repoRootHash: 'sha256:repo',
      worktreeRootHash: 'sha256:root',
      worktreePathHash: 'sha256:path',
      sourceRunHash: 'sha256:source-run',
      dirtyFileCount: 0,
      cleanupAttempted: true,
      cleanupCompleted: true,
      cleanupRequired: false,
      cleanupDeferred: false,
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_worktree_cleanup_m6c'],
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: false,
      gitProcessBoundaryInvoked: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      summary: 'M6c cleanup control-plane completed.',
    });

    expect(approval.sourceRunId).toBe(dryRun.sourceRunId);
    expect(run.cleanupCompleted).toBe(true);
    expect(run.cleanupRequired).toBe(false);
    expect(() =>
      WorktreeCleanupControlPlaneRunSchema.parse({
        ...run,
        rawCommandBody: 'git worktree remove C:\\Users\\Thomas\\CodexHub-worktrees\\feature',
      }),
    ).toThrow();
    expect(() =>
      WorktreeCleanupDryRunRecordSchema.parse({
        ...dryRun,
        rawWorktreePath: 'C:\\Users\\Thomas\\CodexHub-worktrees\\feature',
      }),
    ).toThrow();
  });

  it('parses M7 policy backend and telemetry foundation contracts as metadata-only', () => {
    expect(PolicyBackendKindSchema.options).toEqual(['opa', 'cedar', 'fixture']);
    expect(PolicyBackendEvaluatorSourceSchema.options).toEqual([
      'fixture-inline',
      'fixture-config',
    ]);
    expect(TelemetryExporterKindSchema.options).toEqual(['noop', 'fixture']);
    expect(TelemetrySignalKindSchema.options).toEqual(['trace', 'metric', 'log']);

    const policyEvidence = EvidenceRefSchema.parse({
      id: 'evidence_policy_backend_1',
      schemaVersion,
      createdAt,
      kind: 'policy_backend.raw_evaluation_summary',
      hash: 'sha256:policy-raw',
    });
    const normalizedEvidence = EvidenceRefSchema.parse({
      id: 'evidence_policy_backend_normalized_1',
      schemaVersion,
      createdAt,
      kind: 'policy_backend.normalized_decision_trace',
      hash: 'sha256:policy-normalized',
    });
    const telemetryEvidence = EvidenceRefSchema.parse({
      id: 'evidence_telemetry_span_1',
      schemaVersion,
      createdAt,
      kind: 'telemetry.span_summary',
      hash: 'sha256:span',
    });

    const policyPlan = PolicyBackendEvaluationPlanSchema.parse({
      id: 'policy_backend_plan_1',
      schemaVersion,
      createdAt,
      adapterName: 'policy-backend-adapter',
      backendKind: 'fixture',
      evaluatorSource: 'fixture-config',
      actionIdHash: 'sha256:action',
      actionType: 'git.worktree.create',
      actionMode: 'read',
      riskLevel: 'low',
      inputHash: 'sha256:input',
      policySourceHash: 'sha256:policy-source',
      fixtureConfigHash: 'sha256:fixture-config',
      fixtureRuleCount: 2,
      processBoundaryPlanned: false,
      networkBoundaryPlanned: false,
      rawPolicySourceStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Fixture policy backend evaluation plan.',
    });
    const rawEvaluation = PolicyBackendRawEvaluationSummarySchema.parse({
      id: 'policy_backend_raw_1',
      schemaVersion,
      createdAt,
      planId: policyPlan.id,
      backendKind: 'fixture',
      evaluatorSource: 'fixture-config',
      status: 'completed',
      rawOutcome: 'deny',
      rawEvaluationHash: 'sha256:raw-eval',
      reasonCount: 1,
      matchedRuleCount: 1,
      policySourceHash: 'sha256:policy-source',
      fixtureConfigHash: 'sha256:fixture-config',
      fixtureRuleCount: 2,
      rawPolicySourceStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Raw backend evaluation is advisory.',
    });
    const normalizedTrace = PolicyBackendNormalizedDecisionTraceSchema.parse({
      id: 'policy_backend_trace_1',
      schemaVersion,
      createdAt,
      planId: policyPlan.id,
      rawEvaluationSummaryId: rawEvaluation.id,
      codexhubPolicyDecisionId: 'policy_codexhub_1',
      policyDecisionHash: 'sha256:codexhub-policy',
      backendKind: 'fixture',
      evaluatorSource: 'fixture-config',
      backendOutcome: 'deny',
      normalizedOutcome: 'allow',
      authorityProvider: 'codexhub',
      backendAdvisoryOnly: true,
      evidenceRefs: [normalizedEvidence],
      auditEventIds: ['audit_policy_backend_1'],
      rawPolicySourceStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'CodexHub policy decision remains authoritative.',
    });
    const policyRun = PolicyBackendEvaluationRunSchema.parse({
      id: 'policy_backend_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: policyPlan,
      rawEvaluationSummary: rawEvaluation,
      normalizedDecisionTrace: normalizedTrace,
      evidenceRefs: [policyEvidence, normalizedEvidence],
      auditEventIds: ['audit_policy_backend_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Policy backend fixture evaluation completed.',
    });
    const span = TelemetrySpanSummarySchema.parse({
      id: 'telemetry_span_1',
      schemaVersion,
      createdAt,
      signalKind: 'trace',
      spanKind: 'workflow',
      traceIdHash: 'sha256:trace',
      spanIdHash: 'sha256:span',
      nameHash: 'sha256:name',
      durationMs: 12,
      attributeCount: 3,
      eventCount: 1,
      linkCount: 0,
      payloadHash: 'sha256:payload',
      rawTracePayloadStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      evidenceAuditAuthoritative: false,
      summary: 'Workflow span metadata only.',
    });
    const tracePlan = TelemetryTraceExportPlanSchema.parse({
      id: 'telemetry_plan_1',
      schemaVersion,
      createdAt,
      adapterName: 'otel-adapter',
      exporterKind: 'noop',
      signalKinds: ['trace'],
      spanCount: 1,
      tracePlanHash: 'sha256:trace-plan',
      networkExportPlanned: false,
      processBoundaryPlanned: false,
      rawTracePayloadStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      evidenceAuditAuthoritative: false,
      summary: 'Noop trace export plan.',
    });
    const telemetryRun = TelemetryExportRunSchema.parse({
      id: 'telemetry_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      planId: tracePlan.id,
      exporterKind: 'noop',
      spans: [span],
      exportedSpanCount: 1,
      exportSummaryHash: 'sha256:export',
      evidenceRefs: [telemetryEvidence],
      auditEventIds: ['audit_telemetry_1'],
      networkExportAttempted: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawTracePayloadStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      evidenceAuditAuthoritative: false,
      summary: 'Noop telemetry export completed.',
    });

    expect(policyRun.normalizedDecisionTrace?.authorityProvider).toBe('codexhub');
    expect(policyRun.normalizedDecisionTrace?.backendAdvisoryOnly).toBe(true);
    expect(telemetryRun.evidenceAuditAuthoritative).toBe(false);
    expect(telemetryRun.networkExportAttempted).toBe(false);
    expect(() =>
      PolicyBackendEvaluationPlanSchema.parse({
        ...policyPlan,
        rawPolicySource: 'package codexhub.authz',
      }),
    ).toThrow();
    expect(() =>
      TelemetrySpanSummarySchema.parse({
        ...span,
        rawTracePayload: '{"token":"secret"}',
      }),
    ).toThrow();
    expect(() =>
      EvidenceRefSchema.parse({
        id: 'evidence_bad_m7',
        schemaVersion,
        createdAt,
        kind: 'telemetry.raw_payload',
        hash: 'sha256:bad',
      }),
    ).toThrow();
  });

  it('parses unified governance projection contracts as metadata-only', () => {
    expect(UnifiedRunSourceSchema.options).toEqual([
      'codex',
      'verification',
      'mcp',
      'browser',
      'electron',
      'worktree',
      'policy',
      'telemetry',
      'github',
      'orchestrator',
    ]);

    const evidenceBundle = EvidenceBundleProjectionSchema.parse({
      id: 'evidence_bundle_projection_1',
      schemaVersion,
      createdAt,
      runProjectionId: 'unified_run_projection_1',
      source: 'worktree',
      evidenceRefIds: ['evidence_worktree_1'],
      evidenceCount: 1,
      evidenceKinds: ['worktree.run_summary'],
      bundleHash: 'sha256:evidence-bundle',
      rawPathStored: false,
      bodyStored: false,
      summary: 'Evidence bundle stores ids, hashes, counts, and kinds only.',
    });
    const auditChain = AuditChainProjectionSchema.parse({
      id: 'audit_chain_projection_1',
      schemaVersion,
      createdAt,
      runProjectionId: 'unified_run_projection_1',
      source: 'worktree',
      auditEventIds: ['audit_worktree_1'],
      auditEventCount: 1,
      policyDecisionIds: ['policy_worktree_1'],
      chainHash: 'sha256:audit-chain',
      rawPathStored: false,
      bodyStored: false,
      summary: 'Audit chain stores ids, hashes, and counts only.',
    });
    const timelineEvent = UnifiedTimelineEventSchema.parse({
      id: 'unified_timeline_event_1',
      schemaVersion,
      createdAt,
      runProjectionId: 'unified_run_projection_1',
      source: 'worktree',
      phase: 'worktree.summary',
      status: 'completed',
      order: 0,
      evidenceRefIds: evidenceBundle.evidenceRefIds,
      auditEventIds: auditChain.auditEventIds,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Worktree summary projected without raw data.',
    });
    const projection = UnifiedRunProjectionSchema.parse({
      id: 'unified_run_projection_1',
      schemaVersion,
      createdAt,
      source: 'worktree',
      sourceRunIdHash: 'sha256:source-run',
      titleHash: 'sha256:title',
      status: 'completed',
      timeline: [timelineEvent],
      evidenceBundle,
      auditChain,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      networkBoundaryInvoked: false,
      noRealWrite: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Unified worktree run projection.',
    });
    const summary = GovernanceProjectionSummarySchema.parse({
      id: 'governance_projection_summary_1',
      schemaVersion,
      createdAt,
      status: 'ready',
      runCount: 1,
      sourceBreakdown: { worktree: 1 },
      evidenceCount: 1,
      auditEventCount: 1,
      processBoundaryCount: 1,
      externalProcessStartedCount: 1,
      networkBoundaryCount: 0,
      projectionHash: 'sha256:projection',
      rawPathStored: false,
      bodyStored: false,
      summary: 'Governance projection summary stores metadata only.',
    });

    expect(projection.timeline).toHaveLength(1);
    expect(summary.sourceBreakdown.worktree).toBe(1);
    expect(JSON.stringify(projection)).not.toContain('diff --git');
    expect(JSON.stringify(projection)).not.toContain('C:/');
    expect(JSON.stringify(projection)).not.toContain('stdout');
    expect(JSON.stringify(projection)).not.toContain('stderr');
    expect(() =>
      UnifiedRunProjectionSchema.parse({
        ...projection,
        id: 'unified_run_projection_bad_metadata',
        metadata: { requestBody: 'raw body' },
      }),
    ).toThrow();
    expect(() =>
      UnifiedRunProjectionSchema.parse({
        ...projection,
        id: 'unified_run_projection_bad_field',
        rawDiff: 'diff --git a/file',
      }),
    ).toThrow();
  });

  it('parses operator readiness contracts as metadata-only', () => {
    expect(OperatorReadinessStatusSchema.options).toEqual([
      'pass',
      'warn',
      'fail',
      'unknown',
    ]);

    const configHash = ConfigHashSummarySchema.parse({
      id: 'config_hash_summary_1',
      schemaVersion,
      createdAt,
      name: 'integrations',
      kind: 'integration',
      configured: true,
      hash: 'config:abc123',
      itemCount: 8,
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Integration config is present and summarized by hash.',
    });
    const integration = IntegrationReadinessSummarySchema.parse({
      id: 'integration_readiness_codex',
      schemaVersion,
      createdAt,
      name: 'codex-cli',
      enabled: true,
      defaultEnabled: true,
      riskLevel: 'medium',
      approvalRequired: true,
      evidenceRequired: true,
      auditRequired: true,
      processBoundary: true,
      networkBoundary: false,
      safeToEnable: true,
      envFlagConfigured: true,
      localControlKeyConfigured: true,
      configHash: configHash.hash,
      blockers: [],
      safeEnableNotes: ['Persisted approval remains required for execution.'],
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Codex CLI is configured for governed execution.',
    });
    const check = OperatorReadinessCheckSchema.parse({
      id: 'operator_readiness_check_1',
      schemaVersion,
      createdAt,
      code: 'local_control_key_present',
      label: 'Local control key configured',
      category: 'security',
      status: 'pass',
      required: true,
      configured: true,
      hash: 'env:configured',
      blockers: [],
      safeEnableNotes: ['Configured value is never displayed.'],
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Required local-control key is configured.',
    });
    const report = OperatorReadinessReportSchema.parse({
      id: 'operator_readiness_report_1',
      schemaVersion,
      createdAt,
      status: 'pass',
      checks: [check],
      integrations: [integration],
      configHashes: [configHash],
      passedCheckCount: 1,
      warningCheckCount: 0,
      failedCheckCount: 0,
      unknownCheckCount: 0,
      configuredLocalControlKeyCount: 1,
      storeAvailable: true,
      processBoundaryAllowlistPassed: true,
      policyConfigHash: 'config:policy',
      riskConfigHash: 'config:risk',
      integrationConfigHash: configHash.hash,
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Operator readiness report stores hashes and statuses only.',
    });

    const serialized = JSON.stringify(report);
    expect(report.status).toBe('pass');
    expect(report.integrations[0]?.name).toBe('codex-cli');
    expect(serialized).not.toContain('secret-value');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('raw config body');
    expect(() =>
      OperatorReadinessReportSchema.parse({
        ...report,
        id: 'operator_readiness_report_bad_metadata',
        metadata: { rawValue: 'secret-value' },
      }),
    ).toThrow();
    expect(() =>
      OperatorReadinessCheckSchema.parse({
        ...check,
        id: 'operator_readiness_check_bad_field',
        rawEnv: 'secret-value',
      }),
    ).toThrow();
  });

  it('parses M10 pilot checklist and runbook contracts as metadata-only', () => {
    expect(M10PilotChecklistStatusSchema.options).toEqual(['ready', 'blocked', 'review']);
    expect(M10PilotOperatorStepPhaseSchema.options).toEqual([
      'preflight',
      'approval',
      'pilot',
      'verification',
      'review',
      'rollback',
    ]);
    expect(M10PilotOperatorStepStatusSchema.options).toEqual([
      'ready',
      'blocked',
      'review',
      'done',
    ]);

    const step = M10PilotOperatorStepSchema.parse({
      id: 'm10_pilot_step_1',
      schemaVersion,
      createdAt,
      code: 'doctor_preflight',
      label: 'Doctor preflight',
      phase: 'preflight',
      status: 'blocked',
      required: true,
      blockerCount: 1,
      blockers: ['worktree_manager_disabled'],
      safeEnableNotes: ['Resolve blockers before running the pilot.'],
      evidenceRefIds: [],
      auditEventIds: [],
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'Operator preflight stores only status and blocker metadata.',
    });
    const checklist = M10PilotChecklistSchema.parse({
      id: 'm10_pilot_checklist_1',
      schemaVersion,
      createdAt,
      status: 'blocked',
      steps: [step],
      readyStepCount: 0,
      blockedStepCount: 1,
      reviewStepCount: 0,
      requiredStepCount: 1,
      blockerCount: 1,
      integrationCount: 8,
      configuredLocalControlKeyCount: 0,
      governanceRunCount: 0,
      approvalInboxItemCount: 0,
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'M10 pilot checklist is blocked until operator prerequisites are ready.',
    });
    const runbook = M10PilotRunbookSummarySchema.parse({
      id: 'm10_pilot_runbook_1',
      schemaVersion,
      createdAt,
      checklistId: checklist.id,
      status: checklist.status,
      phaseCount: 6,
      requiredStepCount: checklist.requiredStepCount,
      blockerCount: checklist.blockerCount,
      nextAction: 'Resolve checklist blockers before enabling the local pilot.',
      rollbackSummary: 'Disable pilot flags and use governed cleanup metadata if needed.',
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'Runbook summary is read-only and does not execute the pilot.',
    });
    const serialized = JSON.stringify({ checklist, runbook });

    expect(checklist.status).toBe('blocked');
    expect(runbook.checklistId).toBe(checklist.id);
    expect(serialized).not.toContain('CODEXHUB_SUPERVISOR_LOCAL_TOKEN');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('raw prompt');
    expect(serialized).not.toContain('stdout body');
    expect(serialized).not.toContain('stderr body');
    expect(serialized).not.toContain('diff --git');
    expect(() =>
      M10PilotOperatorStepSchema.parse({
        ...step,
        id: 'm10_pilot_step_bad_token',
        localControlKeyRead: true,
      }),
    ).toThrow();
    expect(() =>
      M10PilotChecklistSchema.parse({
        ...checklist,
        id: 'm10_pilot_checklist_bad_raw',
        rawEnv: 'secret-value',
      }),
    ).toThrow();
    expect(() =>
      M10PilotRunbookSummarySchema.parse({
        ...runbook,
        id: 'm10_pilot_runbook_bad_metadata',
        metadata: { rawEnv: 'secret-value' },
      }),
    ).toThrow();
  });

  it('parses M11 pilot enablement contracts as metadata-only', () => {
    expect(M11PilotEnablementStatusSchema.options).toEqual(['ready', 'blocked', 'review']);
    expect(M11PilotEnablementStepPhaseSchema.options).toEqual([
      'preflight',
      'approval',
      'worktree',
      'codex',
      'verification',
      'projection',
      'rollback',
    ]);
    expect(M11PilotEnablementStepStatusSchema.options).toEqual([
      'ready',
      'blocked',
      'review',
      'done',
    ]);

    const step = M11PilotEnablementStepSchema.parse({
      id: 'm11_enablement_step_1',
      schemaVersion,
      createdAt,
      code: 'm11_env_flags',
      label: 'M11 pilot env flags',
      phase: 'preflight',
      status: 'blocked',
      required: true,
      blockerCount: 1,
      blockers: ['m11_pilot_env_flag_missing'],
      safeEnableNotes: ['Enable only for a local pilot session.'],
      evidenceRefIds: [],
      auditEventIds: [],
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'M11 enablement records blocker metadata only.',
    });
    const checklist = M11PilotEnablementChecklistSchema.parse({
      id: 'm11_enablement_checklist_1',
      schemaVersion,
      createdAt,
      status: 'blocked',
      steps: [step],
      readyStepCount: 0,
      blockedStepCount: 1,
      reviewStepCount: 0,
      requiredStepCount: 1,
      blockerCount: 1,
      integrationCount: 8,
      configuredLocalControlKeyCount: 0,
      governanceRunCount: 0,
      approvalInboxItemCount: 0,
      latestRunCount: 0,
      cleanupRequiredCount: 0,
      requiredEnvFlags: [
        'CODEXHUB_M11_PRODUCTION_PILOT_ENABLED',
        'CODEXHUB_WORKTREE_MANAGER_ENABLED',
      ],
      safeEnableBlockers: ['m11_pilot_env_flag_missing'],
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      codexReadOnlyDryRunOnly: true,
      patchGenerationAllowed: false,
      pushAllowed: false,
      pullRequestOpened: false,
      summary: 'M11 pilot enablement is blocked until prerequisites are ready.',
    });
    const runbook = M11PilotEnablementRunbookSummarySchema.parse({
      id: 'm11_enablement_runbook_1',
      schemaVersion,
      createdAt,
      checklistId: checklist.id,
      status: checklist.status,
      phaseCount: 7,
      requiredStepCount: checklist.requiredStepCount,
      blockerCount: checklist.blockerCount,
      nextAction: 'Resolve M11 pilot blockers before using the gated route.',
      safeEnableSummary: 'Use env flags, persisted approval, and hash-bound runtime input.',
      failureHandlingSummary: 'Inspect failure classification and cleanup handoff metadata.',
      rollbackSummary: 'Disable pilot flags and use governed cleanup if required.',
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'M11 enablement runbook is read-only.',
    });
    const serialized = JSON.stringify({ checklist, runbook });

    expect(checklist.codexReadOnlyDryRunOnly).toBe(true);
    expect(checklist.patchGenerationAllowed).toBe(false);
    expect(runbook.checklistId).toBe(checklist.id);
    expect(serialized).not.toContain('CODEXHUB_SUPERVISOR_LOCAL_TOKEN');
    expect(serialized).not.toContain('raw prompt');
    expect(serialized).not.toContain('stdout body');
    expect(serialized).not.toContain('stderr body');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('C:/');
    expect(() =>
      M11PilotEnablementChecklistSchema.parse({
        ...checklist,
        id: 'm11_enablement_checklist_bad_patch',
        patchGenerationAllowed: true,
      }),
    ).toThrow();
    expect(() =>
      M11PilotEnablementStepSchema.parse({
        ...step,
        id: 'm11_enablement_step_bad_token',
        localControlKeyRead: true,
      }),
    ).toThrow();
    expect(() =>
      M11PilotEnablementRunbookSummarySchema.parse({
        ...runbook,
        id: 'm11_enablement_runbook_bad_metadata',
        metadata: { rawEnv: 'secret-value' },
      }),
    ).toThrow();
  });

  it('parses golden path rehearsal contracts as metadata-only', () => {
    expect(GoldenPathStepStatusSchema.options).toEqual([
      'planned',
      'running',
      'passed',
      'ready',
      'completed',
      'failed',
      'blocked',
      'aborted',
    ]);

    const step = GoldenPathStepSchema.parse({
      id: 'golden_path_step_1',
      schemaVersion,
      createdAt,
      phase: 'worktree.fixture',
      status: 'completed',
      order: 0,
      evidenceRefIds: ['evidence_worktree_1'],
      auditEventIds: ['audit_worktree_1'],
      rawPathStored: false,
      bodyStored: false,
      summary: 'Worktree fixture completed without raw path storage.',
    });
    const evidenceBundle = GoldenPathEvidenceBundleSchema.parse({
      id: 'golden_path_evidence_bundle_1',
      schemaVersion,
      createdAt,
      rehearsalRunId: 'golden_path_rehearsal_1',
      evidenceRefIds: step.evidenceRefIds,
      auditEventIds: step.auditEventIds,
      evidenceCount: 1,
      auditEventCount: 1,
      bundleHash: 'golden:bundle',
      rawPathStored: false,
      bodyStored: false,
      summary: 'Golden path evidence bundle stores ids and hashes only.',
    });
    const run = GoldenPathRehearsalRunSchema.parse({
      id: 'golden_path_rehearsal_1',
      schemaVersion,
      createdAt,
      status: 'passed',
      scenario: 'all-pass',
      requestId: 'development_request_1',
      requestTitleHash: 'golden:title',
      requestDescriptionHash: 'golden:description',
      steps: [step],
      evidenceBundle,
      telemetryProjectionHash: 'golden:telemetry',
      prDraftStatus: 'ready',
      releaseAuditStatus: 'ready',
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      evidenceAuditAuthoritative: true,
      telemetryAuthoritative: false,
      pushAllowed: false,
      pullRequestOpened: false,
      summary: 'Golden path rehearsal passed with fixture metadata only.',
    });

    const serialized = JSON.stringify(run);
    expect(run.steps).toHaveLength(1);
    expect(run.evidenceBundle.evidenceCount).toBe(1);
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('stdout');
    expect(serialized).not.toContain('stderr');
    expect(() =>
      GoldenPathRehearsalRunSchema.parse({
        ...run,
        id: 'golden_path_rehearsal_bad_metadata',
        metadata: { requestBody: 'raw body' },
      }),
    ).toThrow();
  });

  it('parses M10 pilot acceptance rehearsal contracts as fixture-only metadata', () => {
    expect(M10PilotAcceptanceScenarioSchema.options).toEqual([
      'all-pass',
      'readiness-blocked',
      'approval-blocked',
      'codex-failed',
      'nx-failed',
    ]);

    const step = M10PilotAcceptanceStepSchema.parse({
      id: 'm10_acceptance_step_1',
      schemaVersion,
      createdAt,
      code: 'doctor_preflight',
      phase: 'doctor',
      status: 'passed',
      order: 0,
      evidenceRefIds: ['evidence_m10_doctor'],
      auditEventIds: ['audit_m10_doctor'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'M10 acceptance doctor step passed with metadata only.',
    });
    const evidenceSummary = M10PilotAcceptanceEvidenceSummarySchema.parse({
      id: 'm10_acceptance_evidence_summary_1',
      schemaVersion,
      createdAt,
      rehearsalRunId: 'm10_acceptance_run_1',
      evidenceRefIds: step.evidenceRefIds,
      auditEventIds: step.auditEventIds,
      evidenceCount: 1,
      auditEventCount: 1,
      evidenceBundleHash: 'm10:bundle',
      governanceProjectionHash: 'm10:governance',
      telemetryProjectionHash: 'm10:telemetry',
      evidenceAuditAuthoritative: true,
      telemetryAuthoritative: false,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'M10 acceptance evidence summary stores ids and hashes only.',
    });
    const run = M10PilotAcceptanceRehearsalRunSchema.parse({
      id: 'm10_acceptance_run_1',
      schemaVersion,
      createdAt,
      status: 'passed',
      scenario: 'all-pass',
      checklistId: 'm10_pilot_checklist_1',
      approvalHistoryProjectionId: 'approval_decision_history_projection_1',
      governanceProjectionHash: 'm10:governance',
      goldenPathRunId: 'golden_path_rehearsal_1',
      goldenPathStatus: 'passed',
      prActionStatus: 'not_ready_no_live_pr',
      steps: [step],
      evidenceSummary,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      pushAllowed: false,
      pullRequestOpened: false,
      evidenceAuditAuthoritative: true,
      telemetryAuthoritative: false,
      summary: 'M10 acceptance rehearsal passed without live execution.',
    });
    const serialized = JSON.stringify(run);

    expect(run.status).toBe('passed');
    expect(run.prActionStatus).toBe('not_ready_no_live_pr');
    expect(serialized).not.toContain('CODEXHUB_SUPERVISOR_LOCAL_TOKEN');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('raw prompt');
    expect(serialized).not.toContain('stdout');
    expect(serialized).not.toContain('stderr');
    expect(serialized).not.toContain('diff --git');
    expect(() =>
      M10PilotAcceptanceRehearsalRunSchema.parse({
        ...run,
        id: 'm10_acceptance_run_bad_token',
        localControlKeyRead: true,
      }),
    ).toThrow();
    expect(() =>
      M10PilotAcceptanceStepSchema.parse({
        ...step,
        id: 'm10_acceptance_step_bad_metadata',
        metadata: { requestBody: 'raw body' },
      }),
    ).toThrow();
  });

  it('parses M9 pilot contracts as metadata-only', () => {
    const step = M9PilotStepSchema.parse({
      id: 'm9_pilot_step_1',
      schemaVersion,
      createdAt,
      phase: 'codex',
      status: 'completed',
      order: 2,
      evidenceRefIds: ['evidence_codex_1'],
      auditEventIds: ['audit_codex_1'],
      boundaryInvoked: true,
      externalProcessStarted: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Codex read-only dry-run completed with metadata only.',
    });
    const readiness = M9PilotReadinessSchema.parse({
      id: 'm9_pilot_readiness_1',
      schemaVersion,
      createdAt,
      status: 'ready',
      checkCount: 8,
      passedCheckCount: 8,
      blockerCount: 0,
      blockers: [],
      worktreeManagerEnabled: true,
      codexDryRunOnly: true,
      nxVerificationPlanned: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M9 pilot readiness is ready.',
    });
    const evidenceSummary = M9PilotEvidenceSummarySchema.parse({
      id: 'm9_pilot_evidence_summary_1',
      schemaVersion,
      createdAt,
      runId: 'm9_pilot_run_1',
      evidenceRefIds: ['evidence_codex_1'],
      auditEventIds: ['audit_codex_1'],
      evidenceCount: 1,
      auditEventCount: 1,
      bundleHash: 'sha256:m9-bundle',
      rawPathStored: false,
      bodyStored: false,
      summary: 'M9 pilot evidence summary stores ids and hashes only.',
    });
    const run = M9PilotRunSchema.parse({
      id: 'm9_pilot_run_1',
      schemaVersion,
      createdAt,
      status: 'passed',
      requestTitleHash: 'sha256:title',
      requestDescriptionHash: 'sha256:description',
      readiness,
      steps: [step],
      evidenceSummary,
      worktreeRunId: 'worktree_run_1',
      codexStatus: 'passed',
      verificationStatus: 'passed',
      prDraftStatus: 'blocked_no_patch',
      changedFileCount: 0,
      cleanupRequired: true,
      gitProcessBoundaryInvoked: true,
      codexProcessBoundaryInvoked: true,
      nxProcessBoundaryInvoked: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      codexNoRealWrite: true,
      pushAllowed: false,
      pullRequestOpened: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M9 pilot passed while PR draft remains blocked because no patch exists.',
    });

    const serialized = JSON.stringify(run);
    expect(run.prDraftStatus).toBe('blocked_no_patch');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('stdout');
    expect(serialized).not.toContain('stderr');
    expect(serialized).not.toContain('raw prompt body');
    expect(() =>
      M9PilotRunSchema.parse({
        ...run,
        id: 'm9_pilot_run_bad_patch',
        changedFileCount: 1,
      }),
    ).toThrow();
    expect(() =>
      M9PilotRunSchema.parse({
        ...run,
        id: 'm9_pilot_run_bad_metadata',
        metadata: { prompt: 'raw prompt body' },
      }),
    ).toThrow();
  });

  it('parses M11 narrow-path pilot contracts as metadata-only', () => {
    const step = M11PilotStepSchema.parse({
      id: 'm11_pilot_step_codex_1',
      schemaVersion,
      createdAt,
      phase: 'codex',
      status: 'completed',
      order: 2,
      evidenceRefIds: ['evidence_codex_1'],
      auditEventIds: ['audit_codex_1'],
      boundaryInvoked: true,
      externalProcessStarted: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Codex read-only dry-run completed without patch generation.',
    });
    const readiness = M11PilotReadinessSchema.parse({
      id: 'm11_pilot_readiness_1',
      schemaVersion,
      createdAt,
      status: 'ready',
      checkCount: 8,
      passedCheckCount: 8,
      blockerCount: 0,
      blockers: [],
      worktreeManagerEnabled: true,
      codexReadOnlyDryRunOnly: true,
      nxVerificationPlanned: true,
      localControlRequired: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 pilot readiness is ready.',
    });
    const evidenceSummary = M11PilotEvidenceSummarySchema.parse({
      id: 'm11_pilot_evidence_summary_1',
      schemaVersion,
      createdAt,
      runId: 'm11_pilot_run_1',
      evidenceRefIds: ['evidence_codex_1'],
      auditEventIds: ['audit_codex_1'],
      evidenceCount: 1,
      auditEventCount: 1,
      bundleHash: 'sha256:m11-bundle',
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 pilot evidence summary stores ids and hashes only.',
    });
    const failureSummary = M11PilotFailureSummarySchema.parse({
      id: 'm11_pilot_failure_summary_1',
      schemaVersion,
      createdAt,
      runId: 'm11_pilot_run_1',
      classification: 'none',
      blockerCount: 0,
      blockers: [],
      cleanupRequired: true,
      boundaryReached: true,
      approvalConsumed: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 pilot has no failure.',
    });
    expect(M11PilotRecoveryActionSchema.options).toContain('review_cleanup_handoff');
    expect(M11PilotCleanupApprovalStatusSchema.options).toContain('not_requested');
    const cleanupHandoff = M11PilotCleanupHandoffSchema.parse({
      id: 'm11_pilot_cleanup_handoff_1',
      schemaVersion,
      createdAt,
      runId: 'm11_pilot_run_1',
      worktreeRunId: 'worktree_run_1',
      cleanupRequired: true,
      cleanupDeferred: true,
      cleanupCompleted: false,
      cleanupDryRunId: 'worktree_cleanup_dry_run_1',
      cleanupApprovalStatus: 'requested',
      cleanupBlockers: ['cleanup_approval_pending'],
      cleanupEvidenceRefIds: ['evidence_cleanup_1'],
      cleanupAuditEventIds: ['audit_cleanup_1'],
      cleanupEvidenceCount: 1,
      cleanupAuditEventCount: 1,
      worktreePathHash: 'sha256:worktree-path',
      gitProcessBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 cleanup handoff records only ids and hashes.',
    });
    const recovery = M11PilotRecoveryProjectionSchema.parse({
      id: 'm11_pilot_recovery_projection_1',
      schemaVersion,
      createdAt,
      runId: 'm11_pilot_run_1',
      status: 'passed',
      failureClassification: 'none',
      recoveryAction: 'review_cleanup_handoff',
      cleanupHandoff,
      evidenceRefIds: ['evidence_codex_1'],
      auditEventIds: ['audit_codex_1'],
      evidenceCount: 1,
      auditEventCount: 1,
      boundaryReached: true,
      approvalConsumed: true,
      gitProcessBoundaryInvoked: true,
      codexProcessBoundaryInvoked: true,
      nxProcessBoundaryInvoked: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      localControlRequired: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 recovery projection points at cleanup handoff metadata.',
    });
    const run = M11PilotRunSchema.parse({
      id: 'm11_pilot_run_1',
      schemaVersion,
      createdAt,
      status: 'passed',
      requestTitleHash: 'sha256:title',
      requestDescriptionHash: 'sha256:description',
      readiness,
      steps: [step],
      evidenceSummary,
      failureSummary,
      worktreeRunId: 'worktree_run_1',
      codexStatus: 'passed',
      verificationStatus: 'passed',
      prDraftStatus: 'not_ready_no_patch',
      changedFileCount: 0,
      cleanupRequired: true,
      gitProcessBoundaryInvoked: true,
      codexProcessBoundaryInvoked: true,
      nxProcessBoundaryInvoked: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      codexNoRealWrite: true,
      codexReadOnlyDryRunOnly: true,
      patchGenerationAllowed: false,
      pushAllowed: false,
      pullRequestOpened: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 pilot passed while PR draft remains not ready because no patch exists.',
    });

    const serialized = JSON.stringify(run);
    const recoverySerialized = JSON.stringify(recovery);
    expect(run.prDraftStatus).toBe('not_ready_no_patch');
    expect(recovery.recoveryAction).toBe('review_cleanup_handoff');
    expect(recovery.cleanupHandoff.cleanupApprovalStatus).toBe('requested');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('stdout');
    expect(serialized).not.toContain('stderr');
    expect(serialized).not.toContain('raw prompt body');
    expect(recoverySerialized).not.toContain('diff --git');
    expect(recoverySerialized).not.toContain('C:/');
    expect(recoverySerialized).not.toContain('stdout');
    expect(recoverySerialized).not.toContain('stderr');
    expect(recoverySerialized).not.toContain('raw prompt body');
    expect(() =>
      M11PilotRunSchema.parse({
        ...run,
        id: 'm11_pilot_run_bad_patch',
        changedFileCount: 1,
      }),
    ).toThrow();
    expect(() =>
      M11PilotRunSchema.parse({
        ...run,
        id: 'm11_pilot_run_bad_metadata',
        metadata: { localControlKey: 'secret', prompt: 'raw prompt body' },
      }),
    ).toThrow();
    expect(() =>
      M11PilotRecoveryProjectionSchema.parse({
        ...recovery,
        id: 'm11_pilot_recovery_bad_metadata',
        metadata: { path: 'C:/repo/worktree', diff: 'diff --git' },
      }),
    ).toThrow();
  });

  it('parses M11 acceptance smoke contracts as fixture-only metadata', () => {
    expect(M11PilotAcceptanceScenarioSchema.options).toEqual([
      'all-pass',
      'readiness-blocked',
      'worktree-approval-blocked',
      'worktree-boundary-failed',
      'codex-failed',
      'nx-failed',
    ]);

    const step = M11PilotAcceptanceSmokeStepSchema.parse({
      id: 'm11_pilot_acceptance_smoke_step_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      phase: 'codex',
      status: 'passed',
      order: 2,
      evidenceRefIds: ['m11_smoke_evidence_1'],
      auditEventIds: ['m11_smoke_audit_1'],
      boundaryInvoked: false,
      externalProcessStarted: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 smoke fixture marks Codex dry-run stage passed without execution.',
    });
    const run = M11PilotAcceptanceSmokeRunSchema.parse({
      id: 'm11_pilot_acceptance_smoke_run_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      status: 'passed',
      steps: [step],
      failureClassification: 'none',
      recoveryAction: 'review_cleanup_handoff',
      prDraftStatus: 'not_ready_no_patch',
      cleanupRequired: true,
      evidenceRefIds: ['m11_smoke_evidence_1'],
      auditEventIds: ['m11_smoke_audit_1'],
      evidenceCount: 1,
      auditEventCount: 1,
      fixtureOnly: true,
      codexReadOnlyDryRunOnly: true,
      patchGenerationAllowed: false,
      pushAllowed: false,
      pullRequestOpened: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'M11 acceptance smoke passed using fixture-only metadata.',
    });
    const failed = M11PilotAcceptanceSmokeRunSchema.parse({
      ...run,
      id: 'm11_pilot_acceptance_smoke_run_failed',
      scenario: 'nx-failed',
      status: 'failed',
      failureClassification: 'nx_failed',
      recoveryAction: 'review_nx_verification',
      prDraftStatus: 'blocked',
      summary: 'M11 acceptance smoke failed at Nx fixture stage.',
    });
    const serialized = JSON.stringify({ run, failed });

    expect(run.status).toBe('passed');
    expect(run.fixtureOnly).toBe(true);
    expect(run.prDraftStatus).toBe('not_ready_no_patch');
    expect(failed.status).toBe('failed');
    expect(failed.prDraftStatus).toBe('blocked');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('stdout');
    expect(serialized).not.toContain('stderr');
    expect(serialized).not.toContain('raw prompt body');
    expect(() =>
      M11PilotAcceptanceSmokeRunSchema.parse({
        ...run,
        id: 'm11_pilot_acceptance_smoke_bad_pr',
        prDraftStatus: 'blocked',
      }),
    ).toThrow();
    expect(() =>
      M11PilotAcceptanceSmokeStepSchema.parse({
        ...step,
        id: 'm11_pilot_acceptance_smoke_bad_metadata',
        metadata: { prompt: 'raw prompt body', path: 'C:/repo' },
      }),
    ).toThrow();
    expect(() =>
      M11PilotAcceptanceSmokeRunSchema.parse({
        ...run,
        id: 'm11_pilot_acceptance_smoke_bad_metadata',
        metadata: { diff: 'diff --git', localControlKey: 'secret' },
      }),
    ).toThrow();
  });

  it('parses approval UX inbox and decision contracts as metadata-only', () => {
    expect(ApprovalUxTypeSchema.options).toContain('m9_pilot');
    expect(ApprovalUxDecisionSchema.options).toEqual(['approved', 'denied', 'revoked']);

    const item = ApprovalInboxItemSchema.parse({
      id: 'approval_inbox_item_1',
      schemaVersion,
      createdAt,
      approvalType: 'worktree',
      approvalRequestId: 'worktree_approval_request_1',
      approvalRecordId: 'worktree_approval_record_1',
      approvalArtifactIdHash: 'sha256:artifact',
      status: 'requested',
      dryRunIdHash: 'sha256:dry-run',
      targetHash: 'sha256:target',
      riskLevel: 'high',
      actionMode: 'write',
      policyDecisionId: 'policy_1',
      evidenceRefIds: ['evidence_1'],
      auditEventIds: ['audit_1'],
      canApprove: true,
      canDeny: true,
      canRevoke: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'Worktree approval request is ready for operator decision.',
    });
    const projection = ApprovalInboxProjectionSchema.parse({
      id: 'approval_inbox_projection_1',
      schemaVersion,
      createdAt,
      items: [item],
      itemCount: 1,
      requestedCount: 1,
      approvedCount: 0,
      terminalCount: 0,
      typeBreakdown: { worktree: 1 },
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'Approval inbox has one metadata-only item.',
    });
    const decisionRequest = ApprovalDecisionRequestSchema.parse({
      approvalRequestId: 'worktree_approval_request_1',
      approvalType: 'worktree',
      decision: 'approved',
      reason: 'Reviewed evidence bundle',
    });
    const decisionResult = ApprovalDecisionResultSchema.parse({
      id: 'approval_decision_result_1',
      schemaVersion,
      createdAt,
      approvalRequestId: decisionRequest.approvalRequestId,
      approvalType: decisionRequest.approvalType,
      decision: decisionRequest.decision,
      status: 'approved',
      approved: true,
      evidenceRefIds: ['evidence_1'],
      auditEventIds: ['audit_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'Approval decision recorded through Supervisor.',
    });

    const serialized = JSON.stringify({ projection, decisionResult });
    expect(projection.itemCount).toBe(1);
    expect(decisionResult.approved).toBe(true);
    expect(serialized).not.toContain('approval-token');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('raw prompt');
    expect(() =>
      ApprovalDecisionRequestSchema.parse({
        ...decisionRequest,
        approvalArtifact: { id: 'untrusted' },
      }),
    ).toThrow();
    expect(() =>
      ApprovalInboxItemSchema.parse({
        ...item,
        id: 'approval_inbox_item_bad_metadata',
        metadata: { executionAuthority: { allowed: true } },
      }),
    ).toThrow();
  });

  it('parses approval decision history projections as metadata-only', () => {
    const historyItem = ApprovalDecisionHistoryItemSchema.parse({
      id: 'approval_decision_history_item_1',
      schemaVersion,
      createdAt,
      source: 'decision_result',
      approvalType: 'worktree',
      approvalRequestId: 'worktree_approval_request_1',
      approvalDecisionResultId: 'approval_decision_result_1',
      decision: 'approved',
      status: 'approved',
      targetHash: 'sha256:target',
      reasonHash: 'sha256:reason',
      reasonSummary: 'Operator reason stored as hash-only summary.',
      evidenceRefIds: ['evidence_1'],
      auditEventIds: ['audit_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'Approval decision history item stores metadata only.',
    });
    const historySummary = ApprovalDecisionHistorySummarySchema.parse({
      id: 'approval_decision_history_summary_1',
      schemaVersion,
      createdAt,
      projectionId: 'approval_decision_history_projection_1',
      itemCount: 1,
      requestedCount: 0,
      approvedCount: 1,
      deniedCount: 0,
      revokedCount: 0,
      terminalCount: 0,
      typeBreakdown: { worktree: 1 },
      statusBreakdown: { approved: 1 },
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'Approval decision history summary stores counts only.',
    });
    const projection = ApprovalDecisionHistoryProjectionSchema.parse({
      id: 'approval_decision_history_projection_1',
      schemaVersion,
      createdAt,
      items: [historyItem],
      itemCount: 1,
      requestedCount: 0,
      approvedCount: 1,
      deniedCount: 0,
      revokedCount: 0,
      terminalCount: 0,
      typeBreakdown: { worktree: 1 },
      statusBreakdown: { approved: 1 },
      decisionBreakdown: { approved: 1 },
      summaryProjection: historySummary,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'Approval decision history has one metadata-only item.',
    });
    const serialized = JSON.stringify(projection);

    expect(projection.itemCount).toBe(1);
    expect(serialized).not.toContain('approval-token');
    expect(serialized).not.toContain('C:/');
    expect(serialized).not.toContain('raw prompt');
    expect(serialized).not.toContain('Reviewed raw private reason');
    expect(() =>
      ApprovalDecisionHistoryItemSchema.parse({
        ...historyItem,
        id: 'approval_decision_history_item_bad_reason',
        reason: 'Reviewed raw private reason',
      }),
    ).toThrow();
    expect(() =>
      ApprovalDecisionHistoryProjectionSchema.parse({
        ...projection,
        id: 'approval_decision_history_projection_bad_metadata',
        metadata: { token: 'approval-token' },
      }),
    ).toThrow();
  });

  it('rejects M10 hardening raw metadata across operator and approval projections', () => {
    const forbiddenMetadata = {
      raw: {
        reason: 'raw operator reason',
        prompt: 'raw prompt',
        stdout: 'stdout body',
        stderr: 'stderr body',
        diff: 'diff --git',
        path: 'C:/private/worktree',
        body: 'raw body',
        token: 'approval-token',
        cookie: 'approval-cookie',
        session: 'approval-session',
        localControlKey: 'local-control-secret',
      },
    };
    const operatorStep = {
      id: 'm10_operator_step_hardening',
      schemaVersion,
      createdAt,
      code: 'doctor_preflight',
      label: 'Doctor preflight',
      phase: 'preflight',
      status: 'blocked',
      required: true,
      blockerCount: 1,
      blockers: ['integration_disabled'],
      safeEnableNotes: ['Review operator checklist.'],
      evidenceRefIds: [],
      auditEventIds: [],
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'Operator step stores metadata only.',
    };
    const acceptanceStep = {
      id: 'm10_acceptance_step_hardening',
      schemaVersion,
      createdAt,
      code: 'doctor_preflight',
      phase: 'doctor',
      status: 'passed',
      order: 0,
      evidenceRefIds: [],
      auditEventIds: [],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'Acceptance step stores metadata only.',
    };
    const historyItem = {
      id: 'approval_decision_history_item_hardening',
      schemaVersion,
      createdAt,
      source: 'decision_result',
      approvalType: 'worktree',
      approvalRequestId: 'worktree_approval_request_1',
      approvalDecisionResultId: 'approval_decision_result_1',
      decision: 'denied',
      status: 'denied',
      targetHash: 'sha256:target',
      reasonHash: 'sha256:reason',
      reasonSummary: 'Operator reason stored as hash-only summary.',
      evidenceRefIds: [],
      auditEventIds: [],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      tokenStored: false,
      summary: 'Approval decision history item stores metadata only.',
    };

    expect(() => M10PilotOperatorStepSchema.parse({ ...operatorStep, metadata: forbiddenMetadata }))
      .toThrow();
    expect(() => M10PilotChecklistSchema.parse({
      id: 'm10_pilot_checklist_hardening',
      schemaVersion,
      createdAt,
      status: 'blocked',
      steps: [operatorStep],
      readyStepCount: 0,
      blockedStepCount: 1,
      reviewStepCount: 0,
      requiredStepCount: 1,
      blockerCount: 1,
      integrationCount: 8,
      configuredLocalControlKeyCount: 0,
      governanceRunCount: 0,
      approvalInboxItemCount: 0,
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      localControlKeyRead: false,
      supervisorPostAllowed: false,
      adapterExecuteAllowed: false,
      summary: 'Checklist stores metadata only.',
      metadata: forbiddenMetadata,
    })).toThrow();
    expect(() => M10PilotAcceptanceStepSchema.parse({ ...acceptanceStep, metadata: forbiddenMetadata }))
      .toThrow();
    expect(() =>
      M10PilotAcceptanceRehearsalRunSchema.parse({
        id: 'm10_acceptance_run_hardening',
        schemaVersion,
        createdAt,
        status: 'passed',
        scenario: 'all-pass',
        checklistId: 'm10_pilot_checklist_1',
        approvalHistoryProjectionId: 'approval_decision_history_projection_1',
        governanceProjectionHash: 'm10:governance',
        goldenPathRunId: 'golden_path_1',
        goldenPathStatus: 'passed',
        prActionStatus: 'not_ready_no_live_pr',
        steps: [acceptanceStep],
        evidenceSummary: {
          id: 'm10_acceptance_evidence_summary_hardening',
          schemaVersion,
          createdAt,
          rehearsalRunId: 'm10_acceptance_run_hardening',
          evidenceRefIds: [],
          auditEventIds: [],
          evidenceCount: 0,
          auditEventCount: 0,
          evidenceBundleHash: 'm10:bundle',
          governanceProjectionHash: 'm10:governance',
          telemetryProjectionHash: 'm10:telemetry',
          evidenceAuditAuthoritative: true,
          telemetryAuthoritative: false,
          rawPathStored: false,
          bodyStored: false,
          tokenStored: false,
          summary: 'Evidence summary stores metadata only.',
        },
        processBoundaryInvoked: false,
        externalProcessStarted: false,
        networkBoundaryInvoked: false,
        noRealWrite: true,
        rawPathStored: false,
        bodyStored: false,
        tokenStored: false,
        localControlKeyRead: false,
        supervisorPostAllowed: false,
        adapterExecuteAllowed: false,
        pushAllowed: false,
        pullRequestOpened: false,
        evidenceAuditAuthoritative: true,
        telemetryAuthoritative: false,
        summary: 'Acceptance run stores metadata only.',
        metadata: forbiddenMetadata,
      }),
    ).toThrow();
    expect(() => ApprovalDecisionHistoryItemSchema.parse({ ...historyItem, metadata: forbiddenMetadata }))
      .toThrow();
  });

  it('parses verification adapter contract models', () => {
    expect(VerificationTargetSchema.options).toEqual(['lint', 'test', 'build']);
    expect(() => VerificationTargetSchema.parse('release')).toThrow();

    const affectedProject = AffectedProjectSchema.parse({
      id: 'affected_project_1',
      schemaVersion,
      createdAt,
      name: 'contracts',
      nameHash: 'sha256:contracts',
    });

    const plan = VerificationPlanSchema.parse({
      id: 'verification_plan_1',
      schemaVersion,
      createdAt,
      adapterName: 'nx-affected',
      cwdHash: 'sha256:cwd',
      targets: ['lint', 'test'],
      baseRef: 'HEAD~1',
      headRef: 'HEAD',
      affectedProjects: [affectedProject],
      commandHash: 'sha256:command',
      processBoundaryPlanned: true,
      noRealWrite: true,
      bodyStored: false,
      summary: 'Verify affected projects.',
    });

    const commandResult = VerificationCommandResultSchema.parse({
      id: 'verification_command_1',
      schemaVersion,
      createdAt,
      commandKind: 'verification',
      targets: ['lint', 'test'],
      status: 'completed',
      exitCode: 0,
      stdoutHash: 'sha256:stdout',
      stderrHash: 'sha256:stderr',
      stdoutLineCount: 3,
      stderrLineCount: 0,
      outputBodyStored: false,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      summary: 'Verification passed.',
    });

    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_verification_1',
      schemaVersion,
      createdAt,
      kind: 'verification.run_summary',
      hash: 'sha256:verification',
    });

    const run = VerificationRunSchema.parse({
      id: 'verification_run_1',
      schemaVersion,
      createdAt,
      targetId: 'nx-affected',
      status: 'passed',
      checks: ['lint', 'test'],
      evidenceRefs: [evidence],
      planId: plan.id,
      affectedProjects: [affectedProject],
      commandResults: [commandResult],
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      noRealWrite: true,
      auditEventIds: ['audit_1'],
      summary: 'Verification run passed.',
    });

    expect(plan.targets).toEqual(['lint', 'test']);
    expect(run.status).toBe('passed');
    expect(run.commandResults?.[0]?.outputBodyStored).toBe(false);
  });

  it('parses minimal orchestration run contract models without raw bodies', () => {
    expect(OrchestrationRunStatusSchema.options).toEqual([
      'planned',
      'running',
      'passed',
      'failed',
      'blocked',
      'aborted',
    ]);

    const event = OrchestrationTimelineEventSchema.parse({
      id: 'orchestration_timeline_event_1',
      schemaVersion,
      createdAt,
      runId: 'orchestration_run_1',
      phase: 'codex',
      status: 'passed',
      summary: 'Codex adapter completed from governed input.',
      evidenceRefIds: ['evidence_1'],
      auditEventIds: ['audit_1'],
    });
    const run = OrchestrationRunSchema.parse({
      id: 'orchestration_run_1',
      schemaVersion,
      createdAt,
      requestId: 'development_request_1',
      orchestrationPlanId: 'orchestration_plan_1',
      status: 'passed',
      codexRun: {
        adapterName: 'codex-cli',
        status: 'passed',
        capabilityResultId: 'capability_result_codex',
        processBoundaryInvoked: true,
        externalProcessStarted: true,
        noRealWrite: true,
        evidenceRefIds: ['evidence_1'],
        auditEventIds: ['audit_1'],
        summary: 'Codex CLI adapter completed.',
      },
      verificationRun: {
        adapterName: 'nx-affected',
        status: 'passed',
        capabilityResultId: 'capability_result_nx',
        processBoundaryInvoked: true,
        externalProcessStarted: true,
        noRealWrite: true,
        evidenceRefIds: ['evidence_2'],
        auditEventIds: ['audit_2'],
        summary: 'Nx verification passed.',
      },
      timeline: [event],
      evidenceRefIds: ['evidence_1', 'evidence_2'],
      auditEventIds: ['audit_1', 'audit_2'],
      policyDecisionIds: ['policy_1', 'policy_2'],
      summary: {
        requestTitle: 'Minimal governed orchestration',
        status: 'passed',
        codexStatus: 'passed',
        verificationStatus: 'passed',
        affectedProjectCount: 2,
        commandResultCount: 2,
        evidenceCount: 2,
        auditEventCount: 2,
        policyDecisionCount: 2,
        processBoundaryInvoked: true,
        externalProcessStarted: true,
        noRealWrite: true,
        bodyStored: false,
        rawPathStored: false,
      },
    });

    expect(run.timeline[0]?.phase).toBe('codex');
    expect(JSON.stringify(run)).not.toContain('stdout body');
    expect(JSON.stringify(run)).not.toContain('C:\\Users\\Thomas');
  });

  it('parses skill resolution contract models', () => {
    const result = SkillResolutionResultSchema.parse({
      id: 'skill_resolution_1',
      schemaVersion,
      createdAt,
      inputSummary: 'mock resolution',
      selectedSkills: [
        {
          skillId: 'codexhub-architecture-planner',
          score: 90,
          matchedKeywords: ['architecture'],
          reason: 'Required capability architecture.planning matched.',
          required: true,
          skill: {
            id: 'codexhub-architecture-planner',
            displayName: 'Architecture Planner',
            description: 'Mock descriptor',
            capabilities: [
              {
                id: 'architecture.planning',
                description: 'Plan architecture',
                riskLevel: 'low',
                readOnlyDefault: true,
              },
            ],
            triggers: [
              {
                id: 'architecture.trigger',
                keywords: ['architecture'],
                capabilityIds: ['architecture.planning'],
              },
            ],
          },
        },
      ],
    });

    expect(result.selectedSkills[0]?.skillId).toBe('codexhub-architecture-planner');
  });

  it('parses codex exec replay contract models', () => {
    const result = CodexExecReplayResultSchema.parse({
      id: 'codex_replay_1',
      schemaVersion,
      createdAt,
      threadId: 'thread_fixture_1',
      events: [
        {
          id: 'codex_event_1',
          schemaVersion,
          createdAt,
          rawEventType: 'thread.started',
          normalizedType: 'thread.started',
          threadId: 'thread_fixture_1',
          summary: 'Thread started',
          payloadHash: 'sha256:event',
          payloadLength: 42,
          safe: true,
        },
      ],
      eventCount: 1,
      itemCount: 0,
      commandExecutionCount: 0,
      fileChangeCount: 0,
      mcpToolCallCount: 0,
      webSearchCount: 0,
      errorCount: 0,
      finalStatus: 'completed',
      evidenceRefs: [
        {
          id: 'evidence_codex_1',
          schemaVersion,
          createdAt,
          kind: 'codex.exec.jsonl.replay',
          hash: 'sha256:replay',
          summary: 'Codex fixture replay summary',
        },
      ],
      auditEvents: [
        {
          id: 'audit_codex_1',
          schemaVersion,
          createdAt,
          actor: 'codex-kernel.fixture-replay',
          action: 'codex.exec.fixture_replay.completed',
          outcome: 'completed',
          evidenceRefs: [],
        },
      ],
    });

    expect(result.finalStatus).toBe('completed');
    expect(result.evidenceRefs[0]?.kind).toBe('codex.exec.jsonl.replay');
  });

  it('parses codex replay storage records without jsonl body', () => {
    const record = CodexReplayRecordSchema.parse({
      id: 'codex_replay_1',
      schemaVersion,
      createdAt,
      sourceKind: 'fixture',
      fixturePath: 'packages/codex-kernel/fixtures/codex-exec-basic.jsonl',
      threadId: 'thread_fixture_1',
      status: 'completed',
      summary: 'Fixture replay completed: 1 events, 0 errors',
      replayHash: 'sha256:replay',
      eventCount: 1,
      itemCount: 0,
      commandExecutionCount: 0,
      fileChangeCount: 0,
      mcpToolCallCount: 0,
      webSearchCount: 0,
      errorCount: 0,
      mockOnly: true,
      liveExecution: false,
      externalProcessStarted: false,
      evidenceRefs: [],
      auditEventIds: ['audit_codex_1'],
      storageMetadata: {
        sourceKind: 'fixture',
        fixturePath: 'packages/codex-kernel/fixtures/codex-exec-basic.jsonl',
        fixturePathHash: 'sha256:path',
        replayHash: 'sha256:replay',
        bodyStored: false,
        normalizedEventsStored: false,
        eventHashCount: 1,
        mockOnly: true,
        liveExecution: false,
        externalProcessStarted: false,
      },
    });

    expect(record.sourceKind).toBe('fixture');
    expect(JSON.stringify(record)).not.toContain('thread.started');
  });

  it('parses codex live control-plane records without prompt body', () => {
    const intent = {
      id: 'codex_intent_1',
      schemaVersion,
      createdAt,
      title: 'Summarize repository structure',
      cwd: '.',
      sandboxMode: 'read_only',
      approvalMode: 'required',
      promptSummary: 'Summarize repository structure',
      promptHash: 'sha256:prompt',
      promptLength: 30,
      promptBodyStored: false,
      liveAdapterEnabled: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    };
    const dryRunPlan = {
      id: 'codex_dry_run_1',
      schemaVersion,
      createdAt,
      intentId: intent.id,
      intent,
      title: intent.title,
      cwd: '.',
      sandboxMode: 'read_only',
      approvalMode: 'required',
      riskLevel: 'medium',
      promptSummary: intent.promptSummary,
      promptHash: intent.promptHash,
      promptLength: intent.promptLength,
      promptBodyStored: false,
      liveAdapterEnabled: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      summary: 'Dry-run control plan',
    };
    const policyDecision = {
      id: 'policy_1',
      schemaVersion,
      createdAt,
      actionId: dryRunPlan.id,
      actionType: 'codex.exec.live.intent',
      actionMode: 'read',
      riskLevel: 'medium',
      outcome: 'deny',
      reasons: ['disabled'],
      requiresDryRun: true,
      requiresApproval: true,
    };
    const record = CodexExecLiveRunRecordSchema.parse({
      id: 'codex_live_run_1',
      schemaVersion,
      createdAt,
      intentId: intent.id,
      dryRunPlanId: dryRunPlan.id,
      title: intent.title,
      cwd: '.',
      sandboxMode: 'read_only',
      approvalMode: 'required',
      riskLevel: 'medium',
      status: 'blocked',
      intent,
      dryRunPlan,
      commandPreview: {
        id: 'codex_preview_1',
        schemaVersion,
        createdAt,
        intentId: intent.id,
        dryRunPlanId: dryRunPlan.id,
        cwd: '.',
        sandboxMode: 'read_only',
        approvalMode: 'required',
        previewSummary: 'Disabled preview',
        binaryName: 'codex',
        argumentSummary: 'summarized arguments only',
        previewHash: 'sha256:preview',
        redacted: true,
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
      },
      policyDecision,
      approvalRequirement: {
        id: 'codex_approval_1',
        schemaVersion,
        createdAt,
        dryRunPlanId: dryRunPlan.id,
        policyDecisionId: policyDecision.id,
        required: true,
        riskLevel: 'medium',
        approvalMode: 'required',
        status: 'blocked',
        reason: 'approval required',
      },
      disabledError: {
        id: 'codex_disabled_error_1',
        schemaVersion,
        createdAt,
        code: 'CODEX_EXEC_LIVE_DISABLED',
        message: 'Live adapter disabled',
        reason: 'control-plane only',
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
      },
      evidenceRefs: [],
      auditEvents: [],
      promptSummary: intent.promptSummary,
      promptHash: intent.promptHash,
      promptLength: intent.promptLength,
      promptBodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(record.liveExecution).toBe(false);
    expect(record.externalProcessStarted).toBe(false);
    expect(record.promptBodyStored).toBe(false);
    expect(JSON.stringify(record)).not.toContain('list risk areas');
  });

  it('parses codex live config and preflight models', () => {
    const config = CodexExecLiveConfigSchema.parse({
      id: 'codex_live_config_1',
      schemaVersion,
      createdAt,
    });
    const preflight = CodexExecPreflightResultSchema.parse({
      id: 'codex_preflight_1',
      schemaVersion,
      createdAt,
      dryRunPlanId: 'codex_dry_run_1',
      dryRunPlanHash: 'sha256:dry-run',
      configId: config.id,
      status: 'blocked',
      checks: [
        {
          id: 'codex_preflight_check_1',
          schemaVersion,
          createdAt,
          name: 'live-config',
          status: 'failed',
          summary: 'disabled by config',
        },
      ],
      worktreeRequirement: {
        id: 'codex_worktree_requirement_1',
        schemaVersion,
        createdAt,
        sandboxMode: 'read_only',
        requirement: {
          requiresIsolatedWorktree: false,
          isolatedWorktreePresent: false,
        },
        status: 'not_required',
        summary: 'not required',
      },
      summary: 'blocked',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(config.liveEnabled).toBe(false);
    expect(config.allowedSandboxModes).toEqual(['read_only']);
    expect(config.forbiddenSandboxModes).toEqual(['workspace_write', 'danger_full_access']);
    expect(config.liveExecution).toBe(false);
    expect(config.executionDisabled).toBe(true);
    expect(preflight.status).toBe('blocked');
  });

  it('parses real read-only adapter process-start state as authoritative metadata', () => {
    const realBoundaryFlags = {
      liveExecution: false,
      externalProcessStarted: true,
      executionDisabled: true,
      processAdapterStarted: true,
      processAdapterApproved: false,
      implementationApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      dashboardTriggerAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      promptBodyStored: false,
      commandBodyStored: false,
      stdoutBodyStored: false,
      stderrBodyStored: false,
      agentMessageBodyStored: false,
      reasoningBodyStored: false,
    } as const;
    const evidenceSummary = CodexExecRealReadOnlyAdapterEvidenceSummarySchema.parse({
      id: 'codex_real_read_only_adapter_evidence_summary_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      requestId: 'codex_real_read_only_adapter_request_1',
      resultId: 'codex_real_read_only_adapter_result_1',
      evidenceRefIds: ['evidence_1'],
      eventHashCount: 2,
      outputHashCount: 2,
      metadataHash: 'sha256:evidence',
      redacted: true,
      summary: 'Metadata-only evidence summary.',
      ...realBoundaryFlags,
    });
    const auditSummary = CodexExecRealReadOnlyAdapterAuditSummarySchema.parse({
      id: 'codex_real_read_only_adapter_audit_summary_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      requestId: 'codex_real_read_only_adapter_request_1',
      resultId: 'codex_real_read_only_adapter_result_1',
      auditEventIds: ['audit_1'],
      beforeStartRequired: true,
      afterFinishRequired: true,
      abortRequired: true,
      failureRequired: true,
      eventCount: 1,
      summary: 'Metadata-only audit summary.',
      ...realBoundaryFlags,
    });
    const result = CodexExecRealReadOnlyAdapterResultSchema.parse({
      id: 'codex_real_read_only_adapter_result_1',
      schemaVersion,
      createdAt,
      requestId: 'codex_real_read_only_adapter_request_1',
      dryRunId: 'codex_dry_run_1',
      preflightId: 'codex_real_read_only_adapter_preflight_1',
      status: 'completed',
      evidenceSummary,
      auditSummary,
      postRunVerificationRequired: true,
      workspaceMutationAllowed: false,
      unexpectedWorkspaceDiffCritical: true,
      autoRevertAllowed: false,
      summary: 'Boundary completed with metadata-only process state.',
      ...realBoundaryFlags,
    });
    const noLiveEvidence = CodexExecNoLiveEvidenceSummarySchema.parse({
      id: 'codex_no_live_evidence_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      noRealCodexExec: true,
      noExternalProcessStarted: false,
      noBrowserOrCdpAction: true,
      noWorkspaceWrite: true,
      noExecutionApprovalGranted: true,
      evidenceRefCount: 1,
      auditEventCount: 1,
      evidenceKinds: ['codex.exec.real_read_only_adapter.attempt'],
      auditActions: ['codex.exec.real_read_only_adapter.complete'],
      summary: 'Boundary was invoked through the real read-only adapter path.',
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(result.externalProcessStarted).toBe(true);
    expect(result.processAdapterStarted).toBe(true);
    expect(result.evidenceSummary?.externalProcessStarted).toBe(true);
    expect(result.auditSummary?.externalProcessStarted).toBe(true);
    expect(noLiveEvidence.noExternalProcessStarted).toBe(false);
  });

  it('parses codex config load and manual approval records', () => {
    const config = CodexExecLiveConfigSchema.parse({
      id: 'codex_live_config_1',
      schemaVersion,
      createdAt,
      configSource: 'file',
      configPath: '.codexhub/codex-exec.yaml',
      configPathHash: 'sha256:path',
    });
    const configLoad = CodexExecConfigLoadResultSchema.parse({
      id: 'codex_config_load_1',
      schemaVersion,
      createdAt,
      source: 'file',
      status: 'loaded',
      config,
      configFile: {
        id: 'codex_config_file_1',
        schemaVersion,
        createdAt,
        configPath: '.codexhub/codex-exec.yaml',
        configPathHash: 'sha256:path',
        configHash: 'sha256:config',
        bodyStored: false,
        summary: 'Loaded disabled config file',
      },
      summary: 'Loaded config',
    });
    const approvalRecord = CodexExecManualApprovalRecordSchema.parse({
      id: 'codex_approval_record_1',
      schemaVersion,
      createdAt,
      request: {
        id: 'codex_approval_request_1',
        schemaVersion,
        createdAt,
        dryRunPlanId: 'codex_dry_run_1',
        dryRunPlanHash: 'sha256:dry-run',
        policyDecisionId: 'policy_1',
        policyDecisionHash: 'sha256:policy',
        scope: 'read_only_plan',
        status: 'pending',
        riskLevel: 'medium',
        requestedBy: 'local-human',
        reason: 'Review disabled control-plane run',
        expiresAt: '2026-04-28T01:00:00.000Z',
        singleUse: true,
        summary: 'Approval requested',
      },
      decision: {
        id: 'codex_approval_decision_1',
        schemaVersion,
        createdAt,
        approvalRequestId: 'codex_approval_request_1',
        dryRunPlanId: 'codex_dry_run_1',
        policyDecisionId: 'policy_1',
        outcome: 'approved',
        decidedBy: 'local-human',
        reasonSummary: 'Approved for disabled gate evaluation',
        decisionHash: 'sha256:decision',
        approved: true,
        summary: 'Approval approved',
      },
      status: 'approved',
      evidenceRefs: [],
      auditEventIds: [],
      summary: 'Manual approval record',
    });

    expect(configLoad.config.configSource).toBe('file');
    expect(configLoad.configFile?.bodyStored).toBe(false);
    expect(approvalRecord.decision?.approved).toBe(true);
    expect(approvalRecord.liveExecution).toBe(false);
  });

  it('parses codex manual approval state and transition results', () => {
    const approvalState = CodexExecManualApprovalStateSchema.parse({
      id: 'codex_approval_state_1',
      schemaVersion,
      createdAt,
      approvalRecordId: 'codex_approval_record_1',
      approvalRequestId: 'codex_approval_request_1',
      dryRunPlanId: 'codex_dry_run_1',
      dryRunPlanHash: 'sha256:dry-run',
      policyDecisionId: 'policy_1',
      policyDecisionHash: 'sha256:policy',
      status: 'pending',
      requestedStatus: 'pending',
      expiresAt: '2026-04-28T01:00:00.000Z',
      expired: false,
      terminal: false,
      canDecide: true,
      nextAllowedActions: ['approve', 'deny', 'revoke'],
      reasons: ['manual approval is awaiting a decision'],
      summary: 'Manual approval state is pending',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const transition = CodexExecApprovalTransitionResultSchema.parse({
      id: 'codex_approval_transition_1',
      schemaVersion,
      createdAt,
      approvalRecordId: 'codex_approval_record_1',
      approvalRequestId: 'codex_approval_request_1',
      dryRunPlanId: 'codex_dry_run_1',
      action: 'approve',
      fromStatus: 'pending',
      toStatus: 'approved',
      allowed: true,
      reasons: ['manual approval transition approve is allowed'],
      state: approvalState,
      summary: 'Manual approval transition approve allowed',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(approvalState.canDecide).toBe(true);
    expect(transition.allowed).toBe(true);
    expect(transition.liveExecution).toBe(false);
  });

  it('parses codex control-plane timeline models', () => {
    const timeline = CodexExecControlPlaneTimelineSchema.parse({
      id: 'codex_timeline_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      liveRunRecordId: 'codex_live_run_1',
      status: 'gate_blocked',
      events: [
        {
          id: 'codex_timeline_event_1',
          schemaVersion,
          createdAt,
          dryRunId: 'codex_dry_run_1',
          liveRunRecordId: 'codex_live_run_1',
          eventType: 'codex.exec.dry_run.created',
          sourceKind: 'dry_run',
          sourceId: 'codex_dry_run_1',
          status: 'medium',
          summary: 'Dry-run control plan',
          occurredAt: createdAt,
          liveExecution: false,
          externalProcessStarted: false,
          executionDisabled: true,
        },
        {
          id: 'codex_timeline_event_2',
          schemaVersion,
          createdAt,
          dryRunId: 'codex_dry_run_1',
          liveRunRecordId: 'codex_live_run_1',
          eventType: 'codex.exec.gate.evaluated',
          sourceKind: 'gate',
          sourceId: 'codex_gate_1',
          status: 'blocked',
          summary: 'Execution gate blocked',
          occurredAt: createdAt,
          liveExecution: false,
          externalProcessStarted: false,
          executionDisabled: true,
        },
      ],
      eventCount: 2,
      evidenceCount: 1,
      auditEventCount: 1,
      summary: 'Timeline has 2 events',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(timeline.status).toBe('gate_blocked');
    expect(timeline.events.every((event) => event.liveExecution === false)).toBe(true);
    expect(timeline.events.every((event) => event.externalProcessStarted === false)).toBe(true);
    expect(timeline.events.every((event) => event.executionDisabled === true)).toBe(true);
  });

  it('parses codex timeline query and detail view models without body storage', () => {
    const filter = CodexExecTimelineFilterSchema.parse({
      source: 'approval',
      status: 'blocked',
      includeEvidence: false,
      includeAudit: false,
      limit: 10,
    });
    const query = CodexExecTimelineQuerySchema.parse({
      id: 'codex_timeline_query_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      filter,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const detail = CodexExecTimelineDetailViewSchema.parse({
      id: 'codex_timeline_detail_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      liveRunRecordId: 'codex_live_run_1',
      timeline: {
        id: 'codex_timeline_1',
        schemaVersion,
        createdAt,
        dryRunId: 'codex_dry_run_1',
        liveRunRecordId: 'codex_live_run_1',
        status: 'gate_blocked',
        events: [],
        eventCount: 0,
        evidenceCount: 1,
        auditEventCount: 1,
        summary: 'Timeline has no filtered events',
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
      },
      sourceBreakdown: {},
      latestGateStatus: 'blocked',
      approvalStatus: 'approved',
      dryRunSummary: {
        dryRunPlanId: 'codex_dry_run_1',
        title: 'Summarize repository structure',
        sandboxMode: 'read_only',
        approvalMode: 'required',
        riskLevel: 'medium',
        promptSummary: 'Prompt summary only',
        promptHash: 'sha256:prompt',
        promptLength: 30,
        promptBodyStored: false,
      },
      commandPreviewSummary: {
        commandPreviewId: 'codex_preview_1',
        previewSummary: 'Preview summary only',
        previewHash: 'sha256:preview',
        redacted: true,
        argumentSummary: 'promptHash=sha256:prompt',
      },
      policySummary: {
        policyDecisionId: 'policy_1',
        outcome: 'deny',
        riskLevel: 'medium',
        requiresDryRun: true,
        requiresApproval: true,
        reasonCount: 1,
      },
      approvalStateSummary: {
        approvalRequestId: 'codex_approval_request_1',
        status: 'approved',
        canDecide: false,
        terminal: true,
        reasonCount: 1,
      },
      gateSummary: {
        executionGateResultId: 'codex_gate_1',
        status: 'blocked',
        reasonCount: 1,
        liveEnabled: false,
      },
      evidenceSummary: {
        id: 'codex_evidence_summary_1',
        schemaVersion,
        createdAt,
        dryRunId: 'codex_dry_run_1',
        liveRunRecordId: 'codex_live_run_1',
        count: 1,
        evidenceRefIds: ['evidence_1'],
        items: [
          {
            evidenceRefId: 'evidence_1',
            kind: 'codex.exec.dry_run_plan',
            summary: 'Evidence summary only',
            hash: 'sha256:evidence',
            labels: ['codex.dry_run_plan'],
            createdAt,
          },
        ],
        metadataOnly: true,
        bodyStored: false,
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
      },
      auditSummary: {
        id: 'codex_audit_summary_1',
        schemaVersion,
        createdAt,
        dryRunId: 'codex_dry_run_1',
        liveRunRecordId: 'codex_live_run_1',
        count: 1,
        auditEventIds: ['audit_1'],
        items: [
          {
            auditEventId: 'audit_1',
            action: 'codex.exec.policy_evaluated',
            outcome: 'deny',
            createdAt,
            policyDecisionId: 'policy_1',
            evidenceRefIds: ['evidence_1'],
          },
        ],
        metadataOnly: true,
        bodyStored: false,
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
      },
      summary: 'Detail summary only',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(query.filter.source).toBe('approval');
    expect(detail.evidenceSummary.bodyStored).toBe(false);
    expect(detail.auditSummary.metadataOnly).toBe(true);
    expect(JSON.stringify(detail)).not.toContain('full prompt body');
  });

  it('parses codex evidence and audit drilldown views without body storage', () => {
    const evidenceDetail = CodexExecEvidenceDetailViewSchema.parse({
      id: 'codex_evidence_detail_1',
      schemaVersion,
      createdAt,
      status: 'found',
      evidenceRefId: 'evidence_1',
      dryRunId: 'codex_dry_run_1',
      liveRunRecordId: 'codex_live_run_1',
      kind: 'codex.exec.dry_run_plan',
      summary: 'Evidence summary only',
      hash: 'sha256:evidence',
      labels: ['codex.dry_run_plan'],
      refCreatedAt: createdAt,
      relatedAuditEventIds: ['audit_1'],
      metadataSummary: {
        keyCount: 2,
        keys: ['dryRunPlanId', 'riskLevel'],
        relatedIds: ['codex_dry_run_1'],
        bodyStored: false,
      },
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const auditDetail = CodexExecAuditDetailViewSchema.parse({
      id: 'codex_audit_detail_1',
      schemaVersion,
      createdAt,
      status: 'found',
      auditEventId: 'audit_1',
      dryRunId: 'codex_dry_run_1',
      liveRunRecordId: 'codex_live_run_1',
      action: 'codex.exec.policy_evaluated',
      outcome: 'deny',
      actor: 'codex-kernel.control-plane',
      eventCreatedAt: createdAt,
      policyDecisionId: 'policy_1',
      evidenceRefIds: ['evidence_1'],
      metadataSummary: {
        keyCount: 2,
        keys: ['dryRunPlanId', 'policyDecisionId'],
        relatedIds: ['codex_dry_run_1', 'policy_1'],
        bodyStored: false,
      },
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const evidenceSearch = CodexExecEvidenceSearchResultSchema.parse({
      id: 'codex_evidence_search_1',
      schemaVersion,
      createdAt,
      query: {
        id: 'codex_evidence_query_1',
        schemaVersion,
        createdAt,
        dryRunId: 'codex_dry_run_1',
        kind: 'codex.exec.dry_run_plan',
        limit: 20,
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
      },
      count: 1,
      items: [evidenceDetail],
      metadataOnly: true,
      bodyStored: false,
      summary: '1 evidence refs',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const auditSearch = CodexExecAuditSearchResultSchema.parse({
      id: 'codex_audit_search_1',
      schemaVersion,
      createdAt,
      query: {
        id: 'codex_audit_query_1',
        schemaVersion,
        createdAt,
        dryRunId: 'codex_dry_run_1',
        action: 'codex.exec.policy_evaluated',
        limit: 20,
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
      },
      count: 1,
      items: [auditDetail],
      metadataOnly: true,
      bodyStored: false,
      summary: '1 audit events',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const drilldown = CodexExecControlPlaneDrilldownViewSchema.parse({
      id: 'codex_drilldown_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      liveRunRecordId: 'codex_live_run_1',
      status: 'found',
      evidenceSearch,
      auditSearch,
      selectedEvidence: evidenceDetail,
      selectedAudit: auditDetail,
      evidenceCount: 1,
      auditEventCount: 1,
      metadataOnly: true,
      bodyStored: false,
      summary: 'Read-only drilldown summary',
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(drilldown.status).toBe('found');
    expect(drilldown.evidenceSearch.items[0]?.metadataOnly).toBe(true);
    expect(drilldown.auditSearch.items[0]?.bodyStored).toBe(false);
    expect(JSON.stringify(drilldown)).not.toContain('full command body');
  });

  it('parses codex control-plane report export models', () => {
    const query = {
      id: 'codex_report_query_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      format: 'markdown',
      includeEvidence: true,
      includeAudit: true,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    };
    const section = {
      id: 'codex_report_section_1',
      schemaVersion,
      createdAt,
      kind: 'overview',
      title: 'Overview',
      status: 'ok',
      summary: 'Report overview summary only',
      items: [
        {
          label: 'dryRunId',
          value: 'codex_dry_run_1',
          refId: 'codex_dry_run_1',
        },
      ],
      refIds: ['codex_dry_run_1'],
      hashes: ['sha256:summary'],
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    };
    const summary = {
      id: 'codex_report_summary_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      status: 'found',
      sectionCount: 10,
      evidenceCount: 1,
      auditEventCount: 1,
      riskLevel: 'medium',
      finalControlPlaneStatus: 'gate_blocked',
      recommendationCount: 1,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    };
    const report = CodexExecControlPlaneReportSchema.parse({
      id: 'codex_report_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      status: 'found',
      format: 'markdown',
      query,
      summary,
      sections: [section],
      sectionOrder: [
        'overview',
        'dry_run',
        'timeline',
        'approval',
        'gate',
        'evidence',
        'audit',
        'no_live_boundary',
        'risks',
        'recommendations',
      ],
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const exportResult = CodexExecControlPlaneReportExportResultSchema.parse({
      id: 'codex_report_export_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      format: 'markdown',
      status: 'found',
      report,
      renderedContent: '# Safe report',
      renderedContentHash: 'sha256:rendered',
      renderedContentLength: 13,
      metadataOnly: true,
      sourceBodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(report.sections[0]?.bodyStored).toBe(false);
    expect(exportResult.report.summary.sectionCount).toBe(10);
    expect(JSON.stringify(exportResult)).not.toContain('full stdout body');
  });

  it('parses codex report review workflow models', () => {
    const checklistItem = {
      id: 'codex_report_review_check_1',
      schemaVersion,
      createdAt,
      code: 'no_live_flags_present',
      label: 'No-live flags are present',
      status: 'passed',
      required: true,
      summary: 'liveExecution=false, externalProcessStarted=false, executionDisabled=true',
      relatedSection: 'no_live_boundary',
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    };
    const finding = {
      id: 'codex_report_review_finding_1',
      schemaVersion,
      createdAt,
      severity: 'medium',
      code: 'approval_state_missing',
      summary: 'Approval state is missing from the reviewed report summary.',
      relatedSection: 'approval',
      recommendation: 'Create a manual approval request before any later ADR review.',
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    };
    const record = CodexExecReportReviewRecordSchema.parse({
      id: 'codex_report_review_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      reportId: 'codex_report_1',
      reportHash: 'sha256:report',
      reportSectionHashes: ['sha256:overview'],
      sectionSummaryRefs: ['overview'],
      reviewedAt: createdAt,
      reviewerLabel: 'local-operator',
      status: 'reviewed',
      recommendation: 'ready_for_adr',
      recommendationGrantsExecution: false,
      riskClassification: 'medium',
      checklistItems: [checklistItem],
      findings: [finding],
      notesSummary: 'No-live boundary intact; live adapter still requires ADR.',
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const summary = CodexExecReportReviewSummarySchema.parse({
      id: 'codex_report_review_summary_1',
      schemaVersion,
      createdAt,
      reviewId: record.id,
      dryRunId: record.dryRunId,
      reportHash: record.reportHash,
      status: record.status,
      recommendation: record.recommendation,
      recommendationGrantsExecution: false,
      riskClassification: record.riskClassification,
      reviewerLabel: record.reviewerLabel,
      reviewedAt: record.reviewedAt,
      findingCount: record.findings.length,
      failedChecklistCount: 0,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const historyQuery = {
      id: 'codex_report_review_history_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'reviewed',
      recommendation: 'ready_for_adr',
      limit: 20,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    };
    const comparisonItem = CodexExecReportReviewComparisonItemSchema.parse({
      id: 'codex_report_review_comparison_item_1',
      schemaVersion,
      createdAt,
      field: 'status',
      leftValueSummary: 'reviewed',
      rightValueSummary: 'changes_requested',
      changed: true,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const comparison = CodexExecReportReviewComparisonSchema.parse({
      id: 'codex_report_review_comparison_1',
      schemaVersion,
      createdAt,
      leftReviewId: 'codex_report_review_1',
      rightReviewId: 'codex_report_review_2',
      dryRunId: record.dryRunId,
      comparable: true,
      summary: 'Review metadata changed across 1 fields.',
      changedItemCount: 1,
      items: [comparisonItem],
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const history = CodexExecReportReviewHistoryViewSchema.parse({
      id: 'codex_report_review_history_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      query: historyQuery,
      latestReview: summary,
      summaries: [summary],
      comparison,
      historyCount: 1,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const handoff = CodexExecReviewerHandoffSummarySchema.parse({
      id: 'codex_reviewer_handoff_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      fromReviewer: 'local-operator',
      toReviewer: 'next-reviewer',
      latestReviewId: record.id,
      latestStatus: record.status,
      latestRecommendation: record.recommendation,
      latestRiskClassification: record.riskClassification,
      reviewCount: 1,
      findingCount: record.findings.length,
      failedChecklistCount: 0,
      handoffSummary: 'Handoff summary is metadata-only and non-executing.',
      recommendedNextStep: 'Continue read-only ADR preparation.',
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const governanceQuery = CodexExecGovernanceReviewPackageQuerySchema.parse({
      id: 'codex_governance_package_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      includeEvidence: true,
      includeAudit: true,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const adrCheck = CodexExecAdrReadinessChecklistItemSchema.parse({
      id: 'codex_adr_readiness_check_1',
      schemaVersion,
      createdAt,
      code: 'no_live_boundary_confirmed',
      label: 'No-live boundary is confirmed',
      status: 'passed',
      required: true,
      summary: 'No live execution or external process was started.',
      sourceSection: 'no_live_boundary',
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const noLiveEvidence = CodexExecNoLiveEvidenceSummarySchema.parse({
      id: 'codex_no_live_evidence_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      noRealCodexExec: true,
      noExternalProcessStarted: true,
      noBrowserOrCdpAction: true,
      noWorkspaceWrite: true,
      noExecutionApprovalGranted: true,
      evidenceRefCount: 1,
      auditEventCount: 1,
      evidenceKinds: ['codex.exec.dry_run_plan'],
      auditActions: ['codex.exec.policy_evaluated'],
      summary: 'No-live evidence is metadata-only.',
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const governanceBlocker = CodexExecGovernanceBlockerSchema.parse({
      id: 'codex_governance_blocker_1',
      schemaVersion,
      createdAt,
      severity: 'medium',
      code: 'review_history_missing',
      summary: 'Review history should be present before ADR review.',
      sourceSection: 'review_history',
      recommendedResolution: 'Create a non-executing report review history.',
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const governanceSummary = CodexExecGovernanceReviewPackageSummarySchema.parse({
      id: 'codex_governance_package_summary_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'needs_changes',
      riskClassification: 'medium',
      recommendation: 'needs_changes',
      recommendationGrantsExecution: false,
      checklistPassedCount: 1,
      checklistWarningCount: 0,
      checklistFailedCount: 0,
      blockerCount: 1,
      unresolvedBlockerCount: 1,
      evidenceRefCount: 1,
      auditEventCount: 1,
      latestReviewId: record.id,
      reportId: 'codex_report_1',
      handoffId: handoff.id,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const governancePackage = CodexExecGovernanceReviewPackageSchema.parse({
      id: 'codex_governance_package_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'needs_changes',
      query: governanceQuery,
      summary: governanceSummary,
      sectionOrder: [
        'dry_run',
        'timeline',
        'evidence',
        'audit',
        'report',
        'report_review',
        'review_history',
        'handoff',
        'no_live_boundary',
        'adr_readiness',
        'risks',
        'blockers',
        'recommendation',
      ],
      reviewHistory: history,
      latestReview: summary,
      handoff,
      noLiveEvidence,
      adrReadinessChecklist: [adrCheck],
      blockers: [governanceBlocker],
      riskClassification: 'medium',
      recommendation: 'needs_changes',
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const adrDraftQuery = CodexExecLiveAdapterAdrDraftQuerySchema.parse({
      id: 'codex_adr_draft_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      format: 'markdown',
      includeEvidence: true,
      includeAudit: true,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      draftOnly: true,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const adrDraftSection = CodexExecLiveAdapterAdrDraftSectionSchema.parse({
      id: 'codex_adr_draft_section_1',
      schemaVersion,
      createdAt,
      kind: 'recommended_decision',
      title: 'Recommended Decision',
      status: 'blocked',
      summary: 'Recommendation is ADR guidance only and does not grant execution.',
      items: [
        {
          label: 'recommendation',
          value: 'needs_changes',
        },
      ],
      refIds: [governancePackage.id],
      hashes: ['sha256:adr-draft-section'],
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      draftOnly: true,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const adrDraftSummary = CodexExecLiveAdapterAdrDraftSummarySchema.parse({
      id: 'codex_adr_draft_summary_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'blocked',
      title: 'ADR Draft: Codex control-plane live adapter readiness',
      sectionCount: 1,
      governancePackageStatus: governancePackage.status,
      riskClassification: governancePackage.riskClassification,
      recommendation: governancePackage.recommendation,
      recommendationGrantsExecution: false,
      blockerCount: governancePackage.blockers.length,
      readinessPassedCount: 1,
      readinessFailedCount: 0,
      metadataOnly: true,
      bodyStored: false,
      draftOnly: true,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const adrDraft = CodexExecLiveAdapterAdrDraftSchema.parse({
      id: 'codex_adr_draft_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'blocked',
      format: 'markdown',
      query: adrDraftQuery,
      title: 'ADR Draft: Codex control-plane live adapter readiness',
      summary: adrDraftSummary,
      governancePackageSummary: governancePackage.summary,
      governancePackage,
      sections: [adrDraftSection],
      sectionOrder: ['recommended_decision'],
      recommendation: governancePackage.recommendation,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      draftOnly: true,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const adrDraftExport = CodexExecLiveAdapterAdrDraftExportResultSchema.parse({
      id: 'codex_adr_draft_export_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      format: 'markdown',
      status: 'blocked',
      draft: adrDraft,
      renderedContent: '# ADR Draft\n\nThis is metadata-only guidance.',
      renderedContentHash: 'sha256:adr-draft',
      renderedContentLength: 40,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      sourceBodyStored: false,
      draftOnly: true,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(record.recommendationGrantsExecution).toBe(false);
    expect(summary.recommendation).toBe('ready_for_adr');
    expect(history.latestReview?.reviewId).toBe(record.id);
    expect(comparison.recommendationGrantsExecution).toBe(false);
    expect(handoff.recommendationGrantsExecution).toBe(false);
    expect(governancePackage.recommendationGrantsExecution).toBe(false);
    expect(governancePackage.noLiveEvidence.noRealCodexExec).toBe(true);
    expect(adrDraft.draftOnly).toBe(true);
    expect(adrDraft.recommendationGrantsExecution).toBe(false);
    expect(adrDraftExport.sourceBodyStored).toBe(false);
    expect(JSON.stringify(record)).not.toContain('full command body');
    expect(JSON.stringify(history)).not.toContain('full command body');
    expect(JSON.stringify(handoff)).not.toContain('full command body');
    expect(JSON.stringify(governancePackage)).not.toContain('full command body');
    expect(JSON.stringify(adrDraft)).not.toContain('full command body');
  });

  it('parses live adapter ADR decision records without granting execution', () => {
    const gatePolicy = CodexExecLiveAdapterAdrDecisionGatePolicySchema.parse({
      id: 'codex_live_adapter_adr_gate_policy_1',
      schemaVersion,
      createdAt,
      allowedSandboxModes: ['read_only'],
      forbiddenSandboxModes: ['workspace_write', 'danger_full_access'],
      triggerSurface: 'cli_only',
      dashboardTriggerAllowed: false,
      dryRunRequired: true,
      approvalArtifactRequired: true,
      dryRunPlanHashMatchRequired: true,
      policyDecisionHashMatchRequired: true,
      isolatedWorktreeRequired: true,
      postRunVerificationCommand: 'pnpm verify:foundation',
      evidenceRequired: true,
      auditRequired: true,
      implementationApproved: false,
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const evidenceRef = {
      id: 'evidence_adr_decision_1',
      schemaVersion,
      createdAt,
      kind: 'codex.exec.live_adapter_adr_decision',
      summary: 'ADR decision evidence is metadata-only.',
      hash: 'sha256:adr-decision',
      redacted: true,
      labels: ['codex.live_adapter_adr_decision'],
    };
    const record = CodexExecLiveAdapterAdrDecisionRecordSchema.parse({
      id: 'codex_live_adapter_adr_decision_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      adrDocumentPath: 'docs/adr/round-3n-live-adapter-adr.md',
      decisionDocumentPath: 'docs/adr/round-3n-go-no-go-decision.md',
      decision: 'conditional_read_only_go',
      status: 'recorded',
      reviewerLabel: 'local-operator',
      rationaleSummary:
        'Conditional read-only design can continue; implementation is not approved.',
      recordedAt: createdAt,
      gatePolicy,
      allowedSandboxModes: ['read_only'],
      forbiddenSandboxModes: ['workspace_write', 'danger_full_access'],
      futureTriggerPolicy: 'cli_only',
      dashboardTriggerAllowed: false,
      dryRunRequired: true,
      approvalArtifactRequired: true,
      dryRunPlanHashMatchRequired: true,
      policyDecisionHashMatchRequired: true,
      isolatedWorktreeRequired: true,
      postRunVerificationCommand: 'pnpm verify:foundation',
      evidenceRequired: true,
      auditRequired: true,
      implementationApproved: false,
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_adr_decision_1'],
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const summary = CodexExecLiveAdapterAdrDecisionSummarySchema.parse({
      id: 'codex_live_adapter_adr_decision_summary_1',
      schemaVersion,
      createdAt,
      decisionId: record.id,
      dryRunId: record.dryRunId,
      decision: record.decision,
      status: record.status,
      reviewerLabel: record.reviewerLabel,
      rationaleSummary: record.rationaleSummary,
      allowedSandboxModes: record.allowedSandboxModes,
      forbiddenSandboxModes: record.forbiddenSandboxModes,
      futureTriggerPolicy: 'cli_only',
      dashboardTriggerAllowed: false,
      approvalArtifactRequired: true,
      dryRunPlanHashMatchRequired: true,
      policyDecisionHashMatchRequired: true,
      isolatedWorktreeRequired: true,
      postRunVerificationCommand: 'pnpm verify:foundation',
      evidenceCount: 1,
      auditEventCount: 1,
      implementationApproved: false,
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });
    const query = CodexExecLiveAdapterAdrDecisionQuerySchema.parse({
      id: 'codex_live_adapter_adr_decision_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'recorded',
      decision: 'conditional_read_only_go',
      limit: 10,
      implementationApproved: false,
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      dashboardTriggerAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
    });

    expect(record.decision).toBe('conditional_read_only_go');
    expect(record.allowedSandboxModes).toEqual(['read_only']);
    expect(record.forbiddenSandboxModes).toEqual(['workspace_write', 'danger_full_access']);
    expect(record.dashboardTriggerAllowed).toBe(false);
    expect(record.implementationApproved).toBe(false);
    expect(record.processAdapterApproved).toBe(false);
    expect(record.recommendationGrantsExecution).toBe(false);
    expect(summary.evidenceCount).toBe(1);
    expect(query.limit).toBe(10);
    expect(JSON.stringify(record)).not.toContain('full prompt body');
    expect(JSON.stringify(record)).not.toContain('full command body');
  });

  it('parses read-only adapter preflight simulator contracts with fixed no-live flags', () => {
    const checklistItem = CodexExecReadOnlyAdapterOperatorChecklistItemSchema.parse({
      id: 'codex_read_only_checklist_1',
      schemaVersion,
      createdAt,
      code: 'dry_run_reviewed',
      label: 'Dry-run reviewed',
      summary: 'Operator reviewed metadata-only dry-run summary.',
      checked: true,
      required: true,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const check = CodexExecReadOnlyAdapterPreflightSimulationCheckSchema.parse({
      id: 'codex_read_only_preflight_check_1',
      schemaVersion,
      createdAt,
      code: 'sandbox_read_only_only',
      source: 'sandbox',
      status: 'passed',
      required: true,
      summary: 'Sandbox is read_only.',
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const blocker = CodexExecReadOnlyAdapterPreflightSimulationBlockerSchema.parse({
      id: 'codex_read_only_preflight_blocker_1',
      schemaVersion,
      createdAt,
      code: 'approval_artifact_exists',
      source: 'approval',
      severity: 'high',
      relatedCheckCode: 'approval_artifact_exists',
      summary: 'Approval artifact is missing.',
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const input = CodexExecReadOnlyAdapterPreflightSimulationInputSchema.parse({
      id: 'codex_read_only_preflight_input_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      requestedSandboxMode: 'read_only',
      isolatedWorktreePresent: true,
      evidenceStoreReady: true,
      auditStoreReady: true,
      operatorChecklist: [checklistItem],
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const result = CodexExecReadOnlyAdapterPreflightSimulationResultSchema.parse({
      id: 'codex_read_only_preflight_result_1',
      schemaVersion,
      createdAt,
      dryRunId: input.dryRunId,
      status: 'failed',
      requestedSandboxMode: 'read_only',
      checks: [check],
      blockers: [blocker],
      operatorChecklist: [checklistItem],
      configLiveEnabled: false,
      dryRunExists: true,
      policyDecisionExists: true,
      approvalArtifactExists: false,
      approvalArtifactValid: false,
      dryRunPlanHashMatched: false,
      policyDecisionHashMatched: false,
      isolatedWorktreePresent: true,
      evidenceStoreReady: true,
      auditStoreReady: true,
      checklistComplete: true,
      adrDecisionDesignOnly: true,
      passedCheckCount: 1,
      failedCheckCount: 1,
      warningCheckCount: 0,
      blockerCount: 1,
      summary: 'Simulation failed because approval artifact is missing.',
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const summary = CodexExecReadOnlyAdapterPreflightSimulationSummarySchema.parse({
      id: 'codex_read_only_preflight_summary_1',
      schemaVersion,
      createdAt,
      simulationId: result.id,
      dryRunId: result.dryRunId,
      status: result.status,
      requestedSandboxMode: 'read_only',
      passedCheckCount: 1,
      failedCheckCount: 1,
      warningCheckCount: 0,
      blockerCount: 1,
      checklistCompletedCount: 1,
      checklistTotalCount: 1,
      summary: result.summary,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });

    expect(result.liveExecution).toBe(false);
    expect(result.externalProcessStarted).toBe(false);
    expect(result.executionDisabled).toBe(true);
    expect(result.processAdapterStarted).toBe(false);
    expect(result.implementationApproved).toBe(false);
    expect(result.dashboardTriggerAllowed).toBe(false);
    expect(result.recommendationGrantsExecution).toBe(false);
    expect(summary.status).toBe('failed');
  });

  it('parses read-only adapter simulator review contracts without granting execution', () => {
    const checklistItem = CodexExecReadOnlyAdapterSimulatorReviewChecklistItemSchema.parse({
      id: 'codex_read_only_adapter_simulator_review_check_1',
      schemaVersion,
      createdAt,
      code: 'simulator_check_approval_artifact_exists',
      label: 'Approval Artifact Exists',
      checkCode: 'approval_artifact_exists',
      disposition: 'hard_gate',
      status: 'failed',
      required: true,
      summary: 'Approval artifact is missing.',
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const finding = CodexExecReadOnlyAdapterSimulatorReviewFindingSchema.parse({
      id: 'codex_read_only_adapter_simulator_review_finding_1',
      schemaVersion,
      createdAt,
      code: checklistItem.code,
      severity: 'high',
      relatedCheckCode: checklistItem.checkCode,
      disposition: 'hard_gate',
      summary: checklistItem.summary,
      recommendation: 'Resolve this hard gate before implementation planning exits.',
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const blocker = CodexExecReadOnlyAdapterPreflightSimulationBlockerSchema.parse({
      id: 'codex_read_only_preflight_blocker_2',
      schemaVersion,
      createdAt,
      code: 'approval_artifact_exists',
      source: 'approval',
      severity: 'high',
      relatedCheckCode: 'approval_artifact_exists',
      summary: 'Approval artifact is missing.',
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const record = CodexExecReadOnlyAdapterSimulatorReviewDecisionRecordSchema.parse({
      id: 'codex_read_only_adapter_simulator_review_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      simulationId: 'codex_read_only_preflight_result_1',
      simulationStatus: 'failed',
      outcome: 'go_to_implementation_planning',
      status: 'recorded',
      reviewerLabel: 'local-operator',
      rationaleSummary:
        'Implementation planning may continue, but implementation and process adapter remain unapproved.',
      reviewedAt: createdAt,
      checklistItems: [checklistItem],
      findings: [finding],
      simulatorBlockers: [blocker],
      hardGateCount: 1,
      requiresReviewCount: 0,
      informationalCount: 0,
      unresolvedBlockerCount: 1,
      evidenceRefs: [],
      auditEventIds: ['audit_simulator_review_1'],
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const summary = CodexExecReadOnlyAdapterSimulatorReviewSummarySchema.parse({
      id: 'codex_read_only_adapter_simulator_review_summary_1',
      schemaVersion,
      createdAt,
      reviewId: record.id,
      dryRunId: record.dryRunId,
      simulationId: record.simulationId,
      simulationStatus: record.simulationStatus,
      outcome: record.outcome,
      status: record.status,
      reviewerLabel: record.reviewerLabel,
      reviewedAt: record.reviewedAt,
      hardGateCount: 1,
      requiresReviewCount: 0,
      informationalCount: 0,
      unresolvedBlockerCount: 1,
      summary: 'Review allows implementation planning only.',
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const query = CodexExecReadOnlyAdapterSimulatorReviewQuerySchema.parse({
      id: 'codex_read_only_adapter_simulator_review_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'recorded',
      outcome: 'go_to_implementation_planning',
      limit: 10,
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });

    expect(record.outcome).toBe('go_to_implementation_planning');
    expect(record.implementationApproved).toBe(false);
    expect(record.processAdapterApproved).toBe(false);
    expect(record.recommendationGrantsExecution).toBe(false);
    expect(summary.executionDisabled).toBe(true);
    expect(query.limit).toBe(10);
    expect(JSON.stringify(record)).not.toContain('full prompt body');
    expect(JSON.stringify(record)).not.toContain('full command body');
  });

  it('parses implementation plan review contracts without granting execution', () => {
    const checklistItem = CodexExecReadOnlyAdapterImplementationPlanReviewChecklistItemSchema.parse(
      {
        id: 'codex_read_only_adapter_implementation_plan_review_check_1',
        schemaVersion,
        createdAt,
        code: 'cli_only_trigger',
        label: 'CLI-only trigger',
        disposition: 'hard_gate',
        status: 'passed',
        required: true,
        summary: 'Future skeleton remains CLI-only and disabled by default.',
        processAdapterApproved: false,
        recommendationGrantsExecution: false,
        workspaceWriteAllowed: false,
        dangerFullAccessAllowed: false,
        metadataOnly: true,
        bodyStored: false,
        liveExecution: false,
        externalProcessStarted: false,
        executionDisabled: true,
        processAdapterStarted: false,
        implementationApproved: false,
        dashboardTriggerAllowed: false,
      },
    );
    const finding = CodexExecReadOnlyAdapterImplementationPlanReviewFindingSchema.parse({
      id: 'codex_read_only_adapter_implementation_plan_review_finding_1',
      schemaVersion,
      createdAt,
      code: 'skeleton_only_if_approved',
      severity: 'medium',
      relatedChecklistCode: 'skeleton_only_if_approved',
      disposition: 'requires_review',
      summary: 'Round 3T scope must remain disabled-by-default skeleton only.',
      recommendation: 'Confirm this during Round 3S before allowing skeleton work.',
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const record = CodexExecReadOnlyAdapterImplementationPlanReviewDecisionRecordSchema.parse({
      id: 'codex_read_only_adapter_implementation_plan_review_1',
      schemaVersion,
      createdAt,
      planDocumentPath: 'docs/design/round-3r-read-only-adapter-implementation-plan.md',
      planDocumentHash: 'sha256:implementation-plan',
      outcome: 'conditional_go_to_disabled_skeleton',
      status: 'recorded',
      reviewerLabel: 'local-operator',
      rationaleSummary:
        'Conditional skeleton work may continue, but process adapter and execution remain unapproved.',
      reviewedAt: createdAt,
      disabledSkeletonApproved: true,
      checklistItems: [checklistItem],
      findings: [finding],
      hardGateCount: 1,
      requiresReviewCount: 1,
      informationalCount: 0,
      unresolvedFindingCount: 1,
      evidenceRefs: [],
      auditEventIds: ['audit_implementation_plan_review_1'],
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const summary = CodexExecReadOnlyAdapterImplementationPlanReviewSummarySchema.parse({
      id: 'codex_read_only_adapter_implementation_plan_review_summary_1',
      schemaVersion,
      createdAt,
      reviewId: record.id,
      planDocumentPath: record.planDocumentPath,
      planDocumentHash: record.planDocumentHash,
      outcome: record.outcome,
      status: record.status,
      reviewerLabel: record.reviewerLabel,
      reviewedAt: record.reviewedAt,
      disabledSkeletonApproved: true,
      hardGateCount: 1,
      requiresReviewCount: 1,
      informationalCount: 0,
      unresolvedFindingCount: 1,
      summary: 'Conditional skeleton approval is not execution approval.',
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });
    const query = CodexExecReadOnlyAdapterImplementationPlanReviewQuerySchema.parse({
      id: 'codex_read_only_adapter_implementation_plan_review_query_1',
      schemaVersion,
      createdAt,
      status: 'recorded',
      outcome: 'conditional_go_to_disabled_skeleton',
      limit: 10,
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    });

    expect(record.disabledSkeletonApproved).toBe(true);
    expect(record.implementationApproved).toBe(false);
    expect(record.processAdapterApproved).toBe(false);
    expect(record.recommendationGrantsExecution).toBe(false);
    expect(record.workspaceWriteAllowed).toBe(false);
    expect(record.dangerFullAccessAllowed).toBe(false);
    expect(summary.executionDisabled).toBe(true);
    expect(query.limit).toBe(10);
    expect(JSON.stringify(record)).not.toContain('full prompt body');
    expect(JSON.stringify(record)).not.toContain('full command body');

    expect(() =>
      CodexExecReadOnlyAdapterImplementationPlanReviewDecisionRecordSchema.parse({
        ...record,
        id: 'codex_read_only_adapter_implementation_plan_review_invalid',
        outcome: 'no_go',
        disabledSkeletonApproved: true,
      }),
    ).toThrow();
  });

  it('parses disabled skeleton, fixture boundary, and final readiness contracts', () => {
    const flagFields = {
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    } as const;
    const config = CodexExecReadOnlyAdapterSkeletonConfigSchema.parse({
      id: 'codex_read_only_adapter_skeleton_config_1',
      schemaVersion,
      createdAt,
      status: 'disabled',
      explicitEnableRequired: true,
      configuredEnabled: false,
      allowedSandboxModes: ['read_only'],
      forbiddenSandboxModes: ['workspace_write', 'danger_full_access'],
      cliOnly: true,
      noRunnableCommand: true,
      commandPreviewStored: false,
      argvStored: false,
      executablePathStored: false,
      shellSnippetStored: false,
      envPlanStored: false,
      ...flagFields,
    });
    const preview = CodexExecReadOnlyAdapterSkeletonPreviewSchema.parse({
      id: 'codex_read_only_adapter_skeleton_preview_1',
      schemaVersion,
      createdAt,
      status: 'disabled',
      config,
      disabledReasons: [],
      summary: 'Disabled skeleton preview only.',
      noRunnableCommand: true,
      commandPreviewStored: false,
      argvStored: false,
      executablePathStored: false,
      shellSnippetStored: false,
      envPlanStored: false,
      ...flagFields,
    });
    const skeletonReview = CodexExecReadOnlyAdapterSkeletonReviewDecisionRecordSchema.parse({
      id: 'codex_read_only_adapter_skeleton_review_1',
      schemaVersion,
      createdAt,
      skeletonPreviewId: preview.id,
      outcome: 'skeleton_accepted_for_fixture_boundary_only',
      status: 'recorded',
      reviewerLabel: 'local-operator',
      rationaleSummary: 'Fixture-backed boundary only; no process adapter approval.',
      reviewedAt: createdAt,
      fixtureBoundaryAllowed: true,
      checklistItems: [],
      findings: [],
      hardGateCount: 0,
      requiresReviewCount: 0,
      informationalCount: 0,
      unresolvedFindingCount: 0,
      evidenceRefs: [],
      auditEventIds: [],
      ...flagFields,
    });
    const skeletonSummary = CodexExecReadOnlyAdapterSkeletonReviewSummarySchema.parse({
      id: 'codex_read_only_adapter_skeleton_review_summary_1',
      schemaVersion,
      createdAt,
      reviewId: skeletonReview.id,
      skeletonPreviewId: preview.id,
      outcome: skeletonReview.outcome,
      status: skeletonReview.status,
      reviewerLabel: skeletonReview.reviewerLabel,
      reviewedAt: createdAt,
      fixtureBoundaryAllowed: true,
      hardGateCount: 0,
      requiresReviewCount: 0,
      informationalCount: 0,
      unresolvedFindingCount: 0,
      summary: 'Skeleton accepted for fixture-backed replay boundary only.',
      ...flagFields,
    });
    const fixtureBoundary = CodexExecReadOnlyAdapterFixtureBoundaryResultSchema.parse({
      id: 'codex_read_only_adapter_fixture_boundary_1',
      schemaVersion,
      createdAt,
      input: {
        id: 'codex_read_only_adapter_fixture_boundary_input_1',
        schemaVersion,
        createdAt,
        fixturePath: 'packages/codex-kernel/fixtures/codex-exec-basic.jsonl',
        fixtureOnly: true,
        ...flagFields,
      },
      status: 'completed',
      fixturePath: 'packages/codex-kernel/fixtures/codex-exec-basic.jsonl',
      fixturePathHash: 'sha256:fixture-path',
      fixtureOnly: true,
      eventCount: 1,
      itemCount: 0,
      errorCount: 0,
      finalStatus: 'completed',
      events: [],
      evidenceRefs: [],
      auditEvents: [],
      auditEventIds: [],
      summary: 'Fixture-backed replay boundary completed.',
      ...flagFields,
    });
    const fixtureSummary = CodexExecReadOnlyAdapterFixtureBoundarySummarySchema.parse({
      id: 'codex_read_only_adapter_fixture_boundary_summary_1',
      schemaVersion,
      createdAt,
      boundaryResultId: fixtureBoundary.id,
      status: fixtureBoundary.status,
      fixturePath: fixtureBoundary.fixturePath,
      fixturePathHash: fixtureBoundary.fixturePathHash,
      fixtureOnly: true,
      eventCount: 1,
      itemCount: 0,
      errorCount: 0,
      finalStatus: 'completed',
      summary: 'Fixture-backed replay boundary completed.',
      ...flagFields,
    });
    const finalReadiness = CodexExecReadOnlyAdapterFinalReadinessDecisionRecordSchema.parse({
      id: 'codex_read_only_adapter_final_readiness_1',
      schemaVersion,
      createdAt,
      outcome: 'ready_for_separate_read_only_adapter_adr',
      status: 'recorded',
      reviewerLabel: 'local-operator',
      rationaleSummary: 'Separate ADR remains required.',
      reviewedAt: createdAt,
      phaseAStatus: 'disabled',
      phaseBOutcome: 'skeleton_accepted_for_fixture_boundary_only',
      phaseCStatus: 'completed',
      realAdapterRequiresSeparateAdr: true,
      currentRoundApprovesProcessStart: false,
      currentRoundApprovesCodexExecution: false,
      currentRoundApprovesWorkspaceWrites: false,
      evidenceRefs: [],
      auditEventIds: [],
      summary: 'Ready only for a separate future ADR.',
      ...flagFields,
    });
    const finalSummary = CodexExecReadOnlyAdapterFinalReadinessSummarySchema.parse({
      id: 'codex_read_only_adapter_final_readiness_summary_1',
      schemaVersion,
      createdAt,
      decisionId: finalReadiness.id,
      outcome: finalReadiness.outcome,
      status: finalReadiness.status,
      reviewerLabel: finalReadiness.reviewerLabel,
      reviewedAt: createdAt,
      phaseAStatus: 'disabled',
      phaseBOutcome: 'skeleton_accepted_for_fixture_boundary_only',
      phaseCStatus: 'completed',
      realAdapterRequiresSeparateAdr: true,
      summary: 'Ready only for a separate future ADR.',
      ...flagFields,
    });

    expect(config.configuredEnabled).toBe(false);
    expect(preview.noRunnableCommand).toBe(true);
    expect(skeletonSummary.fixtureBoundaryAllowed).toBe(true);
    expect(fixtureSummary.fixtureOnly).toBe(true);
    expect(finalSummary.realAdapterRequiresSeparateAdr).toBe(true);
    expect(finalReadiness.currentRoundApprovesProcessStart).toBe(false);
    expect(finalReadiness.currentRoundApprovesCodexExecution).toBe(false);
    expect(finalReadiness.currentRoundApprovesWorkspaceWrites).toBe(false);
    expect(JSON.stringify(fixtureBoundary)).not.toContain('synthetic stdout body');
  });

  it('parses real read-only adapter readiness contracts without approval semantics', () => {
    const flagFields = {
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    } as const;
    const gate = CodexExecRealReadOnlyAdapterReadinessGateSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_gate_1',
      schemaVersion,
      createdAt,
      code: 'fixture_path_guard_symlink_escape',
      label: 'Fixture path guard symlink escape',
      category: 'fixture_boundary',
      disposition: 'requires_review',
      status: 'requires_review',
      required: true,
      summary: 'Symlink escape verification remains pending.',
      ...flagFields,
    });
    const blocker = CodexExecRealReadOnlyAdapterReadinessBlockerSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_blocker_1',
      schemaVersion,
      createdAt,
      code: 'round_3s_conditional_decision_exists',
      severity: 'critical',
      relatedGateCode: 'round_3s_conditional_decision_exists',
      status: 'blocked',
      summary: 'Round 3S conditional decision is missing.',
      recommendation: 'Create an explicit local governance decision before readiness persistence.',
      ...flagFields,
    });
    const finding = CodexExecRealReadOnlyAdapterReadinessFindingSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_finding_1',
      schemaVersion,
      createdAt,
      code: 'documented_only_3tw_evidence',
      severity: 'medium',
      status: 'requires_review',
      summary: 'Round 3T-W evidence is documented-only.',
      recommendation: 'Treat this package as requires_review until persisted evidence exists.',
      ...flagFields,
    });
    const checklistItem = CodexExecRealReadOnlyAdapterReadinessChecklistItemSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_check_1',
      schemaVersion,
      createdAt,
      code: 'separate_adr_required',
      label: 'Separate ADR required',
      status: 'passed',
      required: true,
      summary: 'A separate ADR is required before any implementation can be considered.',
      ...flagFields,
    });
    const packageRecord = CodexExecRealReadOnlyAdapterReadinessPackageSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_package_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      status: 'ready_for_separate_adr',
      recommendation:
        'Ready for separate ADR review only. Does not grant implementation, process launch, or execution permission.',
      governanceDecisionId: 'codex_read_only_adapter_implementation_plan_review_1',
      documentedArtifactRefs: ['docs/reviews/round-3tw-additional-rules-audit.md'],
      gates: [gate],
      blockers: [],
      findings: [finding],
      checklistItems: [checklistItem],
      hardGateCount: 1,
      passedGateCount: 1,
      requiresReviewCount: 0,
      blockerCount: 0,
      findingCount: 1,
      documentedOnly3twEvidence: false,
      symlinkEscapeVerificationPending: false,
      evidenceRefs: [],
      auditEventIds: ['audit_readiness_1'],
      summary: 'Ready only for a separate ADR review.',
      ...flagFields,
    });
    const summary = CodexExecRealReadOnlyAdapterReadinessSummarySchema.parse({
      id: 'codex_real_read_only_adapter_readiness_summary_1',
      schemaVersion,
      createdAt,
      packageId: packageRecord.id,
      dryRunId: packageRecord.dryRunId,
      status: packageRecord.status,
      recommendation: packageRecord.recommendation,
      hardGateCount: 1,
      passedGateCount: 1,
      requiresReviewCount: 0,
      blockerCount: 0,
      findingCount: 1,
      documentedOnly3twEvidence: false,
      symlinkEscapeVerificationPending: false,
      summary: packageRecord.summary,
      ...flagFields,
    });
    const query = CodexExecRealReadOnlyAdapterReadinessQuerySchema.parse({
      id: 'codex_real_read_only_adapter_readiness_query_1',
      schemaVersion,
      createdAt,
      dryRunId: packageRecord.dryRunId,
      status: 'ready_for_separate_adr',
      limit: 10,
      ...flagFields,
    });

    expect(blocker.status).toBe('blocked');
    expect(packageRecord.status).toBe('ready_for_separate_adr');
    expect(packageRecord.implementationApproved).toBe(false);
    expect(packageRecord.processAdapterApproved).toBe(false);
    expect(packageRecord.recommendationGrantsExecution).toBe(false);
    expect(summary.executionDisabled).toBe(true);
    expect(query.limit).toBe(10);
    expect(JSON.stringify(packageRecord)).not.toContain('full report markdown');
    expect(JSON.stringify(packageRecord)).not.toContain('prompt body');
    expect(JSON.stringify(packageRecord)).not.toContain('stdout');
  });

  it('parses real read-only adapter readiness review contracts without approval semantics', () => {
    const flagFields = {
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
    } as const;
    const checklistItem = CodexExecRealReadOnlyAdapterReadinessReviewChecklistItemSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_review_check_1',
      schemaVersion,
      createdAt,
      code: 'unresolved_findings_acknowledged',
      label: 'Unresolved findings acknowledged',
      status: 'passed',
      required: true,
      summary: 'Required finding codes were acknowledged.',
      ...flagFields,
    });
    const finding = CodexExecRealReadOnlyAdapterReadinessReviewFindingSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_review_finding_1',
      schemaVersion,
      createdAt,
      code: 'symlink_escape_verification_pending',
      severity: 'medium',
      status: 'requires_review',
      relatedReadinessFindingId: 'codex_real_read_only_adapter_readiness_finding_1',
      relatedReadinessFindingCode: 'symlink_escape_verification_pending',
      summary: 'Symlink escape verification is pending.',
      recommendation: 'Acknowledge before ADR drafting and verify before any boundary work.',
      ...flagFields,
    });
    const record = CodexExecRealReadOnlyAdapterReadinessReviewDecisionRecordSchema.parse({
      id: 'codex_real_read_only_adapter_readiness_review_1',
      schemaVersion,
      createdAt,
      packageId: 'codex_real_read_only_adapter_readiness_package_1',
      dryRunId: 'codex_dry_run_1',
      packageStatus: 'requires_review',
      outcome: 'conditional_go_to_separate_adr_draft',
      status: 'recorded',
      reviewerLabel: 'local-operator',
      rationaleSummary:
        'Acknowledges symlink_escape_verification_pending and documented_only_3tw_evidence.',
      reviewedAt: createdAt,
      separateAdrDraftAllowed: true,
      acknowledgedFindingCodes: [
        'symlink_escape_verification_pending',
        'documented_only_3tw_evidence',
      ],
      acknowledgedFindingIds: ['codex_real_read_only_adapter_readiness_finding_1'],
      unresolvedFindingCount: 2,
      checklistItems: [checklistItem],
      findings: [finding],
      evidenceRefs: [
        {
          id: 'evidence_readiness_review_1',
          schemaVersion,
          createdAt,
          kind: 'codex.exec.real_read_only_adapter.readiness_review',
          hash: 'sha256:readiness-review',
          summary: 'Readiness review summary only',
        },
      ],
      auditEventIds: ['audit_readiness_review_1'],
      summary:
        'ADR drafting only. Does not grant implementation, process launch, or execution permission.',
      ...flagFields,
    });
    const summary = CodexExecRealReadOnlyAdapterReadinessReviewSummarySchema.parse({
      id: 'codex_real_read_only_adapter_readiness_review_summary_1',
      schemaVersion,
      createdAt,
      reviewId: record.id,
      packageId: record.packageId,
      dryRunId: record.dryRunId,
      packageStatus: record.packageStatus,
      outcome: record.outcome,
      status: record.status,
      reviewerLabel: record.reviewerLabel,
      reviewedAt: record.reviewedAt,
      separateAdrDraftAllowed: true,
      acknowledgedFindingCodes: record.acknowledgedFindingCodes,
      acknowledgedFindingIds: record.acknowledgedFindingIds,
      unresolvedFindingCount: record.unresolvedFindingCount,
      summary: record.summary,
      ...flagFields,
    });
    const query = CodexExecRealReadOnlyAdapterReadinessReviewQuerySchema.parse({
      id: 'codex_real_read_only_adapter_readiness_review_query_1',
      schemaVersion,
      createdAt,
      packageId: record.packageId,
      dryRunId: record.dryRunId,
      status: 'recorded',
      outcome: 'conditional_go_to_separate_adr_draft',
      limit: 10,
      ...flagFields,
    });

    expect(record.separateAdrDraftAllowed).toBe(true);
    expect(record.implementationApproved).toBe(false);
    expect(record.processAdapterApproved).toBe(false);
    expect(record.recommendationGrantsExecution).toBe(false);
    expect(summary.executionDisabled).toBe(true);
    expect(query.limit).toBe(10);
    expect(JSON.stringify(record)).not.toContain('full report markdown');
    expect(JSON.stringify(record)).not.toContain('prompt body');
    expect(JSON.stringify(record)).not.toContain('stdout');

    expect(() =>
      CodexExecRealReadOnlyAdapterReadinessReviewDecisionRecordSchema.parse({
        ...record,
        id: 'codex_real_read_only_adapter_readiness_review_invalid',
        outcome: 'no_go_to_separate_adr_draft',
        separateAdrDraftAllowed: true,
      }),
    ).toThrow();
  });

  it('parses real read-only adapter contract models as metadata-only non-approval records', () => {
    const flagFields = {
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
      promptBodyStored: false,
      commandBodyStored: false,
      stdoutBodyStored: false,
      stderrBodyStored: false,
      agentMessageBodyStored: false,
      reasoningBodyStored: false,
    } as const;
    const noRunnableFields = {
      noRunnableCommand: true,
      commandPreviewStored: false,
      argvStored: false,
      executablePathStored: false,
      shellSnippetStored: false,
      envPlanStored: false,
    } as const;
    const config = CodexExecRealReadOnlyAdapterConfigSchema.parse({
      id: 'codex_real_read_only_adapter_config_1',
      schemaVersion,
      createdAt,
      status: 'disabled',
      defaultEnabled: false,
      configuredEnabled: false,
      explicitEnableRequired: true,
      cliOnly: true,
      dashboardTriggerForbidden: true,
      allowedSandboxMode: 'read_only',
      forbiddenSandboxModes: ['workspace_write', 'danger_full_access'],
      existingDryRunRequired: true,
      approvalArtifactRequired: true,
      dryRunPlanHashRequired: true,
      policyDecisionHashRequired: true,
      isolatedWorktreeRequired: true,
      cleanWorktreeRequired: true,
      metadataEvidenceOnly: true,
      summary: 'Read-only adapter config is disabled by default.',
      ...flagFields,
      ...noRunnableFields,
    });
    const request = CodexExecRealReadOnlyAdapterRequestSchema.parse({
      id: 'codex_real_read_only_adapter_request_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      configId: config.id,
      approvalArtifactId: 'codex_approval_artifact_1',
      policyDecisionId: 'policy_1',
      requestedSandboxMode: 'read_only',
      triggerKind: 'cli',
      existingDryRunRequired: true,
      summary: 'Metadata-only request for a future read-only adapter attempt.',
      ...flagFields,
      ...noRunnableFields,
    });
    const boundaryPlan = CodexExecRealReadOnlyAdapterBoundaryPlanSchema.parse({
      id: 'codex_real_read_only_adapter_boundary_plan_1',
      schemaVersion,
      createdAt,
      requestId: request.id,
      dryRunId: request.dryRunId,
      processBoundaryDeferred: true,
      adapterModuleRef: '@codexhub/codex-kernel/read-only-adapter',
      dryRunPlanHash: 'sha256:dry-run',
      policyDecisionHash: 'sha256:policy',
      approvalArtifactHash: 'sha256:approval',
      evidencePlanSummary: 'Metadata and hashes only.',
      auditPlanSummary: 'Audit before, after, abort, and failure.',
      summary: 'Boundary planning metadata only; no runnable command is stored.',
      ...flagFields,
      ...noRunnableFields,
    });
    const check = CodexExecRealReadOnlyAdapterPreflightCheckSchema.parse({
      id: 'codex_real_read_only_adapter_preflight_check_1',
      schemaVersion,
      createdAt,
      code: 'config_default_disabled',
      label: 'Config default disabled',
      status: 'passed',
      required: true,
      summary: 'Default config remains disabled.',
      ...flagFields,
    });
    const preflight = CodexExecRealReadOnlyAdapterPreflightSchema.parse({
      id: 'codex_real_read_only_adapter_preflight_1',
      schemaVersion,
      createdAt,
      requestId: request.id,
      dryRunId: request.dryRunId,
      configId: config.id,
      status: 'passed',
      requestedSandboxMode: 'read_only',
      boundaryPlan,
      checks: [check],
      hardGateCount: 1,
      passedGateCount: 1,
      failedGateCount: 0,
      requiresReviewCount: 0,
      blockerCount: 0,
      summary: 'Preflight contract is metadata only.',
      ...flagFields,
    });
    const error = CodexExecRealReadOnlyAdapterErrorSchema.parse({
      id: 'codex_real_read_only_adapter_error_1',
      schemaVersion,
      createdAt,
      code: 'boundary_deferred',
      severity: 'medium',
      relatedCheckCode: check.code,
      messageSummary: 'Boundary remains deferred in P1.',
      remediationSummary: 'Proceed only after the next gated round.',
      absolutePathLeaked: false,
      ...flagFields,
    });
    const evidenceSummary = CodexExecRealReadOnlyAdapterEvidenceSummarySchema.parse({
      id: 'codex_real_read_only_adapter_evidence_summary_1',
      schemaVersion,
      createdAt,
      dryRunId: request.dryRunId,
      requestId: request.id,
      evidenceRefIds: ['evidence_1'],
      eventHashCount: 1,
      outputHashCount: 0,
      metadataHash: 'sha256:metadata',
      redacted: true,
      summary: 'Evidence summary is metadata-only.',
      ...flagFields,
    });
    const auditSummary = CodexExecRealReadOnlyAdapterAuditSummarySchema.parse({
      id: 'codex_real_read_only_adapter_audit_summary_1',
      schemaVersion,
      createdAt,
      dryRunId: request.dryRunId,
      requestId: request.id,
      auditEventIds: ['audit_1'],
      beforeStartRequired: true,
      afterFinishRequired: true,
      abortRequired: true,
      failureRequired: true,
      eventCount: 1,
      summary: 'Audit summary requires before, after, abort, and failure events.',
      ...flagFields,
    });
    const result = CodexExecRealReadOnlyAdapterResultSchema.parse({
      id: 'codex_real_read_only_adapter_result_1',
      schemaVersion,
      createdAt,
      requestId: request.id,
      dryRunId: request.dryRunId,
      preflightId: preflight.id,
      status: 'blocked',
      boundaryPlanId: boundaryPlan.id,
      error,
      evidenceSummary,
      auditSummary,
      postRunVerificationRequired: true,
      workspaceMutationAllowed: false,
      unexpectedWorkspaceDiffCritical: true,
      autoRevertAllowed: false,
      summary: 'P1 result contract is non-executing metadata only.',
      ...flagFields,
    });
    const boundaryDiagnostics = CodexExecRealReadOnlyAdapterBoundaryDiagnosticsSchema.parse({
      id: 'codex_real_read_only_adapter_boundary_diagnostics_1',
      schemaVersion,
      createdAt,
      ...flagFields,
      status: 'failed',
      failureCode: 'process_exit_nonzero',
      startFailureKind: 'none',
      enoentKind: 'none',
      platform: 'win32',
      resolvedExecutableKind: 'native_exe',
      spawnTargetKind: 'native_exe',
      cwdHash: 'sha256:cwd',
      cwdExists: true,
      cwdIsDirectory: true,
      executableHash: 'sha256:executable',
      executableExists: true,
      executableAccessible: true,
      executableResolutionSource: 'direct_path',
      dependencyResolutionStatus: 'not_applicable',
      envAllowlistKeyCount: 5,
      envAllowlistKeyHash: 'sha256:env-keys',
      exitCode: 2,
      nonzeroExitKind: 'codex_cli_usage_error_suspected',
      timedOut: false,
      cancelled: false,
      durationMs: 35,
      stdoutHash: 'sha256:stdout',
      stderrHash: 'sha256:stderr',
      stdoutByteLength: 12,
      stderrByteLength: 14,
      stdoutLineCount: 1,
      stderrLineCount: 1,
      stdoutTruncated: false,
      stderrTruncated: false,
      externalProcessStarted: true,
      governedInputVerified: true,
      governedInputSourceKind: 'governed_file',
      governedInputRelativePathHash: 'sha256:input-path',
      governedInputContentHash: 'sha256:input-content',
      governedInputByteLength: 42,
      governedInputLineCount: 1,
      promptArgumentHash: 'sha256:prompt-argument',
      promptArgumentStored: false,
      summary: 'Boundary diagnostics stores hashes and counts only.',
    });
    const governedBoundaryDeferredDiagnostics =
      CodexExecRealReadOnlyAdapterBoundaryDeferredDiagnosticsSchema.parse({
        id: 'codex_real_read_only_adapter_boundary_deferred_governed_input_1',
        schemaVersion,
        createdAt,
        ...flagFields,
        reasonCode: 'governed_input_missing',
        reasonCodes: ['governed_input_missing'],
        preflightStatus: 'passed',
        runtimeWorktreeProvided: true,
        approvalInputProvided: true,
        executableResolutionStatus: 'resolved',
        cwdSelfCheckStatus: 'passed',
        governedInputProvided: false,
        governedInputVerified: false,
        governedInputReasonCode: 'governed_input_missing',
        processBoundaryReady: false,
        summary: 'Boundary deferred because governed input was not verified.',
      });

    expect(config.defaultEnabled).toBe(false);
    expect(request.dryRunId).toBe('codex_dry_run_1');
    expect(preflight.requestedSandboxMode).toBe('read_only');
    expect(result.executionDisabled).toBe(true);
    expect(result.implementationApproved).toBe(false);
    expect(result.processAdapterApproved).toBe(false);
    expect(result.recommendationGrantsExecution).toBe(false);
    expect(result.workspaceWriteAllowed).toBe(false);
    expect(result.dangerFullAccessAllowed).toBe(false);
    expect(result.dashboardTriggerAllowed).toBe(false);
    expect(result.promptBodyStored).toBe(false);
    expect(result.commandBodyStored).toBe(false);
    expect(result.stdoutBodyStored).toBe(false);
    expect(result.stderrBodyStored).toBe(false);
    expect(boundaryDiagnostics.governedInputVerified).toBe(true);
    expect(boundaryDiagnostics.promptArgumentStored).toBe(false);
    expect(governedBoundaryDeferredDiagnostics.reasonCode).toBe('governed_input_missing');
    const approvalAuthority = CodexExecRealReadOnlyAdapterApprovalAuthoritySummarySchema.parse({
      id: 'codex_real_read_only_adapter_approval_authority_1',
      schemaVersion,
      createdAt,
      dryRunId: request.dryRunId,
      status: 'resolved',
      approvalRecordId: 'codex_approval_record_1',
      approvalArtifactId: 'codex_approval_artifact_1',
      approvalArtifactHash: 'sha256:approval',
      dryRunPlanHash: 'sha256:dry-run',
      policyDecisionHash: 'sha256:policy',
      expectedDryRunPlanHash: 'sha256:dry-run',
      expectedPolicyDecisionHash: 'sha256:policy',
      dryRunHashMatched: true,
      policyHashMatched: true,
      checkedAt: createdAt,
      expiresAt: '2026-05-01T00:00:00.000Z',
      reasonCodes: [],
      summary: 'Approval authority summary stores metadata and hashes only.',
      ...flagFields,
    });
    const approvalAuthorityTrace =
      CodexExecRealReadOnlyAdapterApprovalAuthorityTraceRecordSchema.parse({
        id: 'codex_real_read_only_adapter_approval_authority_trace_1',
        schemaVersion,
        createdAt,
        dryRunId: request.dryRunId,
        status: 'aligned',
        sourcePreparationApprovalArtifactId: 'codex_approval_artifact_1',
        prerequisiteApprovalArtifactId: 'codex_approval_artifact_1',
        inputApprovalArtifactId: 'codex_approval_artifact_1',
        resolvedApprovalRecordId: 'codex_approval_record_1',
        resolvedApprovalArtifactId: 'codex_approval_artifact_1',
        approvalArtifactHash: 'sha256:approval',
        dryRunPlanHash: 'sha256:dry-run',
        policyDecisionHash: 'sha256:policy',
        expectedDryRunPlanHash: 'sha256:dry-run',
        expectedPolicyDecisionHash: 'sha256:policy',
        exactLookupMatched: true,
        sourcePreparationMatched: true,
        prerequisiteMatched: true,
        approvalApproved: true,
        approvalUnused: true,
        approvalNotRevoked: true,
        approvalNotExpired: true,
        dryRunHashMatched: true,
        policyHashMatched: true,
        attemptPreflightWouldAccept: true,
        checkedAt: createdAt,
        expiresAt: '2026-05-01T00:00:00.000Z',
        reasonCodes: [],
        degraded: false,
        notPersisted: false,
        fallbackUsedAsAuthority: false,
        pilotExecuted: false,
        adapterAttemptInvoked: false,
        authoritative: true,
        supervisorBacked: true,
        persisted: true,
        evidenceRefs: [],
        auditEventIds: [],
        summary: 'Approval authority trace aligns source-prep, prerequisite, and attempt ids.',
        ...flagFields,
      });
    const approvalAuthorityTraceSummary =
      CodexExecRealReadOnlyAdapterApprovalAuthorityTraceSummarySchema.parse({
        id: 'codex_real_read_only_adapter_approval_authority_trace_summary_1',
        schemaVersion,
        createdAt,
        recordId: approvalAuthorityTrace.id,
        dryRunId: approvalAuthorityTrace.dryRunId,
        status: approvalAuthorityTrace.status,
        inputApprovalArtifactId: approvalAuthorityTrace.inputApprovalArtifactId,
        resolvedApprovalRecordId: approvalAuthorityTrace.resolvedApprovalRecordId,
        resolvedApprovalArtifactId: approvalAuthorityTrace.resolvedApprovalArtifactId,
        sourcePreparationMatched: true,
        prerequisiteMatched: true,
        exactLookupMatched: true,
        dryRunHashMatched: true,
        policyHashMatched: true,
        attemptPreflightWouldAccept: true,
        checkedAt: createdAt,
        expiresAt: approvalAuthorityTrace.expiresAt,
        reasonCodes: [],
        degraded: false,
        notPersisted: false,
        fallbackUsedAsAuthority: false,
        pilotExecuted: false,
        adapterAttemptInvoked: false,
        summary: approvalAuthorityTrace.summary,
        ...flagFields,
      });
    const approvalAuthorityTraceQuery =
      CodexExecRealReadOnlyAdapterApprovalAuthorityTraceQuerySchema.parse({
        id: 'codex_real_read_only_adapter_approval_authority_trace_query_1',
        schemaVersion,
        createdAt,
        dryRunId: request.dryRunId,
        status: 'aligned',
        limit: 10,
        ...flagFields,
      });
    const postRunSkipReason =
      CodexExecRealReadOnlyAdapterPostRunVerificationSkipReasonSchema.parse(
        'attempt_not_completed',
      );
    const boundaryDeferredDiagnostics =
      CodexExecRealReadOnlyAdapterBoundaryDeferredDiagnosticsSchema.parse({
        id: 'codex_real_read_only_adapter_boundary_deferred_diagnostics_1',
        schemaVersion,
        createdAt,
        reasonCode: 'executable_resolution_blocked',
        reasonCodes: ['executable_resolution_blocked'],
        preflightStatus: 'passed',
        runtimeWorktreeProvided: true,
        approvalInputProvided: true,
        executableResolutionStatus: 'blocked',
        executableResolutionReasonCode: 'executable_inaccessible',
        cwdSelfCheckStatus: 'passed',
        sourcePreparationReady: true,
        prerequisiteReady: true,
        worktreePathHashMatched: true,
        processBoundaryReady: false,
        summary: 'Boundary deferred diagnostics store reason codes only.',
        ...flagFields,
      });
    const attemptRecord = CodexExecRealReadOnlyAdapterAttemptRecordSchema.parse({
      id: 'codex_real_read_only_adapter_attempt_1',
      schemaVersion,
      createdAt,
      dryRunId: request.dryRunId,
      requestId: request.id,
      preflightId: preflight.id,
      resultId: result.id,
      status: 'blocked',
      authoritative: true,
      supervisorBacked: true,
      persisted: true,
      degraded: false,
      notPersisted: false,
      processBoundaryInvoked: false,
      preflightStatus: preflight.status,
      resultStatus: result.status,
      resultErrorCode: error.code,
      failedCheckCodes: [],
      blockedCheckCodes: [],
      boundaryDeferredReasonCode: boundaryDeferredDiagnostics.reasonCode,
      boundaryDeferredReasonCodes: boundaryDeferredDiagnostics.reasonCodes,
      boundaryDeferredDiagnostics,
      boundaryDiagnostics,
      boundaryDiagnosticsComplete: false,
      boundaryDiagnosticsMissingFields: [],
      postRunVerificationStatus: 'not_required',
      workspaceMutationDetected: false,
      evidenceSummary,
      auditSummary,
      evidenceRefIds: ['evidence_1'],
      auditEventIds: ['audit_1'],
      outputHashCount: 0,
      metadataHash: 'sha256:attempt',
      summary: 'Authoritative blocked attempt record stores metadata only.',
      ...flagFields,
    });
    const failedBoundaryAttemptRecord = CodexExecRealReadOnlyAdapterAttemptRecordSchema.parse({
      ...attemptRecord,
      id: 'codex_real_read_only_adapter_attempt_failed_boundary',
      status: 'failed',
      resultStatus: 'failed',
      resultErrorCode: 'boundary_failed',
      processBoundaryInvoked: true,
      processBoundaryModuleRef: 'packages/codex-kernel/src/real-read-only-adapter-process.ts',
      boundaryDiagnostics,
      boundaryDiagnosticsComplete: true,
      boundaryDiagnosticsMissingFields: [],
      postRunVerificationStatus: 'skipped',
      postRunVerificationSkipReason: postRunSkipReason,
      outputHashCount: 2,
      metadataHash: 'sha256:failed-boundary-attempt',
      summary: 'Failed boundary attempt record keeps diagnostics metadata only.',
    });
    const attemptSummary = CodexExecRealReadOnlyAdapterAttemptSummarySchema.parse({
      id: 'codex_real_read_only_adapter_attempt_summary_1',
      schemaVersion,
      createdAt,
      attemptId: attemptRecord.id,
      dryRunId: request.dryRunId,
      status: attemptRecord.status,
      authoritative: true,
      supervisorBacked: true,
      persisted: true,
      degraded: false,
      notPersisted: false,
      processBoundaryInvoked: false,
      preflightStatus: attemptRecord.preflightStatus,
      resultStatus: attemptRecord.resultStatus,
      resultErrorCode: attemptRecord.resultErrorCode,
      failedCheckCodes: attemptRecord.failedCheckCodes,
      blockedCheckCodes: attemptRecord.blockedCheckCodes,
      boundaryDeferredReasonCode: attemptRecord.boundaryDeferredReasonCode,
      boundaryDeferredReasonCodes: attemptRecord.boundaryDeferredReasonCodes,
      boundaryDeferredDiagnostics: attemptRecord.boundaryDeferredDiagnostics,
      boundaryDiagnostics: attemptRecord.boundaryDiagnostics,
      boundaryDiagnosticsComplete: attemptRecord.boundaryDiagnosticsComplete,
      boundaryDiagnosticsMissingFields: attemptRecord.boundaryDiagnosticsMissingFields,
      postRunVerificationStatus: attemptRecord.postRunVerificationStatus,
      postRunVerificationSkipReason: attemptRecord.postRunVerificationSkipReason,
      workspaceMutationDetected: attemptRecord.workspaceMutationDetected,
      evidenceRefCount: 1,
      auditEventCount: 1,
      outputHashCount: 0,
      metadataHash: attemptRecord.metadataHash,
      summary: 'Attempt summary is metadata only.',
      ...flagFields,
    });
    const attemptQuery = CodexExecRealReadOnlyAdapterAttemptQuerySchema.parse({
      id: 'codex_real_read_only_adapter_attempt_query_1',
      schemaVersion,
      createdAt,
      dryRunId: request.dryRunId,
      status: 'blocked',
      limit: 10,
      ...flagFields,
    });
    const attemptTimelineEntry = CodexExecRealReadOnlyAdapterAttemptTimelineEntrySchema.parse({
      id: 'codex_real_read_only_adapter_attempt_timeline_entry_1',
      schemaVersion,
      createdAt,
      attemptId: attemptRecord.id,
      dryRunId: request.dryRunId,
      status: attemptRecord.status,
      occurredAt: createdAt,
      processBoundaryInvoked: false,
      preflightStatus: attemptRecord.preflightStatus,
      resultStatus: attemptRecord.resultStatus,
      resultErrorCode: attemptRecord.resultErrorCode,
      failedCheckCodes: attemptRecord.failedCheckCodes,
      blockedCheckCodes: attemptRecord.blockedCheckCodes,
      boundaryDeferredReasonCode: attemptRecord.boundaryDeferredReasonCode,
      boundaryDeferredReasonCodes: attemptRecord.boundaryDeferredReasonCodes,
      boundaryDeferredDiagnostics: attemptRecord.boundaryDeferredDiagnostics,
      boundaryDiagnostics: attemptRecord.boundaryDiagnostics,
      boundaryDiagnosticsComplete: attemptRecord.boundaryDiagnosticsComplete,
      boundaryDiagnosticsMissingFields: attemptRecord.boundaryDiagnosticsMissingFields,
      postRunVerificationStatus: attemptRecord.postRunVerificationStatus,
      postRunVerificationSkipReason: attemptRecord.postRunVerificationSkipReason,
      workspaceMutationDetected: attemptRecord.workspaceMutationDetected,
      evidenceRefIds: ['evidence_1'],
      auditEventIds: ['audit_1'],
      evidenceRefCount: 1,
      auditEventCount: 1,
      outputHashCount: 0,
      metadataHash: attemptRecord.metadataHash,
      summary: 'Timeline entry is read-only metadata only.',
      ...flagFields,
    });
    const attemptTimelineSummary = CodexExecRealReadOnlyAdapterAttemptTimelineSummarySchema.parse({
      id: 'codex_real_read_only_adapter_attempt_timeline_1',
      schemaVersion,
      createdAt,
      dryRunId: request.dryRunId,
      status: 'blocked',
      entries: [attemptTimelineEntry],
      eventCount: 1,
      evidenceRefCount: 1,
      auditEventCount: 1,
      outputHashCount: 0,
      processBoundaryInvokedCount: 0,
      includeEvidence: true,
      includeAudit: true,
      verificationSummary: 'Post-run verification is represented as metadata only.',
      workspaceMutationSummary: 'Workspace mutation remains forbidden.',
      recommendation: 'Timeline evidence is informational only.',
      summary: 'Attempt timeline stores counts, refs, hashes, and status only.',
      ...flagFields,
    });
    const attemptTimelineQuery = CodexExecRealReadOnlyAdapterAttemptTimelineQuerySchema.parse({
      id: 'codex_real_read_only_adapter_attempt_timeline_query_1',
      schemaVersion,
      createdAt,
      dryRunId: request.dryRunId,
      status: 'blocked',
      includeEvidence: true,
      includeAudit: true,
      limit: 10,
      ...flagFields,
    });

    expect(attemptRecord.status).toBe('blocked');
    expect(attemptRecord.executionDisabled).toBe(true);
    expect(attemptRecord.implementationApproved).toBe(false);
    expect(attemptRecord.processAdapterApproved).toBe(false);
    expect(attemptRecord.recommendationGrantsExecution).toBe(false);
    expect(attemptRecord.workspaceWriteAllowed).toBe(false);
    expect(attemptRecord.dangerFullAccessAllowed).toBe(false);
    expect(attemptRecord.dashboardTriggerAllowed).toBe(false);
    expect(attemptRecord.preflightStatus).toBe('passed');
    expect(attemptRecord.resultStatus).toBe('blocked');
    expect(attemptRecord.resultErrorCode).toBe('boundary_deferred');
    expect(attemptRecord.boundaryDeferredReasonCode).toBe('executable_resolution_blocked');
    expect(attemptRecord.boundaryDeferredDiagnostics?.executableResolutionStatus).toBe(
      'blocked',
    );
    expect(attemptSummary.boundaryDeferredReasonCode).toBe('executable_resolution_blocked');
    expect(attemptTimelineEntry.boundaryDeferredReasonCodes).toEqual([
      'executable_resolution_blocked',
    ]);
    expect(attemptRecord.failedCheckCodes).toHaveLength(0);
    expect(attemptRecord.boundaryDiagnostics?.failureCode).toBe('process_exit_nonzero');
    expect(attemptRecord.boundaryDiagnostics?.stdoutHash).toBe('sha256:stdout');
    expect(attemptRecord.boundaryDiagnostics?.stderrHash).toBe('sha256:stderr');
    expect(attemptRecord.postRunVerificationStatus).toBe('not_required');
    expect(failedBoundaryAttemptRecord.processBoundaryInvoked).toBe(true);
    expect(failedBoundaryAttemptRecord.postRunVerificationStatus).toBe('skipped');
    expect(failedBoundaryAttemptRecord.postRunVerificationSkipReason).toBe(
      'attempt_not_completed',
    );
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.failureCode).toBe(
      'process_exit_nonzero',
    );
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.startFailureKind).toBe('none');
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.resolvedExecutableKind).toBe(
      'native_exe',
    );
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.cwdHash).toBe('sha256:cwd');
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.executableAccessible).toBe(true);
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.envAllowlistKeyHash).toBe(
      'sha256:env-keys',
    );
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.nonzeroExitKind).toBe(
      'codex_cli_usage_error_suspected',
    );
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.stdoutByteLength).toBe(12);
    expect(failedBoundaryAttemptRecord.boundaryDiagnostics?.stderrByteLength).toBe(14);
    expect(failedBoundaryAttemptRecord.boundaryDiagnosticsComplete).toBe(true);
    expect(failedBoundaryAttemptRecord.boundaryDiagnosticsMissingFields).toEqual([]);
    expect(attemptRecord.workspaceMutationDetected).toBe(false);
    expect(attemptRecord.promptBodyStored).toBe(false);
    expect(attemptRecord.commandBodyStored).toBe(false);
    expect(attemptRecord.stdoutBodyStored).toBe(false);
    expect(attemptRecord.stderrBodyStored).toBe(false);
    expect(approvalAuthority.status).toBe('resolved');
    expect(approvalAuthorityTrace.status).toBe('aligned');
    expect(approvalAuthorityTraceSummary.attemptPreflightWouldAccept).toBe(true);
    expect(approvalAuthorityTraceQuery.status).toBe('aligned');
    expect(JSON.stringify(approvalAuthority)).not.toContain('raw prompt body');
    expect(JSON.stringify(approvalAuthorityTrace)).not.toContain('raw stdout body');
    expect(JSON.stringify(approvalAuthority)).not.toContain('raw stdout body');
    expect(JSON.stringify(approvalAuthority)).not.toContain('"argv"');
    expect(JSON.stringify(approvalAuthority)).not.toContain('"executablePath":');
    expect(JSON.stringify(failedBoundaryAttemptRecord)).not.toContain('raw stdout body');
    expect(JSON.stringify(failedBoundaryAttemptRecord)).not.toContain('raw stderr body');
    expect(JSON.stringify(failedBoundaryAttemptRecord)).not.toContain('"argv"');
    expect(JSON.stringify(failedBoundaryAttemptRecord)).not.toContain('"executablePath":');
    expect(attemptRecord.degraded).toBe(false);
    expect(attemptRecord.notPersisted).toBe(false);
    expect(attemptSummary.attemptId).toBe(attemptRecord.id);
    expect(attemptSummary.boundaryDiagnosticsComplete).toBe(false);
    expect(attemptSummary.boundaryDiagnosticsMissingFields).toEqual([]);
    expect(attemptQuery.status).toBe('blocked');
    expect(attemptTimelineEntry.attemptId).toBe(attemptRecord.id);
    expect(attemptTimelineEntry.boundaryDiagnosticsComplete).toBe(false);
    expect(attemptTimelineEntry.boundaryDiagnosticsMissingFields).toEqual([]);
    expect(attemptTimelineSummary.eventCount).toBe(1);
    expect(attemptTimelineSummary.implementationApproved).toBe(false);
    expect(attemptTimelineSummary.processAdapterApproved).toBe(false);
    expect(attemptTimelineSummary.recommendationGrantsExecution).toBe(false);
    expect(attemptTimelineSummary.workspaceWriteAllowed).toBe(false);
    expect(attemptTimelineSummary.dangerFullAccessAllowed).toBe(false);
    expect(attemptTimelineSummary.dashboardTriggerAllowed).toBe(false);
    expect(attemptTimelineQuery.includeEvidence).toBe(true);
    expect(JSON.stringify(attemptRecord)).not.toContain('raw prompt body');
    expect(JSON.stringify(attemptRecord)).not.toContain('raw command body');
    expect(JSON.stringify(attemptRecord)).not.toContain('raw stdout body');
    expect(JSON.stringify(attemptRecord)).not.toContain('raw stderr body');
    expect(JSON.stringify(attemptRecord)).not.toContain('diagnostic stdout body');
    expect(JSON.stringify(attemptRecord)).not.toContain('diagnostic stderr body');
    expect(JSON.stringify(attemptRecord)).not.toContain('argv');
    expect(JSON.stringify(attemptRecord)).not.toContain('executablePath');
    expect(JSON.stringify(attemptRecord)).not.toContain('shellSnippet');
    expect(JSON.stringify(attemptRecord)).not.toContain('envPlan');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('raw prompt body');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('raw command body');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('raw stdout body');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('raw stderr body');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('argv');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('executablePath');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('shellSnippet');
    expect(JSON.stringify(attemptTimelineSummary)).not.toContain('envPlan');
    expect('command' in boundaryPlan).toBe(false);
    expect('argv' in boundaryPlan).toBe(false);
    expect('executablePath' in boundaryPlan).toBe(false);
    expect('shellSnippet' in boundaryPlan).toBe(false);
    expect('envPlan' in boundaryPlan).toBe(false);
    expect(JSON.stringify(result)).not.toContain('raw prompt body');
    expect(JSON.stringify(result)).not.toContain('raw command body');
    expect(JSON.stringify(result)).not.toContain('raw stdout body');
    expect(JSON.stringify(result)).not.toContain('raw stderr body');

    expect(() =>
      CodexExecRealReadOnlyAdapterRequestSchema.parse({
        ...request,
        id: 'codex_real_read_only_adapter_request_invalid_sandbox',
        requestedSandboxMode: 'workspace_write',
      }),
    ).toThrow();
    expect(() =>
      CodexExecRealReadOnlyAdapterConfigSchema.parse({
        ...config,
        id: 'codex_real_read_only_adapter_config_invalid_dashboard_trigger',
        dashboardTriggerAllowed: true,
      }),
    ).toThrow();
    expect(() =>
      CodexExecRealReadOnlyAdapterRequestSchema.parse({
        ...request,
        id: 'codex_real_read_only_adapter_request_invalid_danger',
        dangerFullAccessAllowed: true,
      }),
    ).toThrow();
  });

  it('parses real read-only adapter policy source models and blocks degraded aligned states', () => {
    const createdAt = '2026-04-30T00:00:00.000Z';
    const flagFields = {
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      processAdapterApproved: false,
      implementationApproved: false,
      recommendationGrantsExecution: false,
      dashboardTriggerAllowed: false,
      promptBodyStored: false,
      commandBodyStored: false,
      stdoutBodyStored: false,
      stderrBodyStored: false,
      agentMessageBodyStored: false,
      reasoningBodyStored: false,
    } as const;
    const gate = CodexExecRealReadOnlyAdapterPolicySourceGateSchema.parse({
      id: 'codex_real_read_only_adapter_policy_source_gate_1',
      schemaVersion,
      createdAt,
      code: 'policy_decision_allows_pilot',
      label: 'Policy decision allows guarded pilot route',
      category: 'policy',
      status: 'passed',
      required: true,
      summary: 'Current policy decision is non-deny.',
      ...flagFields,
    });
    const blocker = CodexExecRealReadOnlyAdapterPolicySourceBlockerSchema.parse({
      id: 'codex_real_read_only_adapter_policy_source_blocker_1',
      schemaVersion,
      createdAt,
      code: 'policy_source_persisted_authoritative',
      severity: 'critical',
      relatedGateCode: 'policy_source_persisted_authoritative',
      summary: 'Policy source must be persisted.',
      recommendation: 'Prepare a Supervisor-backed policy source.',
      ...flagFields,
    });
    const finding = CodexExecRealReadOnlyAdapterPolicySourceFindingSchema.parse({
      id: 'codex_real_read_only_adapter_policy_source_finding_1',
      schemaVersion,
      createdAt,
      code: 'policy_review_note',
      severity: 'medium',
      status: 'requires_review',
      summary: 'Policy source needs operator review.',
      recommendation: 'Review before pilot retry.',
      ...flagFields,
    });
    const checklistItem = CodexExecRealReadOnlyAdapterPolicySourceChecklistItemSchema.parse({
      id: 'codex_real_read_only_adapter_policy_source_check_1',
      schemaVersion,
      createdAt,
      code: gate.code,
      label: gate.label,
      status: gate.status,
      required: true,
      summary: gate.summary,
      ...flagFields,
    });
    const record = CodexExecRealReadOnlyAdapterPolicySourceRecordSchema.parse({
      id: 'codex_real_read_only_adapter_policy_source_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      status: 'aligned',
      recommendation: 'Policy source is aligned for future approval binding only.',
      gates: [gate],
      blockers: [],
      findings: [finding],
      checklistItems: [checklistItem],
      hardGateCount: 1,
      passedGateCount: 1,
      blockedGateCount: 0,
      requiresReviewFindingCount: 1,
      missingSources: [],
      degraded: false,
      notPersisted: false,
      dryRunRecordPresent: true,
      configExplicitlyEnabled: true,
      readOnlyOnly: true,
      policyDecisionPresent: true,
      policyDecisionAllowsPilot: true,
      policyDecisionId: 'policy_decision_1',
      policyDecisionHash: 'sha256:policy',
      policyDecisionOutcome: 'approval_required',
      policyDecisionReasonCount: 1,
      dryRunPlanHash: 'sha256:dry-run',
      evidenceAuditReady: true,
      fallbackUsedAsAuthority: false,
      pilotExecuted: false,
      adapterAttemptInvoked: false,
      authoritative: true,
      supervisorBacked: true,
      persisted: true,
      evidenceRefs: [],
      auditEventIds: ['audit_policy_source_1'],
      summary: 'Policy source stores ids, hashes, counts, and refs only.',
      ...flagFields,
    });
    const summary = CodexExecRealReadOnlyAdapterPolicySourceSummarySchema.parse({
      id: 'codex_real_read_only_adapter_policy_source_summary_1',
      schemaVersion,
      createdAt,
      recordId: record.id,
      dryRunId: record.dryRunId,
      status: record.status,
      hardGateCount: record.hardGateCount,
      passedGateCount: record.passedGateCount,
      blockedGateCount: record.blockedGateCount,
      requiresReviewFindingCount: record.requiresReviewFindingCount,
      missingSources: record.missingSources,
      degraded: record.degraded,
      notPersisted: record.notPersisted,
      dryRunRecordPresent: record.dryRunRecordPresent,
      configExplicitlyEnabled: record.configExplicitlyEnabled,
      readOnlyOnly: record.readOnlyOnly,
      policyDecisionPresent: record.policyDecisionPresent,
      policyDecisionAllowsPilot: record.policyDecisionAllowsPilot,
      policyDecisionId: record.policyDecisionId,
      policyDecisionHash: record.policyDecisionHash,
      policyDecisionOutcome: record.policyDecisionOutcome,
      dryRunPlanHash: record.dryRunPlanHash,
      evidenceAuditReady: record.evidenceAuditReady,
      fallbackUsedAsAuthority: false,
      pilotExecuted: false,
      adapterAttemptInvoked: false,
      recommendation: record.recommendation,
      summary: 'Policy source summary stores metadata only.',
      ...flagFields,
    });
    const query = CodexExecRealReadOnlyAdapterPolicySourceQuerySchema.parse({
      id: 'codex_real_read_only_adapter_policy_source_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'aligned',
      limit: 10,
      ...flagFields,
    });

    expect(record.status).toBe('aligned');
    expect(summary.status).toBe('aligned');
    expect(query.status).toBe('aligned');
    expect(blocker.code).toBe('policy_source_persisted_authoritative');
    expect(JSON.stringify(record)).not.toContain('raw prompt body');
    expect(JSON.stringify(record)).not.toContain('raw command body');
    expect(JSON.stringify(record)).not.toContain('"argv"');
    expect(JSON.stringify(record)).not.toContain('"executablePath":');
    expect(() =>
      CodexExecRealReadOnlyAdapterPolicySourceRecordSchema.parse({
        ...record,
        id: 'codex_real_read_only_adapter_policy_source_invalid_aligned',
        status: 'aligned',
        degraded: true,
      }),
    ).toThrow();
  });

  it('parses pilot prerequisite readiness models and blocks degraded ready states', () => {
    const flagFields = {
      processAdapterApproved: false,
      recommendationGrantsExecution: false,
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      implementationApproved: false,
      dashboardTriggerAllowed: false,
      promptBodyStored: false,
      commandBodyStored: false,
      stdoutBodyStored: false,
      stderrBodyStored: false,
      agentMessageBodyStored: false,
      reasoningBodyStored: false,
    } as const;
    const gate = CodexExecRealReadOnlyAdapterPilotPrerequisiteGateSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_prerequisite_gate_1',
      schemaVersion,
      createdAt,
      code: 'authoritative_attempt_evidence',
      label: 'Authoritative attempt evidence',
      category: 'authority',
      status: 'blocked',
      required: true,
      summary: 'Persisted Supervisor-backed attempt evidence is required.',
      ...flagFields,
    });
    const blocker = CodexExecRealReadOnlyAdapterPilotPrerequisiteBlockerSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_prerequisite_blocker_1',
      schemaVersion,
      createdAt,
      code: 'missing_authoritative_attempt_evidence',
      severity: 'high',
      relatedGateCode: gate.code,
      summary: 'No persisted authoritative attempt evidence is available.',
      recommendation: 'Create authoritative attempt evidence before pilot retry.',
      ...flagFields,
    });
    const finding = CodexExecRealReadOnlyAdapterPilotPrerequisiteFindingSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_prerequisite_finding_1',
      schemaVersion,
      createdAt,
      code: 'handoff_context_incomplete',
      severity: 'medium',
      status: 'requires_review',
      summary: 'Handoff context is incomplete.',
      recommendation: 'Record operator handoff context before pilot retry.',
      ...flagFields,
    });
    const checklistItem = CodexExecRealReadOnlyAdapterPilotPrerequisiteChecklistItemSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_prerequisite_check_1',
      schemaVersion,
      createdAt,
      code: 'config_explicitly_enabled',
      label: 'Config explicitly enabled',
      status: 'blocked',
      required: true,
      summary: 'Config is inspected only and remains disabled.',
      ...flagFields,
    });
    const record = CodexExecRealReadOnlyAdapterPilotPrerequisiteRecordSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_prerequisite_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      status: 'blocked',
      recommendation: 'Pilot retry remains blocked until hard prerequisites are present.',
      gates: [gate],
      blockers: [blocker],
      findings: [finding],
      checklistItems: [checklistItem],
      hardGateCount: 1,
      passedGateCount: 0,
      blockedGateCount: 1,
      requiresReviewFindingCount: 1,
      missingPrerequisites: ['authoritative_attempt_evidence'],
      degraded: false,
      notPersisted: false,
      dryRunRecordPresent: true,
      configExplicitlyEnabled: false,
      authoritativePolicySourcePresent: false,
      validUnusedApprovalPresent: false,
      isolatedCleanWorktreeMetadataPresent: false,
      authoritativeSourcePreparationPresent: false,
      authoritativeAttemptEvidencePresent: false,
      evidenceAuditReady: false,
      fallbackUsedAsAuthority: false,
      pilotExecuted: false,
      adapterAttemptInvoked: false,
      authoritative: true,
      supervisorBacked: true,
      persisted: true,
      worktreeLabel: 'operator-provided-isolated-worktree',
      worktreeStatus: 'missing',
      worktreePathHash: 'sha256:worktree-path',
      evidenceRefs: [],
      auditEventIds: ['audit_pilot_prerequisite_1'],
      summary: 'Pilot prerequisite record stores status, counts, hashes, and refs only.',
      ...flagFields,
    });
    const summary = CodexExecRealReadOnlyAdapterPilotPrerequisiteSummarySchema.parse({
      id: 'codex_real_read_only_adapter_pilot_prerequisite_summary_1',
      schemaVersion,
      createdAt,
      recordId: record.id,
      dryRunId: record.dryRunId,
      status: record.status,
      hardGateCount: record.hardGateCount,
      passedGateCount: record.passedGateCount,
      blockedGateCount: record.blockedGateCount,
      requiresReviewFindingCount: record.requiresReviewFindingCount,
      missingPrerequisites: record.missingPrerequisites,
      degraded: record.degraded,
      notPersisted: record.notPersisted,
      dryRunRecordPresent: record.dryRunRecordPresent,
      configExplicitlyEnabled: record.configExplicitlyEnabled,
      authoritativePolicySourcePresent: record.authoritativePolicySourcePresent,
      validUnusedApprovalPresent: record.validUnusedApprovalPresent,
      isolatedCleanWorktreeMetadataPresent: record.isolatedCleanWorktreeMetadataPresent,
      authoritativeSourcePreparationPresent: record.authoritativeSourcePreparationPresent,
      authoritativeAttemptEvidencePresent: record.authoritativeAttemptEvidencePresent,
      evidenceAuditReady: record.evidenceAuditReady,
      fallbackUsedAsAuthority: false,
      pilotExecuted: false,
      adapterAttemptInvoked: false,
      recommendation: record.recommendation,
      summary: 'Pilot prerequisite summary stores metadata only.',
      ...flagFields,
    });
    const query = CodexExecRealReadOnlyAdapterPilotPrerequisiteQuerySchema.parse({
      id: 'codex_real_read_only_adapter_pilot_prerequisite_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'blocked',
      limit: 10,
      ...flagFields,
    });

    expect(record.status).toBe('blocked');
    expect(record.pilotExecuted).toBe(false);
    expect(record.adapterAttemptInvoked).toBe(false);
    expect(record.fallbackUsedAsAuthority).toBe(false);
    expect(summary.status).toBe('blocked');
    expect(query.status).toBe('blocked');
    expect(JSON.stringify(record)).not.toContain('C:/');
    expect(JSON.stringify(record)).not.toContain('raw prompt body');
    expect(JSON.stringify(record)).not.toContain('raw command body');
    expect(JSON.stringify(record)).not.toContain('raw stdout body');
    expect(JSON.stringify(record)).not.toContain('raw stderr body');
    expect(JSON.stringify(record)).not.toContain('"argv"');
    expect(JSON.stringify(record)).not.toContain('"executablePath":');
    expect(() =>
      CodexExecRealReadOnlyAdapterPilotPrerequisiteRecordSchema.parse({
        ...record,
        id: 'codex_real_read_only_adapter_pilot_prerequisite_invalid_ready',
        status: 'ready_for_pilot_retry',
        degraded: true,
      }),
    ).toThrow();
  });

  it('parses pilot prerequisite source preparation models and blocks degraded prepared states', () => {
    const createdAt = '2026-04-30T00:00:00.000Z';
    const flagFields = {
      workspaceWriteAllowed: false,
      dangerFullAccessAllowed: false,
      metadataOnly: true,
      bodyStored: false,
      liveExecution: false,
      externalProcessStarted: false,
      executionDisabled: true,
      processAdapterStarted: false,
      processAdapterApproved: false,
      implementationApproved: false,
      recommendationGrantsExecution: false,
      dashboardTriggerAllowed: false,
      promptBodyStored: false,
      commandBodyStored: false,
      stdoutBodyStored: false,
      stderrBodyStored: false,
      agentMessageBodyStored: false,
      reasoningBodyStored: false,
    } as const;
    const gate = CodexExecRealReadOnlyAdapterPilotSourcePreparationGateSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_source_preparation_gate_1',
      schemaVersion,
      createdAt,
      code: 'isolated_clean_worktree_metadata',
      label: 'Isolated clean worktree metadata',
      category: 'worktree',
      status: 'blocked',
      required: true,
      summary: 'Existing isolated worktree metadata is required.',
      ...flagFields,
    });
    const blocker = CodexExecRealReadOnlyAdapterPilotSourcePreparationBlockerSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_source_preparation_blocker_1',
      schemaVersion,
      createdAt,
      code: gate.code,
      severity: 'high',
      relatedGateCode: gate.code,
      summary: 'No isolated clean worktree metadata is present.',
      recommendation: 'Record worktree label/hash/status before 4F.2.',
      ...flagFields,
    });
    const finding = CodexExecRealReadOnlyAdapterPilotSourcePreparationFindingSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_source_preparation_finding_1',
      schemaVersion,
      createdAt,
      code: 'handoff_context_incomplete',
      severity: 'medium',
      status: 'requires_review',
      summary: 'Operator handoff context needs review.',
      recommendation: 'Record handoff context before 4F.2.',
      ...flagFields,
    });
    const checklistItem =
      CodexExecRealReadOnlyAdapterPilotSourcePreparationChecklistItemSchema.parse({
        id: 'codex_real_read_only_adapter_pilot_source_preparation_check_1',
        schemaVersion,
        createdAt,
        code: gate.code,
        label: gate.label,
        status: gate.status,
        required: true,
        summary: gate.summary,
        ...flagFields,
      });
    const record = CodexExecRealReadOnlyAdapterPilotSourcePreparationRecordSchema.parse({
      id: 'codex_real_read_only_adapter_pilot_source_preparation_1',
      schemaVersion,
      createdAt,
      dryRunId: 'codex_dry_run_1',
      status: 'blocked',
      recommendation: 'Source preparation remains blocked.',
      gates: [gate],
      blockers: [blocker],
      findings: [finding],
      checklistItems: [checklistItem],
      hardGateCount: 1,
      passedGateCount: 0,
      blockedGateCount: 1,
      requiresReviewFindingCount: 1,
      missingSources: ['isolated_clean_worktree_metadata'],
      degraded: false,
      notPersisted: false,
      dryRunRecordPresent: true,
      configExplicitlyEnabled: true,
      authoritativePolicySourcePresent: true,
      validUnusedApprovalPresent: true,
      approvalArtifactId: 'codex_approval_artifact_1',
      approvalArtifactHash: 'sha256:approval',
      dryRunPlanHash: 'sha256:dry-run',
      policyDecisionHash: 'sha256:policy',
      isolatedCleanWorktreeMetadataPresent: false,
      worktreeLabel: 'operator-isolated-worktree',
      worktreeStatus: 'missing',
      worktreePathHash: 'sha256:worktree',
      evidenceAuditReady: true,
      fallbackUsedAsAuthority: false,
      pilotExecuted: false,
      adapterAttemptInvoked: false,
      authoritative: true,
      supervisorBacked: true,
      persisted: true,
      evidenceRefs: [],
      auditEventIds: ['audit_source_preparation_1'],
      summary: 'Source preparation stores metadata only.',
      ...flagFields,
    });
    const summary = CodexExecRealReadOnlyAdapterPilotSourcePreparationSummarySchema.parse({
      id: 'codex_real_read_only_adapter_pilot_source_preparation_summary_1',
      schemaVersion,
      createdAt,
      recordId: record.id,
      dryRunId: record.dryRunId,
      status: record.status,
      hardGateCount: record.hardGateCount,
      passedGateCount: record.passedGateCount,
      blockedGateCount: record.blockedGateCount,
      requiresReviewFindingCount: record.requiresReviewFindingCount,
      missingSources: record.missingSources,
      degraded: record.degraded,
      notPersisted: record.notPersisted,
      dryRunRecordPresent: record.dryRunRecordPresent,
      configExplicitlyEnabled: record.configExplicitlyEnabled,
      authoritativePolicySourcePresent: record.authoritativePolicySourcePresent,
      validUnusedApprovalPresent: record.validUnusedApprovalPresent,
      isolatedCleanWorktreeMetadataPresent: record.isolatedCleanWorktreeMetadataPresent,
      evidenceAuditReady: record.evidenceAuditReady,
      fallbackUsedAsAuthority: false,
      pilotExecuted: false,
      adapterAttemptInvoked: false,
      recommendation: record.recommendation,
      summary: 'Source preparation summary stores metadata only.',
      ...flagFields,
    });
    const query = CodexExecRealReadOnlyAdapterPilotSourcePreparationQuerySchema.parse({
      id: 'codex_real_read_only_adapter_pilot_source_preparation_query_1',
      schemaVersion,
      createdAt,
      dryRunId: record.dryRunId,
      status: 'blocked',
      limit: 10,
      ...flagFields,
    });

    expect(record.status).toBe('blocked');
    expect(summary.status).toBe('blocked');
    expect(query.status).toBe('blocked');
    expect(record.pilotExecuted).toBe(false);
    expect(record.adapterAttemptInvoked).toBe(false);
    expect(JSON.stringify(record)).not.toContain('C:/');
    expect(JSON.stringify(record)).not.toContain('raw prompt body');
    expect(JSON.stringify(record)).not.toContain('"argv"');
    expect(JSON.stringify(record)).not.toContain('"executablePath":');
    expect(() =>
      CodexExecRealReadOnlyAdapterPilotSourcePreparationRecordSchema.parse({
        ...record,
        id: 'codex_real_read_only_adapter_pilot_source_preparation_invalid_prepared',
        status: 'prepared',
        degraded: true,
      }),
    ).toThrow();
  });

  it('parses GitHub provider metadata contracts and rejects raw remote values', () => {
    const createdAt = '2026-05-04T00:00:00.000Z';
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_remote_ref_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:head',
      allowedHost: GithubProviderHostSchema.value,
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      metadata: {
        provider: 'github',
        ownerHash: 'sha256:owner',
      },
      summary: 'GitHub remote target stores hashes only.',
    });
    const tokenReadiness = GithubTokenReadinessSchema.parse({
      id: 'github_token_readiness_1',
      schemaVersion,
      createdAt,
      providerName: 'github-provider',
      envVarNameHash: 'sha256:env-name',
      tokenConfigured: true,
      tokenHash: 'sha256:token',
      tokenValueStored: false,
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub token is configured; value is not stored.',
    });
    const dryRun = GithubMetadataDryRunRecordSchema.parse({
      id: 'github_metadata_dry_run_record_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_metadata_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-http',
      targetRef,
      requestedMetadata: ['repo', 'base_branch', 'head_branch', 'existing_pull_request'],
      policyDecision: {
        id: 'policy_github_metadata_1',
        schemaVersion,
        createdAt,
        actionId: 'github_metadata_1',
        actionType: 'github.metadata.read',
        actionMode: 'read',
        riskLevel: 'high',
        outcome: 'approval_required',
        reasons: ['remote network boundary requires approval'],
        requiresDryRun: true,
        requiresApproval: true,
        allow: false,
      },
      requiresApproval: true,
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [],
      auditEventIds: ['audit_github_metadata_plan_1'],
      summary: 'GitHub metadata dry-run is planned.',
    });
    const approval = GithubMetadataApprovalArtifactRecordSchema.parse({
      id: 'github_metadata_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalRequestId: 'github_metadata_approval_request_1',
      approvalArtifactId: 'github_metadata_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: dryRun.policyDecision.id,
      evidenceRefs: [],
      auditEventIds: ['audit_github_metadata_approval_1'],
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Approval is persisted without raw remote metadata.',
    });
    const run = GithubMetadataControlPlaneRunSchema.parse({
      id: 'github_metadata_run_1',
      schemaVersion,
      createdAt,
      dryRunId: dryRun.dryRunId,
      dryRunRecordId: dryRun.id,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      targetRef,
      repoMetadataHash: 'sha256:repo-metadata',
      baseBranchMetadataHash: 'sha256:base-metadata',
      headBranchMetadataHash: 'sha256:head-metadata',
      existingPullRequestCount: 0,
      responseBodyHashes: ['sha256:repo-body', 'sha256:base-body', 'sha256:head-body'],
      evidenceRefs: [],
      auditEventIds: ['audit_github_metadata_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub metadata run completed with hashes only.',
    });
    const serialized = JSON.stringify([targetRef, tokenReadiness, dryRun, approval, run]);

    expect(run.status).toBe('completed');
    expect(run.networkBoundaryInvoked).toBe(true);
    expect(tokenReadiness.tokenValueStored).toBe(false);
    expect(serialized).not.toContain('octocat');
    expect(serialized).not.toContain('hello-world');
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('https://api.github.com');
    expect(() =>
      GithubRemoteRefSummarySchema.parse({
        ...targetRef,
        id: 'github_remote_ref_invalid_raw',
        metadata: {
          owner: 'octocat',
        },
      }),
    ).toThrow();
    expect(() =>
      GithubMetadataDryRunRecordSchema.parse({
        ...dryRun,
        id: 'github_metadata_dry_run_blocked_invalid',
        status: 'blocked',
        networkBoundaryPlanned: true,
      }),
    ).toThrow();
  });

  it('parses GitHub draft PR contracts while keeping PR body and remote refs hash-only', () => {
    const createdAt = '2026-05-04T00:00:00.000Z';
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_draft_target_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:head',
      allowedHost: GithubProviderHostSchema.value,
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Draft PR target is represented by hashes only.',
    });
    const readiness = GithubDraftPrReadinessSchema.parse({
      id: 'github_draft_pr_readiness_1',
      schemaVersion,
      createdAt,
      sourceKind: 'local_rc_readiness',
      sourceIdHash: 'sha256:source-id',
      sourceSummaryHash: 'sha256:source-summary',
      targetRef,
      status: 'ready_for_draft_pr',
      blockerCount: 0,
      draftOnly: true,
      remoteHeadBranchExistsRequired: true,
      pushAllowed: false,
      createRefAllowed: false,
      mergeAllowed: false,
      labelsAllowed: false,
      reviewersAllowed: false,
      commentsAllowed: false,
      rawPrBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Existing remote head branch is required before draft PR creation.',
    });
    const plan = GithubDraftPrPlanSchema.parse({
      id: 'github_draft_pr_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_draft_pr_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-draft-pr',
      readiness,
      titleHash: 'sha256:title',
      bodyHash: 'sha256:body',
      bodySectionCount: 4,
      bodyCharacterCount: 512,
      policyDecision: {
        id: 'policy_github_draft_pr_1',
        schemaVersion,
        createdAt,
        actionId: 'github_draft_pr_1',
        actionType: 'github.draft_pr.create',
        actionMode: 'write',
        riskLevel: 'high',
        outcome: 'approval_required',
        reasons: ['remote draft PR creation requires approval'],
        requiresDryRun: true,
        requiresApproval: true,
        allow: false,
      },
      requiresApproval: true,
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      draft: true,
      pushAllowed: false,
      createRefAllowed: false,
      mergeAllowed: false,
      rawPrBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      evidenceRefs: [],
      auditEventIds: ['audit_draft_pr_plan_1'],
      summary: 'Draft PR creation plan stores title/body hashes only.',
    });
    const creationSummary = GithubDraftPrCreationSummarySchema.parse({
      id: 'github_draft_pr_creation_summary_1',
      schemaVersion,
      createdAt,
      targetRef,
      prNumberHash: 'sha256:pr-number',
      prUrlHash: 'sha256:pr-url',
      titleHash: plan.titleHash,
      bodyHash: plan.bodyHash,
      draft: true,
      created: true,
      rawUrlStored: false,
      rawPrBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Draft PR creation summary stores only hashes and counts.',
    });
    const auditChain = {
      id: 'github_remote_pr_audit_chain_1',
      schemaVersion,
      createdAt,
      draftPrRunIdHash: 'sha256:run',
      auditEventIds: ['audit_draft_pr_run_1'],
      auditEventCount: 1,
      policyDecisionIds: [plan.policyDecision.id],
      evidenceRefIds: ['evidence_draft_pr_run_1'],
      evidenceRefCount: 1,
      networkBoundaryCount: 1,
      chainHash: 'sha256:chain',
      rawPathStored: false,
      bodyStored: false,
      summary: 'Remote PR audit chain references ids only.',
    };
    const run = GithubDraftPrRunSchema.parse({
      id: 'github_draft_pr_run_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      approvalArtifactId: 'github_draft_pr_approval_artifact_1',
      status: 'completed',
      plan,
      creationSummary,
      auditChain,
      responseBodyHashes: ['sha256:post-response'],
      evidenceRefs: [],
      auditEventIds: ['audit_draft_pr_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      draft: true,
      pushAllowed: false,
      createRefAllowed: false,
      mergeAllowed: false,
      rawPrBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Draft PR run completed after a governed network boundary.',
    });
    const rehearsal = GithubDraftPrAcceptanceRehearsalRunSchema.parse({
      id: 'github_draft_pr_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      status: 'passed',
      stepCount: 6,
      readinessStatus: 'ready_for_draft_pr',
      prCreationStatus: 'fixture_completed',
      evidenceRefCount: 0,
      auditEventCount: 0,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      draft: true,
      pushAllowed: false,
      createRefAllowed: false,
      mergeAllowed: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Fixture rehearsal does not call GitHub.',
    });
    const serialized = JSON.stringify([plan, run, rehearsal]);

    expect(run.status).toBe('completed');
    expect(run.draft).toBe(true);
    expect(run.pushAllowed).toBe(false);
    expect(rehearsal.networkBoundaryInvoked).toBe(false);
    expect(serialized).not.toContain('## Summary');
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('ghp_');
    expect(() =>
      GithubDraftPrReadinessSchema.parse({
        ...readiness,
        id: 'github_draft_pr_readiness_invalid_ready',
        blockerCount: 1,
      }),
    ).toThrow();
    expect(() =>
      GithubDraftPrRunSchema.parse({
        ...run,
        id: 'github_draft_pr_run_invalid_completed',
        noRealWrite: true,
      }),
    ).toThrow();
    expect(() =>
      GithubDraftPrPlanSchema.parse({
        ...plan,
        id: 'github_draft_pr_plan_invalid_raw_body',
        metadata: {
          prBody: 'raw PR markdown body',
        },
      }),
    ).toThrow();
    expect(() =>
      GithubDraftPrAcceptanceRehearsalRunSchema.parse({
        ...rehearsal,
        id: 'github_draft_pr_rehearsal_invalid_raw_token',
        metadata: {
          token: 'ghp_secret',
        },
      }),
    ).toThrow();
  });

  it('parses GitHub branch publish contracts while keeping file content and refs hash-only', () => {
    const createdAt = '2026-05-05T00:00:00.000Z';
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_branch_publish_target_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:branch',
      allowedHost: GithubProviderHostSchema.value,
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Branch publish target is represented by hashes only.',
    });
    const contentManifest = GithubCommitContentManifestSchema.parse({
      id: 'github_commit_content_manifest_1',
      schemaVersion,
      createdAt,
      sourceKind: 'local_rc_readiness',
      sourceIdHash: 'sha256:source',
      worktreePathHash: 'sha256:worktree',
      fileCount: 2,
      totalByteCount: 256,
      filePathHashes: ['sha256:path-a', 'sha256:path-b'],
      fileContentHashes: ['sha256:content-a', 'sha256:content-b'],
      maxFileByteCount: 128,
      textOnly: true,
      deletionsAllowed: false,
      renamesAllowed: false,
      binaryAllowed: false,
      symlinkAllowed: false,
      rawFileContentStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Content manifest stores file path and content hashes only.',
    });
    const readiness = GithubBranchPublishReadinessSchema.parse({
      id: 'github_branch_publish_readiness_1',
      schemaVersion,
      createdAt,
      sourceKind: 'local_rc_readiness',
      sourceIdHash: 'sha256:source',
      sourceSummaryHash: 'sha256:summary',
      targetRef,
      contentManifest,
      status: 'ready_for_branch_publish',
      blockerCount: 0,
      newBranchRequired: true,
      branchPrefix: 'codexhub/',
      existingBranchUpdateAllowed: false,
      forceAllowed: false,
      pushAllowed: false,
      mergeAllowed: false,
      labelsAllowed: false,
      reviewersAllowed: false,
      commentsAllowed: false,
      rawFileContentStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'New codexhub branch is ready for approval-gated publish.',
    });
    const plan = GithubBranchPublishPlanSchema.parse({
      id: 'github_branch_publish_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_branch_publish_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-branch-publish',
      readiness,
      commitMessageHash: 'sha256:message',
      blockReasons: [],
      policyDecision: {
        id: 'policy_github_branch_publish_1',
        schemaVersion,
        createdAt,
        actionId: 'github_branch_publish_1',
        actionType: 'github.branch_publish.create',
        actionMode: 'write',
        riskLevel: 'high',
        outcome: 'approval_required',
        reasons: ['remote branch publish requires approval'],
        requiresDryRun: true,
        requiresApproval: true,
        allow: false,
      },
      requiresApproval: true,
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      createRefAllowed: true,
      updateRefAllowed: false,
      forceAllowed: false,
      pushAllowed: false,
      mergeAllowed: false,
      rawFileContentStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      evidenceRefs: [],
      auditEventIds: ['audit_branch_publish_plan_1'],
      summary: 'Branch publish plan stores remote refs and content manifest as hashes only.',
    });
    const commitSummary = GithubRemoteCommitSummarySchema.parse({
      id: 'github_remote_commit_summary_1',
      schemaVersion,
      createdAt,
      targetRef,
      commitShaHash: 'sha256:commit',
      treeShaHash: 'sha256:tree',
      branchNameHash: 'sha256:branch',
      contentManifestHash: 'sha256:manifest',
      fileCount: 2,
      created: true,
      createRefAllowed: true,
      updateRefAllowed: false,
      forceAllowed: false,
      pushAllowed: false,
      mergeAllowed: false,
      rawFileContentStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Remote commit summary stores SHAs as hashes only.',
    });
    const run = GithubBranchPublishRunSchema.parse({
      id: 'github_branch_publish_run_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      approvalArtifactId: 'github_branch_publish_approval_artifact_1',
      status: 'completed',
      plan,
      commitSummary,
      responseBodyHashes: ['sha256:repo', 'sha256:base', 'sha256:blob'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_branch_publish_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      createRefAllowed: true,
      updateRefAllowed: false,
      forceAllowed: false,
      pushAllowed: false,
      mergeAllowed: false,
      rawFileContentStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Branch publish completed through the governed GitHub API boundary.',
    });
    const rehearsal = GithubBranchPublishAcceptanceRehearsalRunSchema.parse({
      id: 'github_branch_publish_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      status: 'passed',
      readinessStatus: 'ready_for_branch_publish',
      branchPublishStatus: 'fixture_completed',
      stepCount: 6,
      evidenceRefCount: 0,
      auditEventCount: 0,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      createRefAllowed: true,
      updateRefAllowed: false,
      forceAllowed: false,
      pushAllowed: false,
      mergeAllowed: false,
      rawFileContentStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Fixture branch publish rehearsal does not call GitHub.',
    });
    const serialized = JSON.stringify([plan, run, rehearsal]);

    expect(run.status).toBe('completed');
    expect(run.createRefAllowed).toBe(true);
    expect(run.updateRefAllowed).toBe(false);
    expect(run.pushAllowed).toBe(false);
    expect(serialized).not.toContain('octo-org');
    expect(serialized).not.toContain('codexhub/m17');
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('ghp_');
    expect(() =>
      GithubCommitContentManifestSchema.parse({
        ...contentManifest,
        id: 'github_commit_content_manifest_invalid_count',
        fileCount: 1,
      }),
    ).toThrow();
    expect(() =>
      GithubBranchPublishReadinessSchema.parse({
        ...readiness,
        id: 'github_branch_publish_readiness_invalid_ready',
        blockerCount: 1,
      }),
    ).toThrow();
    expect(() =>
      GithubBranchPublishRunSchema.parse({
        ...run,
        id: 'github_branch_publish_run_invalid_completed',
        noRealWrite: true,
      }),
    ).toThrow();
    expect(() =>
      GithubBranchPublishPlanSchema.parse({
        ...plan,
        id: 'github_branch_publish_plan_invalid_raw_content',
        metadata: {
          rawBody: 'file contents',
        },
      }),
    ).toThrow();
  });

  it('parses GitHub publish to draft PR chain and lifecycle observation contracts', () => {
    const createdAt = '2026-05-05T00:00:00.000Z';
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_chain_target_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:head',
      allowedHost: GithubProviderHostSchema.value,
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Publish chain target is hash-only.',
    });
    const chainPlan = GithubPublishDraftPrChainPlanSchema.parse({
      id: 'github_publish_draft_pr_chain_plan_1',
      schemaVersion,
      createdAt,
      chainId: 'github_publish_draft_pr_chain_1',
      sourceKind: 'review_package',
      sourceIdHash: 'sha256:source',
      branchPublishDryRunId: 'github_branch_publish_dry_run_1',
      draftPrDryRunId: 'github_draft_pr_dry_run_1',
      separateApprovalsRequired: true,
      branchPublishApprovalRequired: true,
      draftPrApprovalRequired: true,
      networkBoundaryPlanned: true,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Publish to Draft PR chain requires separate approvals.',
    });
    const step = GithubPublishDraftPrChainStepSchema.parse({
      id: 'github_publish_draft_pr_chain_step_1',
      schemaVersion,
      createdAt,
      phase: 'branch-publish',
      status: 'completed',
      order: 1,
      evidenceRefIds: ['evidence_branch_publish'],
      auditEventIds: ['audit_branch_publish'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Branch publish step completed.',
    });
    const lifecycle = GithubRemotePrLifecycleSummarySchema.parse({
      id: 'github_remote_pr_lifecycle_1',
      schemaVersion,
      createdAt,
      targetRef,
      prNumberHash: 'sha256:pr-number',
      prUrlHash: 'sha256:pr-url',
      stateHash: 'sha256:open',
      checkRunCount: 2,
      statusContextCount: 1,
      failedCheckCount: 0,
      pendingCheckCount: 1,
      passedCheckCount: 1,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'PR lifecycle summary stores check counts and hashes only.',
    });
    const chainRun = GithubPublishDraftPrChainRunSchema.parse({
      id: 'github_publish_draft_pr_chain_run_1',
      schemaVersion,
      createdAt,
      chainId: chainPlan.chainId,
      status: 'completed',
      plan: chainPlan,
      steps: [step],
      stepCount: 1,
      branchPublishRunId: 'github_branch_publish_run_1',
      draftPrRunId: 'github_draft_pr_run_1',
      lifecycleSummary: lifecycle,
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_chain'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Publish to Draft PR chain completed with metadata-only projection.',
    });
    const rehearsal = GithubPublishDraftPrAcceptanceRehearsalRunSchema.parse({
      id: 'github_publish_draft_pr_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      status: 'passed',
      branchPublishStatus: 'fixture_completed',
      draftPrStatus: 'fixture_completed',
      lifecycleStatus: 'checks_passed',
      stepCount: 8,
      evidenceRefCount: 0,
      auditEventCount: 0,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      updateRefAllowed: false,
      forceAllowed: false,
      pushAllowed: false,
      mergeAllowed: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Fixture publish and draft PR rehearsal does not call GitHub.',
    });
    const serialized = JSON.stringify([chainPlan, chainRun, rehearsal]);

    expect(chainRun.status).toBe('completed');
    expect(chainRun.stepCount).toBe(1);
    expect(rehearsal.networkBoundaryInvoked).toBe(false);
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('raw PR markdown');
    expect(() =>
      GithubPublishDraftPrChainRunSchema.parse({
        ...chainRun,
        id: 'github_publish_draft_pr_chain_invalid_step_count',
        stepCount: 2,
      }),
    ).toThrow();
    expect(() =>
      GithubRemotePrLifecycleSummarySchema.parse({
        ...lifecycle,
        id: 'github_remote_pr_lifecycle_invalid_raw_url',
        metadata: {
          url: 'https://github.com/octo-org/codexhub/pull/1',
        },
      }),
    ).toThrow();
  });

  it('parses GitHub PR lifecycle observation contracts as metadata-only records', () => {
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_pr_lifecycle_target_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:head',
      allowedHost: 'api.github.com',
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Remote target stores owner, repo, and refs as hashes only.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_github_pr_lifecycle_1',
      schemaVersion,
      createdAt,
      actionId: 'github_pr_lifecycle_observe',
      actionType: 'github.pr_lifecycle.observe',
      actionMode: 'read',
      riskLevel: 'high',
      outcome: 'approval_required',
      reasons: ['live_github_observation_requires_approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const evidence = EvidenceRefSchema.parse({
      id: 'evidence_github_pr_lifecycle_plan_1',
      schemaVersion,
      createdAt,
      kind: 'github.pr_lifecycle_plan',
      hash: 'sha256:plan',
      summary: 'PR lifecycle plan evidence stores hashes only.',
    });
    const plan = GithubPrLifecycleObservationPlanSchema.parse({
      id: 'github_pr_lifecycle_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_pr_lifecycle_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-pr-lifecycle',
      targetRef,
      prNumberHash: 'sha256:pr-number',
      commitShaHash: 'sha256:commit',
      requestedMetadata: ['repo', 'pull_request', 'branch_ref', 'combined_status', 'check_runs'],
      blockReasons: [],
      policyDecision,
      requiresApproval: true,
      evidenceRefs: [evidence],
      auditEventIds: ['audit_github_pr_lifecycle_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'PR lifecycle observation plan is approval-gated and metadata-only.',
    });
    const approval = GithubPrLifecycleApprovalArtifactRecordSchema.parse({
      id: 'github_pr_lifecycle_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      approvalRequestId: 'github_pr_lifecycle_approval_request_1',
      approvalArtifactId: 'github_pr_lifecycle_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: plan.policyDecision.id,
      reasonHash: 'sha256:reason',
      evidenceRefs: [],
      auditEventIds: ['audit_github_pr_lifecycle_approval_1'],
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'PR lifecycle approval artifact stores decision metadata only.',
    });
    const lifecycleSummary = GithubRemotePrLifecycleSummarySchema.parse({
      id: 'github_pr_lifecycle_summary_1',
      schemaVersion,
      createdAt,
      targetRef,
      prNumberHash: 'sha256:pr-number',
      prUrlHash: 'sha256:pr-url',
      stateHash: 'sha256:open',
      checkRunCount: 3,
      statusContextCount: 2,
      failedCheckCount: 1,
      pendingCheckCount: 1,
      passedCheckCount: 3,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'PR lifecycle summary stores status and check counts only.',
    });
    const run = GithubPrLifecycleObservationRunSchema.parse({
      id: 'github_pr_lifecycle_run_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      plan,
      lifecycleSummary,
      responseBodyHashes: ['sha256:repo', 'sha256:pr', 'sha256:ref', 'sha256:status', 'sha256:checks'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_github_pr_lifecycle_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'PR lifecycle observation completed through fixed GETs.',
    });
    const rehearsal = GithubPrLifecycleAcceptanceRehearsalRunSchema.parse({
      id: 'github_pr_lifecycle_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'checks-passed',
      status: 'passed',
      lifecycleStatus: 'checks_passed',
      stepCount: 5,
      evidenceRefCount: 0,
      auditEventCount: 0,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      blockerCount: 0,
      summary: 'Fixture PR lifecycle rehearsal does not call GitHub.',
    });
    const serialized = JSON.stringify([plan, approval, run, rehearsal]);

    expect(run.status).toBe('completed');
    expect(run.lifecycleSummary.failedCheckCount).toBe(1);
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('raw check log');
    expect(() =>
      GithubPrLifecycleObservationPlanSchema.parse({
        ...plan,
        id: 'github_pr_lifecycle_plan_invalid_blocked_network',
        status: 'blocked',
      }),
    ).toThrow();
    expect(() =>
      GithubPrLifecycleObservationRunSchema.parse({
        ...run,
        id: 'github_pr_lifecycle_run_invalid_completed_boundary',
        networkBoundaryInvoked: false,
      }),
    ).toThrow();
    expect(() =>
      GithubPrLifecycleObservationRunSchema.parse({
        ...run,
        id: 'github_pr_lifecycle_run_invalid_raw_body',
        metadata: {
          responseBody: '{"head":{"sha":"raw"}}',
        },
      }),
    ).toThrow();
  });

  it('parses governed GitHub merge contracts with two approvals and metadata-only output', () => {
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_merge_target_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:head',
      allowedHost: 'api.github.com',
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Remote target stores owner, repo, and refs as hashes only.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_github_merge_1',
      schemaVersion,
      createdAt,
      actionId: 'github_merge_1',
      actionType: 'github.merge',
      actionMode: 'admin',
      riskLevel: 'critical',
      outcome: 'approval_required',
      reasons: ['governed_merge_requires_two_approvals'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const readiness = GithubMergeReadinessSummarySchema.parse({
      id: 'github_merge_readiness_1',
      schemaVersion,
      createdAt,
      targetRef,
      prNumberHash: 'sha256:pr-number',
      headShaHash: 'sha256:head-sha',
      mergeStrategy: 'squash',
      status: 'ready_for_merge',
      prStateHash: 'sha256:open',
      branchProtectionStatus: 'satisfied',
      checkRunCount: 2,
      statusContextCount: 1,
      failedCheckCount: 0,
      pendingCheckCount: 0,
      passedCheckCount: 3,
      reviewDecisionCount: 2,
      approvingReviewCount: 2,
      changesRequestedReviewCount: 0,
      blockerCount: 0,
      checksPassed: true,
      reviewsSatisfied: true,
      branchProtectionSatisfied: true,
      headShaMatchesDryRun: true,
      requiresTwoApprovals: true,
      fixedEndpointOnly: true,
      pushAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      arbitraryEndpointAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPrBodyStored: false,
      rawReviewBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Merge readiness passed from hash-only status and review summaries.',
    });
    const planEvidence = EvidenceRefSchema.parse({
      id: 'evidence_github_merge_plan_1',
      schemaVersion,
      createdAt,
      kind: 'github.merge_readiness_plan',
      hash: 'sha256:merge-plan',
      summary: 'Merge readiness plan evidence stores hashes only.',
    });
    const plan = GithubMergeReadinessPlanSchema.parse({
      id: 'github_merge_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_merge_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-merge',
      targetRef,
      prNumberHash: 'sha256:pr-number',
      expectedHeadShaHash: 'sha256:head-sha',
      mergeStrategy: 'squash',
      readiness,
      blockReasons: [],
      policyDecision,
      requiresApproval: true,
      requiresTwoApprovals: true,
      evidenceRefs: [planEvidence],
      auditEventIds: ['audit_github_merge_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      fixedEndpointOnly: true,
      pushAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      arbitraryEndpointAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPrBodyStored: false,
      rawReviewBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Merge dry-run is planned; live merge requires two approvals.',
    });
    const readinessApproval = GithubMergeApprovalArtifactSchema.parse({
      id: 'github_merge_readiness_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      approvalPhase: 'readiness',
      approvalRequestId: 'github_merge_readiness_approval_request_1',
      approvalArtifactId: 'github_merge_readiness_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: policyDecision.id,
      decidedByHash: 'sha256:operator-a',
      reasonHash: 'sha256:reason-a',
      evidenceRefs: [],
      auditEventIds: ['audit_github_merge_readiness_approval_1'],
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPrBodyStored: false,
      rawReviewBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Merge readiness approval stores decision metadata only.',
    });
    const mergeApproval = GithubMergeApprovalArtifactSchema.parse({
      ...readinessApproval,
      id: 'github_merge_execution_approval_1',
      approvalPhase: 'merge_execution',
      approvalRequestId: 'github_merge_execution_approval_request_1',
      approvalArtifactId: 'github_merge_execution_approval_artifact_1',
      decidedByHash: 'sha256:operator-b',
      reasonHash: 'sha256:reason-b',
      auditEventIds: ['audit_github_merge_execution_approval_1'],
      summary: 'Merge execution approval stores decision metadata only.',
    });
    const resultSummary = GithubMergeResultSummarySchema.parse({
      id: 'github_merge_result_1',
      schemaVersion,
      createdAt,
      targetRef,
      prNumberHash: 'sha256:pr-number',
      expectedHeadShaHash: 'sha256:head-sha',
      mergeCommitShaHash: 'sha256:merge-commit',
      mergeStrategy: 'squash',
      merged: true,
      responseBodyHashCount: 7,
      fixedEndpointOnly: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPrBodyStored: false,
      rawReviewBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Merge result stores commit hash and response hash counts only.',
    });
    const run = GithubMergeRunSchema.parse({
      id: 'github_merge_run_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      readinessApprovalArtifactId: readinessApproval.approvalArtifactId,
      mergeApprovalArtifactId: mergeApproval.approvalArtifactId,
      status: 'completed',
      plan,
      resultSummary,
      responseBodyHashes: [
        'sha256:repo',
        'sha256:pr',
        'sha256:protection',
        'sha256:status',
        'sha256:checks',
        'sha256:reviews',
        'sha256:merge',
      ],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_github_merge_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      fixedEndpointOnly: true,
      pushAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      arbitraryEndpointAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPrBodyStored: false,
      rawReviewBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: false,
      summary: 'Governed merge completed through fixed endpoint boundary.',
    });
    const rehearsal = GithubMergeAcceptanceRehearsalRunSchema.parse({
      id: 'github_merge_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'checks-failed',
      status: 'blocked',
      readinessStatus: 'blocked_checks',
      mergeStatus: 'skipped',
      stepCount: 6,
      blockerCount: 1,
      evidenceRefCount: 1,
      auditEventCount: 1,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      requiresTwoApprovals: true,
      fixedEndpointOnly: true,
      pushAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      arbitraryEndpointAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPrBodyStored: false,
      rawReviewBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Fixture merge rehearsal blocks before merge when checks fail.',
    });
    const serialized = JSON.stringify([plan, readinessApproval, mergeApproval, run, rehearsal]);

    expect(run.status).toBe('completed');
    expect(plan.requiresTwoApprovals).toBe(true);
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('review body');
    expect(() =>
      GithubMergeReadinessSummarySchema.parse({
        ...readiness,
        id: 'github_merge_readiness_invalid_blockers',
        blockerCount: 1,
      }),
    ).toThrow();
    expect(() =>
      GithubMergeRunSchema.parse({
        ...run,
        id: 'github_merge_run_invalid_boundary',
        networkBoundaryInvoked: false,
      }),
    ).toThrow();
    expect(() =>
      GithubMergeReadinessPlanSchema.parse({
        ...plan,
        id: 'github_merge_plan_invalid_raw_review',
        metadata: { rawReviewBody: 'review body' },
      }),
    ).toThrow();
  });

  it('parses GitHub Actions CI/CD contracts with metadata-only output', () => {
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_actions_target_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:head',
      allowedHost: 'api.github.com',
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions target stores owner, repo, and refs as hashes only.',
    });
    const tokenReadiness = GithubTokenReadinessSchema.parse({
      id: 'github_actions_token_1',
      schemaVersion,
      createdAt,
      providerName: 'github-provider',
      envVarNameHash: 'sha256:token-env',
      tokenConfigured: true,
      tokenHash: 'sha256:token',
      tokenValueStored: false,
      rawValueStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Token is configured and only a hash is exposed.',
    });
    const readiness = GithubActionsReadinessSchema.parse({
      id: 'github_actions_readiness_1',
      schemaVersion,
      createdAt,
      provider: 'github-actions',
      tokenReadiness,
      observationEnabled: true,
      rerunEnabled: false,
      cancelEnabled: false,
      dispatchEnabled: false,
      implementedProviderCount: 1,
      futureProviderCount: 3,
      blockerCount: 1,
      blockReasons: ['github_actions_writes_disabled'],
      tokenValueStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions readiness exposes provider state and blockers only.',
    });
    const manifest = CicdProviderManifestSchema.parse({
      id: 'github_actions_manifest_1',
      schemaVersion,
      createdAt,
      provider: 'github-actions',
      implemented: true,
      hostHash: 'sha256:host',
      actionMode: 'read',
      defaultEnabled: false,
      requiresApprovalForWrites: true,
      fixedEndpointOnly: true,
      arbitraryPayloadAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions is the only implemented CI/CD provider in M39.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_github_actions_1',
      schemaVersion,
      createdAt,
      actionId: 'github_actions_observe_1',
      actionType: 'github.actions.observe',
      actionMode: 'read',
      riskLevel: 'high',
      outcome: 'approval_required',
      reasons: ['live GitHub Actions observation requires persisted approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const observationPlan = GithubActionsObservationPlanSchema.parse({
      id: 'github_actions_observation_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_actions_observation_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-actions-observation',
      targetRef,
      requestedMetadata: ['repo', 'workflow_runs', 'workflow_run', 'jobs', 'logs'],
      workflowRunIdHash: 'sha256:run-id',
      logByteCap: 65536,
      blockReasons: [],
      policyDecision,
      requiresApproval: true,
      evidenceRefs: [
        EvidenceRefSchema.parse({
          id: 'evidence_github_actions_observation_1',
          schemaVersion,
          createdAt,
          kind: 'github.actions_observation_plan',
          hash: 'sha256:actions-plan',
          summary: 'GitHub Actions observation plan evidence is hash-only.',
        }),
      ],
      auditEventIds: ['audit_github_actions_observation_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions observation dry-run is planned.',
    });
    const observationApproval = GithubActionsObservationApprovalArtifactRecordSchema.parse({
      id: 'github_actions_observation_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: observationPlan.dryRunId,
      dryRunRecordId: observationPlan.id,
      approvalRequestId: 'github_actions_observation_approval_request_1',
      approvalArtifactId: 'github_actions_observation_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: policyDecision.id,
      decidedByHash: 'sha256:operator',
      reasonHash: 'sha256:reason',
      evidenceRefs: [],
      auditEventIds: ['audit_github_actions_observation_approval_1'],
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions observation approval stores metadata only.',
    });
    const observationRun = GithubActionsObservationRunSchema.parse({
      id: 'github_actions_observation_run_1',
      schemaVersion,
      createdAt,
      dryRunId: observationPlan.dryRunId,
      dryRunRecordId: observationPlan.id,
      approvalArtifactId: observationApproval.approvalArtifactId,
      status: 'completed',
      plan: observationPlan,
      runSummary: {
        id: 'github_actions_run_summary_1',
        schemaVersion,
        createdAt,
        targetRef,
        workflowRunIdHash: 'sha256:run-id',
        workflowNameHash: 'sha256:workflow',
        runStatus: 'completed',
        conclusionHash: 'sha256:success',
        jobCount: 2,
        logHashCount: 1,
        responseBodyHashCount: 5,
        rawUrlStored: false,
        rawResponseBodyStored: false,
        rawLogStored: false,
        rawArtifactStored: false,
        rawPathStored: false,
        bodyStored: false,
        summary: 'Workflow run summary stores status counts and hashes only.',
      },
      jobSummaries: [
        {
          id: 'github_actions_job_summary_1',
          schemaVersion,
          createdAt,
          workflowRunIdHash: 'sha256:run-id',
          jobIdHash: 'sha256:job',
          jobStatus: 'completed',
          conclusionHash: 'sha256:success',
          stepCount: 3,
          rawUrlStored: false,
          rawResponseBodyStored: false,
          rawLogStored: false,
          rawArtifactStored: false,
          rawPathStored: false,
          bodyStored: false,
          summary: 'Job summary stores job id hash and counts only.',
        },
      ],
      logHashSummary: {
        id: 'github_actions_log_summary_1',
        schemaVersion,
        createdAt,
        workflowRunIdHash: 'sha256:run-id',
        logHash: 'sha256:logs',
        byteCount: 4096,
        byteCap: 65536,
        truncated: false,
        rawLogStored: false,
        rawArtifactStored: false,
        rawUrlStored: false,
        rawResponseBodyStored: false,
        rawPathStored: false,
        bodyStored: false,
        summary: 'Log summary stores only hash and byte counts.',
      },
      responseBodyHashes: ['sha256:repo', 'sha256:runs', 'sha256:run', 'sha256:jobs', 'sha256:logs'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_github_actions_observation_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions observation completed through fixed GET endpoints.',
    });
    const rerunPlan = GithubActionsRerunPlanSchema.parse({
      id: 'github_actions_rerun_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_actions_rerun_dry_run_1',
      controlKind: 'rerun',
      status: 'planned',
      runnerMode: 'controlled-github-actions-run-control',
      targetRef,
      workflowRunIdHash: 'sha256:run-id',
      blockReasons: [],
      policyDecision: { ...policyDecision, id: 'policy_github_actions_rerun_1', actionType: 'github.actions.rerun', actionMode: 'write' },
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_github_actions_rerun_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      arbitraryPayloadAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions rerun plan is gated by approval.',
    });
    const cancelPlan = GithubActionsCancelPlanSchema.parse({
      ...rerunPlan,
      id: 'github_actions_cancel_plan_1',
      dryRunId: 'github_actions_cancel_dry_run_1',
      controlKind: 'cancel',
      summary: 'GitHub Actions cancel plan is gated by approval.',
    });
    const runControlApproval = GithubActionsRunControlApprovalArtifactSchema.parse({
      id: 'github_actions_rerun_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: rerunPlan.dryRunId,
      dryRunRecordId: rerunPlan.id,
      controlKind: 'rerun',
      approvalRequestId: 'github_actions_rerun_approval_request_1',
      approvalArtifactId: 'github_actions_rerun_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: rerunPlan.policyDecision.id,
      decidedByHash: 'sha256:operator',
      reasonHash: 'sha256:reason',
      evidenceRefs: [],
      auditEventIds: ['audit_github_actions_rerun_approval_1'],
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Run-control approval stores metadata only.',
    });
    const runControlRun = GithubActionsRunControlRunSchema.parse({
      id: 'github_actions_rerun_run_1',
      schemaVersion,
      createdAt,
      dryRunId: rerunPlan.dryRunId,
      dryRunRecordId: rerunPlan.id,
      approvalArtifactId: runControlApproval.approvalArtifactId,
      controlKind: 'rerun',
      status: 'completed',
      plan: rerunPlan,
      controlSummary: {
        id: 'github_actions_rerun_summary_1',
        schemaVersion,
        createdAt,
        controlKind: 'rerun',
        targetRef,
        workflowRunIdHash: 'sha256:run-id',
        responseBodyHashCount: 3,
        changed: true,
        fixedEndpointOnly: true,
        arbitraryPayloadAllowed: false,
        rawUrlStored: false,
        rawResponseBodyStored: false,
        rawLogStored: false,
        rawArtifactStored: false,
        rawPathStored: false,
        bodyStored: false,
        summary: 'Rerun summary stores response hash counts only.',
      },
      responseBodyHashes: ['sha256:repo', 'sha256:run', 'sha256:rerun'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_github_actions_rerun_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      fixedEndpointOnly: true,
      arbitraryPayloadAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions rerun completed through a fixed endpoint.',
    });
    const dispatchPlan = GithubActionsDispatchPlanSchema.parse({
      id: 'github_actions_dispatch_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_actions_dispatch_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-actions-dispatch',
      targetRef,
      workflowIdHash: 'sha256:workflow-id',
      refHash: 'sha256:ref',
      inputsSupported: false,
      arbitraryInputsRejected: true,
      blockReasons: [],
      policyDecision: { ...policyDecision, id: 'policy_github_actions_dispatch_1', actionType: 'github.actions.dispatch', actionMode: 'write' },
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_github_actions_dispatch_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      arbitraryPayloadAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Dispatch plan supports fixed ref only and no arbitrary inputs.',
    });
    const dispatchApproval = GithubActionsDispatchApprovalArtifactSchema.parse({
      ...runControlApproval,
      id: 'github_actions_dispatch_approval_1',
      dryRunId: dispatchPlan.dryRunId,
      dryRunRecordId: dispatchPlan.id,
      controlKind: 'dispatch',
      approvalRequestId: 'github_actions_dispatch_approval_request_1',
      approvalArtifactId: 'github_actions_dispatch_approval_artifact_1',
      policyDecisionId: dispatchPlan.policyDecision.id,
      summary: 'Dispatch approval stores metadata only.',
    });
    const dispatchRun = GithubActionsDispatchRunSchema.parse({
      id: 'github_actions_dispatch_run_1',
      schemaVersion,
      createdAt,
      dryRunId: dispatchPlan.dryRunId,
      dryRunRecordId: dispatchPlan.id,
      approvalArtifactId: dispatchApproval.approvalArtifactId,
      status: 'completed',
      plan: dispatchPlan,
      dispatchSummary: {
        id: 'github_actions_dispatch_summary_1',
        schemaVersion,
        createdAt,
        targetRef,
        workflowIdHash: 'sha256:workflow-id',
        refHash: 'sha256:ref',
        responseBodyHashCount: 3,
        dispatched: true,
        fixedEndpointOnly: true,
        arbitraryPayloadAllowed: false,
        inputsSupported: false,
        arbitraryInputsRejected: true,
        rawUrlStored: false,
        rawResponseBodyStored: false,
        rawLogStored: false,
        rawArtifactStored: false,
        rawPathStored: false,
        bodyStored: false,
        summary: 'Dispatch summary stores fixed ref hashes only.',
      },
      responseBodyHashes: ['sha256:repo', 'sha256:workflow', 'sha256:dispatch'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_github_actions_dispatch_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      fixedEndpointOnly: true,
      arbitraryPayloadAllowed: false,
      inputsSupported: false,
      arbitraryInputsRejected: true,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub Actions workflow dispatch completed with fixed ref payload only.',
    });
    const rehearsal = GithubActionsAcceptanceRehearsalRunSchema.parse({
      id: 'github_actions_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'dispatch-inputs-rejected',
      status: 'blocked',
      observationStatus: 'skipped',
      rerunStatus: 'skipped',
      cancelStatus: 'skipped',
      dispatchStatus: 'blocked',
      stepCount: 5,
      blockerCount: 1,
      evidenceRefCount: 1,
      auditEventCount: 1,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      arbitraryPayloadAllowed: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawLogStored: false,
      rawArtifactStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Fixture dispatch rehearsal rejects arbitrary inputs.',
    });
    const serialized = JSON.stringify([
      manifest,
      readiness,
      observationPlan,
      observationRun,
      rerunPlan,
      cancelPlan,
      runControlRun,
      dispatchPlan,
      dispatchRun,
      rehearsal,
    ]);

    expect(observationRun.status).toBe('completed');
    expect(runControlRun.status).toBe('completed');
    expect(dispatchRun.dispatchSummary.arbitraryInputsRejected).toBe(true);
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('raw log line');
    expect(() =>
      GithubActionsObservationRunSchema.parse({
        ...observationRun,
        id: 'github_actions_observation_invalid_boundary',
        networkBoundaryInvoked: false,
      }),
    ).toThrow();
    expect(() =>
      GithubActionsDispatchPlanSchema.parse({
        ...dispatchPlan,
        id: 'github_actions_dispatch_invalid_inputs',
        metadata: { inputs: { unsafe: 'raw payload' } },
      }),
    ).toThrow();
    expect(() =>
      GithubActionsObservationPlanSchema.parse({
        ...observationPlan,
        id: 'github_actions_observation_invalid_raw_log',
        metadata: { rawLogBody: 'raw log line' },
      }),
    ).toThrow();
  });

  it('parses M40 release lifecycle and M41 deployment contracts as metadata-only records', () => {
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'm40_target_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:host',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:main',
      allowedHost: 'api.github.com',
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Release target stores repository fields as hashes only.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_release_1',
      schemaVersion,
      createdAt,
      actionId: 'release_tag_1',
      actionType: 'github.release.tag',
      actionMode: 'write',
      riskLevel: 'high',
      outcome: 'approval_required',
      reasons: ['release tag creation requires a persisted approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const versionPlan = ReleaseVersionPlanSchema.parse({
      id: 'release_version_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'release_version_dry_run_1',
      status: 'planned',
      currentVersionHash: 'sha256:v1',
      proposedVersionHash: 'sha256:v2',
      bumpKind: 'minor',
      sourceSummaryHash: 'sha256:source',
      changedFileCount: 0,
      blockReasons: [],
      policyDecision: { ...policyDecision, actionMode: 'dry-run', riskLevel: 'medium' },
      requiresApproval: false,
      evidenceRefs: [
        EvidenceRefSchema.parse({
          id: 'evidence_release_version_1',
          schemaVersion,
          createdAt,
          kind: 'release.version_plan',
          hash: 'sha256:version-plan',
          summary: 'Release version plan evidence stores hashes only.',
        }),
      ],
      auditEventIds: ['audit_release_version_plan_1'],
      networkBoundaryPlanned: false,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      rawPathStored: false,
      rawVersionStored: false,
      bodyStored: false,
      summary: 'Version bump is planned without mutating files.',
    });
    const changelog = ReleaseChangelogSummarySchema.parse({
      id: 'release_changelog_1',
      schemaVersion,
      createdAt,
      sourceSummaryHash: 'sha256:source',
      changelogBodyHash: 'sha256:changelog',
      sectionCount: 3,
      changeCount: 7,
      breakingChangeCount: 0,
      evidenceRefs: [],
      auditEventIds: ['audit_changelog_1'],
      rawChangelogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Changelog summary stores section counts and a body hash only.',
    });
    const tagPlan = GithubReleaseTagPlanSchema.parse({
      id: 'github_release_tag_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_release_tag_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-release-tag',
      targetRef,
      baseRefHash: 'sha256:base-ref',
      tagNameHash: 'sha256:tag',
      tagMessageHash: 'sha256:tag-message',
      targetShaHash: 'sha256:target-sha',
      releaseVersionPlanId: versionPlan.id,
      changelogSummaryId: changelog.id,
      blockReasons: [],
      policyDecision,
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_release_tag_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      localGitTagAllowed: false,
      pushAllowed: false,
      updateRefOutsideTagFlowAllowed: false,
      forceAllowed: false,
      releasePublishAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub release tag dry-run is planned through fixed API endpoints.',
    });
    const tagApproval = GithubReleaseTagApprovalArtifactSchema.parse({
      id: 'github_release_tag_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: tagPlan.dryRunId,
      dryRunRecordId: tagPlan.id,
      approvalRequestId: 'github_release_tag_approval_request_1',
      approvalArtifactId: 'github_release_tag_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: policyDecision.id,
      targetRefHash: 'sha256:target',
      tagNameHash: tagPlan.tagNameHash,
      expectedPlanHash: 'sha256:tag-plan',
      approvedAt: createdAt,
      reasonHash: 'sha256:reason',
      evidenceRefs: [],
      auditEventIds: ['audit_release_tag_approval_1'],
      tokenValueStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      bodyStored: false,
      summary: 'Release tag approval stores hashes and state only.',
    });
    const tagRun = GithubReleaseTagRunSchema.parse({
      id: 'github_release_tag_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: tagPlan,
      tagSummary: {
        id: 'github_release_tag_summary_1',
        schemaVersion,
        createdAt,
        tagNameHash: tagPlan.tagNameHash,
        targetShaHash: 'sha256:target-sha',
        createdTagShaHash: 'sha256:tag-sha',
        createdRefHash: 'sha256:ref',
        responseHashCount: 4,
        tagAlreadyExists: false,
        evidenceRefs: [],
        auditEventIds: ['audit_release_tag_summary_1'],
        rawTagStored: false,
        rawRefStored: false,
        rawUrlStored: false,
        rawResponseBodyStored: false,
        bodyStored: false,
        summary: 'Tag creation summary stores remote hashes only.',
      },
      responseBodyHashes: ['sha256:repo', 'sha256:base', 'sha256:tag', 'sha256:ref'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_release_tag_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      fixedEndpointOnly: true,
      localGitTagAllowed: false,
      pushAllowed: false,
      updateRefOutsideTagFlowAllowed: false,
      forceAllowed: false,
      releasePublishAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub tag creation completed through the fixed boundary.',
    });
    const draftPlan = GithubReleaseDraftPlanSchema.parse({
      id: 'github_release_draft_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_release_draft_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-release-draft',
      targetRef,
      tagNameHash: tagPlan.tagNameHash,
      releaseNameHash: 'sha256:release-name',
      releaseBodyHash: 'sha256:release-body',
      changelogSummaryId: changelog.id,
      tagRunId: tagRun.id,
      blockReasons: [],
      policyDecision,
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_release_draft_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      draft: true,
      releasePublishAllowed: false,
      rawReleaseBodyStored: false,
      rawChangelogStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Release draft plan stores generated body hash only.',
    });
    const draftApproval = GithubReleaseDraftApprovalArtifactSchema.parse({
      ...tagApproval,
      id: 'github_release_draft_approval_1',
      dryRunId: draftPlan.dryRunId,
      dryRunRecordId: draftPlan.id,
      approvalRequestId: 'github_release_draft_approval_request_1',
      approvalArtifactId: 'github_release_draft_approval_artifact_1',
      releaseNameHash: draftPlan.releaseNameHash,
      releaseBodyHash: draftPlan.releaseBodyHash,
      draft: true,
      releasePublishAllowed: false,
      rawReleaseBodyStored: false,
      rawChangelogStored: false,
    });
    const draftRun = GithubReleaseDraftRunSchema.parse({
      id: 'github_release_draft_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: draftPlan,
      draftSummary: {
        id: 'github_release_draft_summary_1',
        schemaVersion,
        createdAt,
        tagNameHash: tagPlan.tagNameHash,
        releaseNameHash: draftPlan.releaseNameHash,
        releaseBodyHash: draftPlan.releaseBodyHash,
        releaseIdHash: 'sha256:release-id',
        responseHashCount: 4,
        draft: true,
        alreadyExists: false,
        evidenceRefs: [],
        auditEventIds: ['audit_release_draft_summary_1'],
        rawReleaseBodyStored: false,
        rawChangelogStored: false,
        rawUrlStored: false,
        rawResponseBodyStored: false,
        bodyStored: false,
        summary: 'Draft release summary stores hashes only.',
      },
      responseBodyHashes: ['sha256:repo', 'sha256:tag', 'sha256:existing-release', 'sha256:draft'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_release_draft_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      fixedEndpointOnly: true,
      draft: true,
      releasePublishAllowed: false,
      rawReleaseBodyStored: false,
      rawChangelogStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'GitHub release draft was created as draft only.',
    });
    const releaseRehearsal = ReleaseLifecycleAcceptanceRehearsalRunSchema.parse({
      id: 'release_lifecycle_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      status: 'passed',
      versionPlanStatus: 'fixture_completed',
      changelogStatus: 'fixture_completed',
      tagStatus: 'fixture_completed',
      releaseDraftStatus: 'fixture_completed',
      stepCount: 4,
      blockerCount: 0,
      evidenceRefCount: 4,
      auditEventCount: 4,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedEndpointOnly: true,
      releasePublishAllowed: false,
      rawReleaseBodyStored: false,
      rawChangelogStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Release lifecycle fixture passes without live writes.',
    });

    const deploymentPolicy = PolicyDecisionSchema.parse({
      ...policyDecision,
      id: 'policy_deployment_1',
      actionId: 'deployment_observation_1',
      actionType: 'deployment.observe',
      actionMode: 'read',
      riskLevel: 'high',
    });
    const deploymentManifest = DeploymentProviderManifestSchema.parse({
      id: 'deployment_manifest_1',
      schemaVersion,
      createdAt,
      name: 'deployment-provider-adapter',
      provider: 'kubernetes',
      version: '0.1.0',
      implemented: true,
      defaultEnabled: false,
      actionMode: 'read',
      riskLevel: 'high',
      fixedReadOnlyRunner: true,
      applyAllowed: false,
      syncAllowed: false,
      rollbackAllowed: false,
      deleteAllowed: false,
      scaleAllowed: false,
      restartAllowed: false,
      arbitraryCommandAllowed: false,
      rawOutputStored: false,
      bodyStored: false,
      summary: 'Kubernetes observation is read-only and disabled by default.',
    });
    const deploymentReadiness = DeploymentReadinessSchema.parse({
      id: 'deployment_readiness_1',
      schemaVersion,
      createdAt,
      provider: 'kubernetes',
      observerEnabled: true,
      providerEnabled: true,
      toolConfigured: true,
      toolHash: 'sha256:kubectl',
      targetHash: 'sha256:cluster',
      blockerCount: 0,
      blockReasons: [],
      tokenValueStored: false,
      rawKubeconfigStored: false,
      rawContextStored: false,
      rawNamespaceStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment readiness exposes hashes and blockers only.',
    });
    const deploymentPlan = DeploymentObservationPlanSchema.parse({
      id: 'deployment_observation_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'deployment_observation_dry_run_1',
      status: 'planned',
      provider: 'kubernetes',
      runnerMode: 'controlled-deployment-readonly',
      targetHash: 'sha256:cluster',
      requestedObservationKinds: ['status', 'drift'],
      blockReasons: [],
      policyDecision: deploymentPolicy,
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_deployment_plan_1'],
      processBoundaryPlanned: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      noRealWrite: true,
      fixedReadOnlyRunner: true,
      applyAllowed: false,
      syncAllowed: false,
      rollbackAllowed: false,
      deleteAllowed: false,
      scaleAllowed: false,
      restartAllowed: false,
      arbitraryCommandAllowed: false,
      rawOutputStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment observation dry-run plans read-only status and drift.',
    });
    const deploymentApproval = DeploymentObservationApprovalArtifactSchema.parse({
      id: 'deployment_observation_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: deploymentPlan.dryRunId,
      dryRunRecordId: deploymentPlan.id,
      approvalRequestId: 'deployment_observation_approval_request_1',
      approvalArtifactId: 'deployment_observation_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: deploymentPolicy.id,
      provider: 'kubernetes',
      targetHash: deploymentPlan.targetHash,
      expectedPlanHash: 'sha256:deployment-plan',
      approvedAt: createdAt,
      reasonHash: 'sha256:reason',
      evidenceRefs: [],
      auditEventIds: ['audit_deployment_approval_1'],
      tokenValueStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment observation approval is hash-only.',
    });
    const providerStatus = DeploymentProviderStatusSummarySchema.parse({
      id: 'deployment_status_1',
      schemaVersion,
      createdAt,
      provider: 'kubernetes',
      targetHash: deploymentPlan.targetHash,
      status: 'healthy',
      resourceCount: 12,
      warningCount: 0,
      errorCount: 0,
      statusHash: 'sha256:status',
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment status summary stores counts and hash only.',
    });
    const driftSummary = DeploymentDriftSummarySchema.parse({
      id: 'deployment_drift_1',
      schemaVersion,
      createdAt,
      provider: 'kubernetes',
      targetHash: deploymentPlan.targetHash,
      driftDetected: false,
      driftItemCount: 0,
      driftHash: 'sha256:drift',
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'No deployment drift is exposed as a hash summary.',
    });
    const planDiffSummary = DeploymentPlanDiffHashSummarySchema.parse({
      id: 'deployment_plan_diff_1',
      schemaVersion,
      createdAt,
      provider: 'kubernetes',
      targetHash: deploymentPlan.targetHash,
      planHash: 'sha256:plan',
      diffHash: 'sha256:diff',
      changedResourceCount: 0,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Plan and diff are represented only by hashes.',
    });
    const deploymentRun = DeploymentObservationRunSchema.parse({
      id: 'deployment_observation_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: deploymentPlan,
      providerStatus,
      driftSummary,
      planDiffSummary,
      responseBodyHashes: [],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_deployment_run_1'],
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      networkBoundaryInvoked: false,
      noRealWrite: true,
      fixedReadOnlyRunner: true,
      applyAllowed: false,
      syncAllowed: false,
      rollbackAllowed: false,
      deleteAllowed: false,
      scaleAllowed: false,
      restartAllowed: false,
      arbitraryCommandAllowed: false,
      rawOutputStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment observation completed without mutation.',
    });
    const deploymentRehearsal = DeploymentAcceptanceRehearsalRunSchema.parse({
      id: 'deployment_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      provider: 'kubernetes',
      status: 'passed',
      readinessStatus: 'fixture_completed',
      observationStatus: 'fixture_completed',
      stepCount: 2,
      blockerCount: 0,
      evidenceRefCount: 2,
      auditEventCount: 2,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: true,
      fixedReadOnlyRunner: true,
      applyAllowed: false,
      syncAllowed: false,
      rollbackAllowed: false,
      deleteAllowed: false,
      scaleAllowed: false,
      restartAllowed: false,
      arbitraryCommandAllowed: false,
      rawOutputStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment fixture observation passes without live access.',
    });
    const serialized = JSON.stringify([
      versionPlan,
      changelog,
      tagPlan,
      tagApproval,
      tagRun,
      draftPlan,
      draftApproval,
      draftRun,
      releaseRehearsal,
      deploymentManifest,
      deploymentReadiness,
      deploymentPlan,
      deploymentApproval,
      deploymentRun,
      deploymentRehearsal,
    ]);

    expect(tagRun.status).toBe('completed');
    expect(draftRun.draft).toBe(true);
    expect(deploymentRun.noRealWrite).toBe(true);
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('release notes raw body');
    expect(serialized).not.toContain('kubeconfig');
    expect(serialized).not.toContain('terraform plan raw output');
    expect(() =>
      GithubReleaseDraftPlanSchema.parse({
        ...draftPlan,
        id: 'github_release_draft_invalid_raw_body',
        metadata: { rawReleaseBody: 'release notes raw body' },
      }),
    ).toThrow();
    expect(() =>
      DeploymentObservationPlanSchema.parse({
        ...deploymentPlan,
        id: 'deployment_plan_invalid_raw_output',
        metadata: { rawPlanBody: 'terraform plan raw output' },
      }),
    ).toThrow();
  });

  it('parses M42 deployment operations and M43 secrets governance contracts as metadata-only records', () => {
    const approvalPolicy = DeploymentEnvironmentApprovalPolicySchema.parse({
      id: 'deployment_approval_policy_1',
      schemaVersion,
      createdAt,
      environment: 'prod',
      requiredApprovalCount: 2,
      requiresDistinctApproverHashes: true,
      criticalRisk: true,
      policyHash: 'sha256:deployment-prod-policy',
      summary: 'Production deployment operations require two approvals.',
    });
    const operationReadiness = DeploymentOperationReadinessSchema.parse({
      id: 'deployment_operation_readiness_1',
      schemaVersion,
      createdAt,
      provider: 'helm',
      action: 'rollback',
      environment: 'prod',
      operatorEnabled: true,
      providerWriteEnabled: true,
      prodWriteEnabled: true,
      toolConfigured: true,
      rollbackPlanRequired: true,
      rollbackPlanPresent: true,
      targetHash: 'sha256:deployment-target',
      artifactHash: 'sha256:artifact',
      blockerCount: 0,
      blockReasons: [],
      approvalPolicy,
      tokenValueStored: false,
      rawManifestStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      arbitraryCommandAllowed: false,
      summary: 'Helm prod rollback is ready after governed approvals.',
    });
    const rollbackPlan = DeploymentRollbackPlanSchema.parse({
      id: 'deployment_rollback_plan_1',
      schemaVersion,
      createdAt,
      rollbackPlanId: 'rollback_plan_1',
      status: 'planned',
      provider: 'helm',
      environment: 'prod',
      targetHash: 'sha256:deployment-target',
      sourceRunHash: 'sha256:source-run',
      rollbackArtifactHash: 'sha256:rollback-artifact',
      approvedRollbackArtifactHash: 'sha256:approved-rollback-artifact',
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_deployment_rollback_plan_1'],
      deleteAllowed: false,
      destroyAllowed: false,
      forceAllowed: false,
      rawManifestStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Rollback plan is hash-bound to an approved source run.',
    });
    const operationPlan = DeploymentOperationPlanSchema.parse({
      id: 'deployment_operation_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'deployment_operation_dry_run_1',
      status: 'planned',
      provider: 'helm',
      action: 'rollback',
      environment: 'prod',
      runnerMode: 'controlled-deployment-operation',
      targetHash: 'sha256:deployment-target',
      artifactHash: 'sha256:artifact',
      rollbackPlanId: rollbackPlan.rollbackPlanId,
      rollbackPlanHash: 'sha256:rollback-plan',
      approvalPolicy,
      blockReasons: [],
      policyDecision: {
        id: 'policy_deployment_operation_1',
        schemaVersion,
        createdAt,
        actionId: 'deployment_operation_1',
        actionType: 'deployment.helm.rollback',
        actionMode: 'write',
        riskLevel: 'critical',
        outcome: 'approval_required',
        reasons: ['production deployment rollback requires dry-run and approval'],
        requiresDryRun: true,
        requiresApproval: true,
      },
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_deployment_operation_plan_1'],
      processBoundaryPlanned: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryPlanned: false,
      networkBoundaryInvoked: false,
      noDelete: true,
      noDestroy: true,
      fixedRunner: true,
      arbitraryCommandAllowed: false,
      rawManifestStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Helm rollback operation is planned through a fixed runner.',
    });
    const operationApproval = DeploymentOperationApprovalArtifactSchema.parse({
      id: 'deployment_operation_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: operationPlan.dryRunId,
      dryRunRecordId: operationPlan.id,
      approvalRequestId: 'deployment_operation_approval_request_1',
      approvalArtifactId: 'deployment_operation_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: operationPlan.policyDecision.id,
      provider: 'helm',
      action: 'rollback',
      environment: 'prod',
      targetHash: operationPlan.targetHash,
      expectedPlanHash: 'sha256:operation-plan',
      approvalSlot: 'primary',
      approvedAt: createdAt,
      reasonHash: 'sha256:reason',
      reasonSummary: 'Operator approved rollback metadata.',
      approverHash: 'sha256:approver-a',
      evidenceRefs: [],
      auditEventIds: ['audit_deployment_operation_approval_1'],
      tokenValueStored: false,
      rawManifestStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Primary deployment approval is metadata-only.',
    });
    const operationResult = DeploymentOperationResultSummarySchema.parse({
      id: 'deployment_operation_result_1',
      schemaVersion,
      createdAt,
      provider: 'helm',
      action: 'rollback',
      environment: 'prod',
      targetHash: operationPlan.targetHash,
      resultHash: 'sha256:deployment-result',
      changedResourceCount: 1,
      warningCount: 0,
      errorCount: 0,
      rawManifestStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment operation result stores counts and hashes only.',
    });
    const operationRun = DeploymentOperationRunSchema.parse({
      id: 'deployment_operation_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: operationPlan,
      resultSummary: operationResult,
      rollbackPlan,
      approvalArtifactIds: [operationApproval.approvalArtifactId, 'deployment_operation_approval_artifact_2'],
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_deployment_operation_run_1'],
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      networkBoundaryInvoked: false,
      noDelete: true,
      noDestroy: true,
      fixedRunner: true,
      arbitraryCommandAllowed: false,
      rawManifestStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment operation completed through fixed metadata-only runner.',
    });
    const operationRehearsal = DeploymentOperationAcceptanceRehearsalRunSchema.parse({
      id: 'deployment_operation_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      provider: 'helm',
      action: 'rollback',
      environment: 'prod',
      status: 'passed',
      readinessStatus: 'fixture_completed',
      operationStatus: 'fixture_completed',
      rollbackStatus: 'fixture_completed',
      stepCount: 4,
      blockerCount: 0,
      evidenceRefCount: 3,
      auditEventCount: 3,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      noDelete: true,
      noDestroy: true,
      fixedRunner: true,
      arbitraryCommandAllowed: false,
      rawManifestStored: false,
      rawPlanStored: false,
      rawDiffStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Deployment operation rehearsal is fixture-only.',
    });
    const secretManifest = SecretProviderManifestSchema.parse({
      id: 'secret_manifest_1',
      schemaVersion,
      createdAt,
      name: 'secret-governance-kernel',
      provider: 'vault',
      version: '0.1.0',
      implemented: true,
      defaultEnabled: false,
      actionMode: 'read',
      riskLevel: 'high',
      readinessOnly: true,
      secretValueReadAllowed: false,
      secretValueStored: false,
      tokenValueStored: false,
      rawConfigStored: false,
      bodyStored: false,
      summary: 'Vault readiness is configured/hash-only.',
    });
    const secretReference = ConfiguredSecretReferenceSummarySchema.parse({
      id: 'secret_reference_1',
      schemaVersion,
      createdAt,
      provider: 'vault',
      environment: 'prod',
      referenceHash: 'sha256:secret-reference',
      purposeHash: 'sha256:secret-purpose',
      configured: true,
      secretValueStored: false,
      tokenValueStored: false,
      rawReferenceStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Secret reference is represented by hash only.',
    });
    const providerReadiness = SecretProviderReadinessSchema.parse({
      id: 'secret_provider_readiness_1',
      schemaVersion,
      createdAt,
      provider: 'vault',
      governanceEnabled: true,
      providerEnabled: true,
      configured: true,
      configHash: 'sha256:provider-config',
      secretRefCount: 1,
      configuredRefHashes: [secretReference.referenceHash],
      blockerCount: 0,
      blockReasons: [],
      secretValueReadAllowed: false,
      secretValueStored: false,
      tokenValueStored: false,
      envValueStored: false,
      rawConfigStored: false,
      rawPathStored: false,
      rawUrlStored: false,
      bodyStored: false,
      summary: 'Vault readiness is configured without secret value access.',
    });
    const environmentReadiness = SecretEnvironmentReadinessSchema.parse({
      id: 'secret_environment_readiness_1',
      schemaVersion,
      createdAt,
      environment: 'prod',
      governanceEnabled: true,
      providerCount: 4,
      configuredProviderCount: 1,
      missingProviderCount: 3,
      configuredReferenceCount: 1,
      blockerCount: 0,
      blockReasons: [],
      secretValueStored: false,
      tokenValueStored: false,
      envValueStored: false,
      rawConfigStored: false,
      bodyStored: false,
      summary: 'Production secret environment readiness stores counts only.',
    });
    const leakAudit = SecretLeakAuditSummarySchema.parse({
      id: 'secret_leak_audit_1',
      schemaVersion,
      createdAt,
      scannedSurfaceCount: 6,
      findingCount: 0,
      leakDetected: false,
      findingHashes: [],
      secretValueStored: false,
      tokenValueStored: false,
      envValueStored: false,
      rawConfigStored: false,
      rawPathStored: false,
      rawUrlStored: false,
      bodyStored: false,
      summary: 'Secret leak audit found no metadata leaks.',
    });
    const secretPlan = SecretReadinessPlanSchema.parse({
      id: 'secret_readiness_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'secret_readiness_dry_run_1',
      status: 'planned',
      provider: 'vault',
      environment: 'prod',
      configHash: 'sha256:provider-config',
      expectedReferenceCount: 1,
      blockReasons: [],
      policyDecision: {
        id: 'policy_secret_readiness_1',
        schemaVersion,
        createdAt,
        actionId: 'secret_readiness_1',
        actionType: 'secrets.vault.readiness',
        actionMode: 'read',
        riskLevel: 'high',
        outcome: 'approval_required',
        reasons: ['secret governance readiness requires approval even without value access'],
        requiresDryRun: true,
        requiresApproval: true,
      },
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_secret_readiness_plan_1'],
      processBoundaryPlanned: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      secretValueReadAllowed: false,
      secretValueStored: false,
      tokenValueStored: false,
      envValueStored: false,
      rawConfigStored: false,
      rawPathStored: false,
      rawUrlStored: false,
      bodyStored: false,
      summary: 'Secret readiness plan is metadata-only.',
    });
    const secretApproval = SecretReadinessApprovalArtifactSchema.parse({
      id: 'secret_readiness_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: secretPlan.dryRunId,
      dryRunRecordId: secretPlan.id,
      approvalRequestId: 'secret_readiness_approval_request_1',
      approvalArtifactId: 'secret_readiness_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: secretPlan.policyDecision.id,
      provider: 'vault',
      environment: 'prod',
      expectedPlanHash: 'sha256:secret-plan',
      approvedAt: createdAt,
      reasonHash: 'sha256:reason',
      reasonSummary: 'Operator approved readiness metadata.',
      approverHash: 'sha256:approver',
      evidenceRefs: [],
      auditEventIds: ['audit_secret_readiness_approval_1'],
      secretValueStored: false,
      tokenValueStored: false,
      envValueStored: false,
      rawConfigStored: false,
      rawPathStored: false,
      rawUrlStored: false,
      bodyStored: false,
      summary: 'Secret readiness approval is metadata-only.',
    });
    const secretRun = SecretReadinessRunSchema.parse({
      id: 'secret_readiness_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: secretPlan,
      providerReadiness,
      environmentReadiness,
      referenceSummaries: [secretReference],
      leakAuditSummary: leakAudit,
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_secret_readiness_run_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      secretValueReadAllowed: false,
      secretValueStored: false,
      tokenValueStored: false,
      envValueStored: false,
      rawConfigStored: false,
      rawPathStored: false,
      rawUrlStored: false,
      bodyStored: false,
      summary: 'Secret readiness run completed without reading secret values.',
    });
    const secretRehearsal = SecretGovernanceAcceptanceRehearsalRunSchema.parse({
      id: 'secret_governance_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      provider: 'vault',
      environment: 'prod',
      status: 'passed',
      readinessStatus: 'fixture_completed',
      leakAuditStatus: 'fixture_completed',
      stepCount: 3,
      blockerCount: 0,
      evidenceRefCount: 2,
      auditEventCount: 2,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      secretValueReadAllowed: false,
      secretValueStored: false,
      tokenValueStored: false,
      envValueStored: false,
      rawConfigStored: false,
      rawPathStored: false,
      rawUrlStored: false,
      bodyStored: false,
      summary: 'Secret governance rehearsal is fixture-only.',
    });

    const serialized = JSON.stringify([
      operationReadiness,
      rollbackPlan,
      operationPlan,
      operationApproval,
      operationRun,
      operationRehearsal,
      secretManifest,
      secretPlan,
      secretApproval,
      secretRun,
      secretRehearsal,
    ]);

    expect(operationRun.status).toBe('completed');
    expect(operationPlan.approvalPolicy.requiredApprovalCount).toBe(2);
    expect(secretRun.secretValueReadAllowed).toBe(false);
    expect(serialized).not.toContain('raw manifest body');
    expect(serialized).not.toContain('terraform plan raw output');
    expect(serialized).not.toContain('VAULT_TOKEN');
    expect(serialized).not.toContain('secret-value-plaintext');
    expect(() =>
      DeploymentOperationPlanSchema.parse({
        ...operationPlan,
        id: 'deployment_operation_invalid_raw_manifest',
        metadata: { rawManifestBody: 'raw manifest body' },
      }),
    ).toThrow();
    expect(() =>
      SecretReadinessPlanSchema.parse({
        ...secretPlan,
        id: 'secret_readiness_invalid_raw_value',
        metadata: { secret: 'secret-value-plaintext' },
      }),
    ).toThrow();
  });

  it('parses M20 rework loop contracts as metadata-only orchestration records', () => {
    const trigger = ReworkTriggerSummarySchema.parse({
      id: 'rework_trigger_1',
      schemaVersion,
      createdAt,
      kind: 'checks_failed',
      sourceRunIdHash: 'sha256:source-run',
      sourceStatus: 'checks_failed',
      sourceSummaryHash: 'sha256:source-summary',
      checkFailureCount: 2,
      reviewFindingCount: 0,
      staleBranch: false,
      reasonHash: 'sha256:reason',
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Failed checks require a new governed rework attempt.',
    });
    const attempt = ReworkAttemptSummarySchema.parse({
      id: 'rework_attempt_1',
      schemaVersion,
      createdAt,
      attemptNumber: 2,
      status: 'planned',
      previousAttemptIdHash: 'sha256:attempt-1',
      sourcePatchLifecycleIdHash: 'sha256:patch',
      sourceReviewPackageIdHash: 'sha256:review',
      sourceRcReadinessIdHash: 'sha256:rc',
      sourceBranchPublishRunIdHash: 'sha256:branch-run',
      sourceDraftPrRunIdHash: 'sha256:draft-pr-run',
      sourcePrLifecycleRunIdHash: 'sha256:pr-lifecycle',
      plannedBranchNameHash: 'sha256:codexhub-rework-r2',
      branchPrefix: 'codexhub/',
      branchAttemptSuffix: 'r2',
      changedFileCount: 2,
      changedFilePathHashes: ['sha256:file-a', 'sha256:file-b'],
      diffHash: 'sha256:diff',
      evidenceRefIds: ['evidence_rework_attempt_1'],
      auditEventIds: ['audit_rework_attempt_1'],
      childApprovalsRequired: true,
      patchApprovalRequired: true,
      branchPublishApprovalRequired: true,
      draftPrApprovalRequired: true,
      directChildExecutionAllowed: false,
      updatesExistingBranch: false,
      forceAllowed: false,
      mergeAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      rawDiffStored: false,
      rawPrBodyStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      summary: 'Next rework attempt is planned on a new branch hash.',
    });
    const supersede = ReworkSupersedeProjectionSchema.parse({
      id: 'rework_supersede_1',
      schemaVersion,
      createdAt,
      sourceAttemptIdHash: 'sha256:attempt-1',
      supersedingAttemptIdHash: 'sha256:attempt-2',
      sourcePackageHash: 'sha256:package',
      sourceBranchHash: 'sha256:branch',
      sourceDraftPrHash: 'sha256:pr',
      superseded: true,
      oldBranchPreserved: true,
      oldDraftPrClosed: false,
      deleteRemoteBranchAllowed: false,
      mergeAllowed: false,
      evidenceRefIds: ['evidence_rework_supersede_1'],
      auditEventIds: ['audit_rework_supersede_1'],
      rawRefStored: false,
      rawUrlStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Supersede projection keeps old branch and PR metadata only.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_rework_1',
      schemaVersion,
      createdAt,
      actionId: 'rework_loop_1',
      actionType: 'rework.loop.plan',
      actionMode: 'dry-run',
      riskLevel: 'high',
      outcome: 'approval_required',
      reasons: ['child control planes require separate persisted approvals'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const plan = ReworkLoopPlanSchema.parse({
      id: 'rework_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'rework_dry_run_1',
      status: 'planned',
      trigger,
      nextAttempt: attempt,
      supersedeProjection: supersede,
      sourceRunIdHash: 'sha256:source-run',
      sourcePackageHash: 'sha256:package',
      sourcePrLifecycleRunIdHash: 'sha256:pr-lifecycle',
      requestedAttemptNumber: 2,
      blockReasons: [],
      policyDecision,
      evidenceRefs: [],
      auditEventIds: ['audit_rework_plan_1'],
      childApprovalsRequired: true,
      directChildExecutionAllowed: false,
      updateExistingBranchAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      rawDiffStored: false,
      rawPrBodyStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      summary: 'Rework loop plan does not execute child planes.',
    });
    const approval = ReworkLoopApprovalArtifactRecordSchema.parse({
      id: 'rework_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      status: 'approved',
      approvalArtifactId: 'rework_approval_artifact_1',
      approved: true,
      policyDecisionId: policyDecision.id,
      requestedBy: 'operator',
      decidedBy: 'operator',
      expiresAt: '2026-05-06T00:00:00.000Z',
      reasonHash: 'sha256:approval-reason',
      evidenceRefs: [],
      auditEventIds: ['audit_rework_approval_1'],
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      summary: 'Rework approval authorizes orchestration metadata only.',
    });
    const run = ReworkLoopRunSchema.parse({
      id: 'rework_run_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      dryRunRecordId: plan.id,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      plan,
      trigger,
      attempts: [attempt],
      attemptCount: 1,
      supersedeProjection: supersede,
      nextActionSummaryHash: 'sha256:next-action',
      blockReasons: [],
      evidenceRefs: [],
      evidenceRefIds: ['evidence_rework_attempt_1'],
      auditEventIds: ['audit_rework_run_1'],
      childApprovalsRequired: true,
      directChildExecutionAllowed: false,
      patchExecuted: false,
      branchPublished: false,
      draftPrCreated: false,
      updateExistingBranchAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      rawDiffStored: false,
      rawPrBodyStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      summary: 'Rework loop run created next-action metadata only.',
    });
    const rehearsal = ReworkLoopAcceptanceRehearsalRunSchema.parse({
      id: 'rework_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'checks-failed-rework',
      status: 'passed',
      triggerKind: 'checks_failed',
      attemptStatus: 'planned',
      supersededSource: true,
      stepCount: 6,
      evidenceRefCount: 2,
      auditEventCount: 2,
      blockerCount: 0,
      nextBranchNameHash: 'sha256:codexhub-rework-r2',
      childApprovalsRequired: true,
      directChildExecutionAllowed: false,
      patchExecuted: false,
      branchPublished: false,
      draftPrCreated: false,
      updateExistingBranchAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      rawDiffStored: false,
      rawPrBodyStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      noRealWrite: true,
      summary: 'Fixture rework rehearsal creates no patch, branch, or PR.',
    });
    const serialized = JSON.stringify([plan, approval, run, rehearsal]);

    expect(run.status).toBe('completed');
    expect(run.directChildExecutionAllowed).toBe(false);
    expect(serialized).not.toContain('diff --git');
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('raw PR markdown');
    expect(() =>
      ReworkAttemptSummarySchema.parse({
        ...attempt,
        id: 'rework_attempt_invalid_count',
        changedFileCount: 1,
      }),
    ).toThrow();
    expect(() =>
      ReworkLoopPlanSchema.parse({
        ...plan,
        id: 'rework_plan_invalid_raw_reason',
        metadata: { rawReason: 'raw review text' },
      }),
    ).toThrow();
    expect(() =>
      ReworkLoopAcceptanceRehearsalRunSchema.parse({
        ...rehearsal,
        id: 'rework_rehearsal_invalid_blockers',
        blockerCount: 1,
      }),
    ).toThrow();
  });

  it('parses M21/M22 remote supersede and cleanup contracts with fixed metadata-only boundaries', () => {
    const createdAt = '2026-05-05T00:00:00.000Z';
    const targetRef = GithubRemoteRefSummarySchema.parse({
      id: 'github_remote_cleanup_target_ref_1',
      schemaVersion,
      createdAt,
      hostHash: 'sha256:api-github-com',
      ownerHash: 'sha256:owner',
      repoHash: 'sha256:repo',
      baseBranchHash: 'sha256:base',
      headBranchHash: 'sha256:old-codexhub-branch',
      allowedHost: 'api.github.com',
      rawOwnerStored: false,
      rawRepoStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Remote cleanup target ref summary stores branch metadata as hashes.',
    });
    const target = RemoteSupersedeTargetSummarySchema.parse({
      id: 'remote_supersede_target_1',
      schemaVersion,
      createdAt,
      targetKind: 'draft_pr_and_branch',
      sourceReworkRunIdHash: 'sha256:rework-run',
      sourceBranchPublishRunIdHash: 'sha256:branch-publish-run',
      sourceDraftPrRunIdHash: 'sha256:draft-pr-run',
      sourcePrLifecycleRunIdHash: 'sha256:pr-lifecycle-run',
      successorBranchPublishRunIdHash: 'sha256:new-branch-publish-run',
      successorDraftPrRunIdHash: 'sha256:new-draft-pr-run',
      oldBranchNameHash: 'sha256:old-codexhub-branch',
      oldPrNumberHash: 'sha256:old-pr-number',
      oldPrUrlHash: 'sha256:old-pr-url',
      oldBranchPreserved: true,
      oldDraftPrClosed: false,
      successorRequired: true,
      closePrRecommended: true,
      deleteBranchRecommended: true,
      cleanupRecommended: true,
      branchPrefix: 'codexhub/',
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Old CodexHub draft PR and branch are superseded by a newer run.',
    });
    const cleanupReadiness = RemoteCleanupReadinessSchema.parse({
      id: 'remote_cleanup_readiness_1',
      schemaVersion,
      createdAt,
      status: 'ready_for_cleanup',
      target,
      blockerCount: 0,
      successorRunIdHash: 'sha256:new-draft-pr-run',
      requiresApproval: true,
      closePrAllowed: true,
      deleteBranchAllowed: true,
      deleteNonCodexhubBranchAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Cleanup is ready after successor metadata exists.',
    });
    const policyDecision = PolicyDecisionSchema.parse({
      id: 'policy_remote_cleanup_1',
      schemaVersion,
      createdAt,
      actionId: 'github_remote_cleanup_dry_run_1',
      actionType: 'github.remote_cleanup.execute',
      actionMode: 'write',
      riskLevel: 'high',
      outcome: 'approval_required',
      reasons: ['remote cleanup requires persisted approval'],
      requiresDryRun: true,
      requiresApproval: true,
    });
    const supersedePlan = RemoteSupersedePlanSchema.parse({
      id: 'remote_supersede_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'remote_supersede_dry_run_1',
      status: 'planned',
      target,
      cleanupReadiness,
      sourceRunIdHash: 'sha256:source-run',
      successorRunIdHash: 'sha256:new-draft-pr-run',
      blockReasons: [],
      policyDecision: {
        ...policyDecision,
        id: 'policy_remote_supersede_1',
        actionType: 'github.remote_supersede.project',
        actionMode: 'read',
        outcome: 'allow',
        requiresApproval: false,
      },
      evidenceRefs: [],
      auditEventIds: ['audit_remote_supersede_plan_1'],
      remoteCleanupRequiresApproval: true,
      cleanupExecutionAllowed: false,
      closePrAllowed: false,
      deleteBranchAllowed: false,
      mergeAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Remote supersede projection does not invoke GitHub writes.',
    });
    const supersedeRun = RemoteSupersedeRunSchema.parse({
      id: 'remote_supersede_run_1',
      schemaVersion,
      createdAt,
      dryRunId: supersedePlan.dryRunId,
      dryRunRecordId: supersedePlan.id,
      status: 'completed',
      plan: supersedePlan,
      target,
      cleanupReadiness,
      blockReasons: [],
      evidenceRefs: [],
      evidenceRefIds: ['evidence_remote_supersede_1'],
      auditEventIds: ['audit_remote_supersede_run_1'],
      remoteCleanupRequiresApproval: true,
      cleanupExecutionAllowed: false,
      closePrAllowed: false,
      deleteBranchAllowed: false,
      mergeAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Supersede run projected cleanup readiness only.',
    });
    const projection = RemoteSupersedeChainProjectionSchema.parse({
      id: 'remote_supersede_chain_1',
      schemaVersion,
      createdAt,
      plan: supersedePlan,
      runs: [supersedeRun],
      runCount: 1,
      cleanupReadiness,
      evidenceRefIds: ['evidence_remote_supersede_1'],
      auditEventIds: ['audit_remote_supersede_run_1'],
      chainHash: 'sha256:remote-supersede-chain',
      rawRefStored: false,
      rawUrlStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Supersede chain projection remains metadata-only.',
    });
    const cleanupPlan = GithubRemoteCleanupPlanSchema.parse({
      id: 'github_remote_cleanup_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'github_remote_cleanup_dry_run_1',
      status: 'planned',
      runnerMode: 'controlled-github-remote-cleanup',
      targetRef,
      target,
      cleanupReadiness,
      oldPrNumberHash: 'sha256:old-pr-number',
      oldBranchNameHash: 'sha256:old-codexhub-branch',
      successorRunIdHash: 'sha256:new-draft-pr-run',
      sourceBranchPublishRunIdHash: 'sha256:branch-publish-run',
      sourceDraftPrRunIdHash: 'sha256:draft-pr-run',
      blockReasons: [],
      policyDecision,
      requiresApproval: true,
      evidenceRefs: [],
      auditEventIds: ['audit_remote_cleanup_plan_1'],
      networkBoundaryPlanned: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      closePrAllowed: true,
      deleteRefAllowed: true,
      deleteNonCodexhubBranchAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      releaseAllowed: false,
      deploymentAllowed: false,
      pushAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Remote cleanup plan allows only close old draft PR and delete codexhub ref.',
    });
    const approval = GithubRemoteCleanupApprovalArtifactRecordSchema.parse({
      id: 'github_remote_cleanup_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: cleanupPlan.dryRunId,
      dryRunRecordId: cleanupPlan.id,
      status: 'approved',
      approvalRequestId: 'github_remote_cleanup_request_1',
      approvalArtifactId: 'github_remote_cleanup_artifact_1',
      approved: true,
      policyDecisionId: policyDecision.id,
      requestedByHash: 'sha256:operator',
      decidedByHash: 'sha256:operator',
      reasonHash: 'sha256:reason',
      expiresAt: '2026-05-06T00:00:00.000Z',
      evidenceRefs: [],
      auditEventIds: ['audit_remote_cleanup_approval_1'],
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      rawReasonStored: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      summary: 'Remote cleanup approval is store-resolved metadata.',
    });
    const cleanupSummary = GithubRemoteCleanupSummarySchema.parse({
      id: 'github_remote_cleanup_summary_1',
      schemaVersion,
      createdAt,
      targetRef,
      oldPrNumberHash: 'sha256:old-pr-number',
      oldBranchNameHash: 'sha256:old-codexhub-branch',
      oldPrClosed: true,
      oldBranchDeleted: true,
      responseBodyHashes: ['sha256:repo', 'sha256:pr', 'sha256:ref', 'sha256:patch', 'sha256:delete'],
      responseBodyHashCount: 5,
      closePrAllowed: true,
      deleteRefAllowed: true,
      deleteNonCodexhubBranchAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      releaseAllowed: false,
      deploymentAllowed: false,
      pushAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Remote cleanup stores response hashes and cleanup booleans only.',
    });
    const cleanupRun = GithubRemoteCleanupRunSchema.parse({
      id: 'github_remote_cleanup_run_1',
      schemaVersion,
      createdAt,
      dryRunId: cleanupPlan.dryRunId,
      dryRunRecordId: cleanupPlan.id,
      approvalArtifactId: approval.approvalArtifactId,
      status: 'completed',
      plan: cleanupPlan,
      cleanupSummary,
      responseBodyHashes: cleanupSummary.responseBodyHashes,
      blockReasons: [],
      evidenceRefs: [],
      auditEventIds: ['audit_remote_cleanup_run_1'],
      networkBoundaryInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      noRealWrite: false,
      closePrAllowed: true,
      deleteRefAllowed: true,
      deleteNonCodexhubBranchAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      releaseAllowed: false,
      deploymentAllowed: false,
      pushAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Remote cleanup completed after approved GitHub boundary.',
    });
    const supersedeRehearsal = RemoteSupersedeAcceptanceRehearsalRunSchema.parse({
      id: 'remote_supersede_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'old-pr-open',
      status: 'passed',
      cleanupReadinessStatus: 'ready_for_cleanup',
      targetKind: 'draft_pr_and_branch',
      stepCount: 4,
      evidenceRefCount: 2,
      auditEventCount: 2,
      blockerCount: 0,
      cleanupRecommended: true,
      remoteWriteInvoked: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawReasonStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Remote supersede rehearsal is fixture-only.',
    });
    const cleanupRehearsal = GithubRemoteCleanupAcceptanceRehearsalRunSchema.parse({
      id: 'github_remote_cleanup_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      status: 'passed',
      cleanupReadinessStatus: 'ready_for_cleanup',
      closePrStatus: 'fixture_completed',
      deleteRefStatus: 'fixture_completed',
      stepCount: 6,
      evidenceRefCount: 2,
      auditEventCount: 2,
      blockerCount: 0,
      closePrAllowed: true,
      deleteRefAllowed: true,
      deleteNonCodexhubBranchAllowed: false,
      updateRefAllowed: false,
      forceAllowed: false,
      mergeAllowed: false,
      commentAllowed: false,
      labelAllowed: false,
      reviewerAllowed: false,
      releaseAllowed: false,
      deploymentAllowed: false,
      pushAllowed: false,
      rawRefStored: false,
      rawUrlStored: false,
      rawResponseBodyStored: false,
      rawPathStored: false,
      bodyStored: false,
      noRealWrite: true,
      networkBoundaryInvoked: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      summary: 'Remote cleanup rehearsal is fixture-only.',
    });
    const serialized = JSON.stringify([
      projection,
      cleanupPlan,
      approval,
      cleanupRun,
      supersedeRehearsal,
      cleanupRehearsal,
    ]);

    expect(cleanupRun.status).toBe('completed');
    expect(cleanupRun.networkBoundaryInvoked).toBe(true);
    expect(cleanupRun.deleteNonCodexhubBranchAllowed).toBe(false);
    expect(cleanupRun.mergeAllowed).toBe(false);
    expect(cleanupRun.commentAllowed).toBe(false);
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('https://github.com');
    expect(serialized).not.toContain('refs/heads');
    expect(serialized).not.toContain('raw PR markdown');
    expect(serialized).not.toContain('comment body');
    expect(() =>
      GithubRemoteCleanupPlanSchema.parse({
        ...cleanupPlan,
        id: 'github_remote_cleanup_invalid_metadata_url',
        metadata: { url: 'https://github.com/octo-org/codexhub/pull/1' },
      }),
    ).toThrow();
    expect(() =>
      RemoteSupersedeRunSchema.parse({
        ...supersedeRun,
        id: 'remote_supersede_invalid_metadata_comment',
        metadata: { comments: 'please merge this old PR' },
      }),
    ).toThrow();
    expect(() =>
      GithubRemoteCleanupAcceptanceRehearsalRunSchema.parse({
        ...cleanupRehearsal,
        id: 'github_remote_cleanup_invalid_non_codexhub_delete',
        deleteNonCodexhubBranchAllowed: true,
      }),
    ).toThrow();
  });

  it('parses custom workflow contracts and rejects raw workflow payloads', () => {
    const createdAt = '2026-04-28T00:00:00.000Z';
    const schemaVersion = '2026-04-28.foundation';
    const binding = CustomWorkflowCapabilityBindingSchema.parse({
      stepKind: 'github-branch-publish',
      capabilityKind: 'git',
      actionMode: 'write',
      riskLevel: 'high',
      requiresApproval: true,
      childApprovalRequired: true,
      adapterExecuteAllowed: false,
      summary: 'Branch publish is coordinated through child control-plane records.',
    });
    const step = CustomWorkflowStepTemplateSchema.parse({
      stepId: 'step-01-github-branch-publish',
      name: 'Publish branch',
      kind: 'github-branch-publish',
      actionMode: 'write',
      riskLevel: 'high',
      required: true,
      capabilityBinding: binding,
      summary: 'Publish branch step references an approved child run.',
    });
    const template = CustomWorkflowTemplateSchema.parse({
      id: 'custom_workflow_template_1',
      schemaVersion,
      createdAt,
      templateId: 'local-to-remote-governed',
      templateVersion: 1,
      name: 'Local to remote governed chain',
      description: 'Metadata-only custom workflow template.',
      riskLevel: 'high',
      templateHash: 'sha256:template',
      configPathHash: 'sha256:path',
      stepCount: 1,
      capabilityCount: 1,
      steps: [step],
      orderedStepsOnly: true,
      directAdapterExecutionAllowed: false,
      weakensPolicy: false,
      bodyStored: false,
      rawPathStored: false,
      configBodyStored: false,
      noRealWrite: true,
      summary: 'One-step custom workflow template.',
    });
    const validation = CustomWorkflowValidationReportSchema.parse({
      id: 'custom_workflow_validation_1',
      schemaVersion,
      createdAt,
      templateId: template.templateId,
      templateHash: template.templateHash,
      status: 'valid',
      issueCount: 0,
      issues: [],
      stepCount: 1,
      unknownStepKindCount: 0,
      policyWeakeningDetected: false,
      loopOrBranchingDetected: false,
      arbitraryConfigPathAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Custom workflow template is valid.',
    });
    const plan = CustomWorkflowPlanSchema.parse({
      id: 'custom_workflow_dry_run_1',
      schemaVersion,
      createdAt,
      dryRunId: 'custom_workflow_dry_run_1',
      templateId: template.templateId,
      templateHash: template.templateHash,
      status: 'planned',
      validationReport: validation,
      stepPlans: [
        {
          stepId: step.stepId,
          kind: step.kind,
          actionMode: step.actionMode,
          riskLevel: step.riskLevel,
          requiresApproval: true,
          childApprovalRequired: true,
          policyRequired: true,
          evidenceRequired: true,
          auditRequired: true,
          directAdapterExecutionAllowed: false,
          summary: 'Step requires workflow and child approvals.',
        },
      ],
      stepCount: 1,
      approvalRequired: true,
      childApprovalsRequired: 1,
      blockReasons: [],
      evidenceRefIds: ['evidence_custom_workflow_plan_1'],
      auditEventIds: ['audit_custom_workflow_plan_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Custom workflow dry-run planned one step.',
    });
    const approval = CustomWorkflowApprovalArtifactRecordSchema.parse({
      id: 'custom_workflow_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      templateId: template.templateId,
      templateHash: template.templateHash,
      approvalArtifactId: 'custom_workflow_artifact_1',
      status: 'approved',
      approvedBy: 'operator',
      reasonHash: 'sha256:reason',
      reasonSummary: 'Operator approved metadata-only coordination.',
      bodyStored: false,
      rawPathStored: false,
      summary: 'Custom workflow approval is store-resolved metadata.',
    });
    const run = CustomWorkflowRunSchema.parse({
      id: 'custom_workflow_run_1',
      schemaVersion,
      createdAt,
      dryRunId: plan.dryRunId,
      approvalArtifactId: approval.approvalArtifactId,
      templateId: template.templateId,
      templateHash: template.templateHash,
      status: 'completed',
      steps: [
        {
          stepId: step.stepId,
          kind: step.kind,
          status: 'completed',
          childRecordIdHash: 'sha256:child-record',
          childHashBindingMatched: true,
          childApprovalRequired: true,
          childExecutionInvoked: false,
          directAdapterExecutionAllowed: false,
          blockReasons: [],
          evidenceRefIds: ['evidence_child_1'],
          auditEventIds: ['audit_child_1'],
          processBoundaryInvoked: false,
          externalProcessStarted: false,
          networkBoundaryInvoked: false,
          bodyStored: false,
          rawPathStored: false,
          summary: 'Step completed by child record projection.',
        },
      ],
      stepCount: 1,
      completedStepCount: 1,
      blockedStepCount: 0,
      childApprovalsRequired: 1,
      blockReasons: [],
      evidenceRefIds: ['evidence_custom_workflow_run_1'],
      auditEventIds: ['audit_custom_workflow_run_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Custom workflow run completed as coordination only.',
    });
    const rehearsal = CustomWorkflowRehearsalRunSchema.parse({
      id: 'custom_workflow_rehearsal_1',
      schemaVersion,
      createdAt,
      templateId: template.templateId,
      templateHash: template.templateHash,
      scenario: 'verification-failed',
      status: 'blocked',
      stepCount: 1,
      blockerCount: 1,
      evidenceRefCount: 1,
      auditEventCount: 1,
      childApprovalsRequired: 1,
      blockReasons: ['custom_workflow_verification_failed'],
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Custom workflow fixture rehearsal blocked by verification.',
    });
    const pilotEvidence = ProductionWorkflowPilotEvidenceSummarySchema.parse({
      evidenceRefCount: 1,
      auditEventCount: 1,
      evidenceBundleHash: 'sha256:pilot-evidence',
      auditChainHash: 'sha256:pilot-audit',
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow pilot evidence remains metadata-only.',
    });
    const pilotReadiness = ProductionWorkflowPilotReadinessSchema.parse({
      id: 'production_workflow_pilot_readiness_1',
      schemaVersion,
      createdAt,
      templateId: template.templateId,
      templateHash: template.templateHash,
      source: 'catalog-template',
      status: 'ready',
      requiredChildStepCount: 1,
      missingChildRecordCount: 0,
      staleChildRecordCount: 0,
      failedChildRecordCount: 0,
      blockerCount: 0,
      blockers: [],
      approvalRequired: true,
      childApprovalsRequired: 1,
      localProductionPilotEnabled: true,
      productionExecutionEnabled: true,
      directAdapterExecutionAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow pilot is ready for metadata-only coordination.',
    });
    const pilotPlan = ProductionWorkflowPilotPlanSchema.parse({
      id: 'production_workflow_pilot_plan_1',
      schemaVersion,
      createdAt,
      pilotPlanId: 'production_workflow_pilot_plan_1',
      templateId: template.templateId,
      templateHash: template.templateHash,
      source: 'catalog-template',
      status: 'ready',
      stepCount: 1,
      childRecordHashCount: 1,
      blockReasons: [],
      evidenceSummary: pilotEvidence,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow pilot plan references child records by hash only.',
    });
    const pilotStep = ProductionWorkflowPilotStepSchema.parse({
      stepId: step.stepId,
      kind: step.kind,
      status: 'completed',
      childRecordIdHash: 'sha256:child-record',
      childHashBindingMatched: true,
      childApprovalRequired: true,
      blockReasons: [],
      evidenceRefIds: ['evidence_child_1'],
      auditEventIds: ['audit_child_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow pilot step completed through child record summary.',
    });
    const pilotRun = ProductionWorkflowPilotRunSchema.parse({
      id: 'production_workflow_pilot_run_1',
      schemaVersion,
      createdAt,
      pilotRunId: 'production_workflow_pilot_run_1',
      templateId: template.templateId,
      templateHash: template.templateHash,
      source: 'catalog-template',
      status: 'completed',
      steps: [pilotStep],
      stepCount: 1,
      completedStepCount: 1,
      blockedStepCount: 0,
      failedStepCount: 0,
      readiness: pilotReadiness,
      evidenceSummary: pilotEvidence,
      blockReasons: [],
      evidenceRefIds: ['evidence_custom_workflow_pilot_1'],
      auditEventIds: ['audit_custom_workflow_pilot_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow pilot completed as coordination only.',
    });
    const localBoundary = LocalProductionWorkflowChildBoundarySummarySchema.parse({
      stepId: step.stepId,
      childActionKind: 'worktree-create',
      childControlPlane: 'worktrees',
      childRecordIdHash: 'sha256:child-record',
      childApprovalRequired: true,
      childApprovalResolvedFromStore: true,
      childHashBindingMatched: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Local production child boundary is represented by metadata only.',
    });
    expect(LocalProductionWorkflowPilotPlanSchema.parse(pilotPlan).status).toBe('ready');
    expect(LocalProductionWorkflowPilotStepSchema.parse(pilotStep).status).toBe('completed');
    expect(LocalProductionWorkflowPilotRunSchema.parse(pilotRun).status).toBe('completed');
    expect(LocalProductionWorkflowAcceptanceRunSchema.parse(pilotRun).status).toBe('completed');
    expect(localBoundary.directAdapterExecutionAllowed).toBe(false);
    const operationsProjection = ProductionWorkflowOperationsProjectionSchema.parse({
      id: 'production_workflow_operations_1',
      schemaVersion,
      createdAt,
      templateId: template.templateId,
      templateHash: template.templateHash,
      latestPilotRunIdHash: 'sha256:pilot-run',
      runHealth: 'healthy',
      approvalState: 'approved',
      blockedReasonCount: 0,
      blockedReasons: [],
      staleChildRecordCount: 0,
      rollbackAvailable: false,
      operatorNextActionSummary: 'Continue monitoring child control-plane records.',
      evidenceRefCount: 1,
      auditEventCount: 1,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow operations projection is healthy.',
    });
    const pause = ProductionWorkflowPauseSummarySchema.parse({
      id: 'production_workflow_pause_1',
      schemaVersion,
      createdAt,
      actionId: 'pause_1',
      sourceRunIdHash: 'sha256:pilot-run',
      affectedTemplateHash: template.templateHash,
      reasonHash: 'sha256:reason',
      status: 'paused',
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Pause is metadata-only operator intent.',
    });
    const resume = ProductionWorkflowResumeSummarySchema.parse({
      ...pause,
      id: 'production_workflow_resume_1',
      actionId: 'resume_1',
      status: 'healthy',
      summary: 'Resume is metadata-only operator intent.',
    });
    const rollback = ProductionWorkflowRollbackSummarySchema.parse({
      ...pause,
      id: 'production_workflow_rollback_1',
      actionId: 'rollback_1',
      status: 'rollback-required',
      summary: 'Rollback is metadata-only operator intent.',
    });
    const operationsSmoke = ProductionWorkflowOperationsSmokeRunSchema.parse({
      id: 'production_workflow_operations_smoke_1',
      schemaVersion,
      createdAt,
      scenario: 'healthy',
      status: 'healthy',
      projection: operationsProjection,
      fixtureOnly: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Operations smoke is fixture-only.',
    });
    const childActionPlan = ProductionWorkflowChildActionPlanSchema.parse({
      actionId: 'child_action_worktree',
      stepId: step.stepId,
      stepKind: step.kind,
      childActionKind: 'github-branch-publish',
      childControlPlane: 'github-branch-publish',
      actionMode: 'write',
      riskLevel: 'high',
      requiresChildApproval: true,
      createsChildDryRun: true,
      createsChildApprovalRequest: true,
      childAutoApprovalAllowed: false,
      childAdapterExecuteAllowed: false,
      hashBindingRequired: true,
      summary: 'Child action is planned through a fixed child control plane.',
    });
    const recoveryPlan = ProductionWorkflowRecoveryPlanSchema.parse({
      id: 'production_workflow_recovery_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'production_workflow_recovery_dry_run_1',
      templateId: template.templateId,
      templateHash: template.templateHash,
      sourceRunIdHash: 'sha256:source-run',
      status: 'planned',
      childActionPlans: [childActionPlan],
      childActionCount: 1,
      approvalRequired: true,
      childApprovalsRequired: 1,
      blockReasons: [],
      evidenceRefIds: ['evidence_recovery_plan_1'],
      auditEventIds: ['audit_recovery_plan_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      childAdapterExecuteAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow recovery plan is metadata-only.',
    });
    const recoveryApproval = ProductionWorkflowRecoveryApprovalArtifactSchema.parse({
      id: 'production_workflow_recovery_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: recoveryPlan.dryRunId,
      templateId: template.templateId,
      templateHash: template.templateHash,
      approvalArtifactId: 'production_workflow_recovery_artifact_1',
      status: 'approved',
      approvedBy: 'operator',
      reasonHash: 'sha256:reason',
      reasonSummary: 'Recovery approval is hash-only.',
      childApprovalsIncluded: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Recovery approval does not include child approvals.',
    });
    const childActionState = ProductionWorkflowChildActionStateSchema.parse({
      actionId: childActionPlan.actionId,
      stepId: childActionPlan.stepId,
      stepKind: childActionPlan.stepKind,
      childActionKind: childActionPlan.childActionKind,
      childControlPlane: childActionPlan.childControlPlane,
      status: 'waiting_for_child_approval',
      childDryRunIdHash: 'sha256:child-dry-run',
      childApprovalRequestIdHash: 'sha256:child-approval-request',
      childHashBindingMatched: true,
      childApprovalRequired: true,
      childApprovalResolvedFromStore: false,
      childAutoApprovalAllowed: false,
      childAdapterExecuteAllowed: false,
      blockReasons: ['child_approval_required'],
      evidenceRefIds: ['evidence_child_action_1'],
      auditEventIds: ['audit_child_action_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Child action waits for its own approval.',
    });
    const recoveryStep = ProductionWorkflowRecoveryStepSchema.parse({
      stepId: step.stepId,
      kind: step.kind,
      status: 'waiting_for_child_approval',
      childActionStates: [childActionState],
      blockReasons: ['child_approval_required'],
      evidenceRefIds: ['evidence_recovery_step_1'],
      auditEventIds: ['audit_recovery_step_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Recovery step is paused on child approval.',
    });
    const childActionStateRecord = ProductionWorkflowChildActionStateRecordSchema.parse({
      id: 'production_workflow_child_action_state_record_1',
      schemaVersion,
      createdAt,
      dryRunId: recoveryPlan.dryRunId,
      recoveryRunId: 'production_workflow_recovery_run_1',
      actionId: childActionState.actionId,
      stepId: childActionState.stepId,
      status: childActionState.status,
      state: childActionState,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Child action state record is metadata-only.',
    });
    const recoveryRun = ProductionWorkflowRecoveryRunSchema.parse({
      id: 'production_workflow_recovery_run_1',
      schemaVersion,
      createdAt,
      recoveryRunId: 'production_workflow_recovery_run_1',
      dryRunId: recoveryPlan.dryRunId,
      approvalArtifactId: recoveryApproval.approvalArtifactId,
      templateId: template.templateId,
      templateHash: template.templateHash,
      status: 'waiting_for_child_approval',
      steps: [recoveryStep],
      childActionStates: [childActionState],
      stepCount: 1,
      childActionCount: 1,
      completedChildActionCount: 0,
      waitingChildApprovalCount: 1,
      failedChildActionCount: 0,
      blockReasons: ['child_approval_required'],
      evidenceRefIds: ['evidence_recovery_run_1'],
      auditEventIds: ['audit_recovery_run_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      childAdapterExecuteAllowed: false,
      noRealWrite: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Recovery run waits for independently governed child approval.',
    });
    const recoveryTimeline = ProductionWorkflowRecoveryTimelineEventSchema.parse({
      id: 'production_workflow_recovery_timeline_1',
      schemaVersion,
      createdAt,
      recoveryRunId: recoveryRun.recoveryRunId,
      eventType: 'child_waiting_for_approval',
      status: recoveryRun.status,
      childActionId: childActionState.actionId,
      evidenceRefIds: ['evidence_recovery_timeline_1'],
      auditEventIds: ['audit_recovery_timeline_1'],
      summary: 'Recovery timeline records child approval wait.',
    });
    const recoveryPublic = ProductionWorkflowRecoveryPublicSummarySchema.parse({
      id: 'production_workflow_recovery_public_1',
      schemaVersion,
      createdAt,
      recoveryRunId: recoveryRun.recoveryRunId,
      templateId: template.templateId,
      templateHash: template.templateHash,
      status: recoveryRun.status,
      childActionCount: 1,
      waitingChildApprovalCount: 1,
      failedChildActionCount: 0,
      evidenceRefIds: ['evidence_recovery_public_1'],
      auditEventIds: ['audit_recovery_public_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Recovery public summary is metadata-only.',
    });
    const childRecordRef = ProductionWorkflowChildRecordRefSchema.parse({
      actionId: childActionPlan.actionId,
      stepId: childActionPlan.stepId,
      childActionKind: 'codex-patch',
      childControlPlane: 'codex.patch-runs',
      childRecordId: 'codex_patch_child_record_1',
      expectedRecordHash: 'sha256:codex-patch-child-record',
      hashBindingRequired: true,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Child record ref contains ids and expected hash only.',
    });
    const childRecordResolution = ProductionWorkflowChildRecordResolutionSchema.parse({
      actionId: childRecordRef.actionId,
      stepId: childRecordRef.stepId,
      childActionKind: childRecordRef.childActionKind,
      childControlPlane: childRecordRef.childControlPlane,
      status: 'completed',
      childDryRunIdHash: 'sha256:codex-patch-dry-run',
      childApprovalArtifactIdHash: 'sha256:codex-patch-approval',
      childRunIdHash: 'sha256:codex-patch-run',
      expectedRecordHash: childRecordRef.expectedRecordHash,
      actualRecordHash: childRecordRef.expectedRecordHash,
      hashMatched: true,
      childApprovalRequired: true,
      childApprovalResolvedFromStore: true,
      childRunResolvedFromStore: true,
      blockReasons: [],
      evidenceRefIds: ['evidence_codex_patch_child_1'],
      auditEventIds: ['audit_codex_patch_child_1'],
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      childAdapterExecuteAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Child record resolution was store-resolved and hash matched.',
    });
    const childRecordSet = LocalProductionWorkflowChildRecordSetSchema.parse({
      id: 'local_production_workflow_child_record_set_1',
      schemaVersion,
      createdAt,
      dryRunId: recoveryPlan.dryRunId,
      templateId: template.templateId,
      templateHash: template.templateHash,
      childRecordRefs: [childRecordRef],
      resolutions: [childRecordResolution],
      childRecordCount: 1,
      resolvedChildRecordCount: 1,
      missingChildRecordCount: 0,
      hashMismatchCount: 0,
      failedChildRecordCount: 0,
      blockedChildRecordCount: 0,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      directAdapterExecutionAllowed: false,
      childAdapterExecuteAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Local production workflow child record set is metadata-only.',
    });
    const codexPatchChildRecord = CodexPatchChildRecordSchema.parse({
      id: 'codex_patch_child_record_1',
      schemaVersion,
      createdAt,
      childRecordId: 'codex_patch_child_record_1',
      dryRunId: 'codex_patch_dry_run_1',
      approvalArtifactId: 'codex_patch_approval_artifact_1',
      runId: 'codex_patch_run_1',
      status: 'completed',
      governedInputHash: 'sha256:governed-input',
      expectedInputHash: 'sha256:governed-input',
      worktreePathHash: 'sha256:worktree-path',
      changedFileCount: 1,
      diffHash: 'sha256:diff',
      evidenceRefIds: ['evidence_codex_patch_child_1'],
      auditEventIds: ['audit_codex_patch_child_1'],
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      networkBoundaryInvoked: false,
      realWriteExecuted: true,
      repoRootWriteAllowed: false,
      rawPromptStored: false,
      rawStdoutStored: false,
      rawStderrStored: false,
      rawDiffStored: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Codex patch child record stores only ids, hashes, counts, and booleans.',
    });
    const nxVerificationChildRecord = NxVerificationChildRecordSchema.parse({
      id: 'nx_verification_child_record_1',
      schemaVersion,
      createdAt,
      childRecordId: 'nx_verification_child_record_1',
      dryRunId: 'nx_verification_dry_run_1',
      runId: 'nx_verification_run_1',
      status: 'completed',
      verificationRunIdHash: 'sha256:nx-run',
      targetCount: 2,
      passedCount: 2,
      failedCount: 0,
      skippedCount: 0,
      commandSummaryHash: 'sha256:nx-command',
      evidenceRefIds: ['evidence_nx_child_1'],
      auditEventIds: ['audit_nx_child_1'],
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      networkBoundaryInvoked: false,
      rawStdoutStored: false,
      rawStderrStored: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Nx verification child record stores only hashes and result counts.',
    });
    expect(
      [
        'pilot-disabled',
        'worktree-failed',
        'codex-patch-failed',
        'nx-failed',
        'review-export-blocked',
        'resume-after-child-approval',
      ].map((scenario) => ProductionWorkflowRecoveryScenarioSchema.parse(scenario)),
    ).toEqual([
      'pilot-disabled',
      'worktree-failed',
      'codex-patch-failed',
      'nx-failed',
      'review-export-blocked',
      'resume-after-child-approval',
    ]);

    const serialized = JSON.stringify([
      template,
      plan,
      approval,
      run,
      rehearsal,
      pilotPlan,
      pilotRun,
      localBoundary,
      operationsProjection,
      pause,
      resume,
      rollback,
      operationsSmoke,
      recoveryPlan,
      recoveryApproval,
      childActionStateRecord,
      recoveryRun,
      recoveryTimeline,
      recoveryPublic,
      childRecordRef,
      childRecordResolution,
      childRecordSet,
      codexPatchChildRecord,
      nxVerificationChildRecord,
    ]);
    expect(run.directAdapterExecutionAllowed).toBe(false);
    expect(run.processBoundaryInvoked).toBe(false);
    expect(rehearsal.status).toBe('blocked');
    expect(pilotRun.directAdapterExecutionAllowed).toBe(false);
    expect(pilotRun.networkBoundaryInvoked).toBe(false);
    expect(operationsProjection.directAdapterExecutionAllowed).toBe(false);
    expect(operationsSmoke.fixtureOnly).toBe(true);
    expect(recoveryApproval.childApprovalsIncluded).toBe(false);
    expect(childActionStateRecord.state.childAdapterExecuteAllowed).toBe(false);
    expect(recoveryRun.childAdapterExecuteAllowed).toBe(false);
    expect(recoveryRun.waitingChildApprovalCount).toBe(1);
    expect(childRecordResolution.childApprovalResolvedFromStore).toBe(true);
    expect(childRecordSet.hashMismatchCount).toBe(0);
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('raw diff');
    expect(serialized).not.toContain('local-control');
    expect(() =>
      CustomWorkflowTemplateSchema.parse({
        ...template,
        id: 'custom_workflow_template_raw_prompt',
        metadata: { prompt: 'write this patch' },
      }),
    ).toThrow();
    expect(() =>
      CustomWorkflowPlanSchema.parse({
        ...plan,
        id: 'custom_workflow_plan_direct_execute',
        directAdapterExecutionAllowed: true,
      }),
    ).toThrow();
    expect(() =>
      CustomWorkflowRehearsalRunSchema.parse({
        ...rehearsal,
        id: 'custom_workflow_rehearsal_raw_path',
        metadata: { path: 'C:/Users/Thomas/CodexHub' },
      }),
    ).toThrow();
    expect(() =>
      ProductionWorkflowPilotRunSchema.parse({
        ...pilotRun,
        id: 'production_workflow_pilot_raw_body',
        metadata: { body: 'raw workflow body' },
      }),
    ).toThrow();
    expect(() =>
      ProductionWorkflowPilotPlanSchema.parse({
        ...pilotPlan,
        id: 'production_workflow_pilot_direct_execute',
        directAdapterExecutionAllowed: true,
      }),
    ).toThrow();
    expect(() =>
      ProductionWorkflowOperationsProjectionSchema.parse({
        ...operationsProjection,
        id: 'production_workflow_operations_raw_body',
        metadata: { body: 'raw operator reason body' },
      }),
    ).toThrow();
    expect(() =>
      ProductionWorkflowRollbackSummarySchema.parse({
        ...rollback,
        id: 'production_workflow_rollback_raw_path',
        metadata: { path: 'C:/Users/Thomas/CodexHub' },
      }),
    ).toThrow();
    expect(() =>
      ProductionWorkflowRecoveryPlanSchema.parse({
        ...recoveryPlan,
        id: 'production_workflow_recovery_raw_prompt',
        metadata: { prompt: 'raw prompt' },
      }),
    ).toThrow();
    expect(() =>
      ProductionWorkflowChildActionPlanSchema.parse({
        ...childActionPlan,
        id: 'production_workflow_child_auto_approval',
        childAutoApprovalAllowed: true,
      }),
    ).toThrow();
    expect(() =>
      CodexPatchChildRecordSchema.parse({
        ...codexPatchChildRecord,
        id: 'codex_patch_child_raw_prompt',
        metadata: { prompt: 'raw prompt' },
      }),
    ).toThrow();
    expect(() =>
      NxVerificationChildRecordSchema.parse({
        ...nxVerificationChildRecord,
        id: 'nx_child_raw_stdout',
        metadata: { stdout: 'raw stdout' },
      }),
    ).toThrow();
  });

  it('parses custom workflow catalog contracts and rejects unsafe public fields', () => {
    const createdAt = '2026-04-28T00:00:00.000Z';
    const schemaVersion = '2026-04-28.foundation';
    const entry = CustomWorkflowCatalogEntrySchema.parse({
      id: 'custom_workflow_catalog_entry_1',
      schemaVersion,
      createdAt,
      templateId: 'local-patch-review',
      templateHash: 'sha256:template',
      family: 'local',
      displayName: 'Local patch review',
      source: 'built-in',
      validationStatus: 'valid',
      riskLevel: 'high',
      stepCount: 6,
      capabilityCount: 6,
      requiredStepKinds: [
        'readiness',
        'worktree',
        'codex-patch',
        'nx-verification',
        'review-package',
        'governance-projection',
      ],
      requiredCapabilityKinds: ['policy', 'git', 'codex', 'verification', 'filesystem'],
      approvalRequired: true,
      childApprovalsRequired: 3,
      enabledByDefault: false,
      productionExecutionEnabled: false,
      directAdapterExecutionAllowed: false,
      bodyStored: false,
      rawPathStored: false,
      configBodyStored: false,
      summary: 'Built-in workflow template catalog entry is metadata-only.',
    });
    const family = CustomWorkflowTemplateFamilySummarySchema.parse({
      id: 'custom_workflow_family_local',
      schemaVersion,
      createdAt,
      family: 'local',
      templateCount: 1,
      validTemplateCount: 1,
      blockedTemplateCount: 0,
      highestRisk: 'high',
      enabledByDefault: false,
      productionExecutionEnabled: false,
      summary: 'Local workflow family is disabled by default.',
    });
    const validation = CustomWorkflowProductionTemplateValidationSummarySchema.parse({
      id: 'custom_workflow_catalog_validation_1',
      schemaVersion,
      createdAt,
      templateId: entry.templateId,
      templateHash: entry.templateHash,
      status: 'valid',
      issueCount: 0,
      unknownStepKindCount: 0,
      policyWeakeningDetected: false,
      rawBodyDetected: false,
      rawPathStored: false,
      bodyStored: false,
      configBodyStored: false,
      summary: 'Production workflow template validation is metadata-only.',
    });
    const readiness = CustomWorkflowCatalogReadinessSchema.parse({
      id: 'custom_workflow_catalog_readiness_1',
      schemaVersion,
      createdAt,
      templateId: entry.templateId,
      templateHash: entry.templateHash,
      status: 'disabled',
      productionExecutionEnabled: false,
      integrationEnabled: false,
      requiredEnvFlags: ['CODEXHUB_CUSTOM_WORKFLOWS_ENABLED'],
      configuredEnvFlagCount: 0,
      missingEnvFlagCount: 1,
      childCapabilityCount: entry.requiredCapabilityKinds.length,
      approvalRequired: true,
      blockerCount: 1,
      blockers: ['custom_workflow_production_disabled'],
      evidenceRefIds: ['evidence_workflow_catalog_1'],
      auditEventIds: ['audit_workflow_catalog_1'],
      directAdapterExecutionAllowed: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      bodyStored: false,
      rawPathStored: false,
      summary: 'Production workflow catalog readiness is disabled by default.',
    });

    const serialized = JSON.stringify([entry, family, validation, readiness]);
    expect(readiness.status).toBe('disabled');
    expect(entry.directAdapterExecutionAllowed).toBe(false);
    expect(serialized).not.toContain('ghp_');
    expect(serialized).not.toContain('raw template');
    expect(() =>
      CustomWorkflowCatalogEntrySchema.parse({
        ...entry,
        id: 'custom_workflow_catalog_entry_raw_body',
        metadata: { body: 'raw template body' },
      }),
    ).toThrow();
    expect(() =>
      CustomWorkflowCatalogReadinessSchema.parse({
        ...readiness,
        id: 'custom_workflow_catalog_readiness_token',
        metadata: { token: 'ghp_secret' },
      }),
    ).toThrow();
    expect(() =>
      CustomWorkflowCatalogEntrySchema.parse({
        ...entry,
        id: 'custom_workflow_catalog_entry_execute',
        directAdapterExecutionAllowed: true,
      }),
    ).toThrow();
  });

  it('parses real policy and telemetry contracts and rejects raw backend output', () => {
    const evidenceRef = EvidenceRefSchema.parse({
      id: 'evidence_policy_telemetry_1',
      schemaVersion,
      createdAt,
      kind: 'policy.real_backend_summary',
      hash: 'sha256:evidence',
      summary: 'Policy telemetry evidence summary is hash-only.',
    });
    const policyManifest = RealPolicyBackendManifestSchema.parse({
      id: 'real_policy_manifest_1',
      schemaVersion,
      createdAt,
      adapterName: 'policy-backend-adapter',
      backendKind: 'opa',
      runtimeModes: ['local-cli', 'loopback-http'],
      defaultEnabled: false,
      advisoryOnly: true,
      authorityProvider: 'codexhub-security-kernel',
      rawPolicySourceStored: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'OPA backend is advisory and disabled by default.',
    });
    const policyReadiness = RealPolicyBackendReadinessSchema.parse({
      id: 'real_policy_readiness_1',
      schemaVersion,
      createdAt,
      backendKind: policyManifest.backendKind,
      runtimeMode: 'local-cli',
      realBackendEnabled: false,
      backendEnabled: false,
      runtimeAvailable: false,
      cliExecutableHash: 'sha256:opa',
      blockerCount: 1,
      blockReasons: ['policy_backend_disabled'],
      advisoryOnly: true,
      authorityProvider: 'codexhub-security-kernel',
      processBoundaryPlanned: true,
      networkBoundaryPlanned: false,
      rawPolicySourceStored: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Real policy backend readiness is blocked by default.',
    });
    const policyPlan = RealPolicyBackendEvaluationPlanSchema.parse({
      id: 'real_policy_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'real_policy_dry_run_1',
      status: 'blocked',
      backendKind: 'opa',
      runtimeMode: 'local-cli',
      actionIdHash: 'sha256:action-id',
      actionTypeHash: 'sha256:action-type',
      inputHash: 'sha256:input',
      policySourceHash: 'sha256:policy',
      queryHash: 'sha256:query',
      processBoundaryPlanned: true,
      networkBoundaryPlanned: false,
      advisoryOnly: true,
      authorityProvider: 'codexhub-security-kernel',
      blockReasons: ['policy_backend_disabled'],
      rawPolicySourceStored: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Policy backend evaluation plan stores hashes only.',
    });
    const policyApproval = RealPolicyBackendApprovalArtifactSchema.parse({
      id: 'real_policy_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: policyPlan.dryRunId,
      dryRunRecordId: policyPlan.id,
      approvalRequestId: 'real_policy_approval_request_1',
      approvalArtifactId: 'real_policy_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: 'policy_decision_1',
      expectedPlanHash: 'sha256:policy-plan',
      approvedAt: createdAt,
      advisoryOnly: true,
      authorityProvider: 'codexhub-security-kernel',
      rawPolicySourceStored: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_policy_1'],
      summary: 'Policy approval artifact is metadata-only.',
    });
    const advisory = RealPolicyAdvisoryDecisionSummarySchema.parse({
      id: 'real_policy_advisory_1',
      schemaVersion,
      createdAt,
      backendKind: 'opa',
      runtimeMode: 'local-cli',
      backendOutcome: 'deny',
      normalizedOutcome: 'deny',
      advisoryOnly: true,
      authorityProvider: 'codexhub-security-kernel',
      codexhubPolicyDecisionId: 'policy_decision_1',
      rawDecisionHash: 'sha256:decision',
      reasonCount: 1,
      matchedRuleCount: 1,
      rawPolicySourceStored: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Backend deny remains advisory and cannot grant authority.',
    });
    const policyRun = RealPolicyBackendEvaluationRunSchema.parse({
      id: 'real_policy_run_1',
      schemaVersion,
      createdAt,
      status: 'blocked',
      plan: policyPlan,
      readiness: policyReadiness,
      advisoryDecision: advisory,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_policy_run_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      advisoryOnly: true,
      authorityProvider: 'codexhub-security-kernel',
      rawPolicySourceStored: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Policy run did not transfer authority.',
    });
    const policyRehearsal = RealPolicyBackendAcceptanceRehearsalRunSchema.parse({
      id: 'real_policy_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'approval-blocked',
      backendKind: 'cedar',
      runtimeMode: 'loopback-http',
      status: 'blocked',
      readinessStatus: 'blocked',
      evaluationStatus: 'blocked',
      advisoryOutcome: 'unknown',
      blockerCount: 1,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_policy_rehearsal_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      advisoryOnly: true,
      authorityProvider: 'codexhub-security-kernel',
      rawPolicySourceStored: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Policy backend rehearsal is fixture-only.',
    });

    const telemetryManifest = RealTelemetryRuntimeManifestSchema.parse({
      id: 'real_telemetry_manifest_1',
      schemaVersion,
      createdAt,
      adapterName: 'otel-adapter',
      exporterKinds: ['in-memory', 'otlp-http'],
      defaultEnabled: false,
      networkExporterDefaultEnabled: false,
      evidenceAuditAuthoritative: false,
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Real telemetry is disabled by default and non-authoritative.',
    });
    const telemetryReadiness = RealTelemetryReadinessSchema.parse({
      id: 'real_telemetry_readiness_1',
      schemaVersion,
      createdAt,
      exporterKind: 'otlp-http',
      realTelemetryEnabled: false,
      networkExporterEnabled: false,
      endpointHash: 'sha256:endpoint',
      endpointAllowed: true,
      blockerCount: 1,
      blockReasons: ['telemetry_disabled'],
      networkBoundaryPlanned: true,
      processBoundaryPlanned: false,
      evidenceAuditAuthoritative: false,
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Network telemetry exporter is disabled by default.',
    });
    const telemetryPlan = RealTelemetryExportPlanSchema.parse({
      id: 'real_telemetry_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'real_telemetry_dry_run_1',
      status: 'blocked',
      exporterKind: 'otlp-http',
      signalKinds: ['trace'],
      spanCount: 2,
      tracePlanHash: 'sha256:trace-plan',
      endpointHash: 'sha256:endpoint',
      networkExportPlanned: true,
      processBoundaryPlanned: false,
      blockReasons: ['telemetry_disabled'],
      evidenceAuditAuthoritative: false,
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Telemetry export plan stores trace hashes only.',
    });
    const telemetryApproval = RealTelemetryExportApprovalArtifactSchema.parse({
      id: 'real_telemetry_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: telemetryPlan.dryRunId,
      dryRunRecordId: telemetryPlan.id,
      approvalRequestId: 'real_telemetry_approval_request_1',
      approvalArtifactId: 'real_telemetry_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: 'policy_decision_telemetry_1',
      expectedPlanHash: 'sha256:telemetry-plan',
      approvedAt: createdAt,
      evidenceAuditAuthoritative: false,
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_telemetry_1'],
      summary: 'Telemetry approval remains non-authoritative.',
    });
    const localExport = RealTelemetryLocalExportSummarySchema.parse({
      id: 'real_telemetry_local_summary_1',
      schemaVersion,
      createdAt,
      exporterKind: 'in-memory',
      exportedSpanCount: 2,
      exportSummaryHash: 'sha256:local-export',
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceAuditAuthoritative: false,
      summary: 'In-memory telemetry export summary is hash-only.',
    });
    const networkExport = RealTelemetryNetworkExportSummarySchema.parse({
      id: 'real_telemetry_network_summary_1',
      schemaVersion,
      createdAt,
      exporterKind: 'otlp-http',
      exportedSpanCount: 2,
      endpointHash: 'sha256:endpoint',
      exportSummaryHash: 'sha256:network-export',
      networkBoundaryInvoked: false,
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceAuditAuthoritative: false,
      summary: 'OTLP HTTP telemetry summary does not store raw spans.',
    });
    const telemetryRun = RealTelemetryExportRunSchema.parse({
      id: 'real_telemetry_run_1',
      schemaVersion,
      createdAt,
      status: 'blocked',
      plan: telemetryPlan,
      readiness: telemetryReadiness,
      localExportSummary: localExport,
      networkExportSummary: networkExport,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_telemetry_run_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      evidenceAuditAuthoritative: false,
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Telemetry run is metadata-only.',
    });
    const telemetryRehearsal = RealTelemetryAcceptanceRehearsalRunSchema.parse({
      id: 'real_telemetry_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'network-exporter-disabled',
      exporterKind: 'otlp-http',
      status: 'blocked',
      readinessStatus: 'blocked',
      exportStatus: 'blocked',
      exportedSpanCount: 0,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_telemetry_rehearsal_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      evidenceAuditAuthoritative: false,
      rawTracePayloadStored: false,
      rawLogStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Telemetry rehearsal is fixture-only.',
    });

    const serialized = JSON.stringify([
      policyManifest,
      policyReadiness,
      policyPlan,
      policyApproval,
      advisory,
      policyRun,
      policyRehearsal,
      telemetryManifest,
      telemetryReadiness,
      telemetryPlan,
      telemetryApproval,
      localExport,
      networkExport,
      telemetryRun,
      telemetryRehearsal,
    ]);
    expect(policyRun.advisoryOnly).toBe(true);
    expect(telemetryRun.evidenceAuditAuthoritative).toBe(false);
    expect(
      findAdversarialPublicOutputRoundTripLeaks([
        policyManifest,
        policyReadiness,
        policyPlan,
        policyApproval,
        advisory,
        policyRun,
        policyRehearsal,
        telemetryManifest,
        telemetryReadiness,
        telemetryPlan,
        telemetryApproval,
        localExport,
        networkExport,
        telemetryRun,
        telemetryRehearsal,
      ]),
    ).toEqual([]);
    expect(serialized).not.toContain('package main');
    expect(serialized).not.toContain('raw span body');
    expect(serialized).not.toContain('ghp_');
    expect(() =>
      RealPolicyBackendEvaluationPlanSchema.parse({
        ...policyPlan,
        id: 'real_policy_plan_raw_source',
        metadata: { policySource: 'package main' },
      }),
    ).toThrow();
    expect(() =>
      RealTelemetryExportPlanSchema.parse({
        ...telemetryPlan,
        id: 'real_telemetry_plan_raw_span',
        metadata: { rawSpan: 'raw span body' },
      }),
    ).toThrow();
  });

  it('parses controlled write contracts and keeps raw action input out of public output', () => {
    const evidenceRef = EvidenceRefSchema.parse({
      id: 'evidence_controlled_write_1',
      schemaVersion,
      createdAt,
      kind: 'browser.action_summary',
      hash: 'sha256:evidence',
      summary: 'Controlled write evidence is hash-only.',
    });
    const browserPlan = BrowserActionPlanSchema.parse({
      id: 'browser_action_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'browser_action_dry_run_1',
      status: 'planned',
      actionKind: 'click',
      targetUrlHash: 'sha256:url',
      selectorHash: 'sha256:selector',
      blockReasons: [],
      processBoundaryPlanned: true,
      browserActPlanned: true,
      rawSelectorStored: false,
      rawTypedTextStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Browser click action is planned through a fixed allowlist.',
    });
    const browserApproval = BrowserActionApprovalArtifactSchema.parse({
      id: 'browser_action_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: browserPlan.dryRunId,
      dryRunRecordId: browserPlan.id,
      approvalRequestId: 'browser_action_approval_request_1',
      approvalArtifactId: 'browser_action_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: 'policy_decision_browser_1',
      expectedPlanHash: 'sha256:browser-plan',
      reasonHash: 'sha256:reason',
      rawSelectorStored: false,
      rawTypedTextStored: false,
      bodyStored: false,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_browser_1'],
      summary: 'Browser action approval stores selector hashes only.',
    });
    const browserStep = BrowserActionStepSummarySchema.parse({
      id: 'browser_action_step_1',
      schemaVersion,
      createdAt,
      actionKind: 'click',
      targetUrlHash: browserPlan.targetUrlHash,
      selectorHash: browserPlan.selectorHash,
      status: 'completed',
      rawSelectorStored: false,
      rawTypedTextStored: false,
      bodyStored: false,
      summary: 'Browser action step is metadata-only.',
    });
    const browserRun = BrowserActionRunSchema.parse({
      id: 'browser_action_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: browserPlan,
      stepSummaries: [browserStep],
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_browser_run_1'],
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      browserActionInvoked: true,
      rawSelectorStored: false,
      rawTypedTextStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Browser action run invoked only the reviewed boundary.',
    });
    const electronPlan = ElectronMainInspectorPlanSchema.parse({
      id: 'electron_main_inspector_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'electron_main_inspector_dry_run_1',
      status: 'planned',
      endpointHash: 'sha256:endpoint',
      targetIdHash: 'sha256:target',
      snippetId: 'inspect-app-state',
      snippetSourceHash: 'sha256:snippet',
      blockReasons: [],
      cdpHttpBoundaryPlanned: true,
      cdpWebSocketBoundaryPlanned: true,
      mainInspectorPlanned: true,
      rawJavascriptStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Electron Runtime.evaluate is planned by snippet hash.',
    });
    const electronApproval = ElectronMainInspectorApprovalArtifactSchema.parse({
      id: 'electron_main_inspector_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: electronPlan.dryRunId,
      dryRunRecordId: electronPlan.id,
      approvalRequestId: 'electron_main_inspector_approval_request_1',
      approvalArtifactId: 'electron_main_inspector_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: 'policy_decision_electron_1',
      expectedPlanHash: 'sha256:electron-plan',
      reasonHash: 'sha256:reason',
      rawJavascriptStored: false,
      bodyStored: false,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_electron_1'],
      summary: 'Electron inspector approval stores snippet hash only.',
    });
    const electronRun = ElectronMainInspectorRunSchema.parse({
      id: 'electron_main_inspector_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: electronPlan,
      resultHash: 'sha256:result',
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_electron_run_1'],
      cdpHttpBoundaryInvoked: true,
      cdpWebSocketBoundaryInvoked: true,
      mainInspectorInvoked: true,
      rawJavascriptStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Electron inspector run returned a result hash.',
    });
    const mcpManifest = McpWriteToolManifestSchema.parse({
      id: 'mcp_write_manifest_1',
      schemaVersion,
      createdAt,
      name: 'workspace.applyPatchToControlledWorktree',
      enabled: false,
      riskLevel: 'critical',
      actionMode: 'write',
      approvalPolicy: 'required',
      directExecutionAllowed: true,
      controlledWorktreeOnly: true,
      repoRootMutationAllowed: false,
      rawPatchStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'MCP write tool is disabled by default and limited to a controlled worktree.',
    });
    const mcpPlan = McpWriteToolPlanSchema.parse({
      id: 'mcp_write_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'mcp_write_dry_run_1',
      status: 'planned',
      toolName: mcpManifest.name,
      worktreePathHash: 'sha256:worktree',
      patchHash: 'sha256:patch',
      changedFileCount: 1,
      blockReasons: [],
      directExecutionPlanned: true,
      controlledWorktreeOnly: true,
      repoRootMutationAllowed: false,
      rawPatchStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'MCP write plan stores the patch hash only.',
    });
    const mcpApproval = McpWriteToolApprovalArtifactSchema.parse({
      id: 'mcp_write_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: mcpPlan.dryRunId,
      dryRunRecordId: mcpPlan.id,
      approvalRequestId: 'mcp_write_approval_request_1',
      approvalArtifactId: 'mcp_write_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: 'policy_decision_mcp_1',
      expectedPlanHash: 'sha256:mcp-plan',
      reasonHash: 'sha256:reason',
      rawPatchStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_mcp_1'],
      summary: 'MCP write approval is hash-bound.',
    });
    const mcpRun = McpWriteToolRunSchema.parse({
      id: 'mcp_write_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      plan: mcpPlan,
      appliedFileCount: 1,
      resultHash: 'sha256:result',
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_mcp_run_1'],
      directExecutionInvoked: true,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      controlledWorktreeOnly: true,
      repoRootMutationAllowed: false,
      rawPatchStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'MCP direct execution is limited to a controlled worktree patch.',
    });
    const rehearsal = ControlledWriteAcceptanceRehearsalRunSchema.parse({
      id: 'controlled_write_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'path-traversal-blocked',
      surface: 'mcp',
      status: 'blocked',
      readinessStatus: 'blocked',
      runStatus: 'blocked',
      blockerCount: 1,
      evidenceRefs: [evidenceRef],
      auditEventIds: ['audit_controlled_write_rehearsal_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Controlled write rehearsal blocks path traversal.',
    });

    const serialized = JSON.stringify([
      browserPlan,
      browserApproval,
      browserRun,
      electronPlan,
      electronApproval,
      electronRun,
      mcpManifest,
      mcpPlan,
      mcpApproval,
      mcpRun,
      rehearsal,
    ]);
    expect(browserRun.browserActionInvoked).toBe(true);
    expect(electronRun.mainInspectorInvoked).toBe(true);
    expect(mcpRun.repoRootMutationAllowed).toBe(false);
    expect(
      findAdversarialPublicOutputRoundTripLeaks([
        browserPlan,
        browserApproval,
        browserRun,
        electronPlan,
        electronApproval,
        electronRun,
        mcpManifest,
        mcpPlan,
        mcpApproval,
        mcpRun,
        rehearsal,
      ]),
    ).toEqual([]);
    expect(serialized).not.toContain('#password');
    expect(serialized).not.toContain('Runtime.evaluate source');
    expect(serialized).not.toContain('raw patch');
    expect(() =>
      BrowserActionPlanSchema.parse({
        ...browserPlan,
        id: 'browser_action_raw_selector',
        metadata: { selector: '#password' },
      }),
    ).toThrow();
    expect(() =>
      ElectronMainInspectorPlanSchema.parse({
        ...electronPlan,
        id: 'electron_main_inspector_raw_source',
        metadata: { javascript: 'Runtime.evaluate source' },
      }),
    ).toThrow();
    expect(() =>
      McpWriteToolPlanSchema.parse({
        ...mcpPlan,
        id: 'mcp_write_raw_patch',
        metadata: { patch: 'raw patch' },
      }),
    ).toThrow();
    expect(() =>
      McpWriteToolPlanSchema.parse({
        ...mcpPlan,
        id: 'mcp_write_repo_root_allowed',
        repoRootMutationAllowed: true,
      }),
    ).toThrow();
  });

  it('parses runtime scheduler and external agent contracts while keeping agent input hash-only', () => {
    const runtimeEvidence = EvidenceRefSchema.parse({
      id: 'evidence_runtime_1',
      schemaVersion,
      createdAt,
      kind: 'runtime.job_run_summary',
      hash: 'sha256:runtime-evidence',
      summary: 'Runtime evidence is metadata-only.',
    });
    const agentEvidence = EvidenceRefSchema.parse({
      id: 'evidence_external_agent_1',
      schemaVersion,
      createdAt,
      kind: 'external_agent.patch_summary',
      hash: 'sha256:agent-evidence',
      summary: 'External agent patch evidence is hash-only.',
    });
    const retryPolicy = RuntimeRetryPolicySchema.parse({
      maxAttempts: 3,
      attemptCount: 1,
      backoffStrategy: 'fixed',
      backoffSeconds: 30,
      retryableStatuses: ['failed', 'timed_out'],
    });
    const concurrencyPolicy = RuntimeConcurrencyPolicySchema.parse({
      scope: 'workflow-template',
      scopeHash: 'sha256:workflow-template',
      maxConcurrent: 2,
      currentRunningCount: 1,
    });
    const runtimePlan = RuntimeJobPlanSchema.parse({
      id: 'runtime_job_plan_1',
      schemaVersion,
      createdAt,
      jobKind: 'workflow',
      targetKind: 'production-recovery',
      targetRecordIdHash: 'sha256:target',
      templateIdHash: 'sha256:template-id',
      templateHash: 'sha256:template',
      sourceRecordHash: 'sha256:source',
      status: 'planned',
      riskLevel: 'high',
      actionMode: 'write',
      approvalRequired: true,
      lockKeyHashes: ['sha256:lock'],
      retryPolicy,
      concurrencyPolicy,
      timeoutSeconds: 600,
      checkpointRequired: true,
      schedulerEnabled: false,
      childWorkflowCoordinationEnabled: false,
      processBoundaryPlanned: false,
      externalProcessPlanned: false,
      networkBoundaryPlanned: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      blockReasons: ['runtime_scheduler_disabled'],
      evidenceRefs: [runtimeEvidence],
      auditEventIds: ['audit_runtime_plan_1'],
      summary: 'Runtime job plan is queued through governed scheduler metadata.',
    });
    const queueEntry = RuntimeQueueEntrySchema.parse({
      id: 'runtime_queue_entry_1',
      schemaVersion,
      createdAt,
      jobPlanId: runtimePlan.id,
      jobPlanHash: 'sha256:runtime-plan',
      status: 'pending',
      priority: 50,
      enqueueOrder: 1,
      lockKeyHashes: ['sha256:lock'],
      retryPolicy,
      rawPayloadStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Queue entry stores only plan and lock hashes.',
    });
    const lease = RuntimeLeaseSchema.parse({
      id: 'runtime_lease_1',
      schemaVersion,
      createdAt,
      queueEntryId: queueEntry.id,
      workerIdHash: 'sha256:worker',
      leaseTokenHash: 'sha256:lease-token',
      acquiredAt: createdAt,
      expiresAt: '2026-04-28T00:10:00.000Z',
      status: 'active',
      rawTokenStored: false,
      bodyStored: false,
      summary: 'Lease token is hash-only.',
    });
    const lock = RuntimeLockSchema.parse({
      id: 'runtime_lock_1',
      schemaVersion,
      createdAt,
      lockKeyHash: 'sha256:lock',
      holderJobIdHash: 'sha256:job',
      holderLeaseIdHash: 'sha256:lease',
      status: 'held',
      acquiredAt: createdAt,
      expiresAt: '2026-04-28T00:10:00.000Z',
      rawKeyStored: false,
      bodyStored: false,
      summary: 'Lock state is keyed by hash.',
    });
    const checkpoint = RuntimeCheckpointSchema.parse({
      id: 'runtime_checkpoint_1',
      schemaVersion,
      createdAt,
      jobRunId: 'runtime_job_run_1',
      checkpointHash: 'sha256:checkpoint',
      stepIdHash: 'sha256:step',
      resumable: true,
      completedStepCount: 2,
      nextStepIdHash: 'sha256:next-step',
      rawStateStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [runtimeEvidence],
      auditEventIds: ['audit_runtime_checkpoint_1'],
      summary: 'Checkpoint stores resumable step hashes only.',
    });
    const runtimeRun = RuntimeJobRunSchema.parse({
      id: 'runtime_job_run_1',
      schemaVersion,
      createdAt,
      status: 'blocked',
      plan: runtimePlan,
      queueEntryId: queueEntry.id,
      leaseId: lease.id,
      attemptNumber: 1,
      startedAt: createdAt,
      checkpoint,
      boundaryReached: false,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [runtimeEvidence],
      auditEventIds: ['audit_runtime_run_1'],
      summary: 'Runtime run is blocked before any child boundary.',
    });
    const coordinationPlan = MultiAgentCoordinationPlanSchema.parse({
      id: 'multi_agent_coordination_1',
      schemaVersion,
      createdAt,
      coordinatorKind: 'workflow-kernel',
      workflowTemplateIdHash: 'sha256:workflow-template-id',
      workflowTemplateHash: 'sha256:workflow-template',
      slotCount: 2,
      maxConcurrentSlots: 1,
      childRunIdHashes: ['sha256:child-run'],
      directAgentSpawnAllowed: false,
      dashboardDirectAdapterAllowed: false,
      cliDirectAdapterAllowed: false,
      mcpDirectAdapterAllowed: false,
      rawPromptStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Multi-agent coordination remains inside workflow-kernel.',
    });
    const slotSummary = MultiAgentSlotSummarySchema.parse({
      id: 'multi_agent_slot_1',
      schemaVersion,
      createdAt,
      coordinationPlanId: coordinationPlan.id,
      slotIdHash: 'sha256:slot',
      provider: 'workflow-kernel',
      status: 'waiting',
      childRunIdHash: 'sha256:child-run',
      directAdapterInvoked: false,
      rawPromptStored: false,
      rawOutputStored: false,
      bodyStored: false,
      summary: 'Agent slot summary is a logical workflow slot.',
    });
    const runtimeRehearsal = RuntimeSchedulerRehearsalRunSchema.parse({
      id: 'runtime_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'resume-from-checkpoint',
      status: 'passed',
      queueStatus: 'leased',
      jobStatus: 'running',
      lockStatus: 'held',
      checkpointCreated: true,
      retryAttemptCount: 1,
      evidenceRefs: [runtimeEvidence],
      auditEventIds: ['audit_runtime_rehearsal_1'],
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      rawInputStored: false,
      rawOutputStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Runtime scheduler rehearsal resumes from a checkpoint.',
    });

    const manifest = ExternalAgentManifestSchema.parse({
      id: 'external_agent_manifest_1',
      schemaVersion,
      createdAt,
      provider: 'codex-cli',
      enabledByDefault: false,
      actionMode: 'write',
      riskLevel: 'critical',
      approvalPolicy: 'required',
      fixedArgvShapeHash: 'sha256:argv-shape',
      controlledSiblingWorktreeOnly: true,
      repoRootMutationAllowed: false,
      arbitraryCommandAllowed: false,
      rawPromptStored: false,
      rawCommandStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'Codex CLI external agent is disabled by default and fixed-argv only.',
    });
    const readiness = ExternalAgentReadinessSchema.parse({
      id: 'external_agent_readiness_1',
      schemaVersion,
      createdAt,
      provider: manifest.provider,
      externalAgentsEnabled: false,
      providerEnabled: false,
      cliConfigured: false,
      worktreeResolved: true,
      worktreeRecordHash: 'sha256:worktree-record',
      controlledSiblingWorktreeOnly: true,
      repoRootMutationAllowed: false,
      blockerCount: 2,
      blockReasons: ['external_agents_disabled', 'codex_agent_disabled'],
      rawPromptStored: false,
      rawCommandStored: false,
      rawPathStored: false,
      bodyStored: false,
      summary: 'External agent readiness is disabled until runtime gates are configured.',
    });
    const agentPlan = ExternalAgentPatchPlanSchema.parse({
      id: 'external_agent_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'external_agent_dry_run_1',
      status: 'blocked',
      provider: manifest.provider,
      sourceWorktreeRecordHash: 'sha256:worktree-record',
      worktreePathHash: 'sha256:worktree-path',
      promptHash: 'sha256:prompt',
      instructionHash: 'sha256:instructions',
      changedFileCount: 2,
      maxRuntimeSeconds: 900,
      fixedArgvShapeHash: manifest.fixedArgvShapeHash,
      processBoundaryPlanned: true,
      externalProcessPlanned: true,
      controlledSiblingWorktreeOnly: true,
      repoRootMutationAllowed: false,
      arbitraryCommandAllowed: false,
      blockReasons: ['external_agents_disabled'],
      rawPromptStored: false,
      rawDiffStored: false,
      rawPatchStored: false,
      rawCommandStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [agentEvidence],
      auditEventIds: ['audit_agent_plan_1'],
      summary: 'External agent dry-run stores only prompt and worktree hashes.',
    });
    const approval = ExternalAgentApprovalArtifactSchema.parse({
      id: 'external_agent_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: agentPlan.dryRunId,
      dryRunRecordId: agentPlan.id,
      approvalRequestId: 'external_agent_approval_request_1',
      approvalArtifactId: 'external_agent_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: 'policy_decision_agent_1',
      expectedPlanHash: 'sha256:agent-plan',
      decidedByHash: 'sha256:operator',
      reasonHash: 'sha256:reason',
      rawPromptStored: false,
      rawDiffStored: false,
      rawPatchStored: false,
      rawCommandStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [agentEvidence],
      auditEventIds: ['audit_agent_approval_1'],
      summary: 'External agent approval is hash-bound to the dry-run plan.',
    });
    const patchSummary = ExternalAgentPatchSummarySchema.parse({
      id: 'external_agent_patch_summary_1',
      schemaVersion,
      createdAt,
      provider: manifest.provider,
      patchHash: 'sha256:patch',
      changedFileCount: 2,
      addedLineCount: 12,
      deletedLineCount: 3,
      worktreePathHash: agentPlan.worktreePathHash,
      rawPromptStored: false,
      rawDiffStored: false,
      rawPatchStored: false,
      rawCommandStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [agentEvidence],
      auditEventIds: ['audit_agent_patch_1'],
      summary: 'External agent patch summary stores counts and hash only.',
    });
    const agentRun = ExternalAgentRunSchema.parse({
      id: 'external_agent_run_1',
      schemaVersion,
      createdAt,
      status: 'completed',
      provider: manifest.provider,
      plan: agentPlan,
      readiness,
      patchSummary,
      approvalArtifactId: approval.approvalArtifactId,
      approvalConsumed: true,
      boundaryReached: true,
      processBoundaryInvoked: true,
      externalProcessStarted: true,
      networkBoundaryInvoked: false,
      controlledSiblingWorktreeOnly: true,
      repoRootMutationAllowed: false,
      rawPromptStored: false,
      rawDiffStored: false,
      rawPatchStored: false,
      rawCommandStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [agentEvidence],
      auditEventIds: ['audit_agent_run_1'],
      summary: 'External agent run invoked only the fixed CLI boundary.',
    });
    const agentRehearsal = ExternalAgentRehearsalRunSchema.parse({
      id: 'external_agent_rehearsal_1',
      schemaVersion,
      createdAt,
      scenario: 'codex-all-pass',
      provider: manifest.provider,
      status: 'passed',
      readinessStatus: 'fixture_completed',
      runStatus: 'fixture_completed',
      patchSummary,
      blockerCount: 0,
      processBoundaryInvoked: false,
      externalProcessStarted: false,
      networkBoundaryInvoked: false,
      controlledSiblingWorktreeOnly: true,
      repoRootMutationAllowed: false,
      rawPromptStored: false,
      rawDiffStored: false,
      rawPatchStored: false,
      rawCommandStored: false,
      rawPathStored: false,
      bodyStored: false,
      evidenceRefs: [agentEvidence],
      auditEventIds: ['audit_agent_rehearsal_1'],
      summary: 'External agent rehearsal is fixture-only.',
    });

    expect(runtimeRun.boundaryReached).toBe(false);
    expect(slotSummary.directAdapterInvoked).toBe(false);
    expect(agentRun.repoRootMutationAllowed).toBe(false);
    expect(
      findAdversarialPublicOutputRoundTripLeaks([
        runtimePlan,
        queueEntry,
        lease,
        lock,
        checkpoint,
        runtimeRun,
        coordinationPlan,
        slotSummary,
        runtimeRehearsal,
        manifest,
        readiness,
        agentPlan,
        approval,
        patchSummary,
        agentRun,
        agentRehearsal,
      ]),
    ).toEqual([]);
    expect(() =>
      RuntimeJobPlanSchema.parse({
        ...runtimePlan,
        id: 'runtime_plan_without_approval',
        approvalRequired: false,
      }),
    ).toThrow();
    expect(() =>
      ExternalAgentPatchPlanSchema.parse({
        ...agentPlan,
        id: 'external_agent_raw_prompt',
        metadata: { prompt: 'raw prompt' },
      }),
    ).toThrow();
    expect(() =>
      ExternalAgentPatchSummarySchema.parse({
        ...patchSummary,
        id: 'external_agent_raw_patch',
        metadata: { patch: 'raw patch' },
      }),
    ).toThrow();
    expect(() =>
      ExternalAgentRunSchema.parse({
        ...agentRun,
        id: 'external_agent_raw_command',
        metadata: { command: 'raw command fixture', argv: 'raw argv fixture' },
      }),
    ).toThrow();
    expect(() =>
      ExternalAgentRunSchema.parse({
        ...agentRun,
        id: 'external_agent_boundary_without_consumption',
        approvalConsumed: false,
      }),
    ).toThrow();
  });

  it('parses platform operations contracts and rejects raw operational payloads', () => {
    const platformEvidence = EvidenceRefSchema.parse({
      id: 'evidence_platform_backup_1',
      schemaVersion,
      createdAt,
      kind: 'platform.backup_plan',
      hash: 'sha256:platform-backup',
      redacted: true,
    });
    const platformFlags = {
      rawPathStored: false,
      rawSqlStored: false,
      rawDbRowsStored: false,
      rawBackupBodyStored: false,
      rawAuditBodyStored: false,
      rawTokenStored: false,
      rawEnvStored: false,
      rawRequestBodyStored: false,
      rawResponseBodyStored: false,
      bodyStored: false,
    };
    const evidenceAudit = {
      evidenceRefs: [platformEvidence],
      auditEventIds: ['audit_platform_1'],
    };

    const backupPlan = PlatformBackupPlanSchema.parse({
      id: 'platform_backup_plan_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      dryRunId: 'platform_backup_dry_run_1',
      status: 'planned',
      scope: 'store-sqlite',
      storeSnapshotHash: 'sha256:store-snapshot',
      backupRootHash: 'sha256:backup-root',
      manifestHash: 'sha256:backup-manifest',
      fileCount: 3,
      estimatedByteCount: 1024,
      localFilesystemOnly: true,
      networkExportAllowed: false,
      arbitraryBackupTargetAllowed: false,
      blockReasons: [],
      summary: 'Backup plan is local filesystem only.',
    });
    const backupRun = PlatformBackupRunSchema.parse({
      id: 'platform_backup_run_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      status: 'completed',
      plan: backupPlan,
      manifestHash: backupPlan.manifestHash,
      artifactCount: 2,
      byteCount: 1024,
      approvalArtifactIds: ['platform_backup_approval_1'],
      boundaryReached: true,
      approvalConsumed: true,
      localFilesystemBoundaryInvoked: true,
      networkBoundaryInvoked: false,
      summary: 'Backup run wrote metadata-only local artifacts.',
    });

    const restorePlan = PlatformRestorePlanSchema.parse({
      id: 'platform_restore_plan_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      dryRunId: 'platform_restore_dry_run_1',
      status: 'planned',
      mode: 'replace-active-store',
      sourceBackupManifestHash: backupPlan.manifestHash,
      targetStoreHash: 'sha256:target-store',
      isolatedRestoreDefault: true,
      replaceActiveStoreEnabled: false,
      schedulerQuiescenceRequired: true,
      backupManifestHashMatchRequired: true,
      twoApprovalsRequiredForReplace: true,
      blockReasons: ['restore_replace_disabled'],
      summary: 'Restore replacement remains disabled until critical gates pass.',
    });
    const restoreRun = PlatformRestoreRunSchema.parse({
      id: 'platform_restore_run_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      status: 'blocked',
      plan: restorePlan,
      approvalArtifactIds: ['platform_restore_approval_1', 'platform_restore_approval_2'],
      approvalConsumedCount: 0,
      boundaryReached: false,
      isolatedRestoreBoundaryInvoked: false,
      storeReplacementBoundaryInvoked: false,
      networkBoundaryInvoked: false,
      restoredRecordCount: 0,
      summary: 'Restore is blocked before active store replacement.',
    });

    const migrationPlan = StoreMigrationPlanSchema.parse({
      id: 'store_migration_plan_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      dryRunId: 'store_migration_dry_run_1',
      status: 'planned',
      builtInMigrationId: 'm47_add_platform_operations',
      builtInMigrationIdHash: 'sha256:migration-id',
      currentSchemaHash: 'sha256:schema-current',
      targetSchemaHash: 'sha256:schema-target',
      requestBodySqlAccepted: false,
      arbitrarySqlAllowed: false,
      blockReasons: [],
      summary: 'Migration plan references only a built-in migration id.',
    });
    const migrationRun = StoreMigrationRunSchema.parse({
      id: 'store_migration_run_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      status: 'completed',
      plan: migrationPlan,
      approvalArtifactIds: ['store_migration_approval_1'],
      boundaryReached: true,
      approvalConsumed: true,
      migrationBoundaryInvoked: true,
      migratedRecordCount: 4,
      summary: 'Migration run used a built-in migration id only.',
    });

    const retentionPlan = RetentionPolicyPlanSchema.parse({
      id: 'retention_policy_plan_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      dryRunId: 'retention_dry_run_1',
      status: 'planned',
      target: 'audit',
      policyHash: 'sha256:retention-policy',
      previewRecordCount: 12,
      deletionPlanned: true,
      backupRequiredBeforeDelete: true,
      backupManifestHash: backupPlan.manifestHash,
      blockReasons: [],
      summary: 'Retention dry-run requires backup before deletion.',
    });
    const retentionRun = RetentionPolicyRunSchema.parse({
      id: 'retention_policy_run_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      status: 'completed',
      plan: retentionPlan,
      approvalArtifactIds: ['retention_approval_1'],
      boundaryReached: true,
      approvalConsumed: true,
      retentionBoundaryInvoked: true,
      affectedRecordCount: 12,
      summary: 'Retention run records metadata only.',
    });

    const auditExportPlan = AuditExportPlanSchema.parse({
      id: 'audit_export_plan_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      dryRunId: 'audit_export_dry_run_1',
      status: 'planned',
      exportFormat: 'jsonl',
      destinationHash: 'sha256:audit-export-destination',
      manifestHash: 'sha256:audit-export-manifest',
      recordCount: 22,
      metadataOnly: true,
      networkExportAllowed: false,
      rawAuditRowsStored: false,
      blockReasons: [],
      summary: 'Audit export writes local metadata-only JSONL.',
    });
    const auditExportRun = AuditExportRunSchema.parse({
      id: 'audit_export_run_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      status: 'completed',
      plan: auditExportPlan,
      manifestHash: auditExportPlan.manifestHash,
      recordCount: auditExportPlan.recordCount,
      approvalArtifactIds: ['audit_export_approval_1'],
      boundaryReached: true,
      approvalConsumed: true,
      localFilesystemBoundaryInvoked: true,
      networkBoundaryInvoked: false,
      summary: 'Audit export run did not use a network exporter.',
    });

    const rolePlan = OperatorRoleAssignmentPlanSchema.parse({
      id: 'operator_role_plan_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      dryRunId: 'operator_role_dry_run_1',
      status: 'planned',
      operatorHash: 'sha256:operator',
      role: 'approver',
      scopeHashes: ['sha256:scope'],
      roleEnforcementEnabled: true,
      localControlTokenReplacementAllowed: false,
      rawOperatorIdentityStored: false,
      blockReasons: [],
      summary: 'Operator role assignment stores only operator hash and scope hashes.',
    });
    const roleRun = OperatorRoleAssignmentRunSchema.parse({
      id: 'operator_role_run_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      status: 'completed',
      plan: rolePlan,
      approvalArtifactIds: ['operator_role_approval_1'],
      boundaryReached: true,
      approvalConsumed: true,
      roleStoreBoundaryInvoked: true,
      assignedRoleHash: 'sha256:role',
      summary: 'Role assignment run stores only hashes.',
    });

    const approval = PlatformOperationApprovalArtifactSchema.parse({
      id: 'platform_operation_approval_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      operationKind: 'backup',
      dryRunId: backupPlan.dryRunId,
      dryRunRecordId: backupPlan.id,
      approvalRequestId: 'platform_approval_request_1',
      approvalArtifactId: 'platform_approval_artifact_1',
      status: 'approved',
      approved: true,
      policyDecisionId: 'policy_decision_platform_1',
      expectedPlanHash: 'sha256:platform-plan',
      approverHash: 'sha256:approver',
      reasonHash: 'sha256:reason',
      summary: 'Platform operation approval is hash-bound.',
    });

    const disasterRecovery = DisasterRecoveryRehearsalRunSchema.parse({
      id: 'disaster_recovery_rehearsal_1',
      schemaVersion,
      createdAt,
      ...platformFlags,
      ...evidenceAudit,
      scenario: 'disaster-recovery-drill',
      status: 'passed',
      backupStatus: 'completed',
      restoreStatus: 'completed',
      migrationStatus: 'rehearsed',
      retentionStatus: 'rehearsed',
      auditExportStatus: 'completed',
      operatorRoleStatus: 'rehearsed',
      blockerCount: 0,
      boundaryReached: false,
      localFilesystemBoundaryInvoked: false,
      storeReplacementBoundaryInvoked: false,
      networkBoundaryInvoked: false,
      summary: 'Disaster recovery drill records metadata-only completion.',
    });

    const retentionPreview = DisasterRecoveryRehearsalRunSchema.parse({
      ...disasterRecovery,
      id: 'disaster_recovery_retention_preview',
      scenario: 'retention-preview',
      restoreStatus: 'rehearsed',
      auditExportStatus: 'rehearsed',
      summary: 'Retention preview records metadata-only completion.',
    });

    expect(
      findAdversarialPublicOutputRoundTripLeaks([
        backupPlan,
        backupRun,
        restorePlan,
        restoreRun,
        migrationPlan,
        migrationRun,
        retentionPlan,
        retentionRun,
        auditExportPlan,
        auditExportRun,
        rolePlan,
        roleRun,
        approval,
        disasterRecovery,
        retentionPreview,
      ]),
    ).toEqual([]);
    expect(() =>
      PlatformBackupPlanSchema.parse({
        ...backupPlan,
        id: 'platform_backup_raw_path',
        metadata: { rawPath: 'C:/secret/store.db' },
      }),
    ).toThrow();
    expect(() =>
      StoreMigrationPlanSchema.parse({
        ...migrationPlan,
        id: 'store_migration_raw_sql',
        metadata: { sql: 'drop table audit_events' },
      }),
    ).toThrow();
    expect(() =>
      AuditExportRunSchema.parse({
        ...auditExportRun,
        id: 'audit_export_raw_body',
        metadata: {
          rawDbRows: 'raw database row fixture',
          rawAuditBody: 'raw audit body fixture',
          rawBackupBody: 'raw backup body fixture',
        },
      }),
    ).toThrow();
    expect(() =>
      RetentionPolicyPlanSchema.parse({
        ...retentionPlan,
        id: 'retention_without_backup',
        backupManifestHash: undefined,
      }),
    ).toThrow();
    expect(() =>
      PlatformRestorePlanSchema.parse({
        ...restorePlan,
        id: 'restore_without_two_approvals',
        twoApprovalsRequiredForReplace: false,
      }),
    ).toThrow();
    expect(() =>
      PlatformOperationApprovalArtifactSchema.parse({
        ...approval,
        id: 'platform_approval_mismatch',
        approved: false,
      }),
    ).toThrow();
  });

  it('parses metadata-only Production GA contracts and rejects raw public output', () => {
    const evidence = EvidenceRefSchema.parse({
      id: 'production_ga_evidence_1',
      schemaVersion,
      createdAt,
      kind: 'production_ga.evidence_bundle_summary',
      hash: 'sha256:ga-evidence',
      redacted: true,
    });
    const matrix = ProductionGaCapabilityMatrixSchema.parse({
      id: 'production_ga_matrix_1',
      schemaVersion,
      createdAt,
      matrixHash: 'sha256:ga-matrix',
      surfaceCount: 9,
      surfaces: [
        'local-patch-review-rc',
        'github-pr-lifecycle',
        'github-merge-actions-release',
        'deployment-observe-apply-rollback',
        'secrets-governance',
        'policy-telemetry',
        'browser-electron-mcp-controlled-write',
        'runtime-external-agents',
        'platform-operations',
      ],
      readySurfaceCount: 9,
      blockedSurfaceCount: 0,
      defaultDisabledSurfaceCount: 9,
      criticalRiskSurfaceCount: 6,
      liveBoundaryAllowlistExpanded: false,
      childAdapterDirectExecutionAllowed: false,
      publicOutputMetadataOnly: true,
      summary: 'All GA capability surfaces are represented as metadata.',
    });
    const threatModel = ProductionGaThreatModelSchema.parse({
      id: 'production_ga_threat_model_1',
      schemaVersion,
      createdAt,
      threatModelHash: 'sha256:threat-model',
      assetCount: 8,
      trustBoundaryCount: 7,
      liveBoundaryCount: 0,
      authorityModelHash: 'sha256:authority',
      approvalModelHash: 'sha256:approval',
      evidenceAuditModelHash: 'sha256:evidence-audit',
      rollbackModelHash: 'sha256:rollback',
      residualRiskCount: 2,
      unresolvedCriticalRiskCount: 0,
      summary: 'GA threat model records boundary counts and hashes only.',
    });
    const readinessPlan = ProductionGaReadinessPlanSchema.parse({
      id: 'production_ga_readiness_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'production_ga_dry_run_1',
      matrixHash: matrix.matrixHash,
      threatModelHash: threatModel.threatModelHash,
      requiredApprovalCount: 2,
      requiredDistinctApproverHashes: true,
      requiredFoundationGateCount: 8,
      requiredTrainingModuleCount: 8,
      e2eFixtureRequired: true,
      conditionalLiveSmokeAllowed: true,
      childAdapterDirectExecutionAllowed: false,
      summary: 'GA readiness requires two approvals and fixture E2E.',
    });
    const readiness = ProductionGaReadinessSummarySchema.parse({
      id: 'production_ga_readiness_summary_1',
      schemaVersion,
      createdAt,
      readinessPlanId: readinessPlan.id,
      status: 'ready',
      matrixStatus: 'ready',
      threatModelStatus: 'ready',
      trainingStatus: 'ready',
      e2eFixtureStatus: 'ready',
      conditionalLiveStatus: 'conditionally_ready',
      unresolvedCriticalRiskCount: 0,
      blockerCount: 0,
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_ga_readiness'],
      summary: 'GA readiness is metadata-only.',
    });
    const rehearsalPlan = ProductionGaE2ERehearsalPlanSchema.parse({
      id: 'production_ga_e2e_plan_1',
      schemaVersion,
      createdAt,
      scenario: 'all-pass',
      chainHash: 'sha256:e2e-chain',
      stepCount: 8,
      fixtureRequired: true,
      liveSmokeMode: 'conditional',
      childControlPlaneRecordCount: 8,
      rawPayloadAccepted: false,
      summary: 'Patch to rollback fixture chain is planned.',
    });
    const rehearsalRun = ProductionGaE2ERehearsalRunSchema.parse({
      id: 'production_ga_e2e_run_1',
      schemaVersion,
      createdAt,
      rehearsalPlanId: rehearsalPlan.id,
      scenario: 'all-pass',
      status: 'ready',
      completedStepCount: 8,
      blockedStepCount: 0,
      failedStepCount: 0,
      liveSmokeStatus: 'readiness_blocked',
      liveSmokeBlockerCount: 1,
      timelineHash: 'sha256:e2e-timeline',
      processBoundaryInvoked: false,
      networkBoundaryInvoked: false,
      remoteProviderBoundaryInvoked: false,
      childAdapterInvokedDirectly: false,
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_ga_e2e'],
      summary: 'Fixture E2E passed; live smoke is readiness-blocked.',
    });
    const trainingPlan = ProductionGaOperatorTrainingPlanSchema.parse({
      id: 'production_ga_training_plan_1',
      schemaVersion,
      createdAt,
      trainingPlanHash: 'sha256:training',
      moduleCount: 2,
      moduleIdHashes: ['sha256:module-governance', 'sha256:module-recovery'],
      requiredForGa: true,
      rawOperatorIdentityStored: false,
      summary: 'GA training modules are hash-bound.',
    });
    const trainingCompletion =
      ProductionGaOperatorTrainingCompletionSummarySchema.parse({
        id: 'production_ga_training_completion_1',
        schemaVersion,
        createdAt,
        trainingPlanHash: trainingPlan.trainingPlanHash,
        operatorHash: 'sha256:operator',
        completedModuleCount: 2,
        requiredModuleCount: 2,
        status: 'ready',
        rawOperatorIdentityStored: false,
        evidenceRefIds: [evidence.id],
        auditEventIds: ['audit_ga_training'],
        summary: 'Operator training completion stores hashes only.',
      });
    const signoffPlan = ProductionGaReleaseCandidateSignoffPlanSchema.parse({
      id: 'production_ga_signoff_plan_1',
      schemaVersion,
      createdAt,
      dryRunId: 'production_ga_dry_run_1',
      matrixHash: matrix.matrixHash,
      threatModelHash: threatModel.threatModelHash,
      readinessSummaryId: readiness.id,
      e2eRehearsalRunId: rehearsalRun.id,
      requiredApprovalCount: 2,
      requiredDistinctApproverHashes: true,
      unresolvedCriticalRiskCount: 0,
      conditionalLiveSmokeAllowed: true,
      summary: 'GA signoff plan is ready for two approvals.',
    });
    const approvalA = ProductionGaApprovalArtifactSchema.parse({
      id: 'production_ga_approval_1',
      schemaVersion,
      createdAt,
      dryRunId: signoffPlan.dryRunId,
      approverHash: 'sha256:approver-a',
      decision: 'approved',
      approved: true,
      reasonHash: 'sha256:reason-a',
      requestBodyStored: false,
      rawReasonStored: false,
      summary: 'First GA approval is hash-bound.',
    });
    const approvalB = ProductionGaApprovalArtifactSchema.parse({
      ...approvalA,
      id: 'production_ga_approval_2',
      approverHash: 'sha256:approver-b',
      reasonHash: 'sha256:reason-b',
    });
    const signoff = ProductionGaSignoffRunSchema.parse({
      id: 'production_ga_signoff_run_1',
      schemaVersion,
      createdAt,
      signoffPlanId: signoffPlan.id,
      status: 'conditionally_ready',
      approvalArtifactIds: [approvalA.id, approvalB.id],
      approverHashes: [approvalA.approverHash, approvalB.approverHash],
      approvalConsumedCount: 2,
      foundationGateStatus: 'ready',
      matrixStatus: 'ready',
      threatModelStatus: 'ready',
      trainingStatus: 'ready',
      e2eFixtureStatus: 'ready',
      conditionalLiveStatus: 'conditionally_ready',
      unresolvedCriticalRiskCount: 0,
      signoffHash: 'sha256:signoff',
      processBoundaryInvoked: false,
      networkBoundaryInvoked: false,
      remoteProviderBoundaryInvoked: false,
      childAdapterInvokedDirectly: false,
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_ga_signoff'],
      summary: 'GA signoff is conditionally ready because live smoke is environment-blocked.',
    });
    const risks = ProductionGaResidualRiskRegisterSchema.parse({
      id: 'production_ga_risk_register_1',
      schemaVersion,
      createdAt,
      registerHash: 'sha256:risk-register',
      riskCount: 2,
      criticalRiskCount: 0,
      unresolvedCriticalRiskCount: 0,
      acceptedNonCriticalRiskCount: 2,
      mitigationSummaryHash: 'sha256:mitigations',
      summary: 'Only accepted non-critical risks remain.',
    });
    const bundle = ProductionGaEvidenceBundleSummarySchema.parse({
      id: 'production_ga_evidence_bundle_1',
      schemaVersion,
      createdAt,
      bundleHash: 'sha256:bundle',
      gateCount: 8,
      passedGateCount: 8,
      evidenceCount: 12,
      auditEventCount: 12,
      missingEvidenceCount: 0,
      metadataOnly: true,
      evidenceRefIds: [evidence.id],
      auditEventIds: ['audit_ga_bundle'],
      summary: 'GA evidence bundle is metadata-only.',
    });

    expect(
      findAdversarialPublicOutputRoundTripLeaks([
        matrix,
        threatModel,
        readinessPlan,
        readiness,
        rehearsalPlan,
        rehearsalRun,
        trainingPlan,
        trainingCompletion,
        signoffPlan,
        approvalA,
        approvalB,
        signoff,
        risks,
        bundle,
      ]),
    ).toEqual([]);
    expect(() =>
      ProductionGaCapabilityMatrixSchema.parse({
        ...matrix,
        id: 'production_ga_matrix_bad_count',
        surfaceCount: matrix.surfaceCount + 1,
      }),
    ).toThrow();
    expect(() =>
      ProductionGaApprovalArtifactSchema.parse({
        ...approvalA,
        id: 'production_ga_approval_bad_decision',
        approved: false,
      }),
    ).toThrow();
    expect(() =>
      ProductionGaSignoffRunSchema.parse({
        ...signoff,
        id: 'production_ga_signoff_duplicate_approver',
        approverHashes: ['sha256:same-approver', 'sha256:same-approver'],
      }),
    ).toThrow();
    const blockedDuplicateApproverSignoff = ProductionGaSignoffRunSchema.parse({
      ...signoff,
      id: 'production_ga_signoff_blocked_duplicate_approver',
      status: 'blocked',
      approverHashes: ['sha256:same-approver', 'sha256:same-approver'],
      approvalConsumedCount: 0,
    });
    expect(blockedDuplicateApproverSignoff.status).toBe('blocked');
    expect(() =>
      ProductionGaSignoffRunSchema.parse({
        ...signoff,
        id: 'production_ga_signoff_unresolved_critical',
        status: 'ready',
        unresolvedCriticalRiskCount: 1,
      }),
    ).toThrow();
    expect(() =>
      ProductionGaE2ERehearsalRunSchema.parse({
        ...rehearsalRun,
        id: 'production_ga_e2e_raw_payload',
        metadata: {
          releaseBody: 'raw release body fixture',
          deployPayload: 'raw deploy payload fixture',
          rawLog: 'raw log fixture',
          rawPatch: 'raw patch fixture',
          dbRow: 'raw database row fixture',
        },
      }),
    ).toThrow();
  });

  it('rejects M48-D7 adversarial raw metadata across late-stage governance contracts', () => {
    const rawMetadata = {
      nested: {
        rawPrompt: 'patch the production system',
        stdout: 'raw command output',
        stderr: 'raw command error',
        rawDiff: 'diff --git a/app.ts b/app.ts',
        rawPath: 'C:\\Users\\Thomas\\CodexHub\\.env',
        rawUrl: 'https://api.github.com/repos/example/repo',
        token: 'ghp_example',
        envValue: 'CODEXHUB_SECRET=value',
        releaseBody: 'raw release body',
        deployPayload: 'raw deployment payload',
        rawLog: 'raw telemetry log',
        rawPatch: 'raw mcp patch',
        dbRow: 'raw sqlite row',
        auditBody: 'raw audit export body',
      },
    };
    const retryPolicy = RuntimeRetryPolicySchema.parse({
      maxAttempts: 2,
      attemptCount: 0,
      backoffStrategy: 'fixed',
      backoffSeconds: 15,
      retryableStatuses: ['failed'],
    });
    const concurrencyPolicy = RuntimeConcurrencyPolicySchema.parse({
      scope: 'workflow-template',
      scopeHash: 'sha256:workflow-template',
      maxConcurrent: 1,
      currentRunningCount: 0,
    });

    const representativeContracts = [
      {
        name: 'production workflow recovery',
        parse: (payload: unknown) => ProductionWorkflowRecoveryRunSchema.parse(payload),
        payload: {
          id: 'm48_d7_recovery_run',
          schemaVersion,
          createdAt,
          recoveryRunId: 'm48_d7_recovery_run',
          dryRunId: 'm48_d7_recovery_dry_run',
          templateId: 'local-patch-review',
          templateHash: 'sha256:template',
          status: 'blocked',
          steps: [],
          childActionStates: [],
          stepCount: 0,
          childActionCount: 0,
          completedChildActionCount: 0,
          waitingChildApprovalCount: 0,
          failedChildActionCount: 0,
          bodyStored: false,
          rawPathStored: false,
          summary: 'Recovery run blocks before child state is trusted.',
        },
      },
      {
        name: 'browser action',
        parse: (payload: unknown) => BrowserActionPlanSchema.parse(payload),
        payload: {
          id: 'm48_d7_browser_plan',
          schemaVersion,
          createdAt,
          dryRunId: 'm48_d7_browser_dry_run',
          status: 'planned',
          actionKind: 'click',
          targetUrlHash: 'sha256:url',
          selectorHash: 'sha256:selector',
          processBoundaryPlanned: true,
          browserActPlanned: true,
          rawSelectorStored: false,
          rawTypedTextStored: false,
          rawPathStored: false,
          bodyStored: false,
          summary: 'Browser action plan stores selector and URL hashes only.',
        },
      },
      {
        name: 'electron inspector',
        parse: (payload: unknown) => ElectronMainInspectorPlanSchema.parse(payload),
        payload: {
          id: 'm48_d7_electron_plan',
          schemaVersion,
          createdAt,
          dryRunId: 'm48_d7_electron_dry_run',
          status: 'planned',
          endpointHash: 'sha256:endpoint',
          targetIdHash: 'sha256:target',
          snippetId: 'inspect-app-state',
          snippetSourceHash: 'sha256:snippet',
          cdpHttpBoundaryPlanned: true,
          cdpWebSocketBoundaryPlanned: true,
          mainInspectorPlanned: true,
          rawJavascriptStored: false,
          rawPathStored: false,
          bodyStored: false,
          summary: 'Electron inspector plan stores named snippet hashes only.',
        },
      },
      {
        name: 'MCP write',
        parse: (payload: unknown) => McpWriteToolPlanSchema.parse(payload),
        payload: {
          id: 'm48_d7_mcp_write_plan',
          schemaVersion,
          createdAt,
          dryRunId: 'm48_d7_mcp_write_dry_run',
          status: 'planned',
          toolName: 'workspace.applyPatchToControlledWorktree',
          worktreePathHash: 'sha256:worktree',
          patchHash: 'sha256:patch',
          changedFileCount: 1,
          directExecutionPlanned: true,
          controlledWorktreeOnly: true,
          repoRootMutationAllowed: false,
          rawPatchStored: false,
          rawPathStored: false,
          bodyStored: false,
          summary: 'MCP write plan stores patch hash only.',
        },
      },
      {
        name: 'runtime scheduler',
        parse: (payload: unknown) => RuntimeJobPlanSchema.parse(payload),
        payload: {
          id: 'm48_d7_runtime_plan',
          schemaVersion,
          createdAt,
          jobKind: 'workflow',
          targetKind: 'production-recovery',
          targetRecordIdHash: 'sha256:target',
          templateIdHash: 'sha256:template-id',
          templateHash: 'sha256:template',
          sourceRecordHash: 'sha256:source',
          status: 'planned',
          riskLevel: 'high',
          actionMode: 'write',
          approvalRequired: true,
          lockKeyHashes: ['sha256:lock'],
          retryPolicy,
          concurrencyPolicy,
          timeoutSeconds: 300,
          checkpointRequired: true,
          schedulerEnabled: false,
          childWorkflowCoordinationEnabled: false,
          processBoundaryPlanned: false,
          externalProcessPlanned: false,
          networkBoundaryPlanned: false,
          rawInputStored: false,
          rawOutputStored: false,
          rawPathStored: false,
          bodyStored: false,
          summary: 'Runtime scheduler plan stores target hashes only.',
        },
      },
      {
        name: 'external agent',
        parse: (payload: unknown) => ExternalAgentPatchPlanSchema.parse(payload),
        payload: {
          id: 'm48_d7_external_agent_plan',
          schemaVersion,
          createdAt,
          dryRunId: 'm48_d7_external_agent_dry_run',
          status: 'blocked',
          provider: 'codex-cli',
          sourceWorktreeRecordHash: 'sha256:worktree-record',
          worktreePathHash: 'sha256:worktree-path',
          promptHash: 'sha256:prompt',
          instructionHash: 'sha256:instructions',
          changedFileCount: 2,
          maxRuntimeSeconds: 900,
          fixedArgvShapeHash: 'sha256:argv',
          processBoundaryPlanned: true,
          externalProcessPlanned: true,
          controlledSiblingWorktreeOnly: true,
          repoRootMutationAllowed: false,
          arbitraryCommandAllowed: false,
          rawPromptStored: false,
          rawDiffStored: false,
          rawPatchStored: false,
          rawCommandStored: false,
          rawPathStored: false,
          bodyStored: false,
          summary: 'External agent plan stores prompt and worktree hashes only.',
        },
      },
      {
        name: 'platform backup',
        parse: (payload: unknown) => PlatformBackupPlanSchema.parse(payload),
        payload: {
          id: 'm48_d7_platform_backup_plan',
          schemaVersion,
          createdAt,
          dryRunId: 'm48_d7_platform_backup_dry_run',
          status: 'planned',
          scope: 'store-sqlite',
          storeSnapshotHash: 'sha256:store',
          backupRootHash: 'sha256:backup-root',
          manifestHash: 'sha256:manifest',
          fileCount: 3,
          estimatedByteCount: 1024,
          localFilesystemOnly: true,
          networkExportAllowed: false,
          arbitraryBackupTargetAllowed: false,
          rawPathStored: false,
          rawSqlStored: false,
          rawDbRowsStored: false,
          rawBackupBodyStored: false,
          rawAuditBodyStored: false,
          rawTokenStored: false,
          rawEnvStored: false,
          rawRequestBodyStored: false,
          rawResponseBodyStored: false,
          bodyStored: false,
          summary: 'Platform backup plan stores manifest hashes only.',
        },
      },
      {
        name: 'Production GA',
        parse: (payload: unknown) => ProductionGaE2ERehearsalRunSchema.parse(payload),
        payload: {
          id: 'm48_d7_ga_e2e_run',
          schemaVersion,
          createdAt,
          rehearsalPlanId: 'm48_d7_ga_e2e_plan',
          scenario: 'all-pass',
          status: 'ready',
          completedStepCount: 8,
          blockedStepCount: 0,
          failedStepCount: 0,
          liveSmokeStatus: 'readiness_blocked',
          liveSmokeBlockerCount: 1,
          timelineHash: 'sha256:timeline',
          processBoundaryInvoked: false,
          networkBoundaryInvoked: false,
          remoteProviderBoundaryInvoked: false,
          childAdapterInvokedDirectly: false,
          summary: 'Production GA rehearsal stores timeline hashes only.',
        },
      },
    ];

    for (const contract of representativeContracts) {
      expect(contract.parse(contract.payload), contract.name).toBeTruthy();
      expect(
        () =>
          contract.parse({
            ...contract.payload,
            id: `${contract.payload.id}_raw_metadata`,
            metadata: rawMetadata,
          }),
        contract.name,
      ).toThrow();
    }
  });

  it('keeps late-stage rehearsal scenario matrices covering critical debug archetypes', () => {
    const matrices: Array<{
      name: string;
      actual: readonly string[];
      required: readonly string[];
    }> = [
      {
        name: 'M40 release lifecycle',
        actual: ReleaseLifecycleAcceptanceScenarioSchema.options,
        required: [
          'all-pass',
          'provider-disabled',
          'token-missing',
          'version-plan-blocked',
          'changelog-blocked',
          'tag-exists',
          'tag-approval-blocked',
          'tag-create-failed',
          'release-draft-approval-blocked',
          'release-draft-failed',
          'network-timeout',
        ],
      },
      {
        name: 'M41 deployment observation',
        actual: DeploymentAcceptanceScenarioSchema.options,
        required: [
          'all-pass',
          'provider-disabled',
          'tool-missing',
          'approval-blocked',
          'target-hash-mismatch',
          'status-unavailable',
          'plan-diff-detected',
          'drift-detected',
          'raw-output-rejected',
          'network-timeout',
        ],
      },
      {
        name: 'M42 deployment operation',
        actual: DeploymentOperationAcceptanceScenarioSchema.options,
        required: [
          'all-pass',
          'provider-disabled',
          'tool-missing',
          'approval-blocked',
          'prod-second-approval-missing',
          'env-approval-mismatch',
          'rollback-plan-missing',
          'target-hash-mismatch',
          'apply-failed',
          'sync-failed',
          'rollback-failed',
          'raw-output-rejected',
          'network-timeout',
        ],
      },
      {
        name: 'M43 secrets governance',
        actual: SecretGovernanceAcceptanceScenarioSchema.options,
        required: [
          'all-pass',
          'provider-disabled',
          'config-missing',
          'token-configured-hash-only',
          'secret-value-rejected',
          'env-value-rejected',
          'approval-blocked',
          'leak-audit-failed',
          'raw-output-rejected',
        ],
      },
      {
        name: 'M44 policy backend',
        actual: RealPolicyBackendAcceptanceScenarioSchema.options,
        required: [
          'all-pass',
          'provider-disabled',
          'opa-cli-missing',
          'cedar-cli-missing',
          'loopback-unavailable',
          'approval-blocked',
          'advisory-deny',
          'backend-error',
          'raw-output-rejected',
        ],
      },
      {
        name: 'M44 telemetry export',
        actual: RealTelemetryAcceptanceScenarioSchema.options,
        required: [
          'all-pass-local',
          'exporter-disabled',
          'network-exporter-disabled',
          'endpoint-blocked',
          'approval-blocked',
          'export-failed',
          'raw-span-rejected',
          'network-timeout',
        ],
      },
      {
        name: 'M45 controlled write',
        actual: ControlledWriteAcceptanceScenarioSchema.options,
        required: [
          'all-pass-click',
          'all-pass-type',
          'browser-disabled',
          'selector-hash-mismatch',
          'credential-field-blocked',
          'all-pass-runtime-evaluate',
          'inspector-disabled',
          'non-loopback-blocked',
          'target-hash-mismatch',
          'snippet-not-allowlisted',
          'all-pass-controlled-worktree-patch',
          'mcp-write-disabled',
          'patch-hash-mismatch',
          'repo-root-blocked',
          'path-traversal-blocked',
          'approval-blocked',
          'action-failed',
          'runtime-failed',
          'patch-apply-failed',
          'network-timeout',
          'raw-input-rejected',
          'raw-source-rejected',
          'raw-output-rejected',
        ],
      },
      {
        name: 'M46 runtime scheduler',
        actual: RuntimeSchedulerRehearsalScenarioSchema.options,
        required: [
          'scheduler-disabled',
          'queue-full',
          'lock-held',
          'concurrency-limit-hit',
          'timeout',
          'cancel-requested',
          'retry-exhausted',
          'resume-from-checkpoint',
          'child-workflow-blocked',
        ],
      },
      {
        name: 'M46 external agent',
        actual: ExternalAgentRehearsalScenarioSchema.options,
        required: [
          'codex-all-pass',
          'claude-all-pass',
          'provider-disabled',
          'cli-missing',
          'worktree-missing',
          'approval-blocked',
          'prompt-hash-mismatch',
          'patch-hash-mismatch',
          'repo-root-blocked',
          'command-passthrough-blocked',
          'agent-run-failed',
          'timeout',
        ],
      },
      {
        name: 'M47 platform operations',
        actual: DisasterRecoveryScenarioSchema.options,
        required: [
          'backup-all-pass',
          'backup-dir-missing',
          'backup-hash-mismatch',
          'restore-rehearsal-pass',
          'restore-replace-disabled',
          'restore-second-approval-missing',
          'migration-pending',
          'migration-failed',
          'retention-preview',
          'retention-backup-required',
          'audit-export-pass',
          'role-missing',
          'role-insufficient',
          'disaster-recovery-drill',
        ],
      },
      {
        name: 'M48 production GA E2E',
        actual: ProductionGaE2EScenarioSchema.options,
        required: [
          'all-pass',
          'patch-blocked',
          'verification-failed',
          'pr-blocked',
          'merge-blocked',
          'release-blocked',
          'deploy-blocked',
          'observe-blocked',
          'rollback-plan-missing',
          'rollback-failed',
          'child-hash-mismatch',
          'approval-blocked',
          'live-env-not-configured',
          'evidence-missing',
          'audit-gap',
        ],
      },
    ];

    for (const matrix of matrices) {
      const missing = matrix.required.filter((scenario) => !matrix.actual.includes(scenario));
      expect(missing, `${matrix.name} rehearsal matrix drift`).toEqual([]);
    }
  });

  it('keeps rehearsal matrices covering success, block, failure, timeout, hash, and approval archetypes', () => {
    const coverage: Array<{
      name: string;
      actual: readonly string[];
      archetypes: Record<string, readonly string[]>;
    }> = [
      {
        name: 'M40 release lifecycle',
        actual: ReleaseLifecycleAcceptanceScenarioSchema.options,
        archetypes: {
          success: ['all-pass'],
          blocked: ['version-plan-blocked', 'changelog-blocked', 'tag-exists'],
          approval: ['tag-approval-blocked', 'release-draft-approval-blocked'],
          failure: ['tag-create-failed', 'release-draft-failed'],
          timeout: ['network-timeout'],
        },
      },
      {
        name: 'M41 deployment observation',
        actual: DeploymentAcceptanceScenarioSchema.options,
        archetypes: {
          success: ['all-pass'],
          blocked: ['provider-disabled', 'tool-missing', 'status-unavailable'],
          approval: ['approval-blocked'],
          hashMismatch: ['target-hash-mismatch'],
          failure: ['plan-diff-detected', 'drift-detected', 'raw-output-rejected'],
          timeout: ['network-timeout'],
        },
      },
      {
        name: 'M42 deployment operation',
        actual: DeploymentOperationAcceptanceScenarioSchema.options,
        archetypes: {
          success: ['all-pass'],
          blocked: ['provider-disabled', 'tool-missing', 'rollback-plan-missing'],
          approval: ['approval-blocked', 'prod-second-approval-missing'],
          hashMismatch: ['target-hash-mismatch'],
          failure: ['apply-failed', 'sync-failed', 'rollback-failed'],
          timeout: ['network-timeout'],
        },
      },
      {
        name: 'M43 secrets governance',
        actual: SecretGovernanceAcceptanceScenarioSchema.options,
        archetypes: {
          success: ['all-pass'],
          blocked: ['provider-disabled', 'config-missing'],
          approval: ['approval-blocked'],
          failure: ['leak-audit-failed', 'raw-output-rejected'],
        },
      },
      {
        name: 'M44 policy backend',
        actual: RealPolicyBackendAcceptanceScenarioSchema.options,
        archetypes: {
          success: ['all-pass'],
          blocked: ['provider-disabled', 'loopback-unavailable'],
          approval: ['approval-blocked'],
          failure: ['advisory-deny', 'backend-error', 'raw-output-rejected'],
        },
      },
      {
        name: 'M44 telemetry export',
        actual: RealTelemetryAcceptanceScenarioSchema.options,
        archetypes: {
          success: ['all-pass-local'],
          blocked: ['exporter-disabled', 'network-exporter-disabled', 'endpoint-blocked'],
          approval: ['approval-blocked'],
          failure: ['export-failed', 'raw-span-rejected'],
          timeout: ['network-timeout'],
        },
      },
      {
        name: 'M45 controlled write',
        actual: ControlledWriteAcceptanceScenarioSchema.options,
        archetypes: {
          success: ['all-pass-click', 'all-pass-type', 'all-pass-runtime-evaluate'],
          blocked: ['browser-disabled', 'inspector-disabled', 'mcp-write-disabled'],
          approval: ['approval-blocked'],
          hashMismatch: ['selector-hash-mismatch', 'target-hash-mismatch', 'patch-hash-mismatch'],
          failure: ['action-failed', 'runtime-failed', 'patch-apply-failed'],
          timeout: ['network-timeout'],
        },
      },
      {
        name: 'M46 runtime scheduler',
        actual: RuntimeSchedulerRehearsalScenarioSchema.options,
        archetypes: {
          blocked: ['scheduler-disabled', 'queue-full', 'lock-held', 'child-workflow-blocked'],
          failure: ['retry-exhausted', 'cancel-requested'],
          timeout: ['timeout'],
          resume: ['resume-from-checkpoint'],
        },
      },
      {
        name: 'M46 external agent',
        actual: ExternalAgentRehearsalScenarioSchema.options,
        archetypes: {
          success: ['codex-all-pass', 'claude-all-pass'],
          blocked: ['provider-disabled', 'cli-missing', 'worktree-missing'],
          approval: ['approval-blocked'],
          hashMismatch: ['prompt-hash-mismatch', 'patch-hash-mismatch'],
          failure: ['agent-run-failed', 'command-passthrough-blocked'],
          timeout: ['timeout'],
        },
      },
      {
        name: 'M47 platform operations',
        actual: DisasterRecoveryScenarioSchema.options,
        archetypes: {
          success: ['backup-all-pass', 'restore-rehearsal-pass', 'audit-export-pass'],
          blocked: ['backup-dir-missing', 'restore-replace-disabled', 'role-missing'],
          approval: ['restore-second-approval-missing'],
          hashMismatch: ['backup-hash-mismatch'],
          failure: ['migration-failed', 'role-insufficient'],
          resume: ['disaster-recovery-drill'],
        },
      },
      {
        name: 'M48 production GA E2E',
        actual: ProductionGaE2EScenarioSchema.options,
        archetypes: {
          success: ['all-pass'],
          blocked: ['patch-blocked', 'pr-blocked', 'merge-blocked', 'live-env-not-configured'],
          approval: ['approval-blocked'],
          hashMismatch: ['child-hash-mismatch'],
          failure: ['verification-failed', 'rollback-failed', 'audit-gap'],
        },
      },
    ];

    for (const matrix of coverage) {
      for (const [archetype, scenarios] of Object.entries(matrix.archetypes)) {
        const hasArchetype = scenarios.some((scenario) => matrix.actual.includes(scenario));

        expect(hasArchetype, `${matrix.name} missing ${archetype} rehearsal archetype`).toBe(true);
      }
    }
  });

  it('parses M51 unified account, client, task, quota, evidence, and recovery contracts metadata-only', () => {
    const workspace = BusinessWorkspaceSchema.parse({
      id: 'business_workspace_1',
      schemaVersion,
      observedAt: createdAt,
      workspaceIdHash: 'sha256:workspace',
      workspaceNameHash: 'sha256:workspace-name',
      status: 'active',
      membershipCount: 1,
      ownerCount: 1,
      adminCount: 0,
      evidenceRefIds: ['evidence_business_workspace'],
      auditEventIds: ['audit_business_workspace'],
      summary: 'Business workspace mirror stores metadata only.',
    });
    const membership = BusinessMembershipMirrorSchema.parse({
      id: 'business_membership_1',
      schemaVersion,
      observedAt: createdAt,
      workspaceIdHash: workspace.workspaceIdHash,
      memberHash: 'sha256:member',
      emailHash: 'sha256:email',
      role: 'owner',
      status: 'active',
      seatActive: true,
      ownerProtected: true,
      evidenceRefIds: ['evidence_business_membership'],
      auditEventIds: ['audit_business_membership'],
      summary: 'Membership mirror stores hashes and role/status only.',
    });
    const profileBinding = ChromeProfileBindingSchema.parse({
      id: 'chrome_profile_binding_1',
      schemaVersion,
      createdAt,
      profileId: 'profile-default',
      profilePathHash: 'sha256:profile-path',
      accountHash: membership.memberHash,
      workspaceIdHash: workspace.workspaceIdHash,
      healthStatus: 'healthy',
      summary: 'Chrome profile binding stores the profile path hash only.',
    });
    const sessionHealth = ChatGptSessionHealthSchema.parse({
      id: 'chatgpt_session_health_1',
      schemaVersion,
      observedAt: createdAt,
      profileBindingId: profileBinding.id,
      accountHash: membership.memberHash,
      workspaceIdHash: workspace.workspaceIdHash,
      status: 'healthy',
      accountMatchesExpected: true,
      workspaceMatchesExpected: true,
      summary: 'ChatGPT session health is metadata-only.',
    });
    const checkpoint = HumanCheckpointSchema.parse({
      id: 'human_checkpoint_1',
      schemaVersion,
      createdAt,
      checkpointKind: 'mfa_required',
      status: 'requested',
      targetHash: 'sha256:target',
      accountHash: membership.memberHash,
      workspaceIdHash: workspace.workspaceIdHash,
      reasonHash: 'sha256:reason',
      evidenceRefIds: ['evidence_checkpoint'],
      auditEventIds: ['audit_checkpoint'],
      summary: 'Human checkpoint stores no MFA code or credential data.',
    });
    const client = CodexClientInstanceSchema.parse({
      id: 'codex_client_1',
      schemaVersion,
      observedAt: createdAt,
      clientKind: 'codex-app-server',
      clientInstanceHash: 'sha256:client',
      status: 'available',
      protocolVersionHash: 'sha256:protocol',
      activeTaskCount: 0,
      evidenceRefIds: ['evidence_client'],
      auditEventIds: ['audit_client'],
      summary: 'Codex client instance is available in metadata projection.',
    });
    const appServerSession = CodexAppServerSessionSchema.parse({
      id: 'codex_app_server_session_1',
      schemaVersion,
      observedAt: createdAt,
      clientInstanceId: client.id,
      appServerSessionHash: 'sha256:app-server-session',
      status: 'initialized',
      initialized: true,
      protocolDriftDetected: false,
      evidenceRefIds: ['evidence_app_server'],
      auditEventIds: ['audit_app_server'],
      summary: 'App Server session summary is initialized without raw response data.',
    });
    const accountBinding = CodexAccountBindingSchema.parse({
      id: 'codex_account_binding_1',
      schemaVersion,
      observedAt: createdAt,
      codexAccountHash: 'sha256:codex-account',
      businessMembershipMirrorId: membership.id,
      workspaceIdHash: workspace.workspaceIdHash,
      status: 'matched',
      summary: 'Codex account matches the Business membership mirror.',
    });
    const intent = CodexTaskIntentSchema.parse({
      id: 'codex_task_intent_1',
      schemaVersion,
      createdAt,
      intentHash: 'sha256:intent',
      promptHash: 'sha256:prompt',
      promptLength: 42,
      requestedByHash: 'sha256:operator',
      workflowHash: 'sha256:workflow',
      status: 'planned',
      evidenceRefIds: ['evidence_intent'],
      auditEventIds: ['audit_intent'],
      summary: 'Task intent stores prompt hash and length only.',
    });
    const taskRun = CodexTaskRunSchema.parse({
      id: 'codex_task_run_1',
      schemaVersion,
      createdAt,
      intentId: intent.id,
      status: 'queued',
      accountBindingId: accountBinding.id,
      clientInstanceId: client.id,
      appServerSessionId: appServerSession.id,
      threadHash: 'sha256:thread',
      turnCount: 0,
      eventCount: 0,
      evidenceRefIds: ['evidence_task_run'],
      auditEventIds: ['audit_task_run'],
      summary: 'Task run is queued without live dispatch.',
    });
    const diagnosis = CodexTaskDiagnosisSchema.parse({
      id: 'codex_task_diagnosis_1',
      schemaVersion,
      observedAt: createdAt,
      taskRunId: taskRun.id,
      diagnosisKind: 'failed_auth',
      status: 'actionable',
      confidence: 0.9,
      recommendedRecoveryKind: 'human_checkpoint',
      evidenceRefIds: ['evidence_diagnosis'],
      auditEventIds: ['audit_diagnosis'],
      summary: 'Diagnosis points to a human checkpoint.',
    });
    const recovery = CodexRecoveryRunSchema.parse({
      id: 'codex_recovery_run_1',
      schemaVersion,
      createdAt,
      taskRunId: taskRun.id,
      diagnosisId: diagnosis.id,
      recoveryKind: 'human_checkpoint',
      status: 'needs_human',
      dryRunId: 'recovery_dry_run_1',
      approvalRequired: true,
      evidenceRefIds: ['evidence_recovery'],
      auditEventIds: ['audit_recovery'],
      summary: 'Recovery run remains disabled and awaits a human checkpoint.',
    });
    const accountPool = AccountPoolSchema.parse({
      id: 'account_pool_1',
      schemaVersion,
      observedAt: createdAt,
      poolHash: 'sha256:account-pool',
      status: 'ready',
      accountCount: 1,
      readyCount: 1,
      blockedCount: 0,
      entries: [
        {
          entryId: 'account_pool_entry_1',
          targetIdHash: accountBinding.codexAccountHash,
          status: 'ready',
          score: 90,
          evidenceRefIds: ['evidence_pool_entry'],
        },
      ],
      summary: 'Account pool has one ready account.',
    });
    const clientPool = ClientPoolSchema.parse({
      id: 'client_pool_1',
      schemaVersion,
      observedAt: createdAt,
      poolHash: 'sha256:client-pool',
      status: 'ready',
      clientCount: 1,
      readyCount: 1,
      blockedCount: 0,
      entries: [
        {
          entryId: 'client_pool_entry_1',
          targetIdHash: client.clientInstanceHash,
          status: 'ready',
          score: 90,
          evidenceRefIds: ['evidence_client_pool_entry'],
        },
      ],
      summary: 'Client pool has one ready client.',
    });
    const lease = LeaseSchema.parse({
      id: 'pool_lease_1',
      schemaVersion,
      createdAt,
      targetKind: 'account',
      targetIdHash: accountBinding.codexAccountHash,
      holderHash: 'sha256:task-holder',
      status: 'active',
      evidenceRefIds: ['evidence_lease'],
      auditEventIds: ['audit_lease'],
      summary: 'Lease stores the holder hash and no secret.',
    });
    const quota = QuotaSnapshotSchema.parse({
      id: 'quota_snapshot_1',
      schemaVersion,
      observedAt: createdAt,
      subjectKind: 'unified-account',
      subjectHash: accountBinding.codexAccountHash,
      status: 'available',
      limitCount: 100,
      usedCount: 1,
      remainingCount: 99,
      resetAtHash: 'sha256:reset-window',
      sourceRefIds: [workspace.id, appServerSession.id],
      evidenceRefIds: ['evidence_quota'],
      auditEventIds: ['audit_quota'],
      summary: 'Quota snapshot stores counts and hashed reset metadata.',
    });
    const evidenceBundle = EvidenceBundleSchema.parse({
      id: 'evidence_bundle_1',
      schemaVersion,
      createdAt,
      bundleHash: 'sha256:evidence-bundle',
      evidenceRefIds: ['evidence_quota', 'evidence_task_run'],
      auditEventIds: ['audit_quota', 'audit_task_run'],
      evidenceCount: 2,
      auditEventCount: 2,
      summary: 'Evidence bundle stores refs and counts only.',
    });

    const records = [
      workspace,
      membership,
      profileBinding,
      sessionHealth,
      checkpoint,
      client,
      appServerSession,
      accountBinding,
      intent,
      taskRun,
      diagnosis,
      recovery,
      accountPool,
      clientPool,
      lease,
      quota,
      evidenceBundle,
    ];
    const serialized = JSON.stringify(records);

    expect(records).toHaveLength(17);
    expect(profileBinding.rawPathStored).toBe(false);
    expect(sessionHealth.storageRead).toBe(false);
    expect(checkpoint.sensitiveInputStored).toBe(false);
    expect(intent.rawPromptStored).toBe(false);
    expect(recovery.executionDisabled).toBe(true);
    expect(lease.leaseSecretStored).toBe(false);
    expect(quota.ambiguous).toBe(false);
    expect(evidenceBundle.evidenceCount).toBe(evidenceBundle.evidenceRefIds.length);
    expect(serialized).not.toContain(adversarialPublicOutputFixture);
    expect(findAdversarialPublicOutputRoundTripLeaks(records)).toEqual([]);
  });

  it('rejects M51 contracts that expose raw prompt, path, body, token, cookie, session, or MFA metadata', () => {
    expect(() =>
      CodexTaskIntentSchema.parse({
        id: 'codex_task_intent_raw_1',
        schemaVersion,
        createdAt,
        intentHash: 'sha256:intent',
        status: 'planned',
        summary: 'Unsafe task intent.',
        metadata: {
          rawPrompt: adversarialPublicOutputFixture,
        },
      }),
    ).toThrow();
    expect(() =>
      ChromeProfileBindingSchema.parse({
        id: 'chrome_profile_binding_raw_1',
        schemaVersion,
        createdAt,
        profileId: 'default',
        profilePathHash: 'sha256:profile-path',
        rawPath: adversarialPublicOutputFixture,
        summary: 'Unsafe profile binding.',
      }),
    ).toThrow();
    expect(() =>
      ChatGptSessionHealthSchema.parse({
        id: 'chatgpt_session_raw_1',
        schemaVersion,
        observedAt: createdAt,
        profileBindingId: 'chrome_profile_binding_1',
        status: 'blocked',
        summary: 'Unsafe session health.',
        metadata: {
          token: adversarialPublicOutputFixture,
          cookie: adversarialPublicOutputFixture,
          session: adversarialPublicOutputFixture,
          body: adversarialPublicOutputFixture,
        },
      }),
    ).toThrow();
    expect(() =>
      HumanCheckpointSchema.parse({
        id: 'human_checkpoint_raw_1',
        schemaVersion,
        createdAt,
        checkpointKind: 'mfa_required',
        status: 'requested',
        summary: 'Unsafe checkpoint.',
        metadata: {
          mfa: adversarialPublicOutputFixture,
        },
      }),
    ).toThrow();
    expect(() =>
      EvidenceBundleSchema.parse({
        id: 'evidence_bundle_bad_count',
        schemaVersion,
        createdAt,
        bundleHash: 'sha256:bundle',
        evidenceRefIds: ['evidence_1'],
        auditEventIds: [],
        evidenceCount: 2,
        auditEventCount: 0,
        summary: 'Evidence count mismatch.',
      }),
    ).toThrow();
  });

  it('parses M54 App Server wire, mirror, approval, and drift contracts metadata-only', () => {
    const wire = CodexAppServerWireMessageSummarySchema.parse({
      id: 'codex_app_server_wire_1',
      schemaVersion,
      observedAt: createdAt,
      appServerSessionId: 'codex_app_server_session_1',
      transportKind: 'stdio-jsonl',
      direction: 'request',
      method: 'initialize',
      requestIdHash: 'sha256:request-id',
      messageHash: 'sha256:wire-message',
      payloadSummaryHash: 'sha256:payload-summary',
      payloadByteCount: 120,
      lineCount: 1,
      redactedFieldCount: 0,
      status: 'sent',
      initializedRequired: true,
      initializedObserved: false,
      processBoundaryInvoked: true,
      evidenceRefIds: ['evidence_app_server_wire'],
      auditEventIds: ['audit_app_server_wire'],
      summary: 'Initialize request stored as a hash-only wire summary.',
    });
    const threadMirror = CodexAppServerThreadMirrorSchema.parse({
      id: 'codex_app_server_thread_1',
      schemaVersion,
      observedAt: createdAt,
      appServerSessionId: wire.appServerSessionId,
      taskRunId: 'codex_task_run_1',
      threadIdHash: 'sha256:thread-id',
      status: 'loaded',
      ephemeral: true,
      turnCount: 1,
      activeTurnIdHash: 'sha256:turn-id',
      subscribed: true,
      permissionProfileHash: 'sha256:permission-profile',
      workspaceTrustMutationAllowed: false,
      workspaceTrustMutationObserved: false,
      processBoundaryInvoked: true,
      evidenceRefIds: ['evidence_thread_mirror'],
      auditEventIds: ['audit_thread_mirror'],
      summary: 'Thread mirror contains no raw path or conversation body.',
    });
    const turnMirror = CodexAppServerTurnMirrorSchema.parse({
      id: 'codex_app_server_turn_1',
      schemaVersion,
      observedAt: createdAt,
      appServerSessionId: wire.appServerSessionId,
      threadMirrorId: threadMirror.id,
      taskRunId: 'codex_task_run_1',
      threadIdHash: threadMirror.threadIdHash,
      turnIdHash: 'sha256:turn-id',
      status: 'running',
      itemCount: 2,
      eventCount: 3,
      inputSummaryHash: 'sha256:input-summary',
      outputSummaryHash: 'sha256:output-summary',
      tokenCount: 42,
      approvalPendingCount: 1,
      processBoundaryInvoked: true,
      evidenceRefIds: ['evidence_turn_mirror'],
      auditEventIds: ['audit_turn_mirror'],
      summary: 'Turn mirror stores summary hashes and counts only.',
    });
    const eventSummary = CodexAppServerEventSummarySchema.parse({
      id: 'codex_app_server_event_1',
      schemaVersion,
      observedAt: createdAt,
      appServerSessionId: wire.appServerSessionId,
      threadMirrorId: threadMirror.id,
      turnMirrorId: turnMirror.id,
      method: 'item/commandExecution/requestApproval',
      eventHash: 'sha256:event',
      threadIdHash: threadMirror.threadIdHash,
      turnIdHash: turnMirror.turnIdHash,
      itemIdHash: 'sha256:item',
      status: 'started',
      sequenceNumber: 1,
      deltaCount: 0,
      payloadByteCount: 256,
      itemKindHash: 'sha256:item-kind',
      terminal: false,
      processBoundaryInvoked: true,
      evidenceRefIds: ['evidence_event_summary'],
      auditEventIds: ['audit_event_summary'],
      summary: 'Event summary records an approval request without raw payload.',
    });
    const approvalBridge = CodexAppServerApprovalBridgeRecordSchema.parse({
      id: 'codex_app_server_approval_1',
      schemaVersion,
      createdAt,
      appServerSessionId: wire.appServerSessionId,
      taskRunId: 'codex_task_run_1',
      threadIdHash: threadMirror.threadIdHash,
      turnIdHash: turnMirror.turnIdHash,
      itemIdHash: eventSummary.itemIdHash,
      requestIdHash: 'sha256:server-request',
      approvalKind: 'command-execution',
      status: 'pending',
      proposalHash: 'sha256:proposal',
      proposalSummaryHash: 'sha256:proposal-summary',
      availableDecisionCount: 2,
      availableDecisionHashes: ['sha256:decline', 'sha256:cancel'],
      processBoundaryInvoked: true,
      evidenceRefIds: ['evidence_approval_bridge'],
      auditEventIds: ['audit_approval_bridge'],
      summary: 'Approval bridge is pending and cannot silently approve.',
    });
    const driftReport = CodexAppServerProtocolDriftReportSchema.parse({
      id: 'codex_app_server_drift_1',
      schemaVersion,
      observedAt: createdAt,
      appServerSessionId: wire.appServerSessionId,
      baselineKind: 'generate-json-schema',
      baselineHash: 'sha256:baseline-schema',
      observedSchemaHash: 'sha256:observed-schema',
      status: 'compatible',
      driftCount: 0,
      missingMethodCount: 0,
      changedMethodCount: 0,
      unknownMethodCount: 0,
      liveDispatchBlocked: false,
      processBoundaryInvoked: true,
      evidenceRefIds: ['evidence_protocol_drift'],
      auditEventIds: ['audit_protocol_drift'],
      summary: 'Protocol drift report confirms compatible generated schema hashes.',
    });

    const records = [
      wire,
      threadMirror,
      turnMirror,
      eventSummary,
      approvalBridge,
      driftReport,
    ];
    const serialized = JSON.stringify(records);

    expect(records).toHaveLength(6);
    expect(wire.rawBodyStored).toBe(false);
    expect(wire.jsonRpcHeaderStored).toBe(false);
    expect(threadMirror.workspaceTrustMutationAllowed).toBe(false);
    expect(threadMirror.workspaceTrustMutationObserved).toBe(false);
    expect(approvalBridge.silentApprovalAllowed).toBe(false);
    expect(approvalBridge.rawProposalStored).toBe(false);
    expect(approvalBridge.approvalSecretStored).toBe(false);
    expect(driftReport.rawSchemaStored).toBe(false);
    expect(driftReport.generatedSchemaRequired).toBe(true);
    expect(serialized).not.toContain(adversarialPublicOutputFixture);
    expect(findAdversarialPublicOutputRoundTripLeaks(records)).toEqual([]);
  });

  it('rejects M54 App Server contracts that expose raw payloads or unblock unknown drift', () => {
    expect(() =>
      CodexAppServerWireMessageSummarySchema.parse({
        id: 'codex_app_server_wire_raw_1',
        schemaVersion,
        observedAt: createdAt,
        appServerSessionId: 'codex_app_server_session_1',
        transportKind: 'stdio-jsonl',
        direction: 'response',
        method: 'turn/start',
        messageHash: 'sha256:wire-message',
        status: 'received',
        summary: 'Unsafe wire summary.',
        metadata: {
          rawBody: adversarialPublicOutputFixture,
        },
      }),
    ).toThrow();
    expect(() =>
      CodexAppServerThreadMirrorSchema.parse({
        id: 'codex_app_server_thread_raw_1',
        schemaVersion,
        observedAt: createdAt,
        appServerSessionId: 'codex_app_server_session_1',
        threadIdHash: 'sha256:thread-id',
        status: 'loaded',
        rawPath: adversarialPublicOutputFixture,
        summary: 'Unsafe thread mirror.',
      }),
    ).toThrow();
    expect(() =>
      CodexAppServerEventSummarySchema.parse({
        id: 'codex_app_server_event_raw_1',
        schemaVersion,
        observedAt: createdAt,
        appServerSessionId: 'codex_app_server_session_1',
        method: 'item/fileChange/requestApproval',
        eventHash: 'sha256:event',
        status: 'started',
        sequenceNumber: 0,
        summary: 'Unsafe event summary.',
        metadata: {
          rawDiff: adversarialPublicOutputFixture,
        },
      }),
    ).toThrow();
    expect(() =>
      CodexAppServerApprovalBridgeRecordSchema.parse({
        id: 'codex_app_server_approval_mismatch_1',
        schemaVersion,
        createdAt,
        appServerSessionId: 'codex_app_server_session_1',
        threadIdHash: 'sha256:thread-id',
        turnIdHash: 'sha256:turn-id',
        requestIdHash: 'sha256:request-id',
        approvalKind: 'file-change',
        status: 'pending',
        proposalHash: 'sha256:proposal',
        availableDecisionCount: 2,
        availableDecisionHashes: ['sha256:decline'],
        summary: 'Unsafe approval decision count.',
      }),
    ).toThrow();
    expect(() =>
      CodexAppServerProtocolDriftReportSchema.parse({
        id: 'codex_app_server_drift_unblocked_1',
        schemaVersion,
        observedAt: createdAt,
        baselineKind: 'generate-ts',
        baselineHash: 'sha256:baseline',
        observedSchemaHash: 'sha256:observed',
        status: 'unknown',
        liveDispatchBlocked: false,
        summary: 'Unknown drift must not allow dispatch.',
      }),
    ).toThrow();
  });
});
