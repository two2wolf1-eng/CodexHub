{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "operator-readiness-kernel",
  "projectType": "library",
  "sourceRoot": "packages/operator-readiness-kernel/src",
  "implicitDependencies": ["contracts"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/operator-readiness-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/operator-readiness-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/operator-readiness-kernel"
      }
    }
  }
}
