{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "mcp-tool-contracts",
  "projectType": "library",
  "sourceRoot": "packages/mcp-tool-contracts/src",
  "implicitDependencies": ["contracts", "security-kernel"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/mcp-tool-contracts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/mcp-tool-contracts"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/mcp-tool-contracts"
      }
    }
  }
}
