{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "workflow-kernel",
  "projectType": "library",
  "sourceRoot": "packages/workflow-kernel/src",
  "implicitDependencies": ["contracts", "security-kernel", "evidence-kernel", "store-core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/workflow-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/workflow-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/workflow-kernel"
      }
    }
  }
}
