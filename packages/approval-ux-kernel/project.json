{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "approval-ux-kernel",
  "projectType": "library",
  "sourceRoot": "packages/approval-ux-kernel/src",
  "implicitDependencies": ["contracts", "evidence-kernel"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/approval-ux-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/approval-ux-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/approval-ux-kernel"
      }
    }
  }
}
