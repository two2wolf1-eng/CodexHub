{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "release-candidate-kernel",
  "projectType": "library",
  "sourceRoot": "packages/release-candidate-kernel/src",
  "implicitDependencies": ["contracts", "evidence-kernel", "review-package-kernel"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/release-candidate-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/release-candidate-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/release-candidate-kernel"
      }
    }
  }
}
