{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "codex-exec-adapter",
  "projectType": "library",
  "sourceRoot": "packages/codex-exec-adapter/src",
  "implicitDependencies": ["contracts", "codex-kernel", "evidence-kernel"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/codex-exec-adapter"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/codex-exec-adapter"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/codex-exec-adapter"
      }
    }
  }
}
