{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "orchestrator-kernel",
  "projectType": "library",
  "sourceRoot": "packages/orchestrator-kernel/src",
  "implicitDependencies": ["contracts", "workflow-kernel", "security-kernel", "skill-registry"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/orchestrator-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/orchestrator-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/orchestrator-kernel"
      }
    }
  }
}
