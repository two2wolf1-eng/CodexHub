{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "production-real-client-kernel",
  "projectType": "library",
  "sourceRoot": "packages/production-real-client-kernel/src",
  "implicitDependencies": ["contracts", "evidence-kernel"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/production-real-client-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/production-real-client-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/production-real-client-kernel"
      }
    }
  }
}
