{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "chatgpt-business-adapter",
  "projectType": "library",
  "sourceRoot": "packages/chatgpt-business-adapter/src",
  "implicitDependencies": ["contracts", "capability-adapter-kernel", "evidence-kernel"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/chatgpt-business-adapter"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/chatgpt-business-adapter"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/chatgpt-business-adapter"
      }
    }
  }
}
