{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "capability-adapter-kernel",
  "projectType": "library",
  "sourceRoot": "packages/capability-adapter-kernel/src",
  "implicitDependencies": ["contracts"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json",
        "cwd": "packages/capability-adapter-kernel"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint . --max-warnings=0",
        "cwd": "packages/capability-adapter-kernel"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --passWithNoTests --config ../../vitest.config.ts",
        "cwd": "packages/capability-adapter-kernel"
      }
    }
  }
}
